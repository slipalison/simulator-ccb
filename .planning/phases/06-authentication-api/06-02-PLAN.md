---
phase: 06-authentication-api
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01-PLAN.md
files_modified:
  - src/Onboarding.Application/Common/IKeycloakTokenService.cs
  - src/Onboarding.Application/Auth/DTOs/TokenResponse.cs
  - src/Onboarding.Domain/Repositories/IClientRepository.cs
  - src/Onboarding.Infrastructure/Repositories/ClientRepository.cs
  - src/Onboarding.API/Program.cs
  - src/Onboarding.API/Onboarding.API.csproj
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "IKeycloakTokenService existe em Onboarding.Application/Common com métodos ExchangePasswordAsync e RefreshTokenAsync"
    - "TokenResponse DTO existe com campos access_token, refresh_token, expires_in, token_type, refresh_expires_in, scope"
    - "IClientRepository tem GetByEmailAsync(string email, CancellationToken ct)"
    - "ClientRepository implementa GetByEmailAsync com normalização ToLowerInvariant"
    - "Program.cs tem AddAuthentication().AddJwtBearer() com Authority=Keycloak:RealmUrl e ValidateAudience=false e MapInboundClaims=false"
    - "Program.cs tem UseAuthentication() ANTES de UseAuthorization() ANTES de MapControllers()"
    - "Microsoft.AspNetCore.Authentication.JwtBearer 10.0.5 adicionado ao Onboarding.API.csproj"
    - "AuthTestApiFactory no projeto de testes compila sem erros"
  artifacts:
    - path: "src/Onboarding.Application/Common/IKeycloakTokenService.cs"
      provides: "Abstração da camada Application sobre o endpoint de token do Keycloak"
      exports: ["IKeycloakTokenService"]
    - path: "src/Onboarding.Application/Auth/DTOs/TokenResponse.cs"
      provides: "DTO de resposta de token JWT"
      exports: ["TokenResponse"]
    - path: "src/Onboarding.Domain/Repositories/IClientRepository.cs"
      provides: "Interface do repositório com GetByEmailAsync adicionado"
      exports: ["IClientRepository"]
    - path: "src/Onboarding.Infrastructure/Repositories/ClientRepository.cs"
      provides: "Implementação de GetByEmailAsync"
      exports: []
    - path: "src/Onboarding.API/Program.cs"
      provides: "JWT Bearer middleware configurado (D-04, D-05, D-06, ordem correta)"
      exports: []
  key_links:
    - from: "src/Onboarding.API/Program.cs"
      to: "src/Onboarding.Application/Common/IKeycloakTokenService.cs"
      via: "AddAuthentication().AddJwtBearer() — middleware valida tokens emitidos via IKeycloakTokenService"
      pattern: "AddJwtBearer"
    - from: "src/Onboarding.Infrastructure/Repositories/ClientRepository.cs"
      to: "src/Onboarding.Domain/Repositories/IClientRepository.cs"
      via: "implementa GetByEmailAsync"
      pattern: "GetByEmailAsync"
---

<objective>
Criar os contratos e a configuração de infraestrutura de autenticação: interface IKeycloakTokenService, DTO TokenResponse, método GetByEmailAsync no repositório, e AddJwtBearer no Program.cs.

Purpose: Estabelecer todos os contratos que os planos subsequentes (Plan 03) implementarão. AuthTestApiFactory do Plan 01 compila apenas depois deste plano.
Output: 6 arquivos modificados/criados. `dotnet build` passa sem erros. Stubs RED do Plan 01 ainda falham (nenhuma implementação de controller criada aqui).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-authentication-api/06-CONTEXT.md
@.planning/phases/06-authentication-api/06-RESEARCH.md

<interfaces>
<!-- IKeycloakUserService — padrão a seguir para IKeycloakTokenService -->
<!-- src/Onboarding.Application/Common/IKeycloakUserService.cs -->
```csharp
namespace Onboarding.Application.Common;

public interface IKeycloakUserService
{
    Task<string> CreateUserAsync(
        string username, string email, string password,
        string firstName, CancellationToken ct = default);

    Task DeleteUserByEmailAsync(string email, CancellationToken ct = default);
}
```

<!-- IClientRepository atual — GetByEmailAsync será adicionado -->
<!-- src/Onboarding.Domain/Repositories/IClientRepository.cs -->
```csharp
public interface IClientRepository
{
    Task AddAsync(Client client, CancellationToken ct = default);
    Task<Client?> GetByIdAsync(Guid id, CancellationToken ct = default);
    Task<bool> ExistsByEmailAsync(string email, CancellationToken ct = default);
    Task<bool> ExistsByCpfAsync(string cpf, CancellationToken ct = default);
    Task<bool> ExistsByCnpjAsync(string cnpj, CancellationToken ct = default);
    Task DeleteAsync(Guid id, CancellationToken ct = default);
}
```

<!-- ClientRepository.ExistsByEmailAsync — padrão de normalização a replicar em GetByEmailAsync -->
```csharp
public async Task<bool> ExistsByEmailAsync(string email, CancellationToken ct = default)
{
    var normalized = email.ToLowerInvariant();
    return await _db.Clients.AnyAsync(c => c.Email.Value == normalized, ct);
}
```

<!-- Program.cs atual — UseAuthentication/UseAuthorization devem ser inseridos ANTES de MapControllers() -->
```csharp
app.UseSerilogRequestLogging();
// UseAuthentication() → aqui (Plan 02)
// UseAuthorization() → aqui (Plan 02)
app.MapControllers();  // já existe
```

<!-- Onboarding.API.csproj — packages existentes (sem JwtBearer ainda) -->
```xml
<PackageReference Include="Serilog.AspNetCore" Version="10.0.0" />
<PackageReference Include="OpenTelemetry.Extensions.Hosting" Version="1.15.1" />
<!-- Microsoft.AspNetCore.Authentication.JwtBearer 10.0.5 a adicionar -->
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar IKeycloakTokenService, TokenResponse e adicionar GetByEmailAsync</name>
  <files>
    src/Onboarding.Application/Common/IKeycloakTokenService.cs
    src/Onboarding.Application/Auth/DTOs/TokenResponse.cs
    src/Onboarding.Domain/Repositories/IClientRepository.cs
    src/Onboarding.Infrastructure/Repositories/ClientRepository.cs
  </files>
  <read_first>
    - src/Onboarding.Application/Common/IKeycloakUserService.cs (padrão de interface Application layer)
    - src/Onboarding.Domain/Repositories/IClientRepository.cs (interface existente — adicionar GetByEmailAsync)
    - src/Onboarding.Infrastructure/Repositories/ClientRepository.cs (implementação existente — adicionar GetByEmailAsync)
    - src/Onboarding.Domain/Aggregates/ClientAggregate/Client.cs (propriedade Email.Value usada no filtro EF Core)
  </read_first>
  <behavior>
    IKeycloakTokenService.ExchangePasswordAsync("test@email.com", "pass") retorna Task&lt;TokenResponse&gt;.
    IKeycloakTokenService.RefreshTokenAsync("refresh-token") retorna Task&lt;TokenResponse&gt;.
    IClientRepository.GetByEmailAsync("TEST@EMAIL.COM") busca por email normalizado ToLowerInvariant.
    TokenResponse tem 6 campos: AccessToken, RefreshToken, ExpiresIn, TokenType, RefreshExpiresIn, Scope.
  </behavior>
  <action>
Criar `src/Onboarding.Application/Common/IKeycloakTokenService.cs`:

```csharp
using Onboarding.Application.Auth.DTOs;

namespace Onboarding.Application.Common;

/// <summary>
/// Application-layer abstraction over the Keycloak OIDC token endpoint.
/// Infrastructure implements this with KeycloakTokenService via IHttpClientFactory.
/// Keeping this in Application allows unit-testing auth handlers without HTTP dependencies.
/// </summary>
public interface IKeycloakTokenService
{
    /// <summary>
    /// Exchanges user credentials for a token pair using ROPC grant (grant_type=password).
    /// Throws KeycloakAuthException if credentials are invalid (Keycloak returns 401).
    /// </summary>
    Task<TokenResponse> ExchangePasswordAsync(
        string email, string password, CancellationToken ct = default);

    /// <summary>
    /// Exchanges a refresh token for a new token pair (grant_type=refresh_token).
    /// Throws KeycloakAuthException if the refresh token is invalid or expired.
    /// </summary>
    Task<TokenResponse> RefreshTokenAsync(
        string refreshToken, CancellationToken ct = default);
}
```

Criar diretório `src/Onboarding.Application/Auth/DTOs/` se não existir, depois criar `TokenResponse.cs`:

```csharp
namespace Onboarding.Application.Auth.DTOs;

/// <summary>
/// Token response returned by Keycloak OIDC token endpoint.
/// Maps to Keycloak JSON: access_token, refresh_token, expires_in, token_type, refresh_expires_in, scope.
/// [Claude's Discretion] — all Keycloak fields included for full compatibility with Phase 9 frontend.
/// </summary>
public sealed record TokenResponse(
    string AccessToken,
    string RefreshToken,
    int ExpiresIn,
    string TokenType,
    int RefreshExpiresIn,
    string Scope);
```

Modificar `src/Onboarding.Domain/Repositories/IClientRepository.cs` — adicionar `GetByEmailAsync` ao final da interface (ANTES do fechamento `}`):

```csharp
// Adicionar após DeleteAsync:
/// <summary>
/// Returns the client with the given email address, or null if not found.
/// Used by GET /api/clients/me to look up the authenticated user's profile (D-07, D-08).
/// Email is normalized to lowercase before querying (matches Email value object behavior).
/// </summary>
Task<Client?> GetByEmailAsync(string email, CancellationToken ct = default);
```

Modificar `src/Onboarding.Infrastructure/Repositories/ClientRepository.cs` — adicionar implementação ao final da classe (ANTES do fechamento `}`):

```csharp
// Adicionar após DeleteAsync:
public async Task<Client?> GetByEmailAsync(string email, CancellationToken ct = default)
{
    // Normalize to lowercase to match Email value object behavior (same as ExistsByEmailAsync)
    var normalized = email.ToLowerInvariant();
    return await _db.Clients
        .FirstOrDefaultAsync(c => c.Email.Value == normalized, ct);
}
```
  </action>
  <verify>
    <automated>dotnet build src/Onboarding.Domain/Onboarding.Domain.csproj && dotnet build src/Onboarding.Application/Onboarding.Application.csproj && dotnet build src/Onboarding.Infrastructure/Onboarding.Infrastructure.csproj</automated>
  </verify>
  <acceptance_criteria>
    - src/Onboarding.Application/Common/IKeycloakTokenService.cs existe
    - IKeycloakTokenService.cs contém "Task&lt;TokenResponse&gt; ExchangePasswordAsync("
    - IKeycloakTokenService.cs contém "Task&lt;TokenResponse&gt; RefreshTokenAsync("
    - src/Onboarding.Application/Auth/DTOs/TokenResponse.cs existe
    - TokenResponse.cs contém "string AccessToken" e "string RefreshToken" e "int ExpiresIn"
    - TokenResponse.cs contém "int RefreshExpiresIn" e "string Scope"
    - src/Onboarding.Domain/Repositories/IClientRepository.cs contém "GetByEmailAsync"
    - src/Onboarding.Infrastructure/Repositories/ClientRepository.cs contém "GetByEmailAsync"
    - ClientRepository.cs GetByEmailAsync contém "ToLowerInvariant()"
    - `dotnet build src/Onboarding.Domain/Onboarding.Domain.csproj` sai com código 0
    - `dotnet build src/Onboarding.Application/Onboarding.Application.csproj` sai com código 0
    - `dotnet build src/Onboarding.Infrastructure/Onboarding.Infrastructure.csproj` sai com código 0
  </acceptance_criteria>
  <done>Contratos de autenticação criados. IKeycloakTokenService, TokenResponse e GetByEmailAsync existem. Camadas Domain, Application e Infrastructure compilam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instalar JwtBearer e configurar AddAuthentication em Program.cs</name>
  <files>
    src/Onboarding.API/Onboarding.API.csproj
    src/Onboarding.API/Program.cs
  </files>
  <read_first>
    - src/Onboarding.API/Onboarding.API.csproj (packages existentes — verificar se JwtBearer já presente)
    - src/Onboarding.API/Program.cs (pipeline atual — encontrar onde inserir AddAuthentication e UseAuthentication)
    - .planning/phases/06-authentication-api/06-RESEARCH.md §Padrão 1 (código exato de configuração AddJwtBearer)
    - .planning/phases/06-authentication-api/06-RESEARCH.md §Pitfall 1 (ordem obrigatória do middleware)
  </read_first>
  <behavior>
    Após este task: GET /api/clients/me sem token retorna 401 (mesmo sem o controller existir ainda — o middleware bloqueia antes).
    AddAuthentication é registrado ANTES de AddControllers no DI container.
    UseAuthentication() é chamado ANTES de UseAuthorization() ANTES de MapControllers() no pipeline.
  </behavior>
  <action>
Passo 1 — Instalar pacote (executar na raiz do repo):
```bash
dotnet add src/Onboarding.API/Onboarding.API.csproj package Microsoft.AspNetCore.Authentication.JwtBearer --version 10.0.5
```

Passo 2 — Modificar `src/Onboarding.API/Program.cs`:

Adicionar using no topo do arquivo (após os outros usings existentes):
```csharp
using Microsoft.AspNetCore.Authentication.JwtBearer;
```

Adicionar bloco de autenticação/autorização APÓS `builder.Services.AddDistributedMemoryCache()` e ANTES de `builder.Services.AddControllers()`:

```csharp
// Authentication — JWT Bearer with Keycloak OIDC auto-discovery (D-04)
// Authority reads /.well-known/openid-configuration lazily on first request
builder.Services.AddAuthentication(JwtBearerDefaults.AuthenticationScheme)
    .AddJwtBearer(options =>
    {
        // D-04: Auto-discovery via OIDC metadata endpoint
        options.Authority = builder.Configuration["Keycloak:RealmUrl"]
            ?? throw new InvalidOperationException("Keycloak:RealmUrl not configured.");

        // D-05: ROPC tokens have aud: ["account"] — not our API audience. Disable to avoid 401 false positive.
        options.TokenValidationParameters.ValidateAudience = false;

        // D-04: Preserve Keycloak claim names as-is (e.g. "email" stays "email", not XML namespace URI)
        // Without this, User.FindFirst("email") returns null — Pitfall 2 from RESEARCH.md
        options.MapInboundClaims = false;
    });

builder.Services.AddAuthorization();
```

Modificar pipeline de request — localizar `app.UseSerilogRequestLogging();` e `app.MapControllers();` e inserir entre eles:

```csharp
app.UseSerilogRequestLogging();
app.UseAuthentication();   // D-04: populate HttpContext.User — MUST come before UseAuthorization
app.UseAuthorization();    // D-06: enforce [Authorize] attributes — MUST come after UseAuthentication
app.MapControllers();
```

ATENÇÃO: verificar se UseAuthentication e UseAuthorization já existem antes de adicionar — se existirem, NÃO duplicar.
  </action>
  <verify>
    <automated>dotnet build src/Onboarding.API/Onboarding.API.csproj 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/Onboarding.API/Onboarding.API.csproj contém `PackageReference Include="Microsoft.AspNetCore.Authentication.JwtBearer" Version="10.0.5"`
    - src/Onboarding.API/Program.cs contém "AddAuthentication(JwtBearerDefaults.AuthenticationScheme)"
    - src/Onboarding.API/Program.cs contém "options.Authority = builder.Configuration[\"Keycloak:RealmUrl\"]"
    - src/Onboarding.API/Program.cs contém "ValidateAudience = false"
    - src/Onboarding.API/Program.cs contém "MapInboundClaims = false"
    - src/Onboarding.API/Program.cs contém "builder.Services.AddAuthorization()"
    - src/Onboarding.API/Program.cs contém "app.UseAuthentication();"
    - src/Onboarding.API/Program.cs contém "app.UseAuthorization();"
    - No Program.cs, UseAuthentication() aparece ANTES de UseAuthorization() (linha menor)
    - No Program.cs, UseAuthorization() aparece ANTES de MapControllers() (linha menor)
    - `dotnet build src/Onboarding.API/Onboarding.API.csproj` sai com código 0
    - `dotnet build tests/Onboarding.API.Tests/Onboarding.API.Tests.csproj` sai com código 0 (AuthTestApiFactory do Plan 01 agora compila)
  </acceptance_criteria>
  <done>JWT Bearer configurado em Program.cs. Microsoft.AspNetCore.Authentication.JwtBearer 10.0.5 instalado. Middleware de autenticação e autorização wired na ordem correta. AuthTestApiFactory compila.</done>
</task>

</tasks>

<threat_model>
  <threat id="T-6-02" severity="critical">
    <description>Keycloak:RealmUrl ausente ou incorreto na configuração causa InvalidOperationException na inicialização ou IDX20803 ("Unable to obtain configuration") no primeiro request — API totalmente inoperante.</description>
    <mitigation>Adicionado ?? throw new InvalidOperationException("Keycloak:RealmUrl not configured.") na configuração do Authority (linha explícita na Task 2). appsettings.json já tem Keycloak:RealmUrl de fases anteriores — verificado no código existente de Program.cs.</mitigation>
  </threat>
  <threat id="T-6-03" severity="high">
    <description>UseAuthorization() antes de UseAuthentication() faz com que rotas [Authorize] retornem 401 mesmo com token válido — falha silenciosa de segurança.</description>
    <mitigation>Ordem explícita documentada na task action: UseAuthentication() → UseAuthorization() → MapControllers(). acceptance_criteria verifica que UseAuthentication aparece em linha anterior à UseAuthorization no arquivo.</mitigation>
  </threat>
  <threat id="T-6-04" severity="high">
    <description>MapInboundClaims = true (default) causa User.FindFirst("email") retornar null — autenticação aparentemente funciona mas lookup do cliente em GET /api/clients/me falha para todos os usuários.</description>
    <mitigation>MapInboundClaims = false explicitamente configurado na Task 2. acceptance_criteria verifica presença no arquivo. Pitfall 2 do RESEARCH.md documentado.</mitigation>
  </threat>
  <threat id="T-6-05" severity="medium">
    <description>ValidateAudience = true rejeitaria todos os tokens ROPC do Keycloak com "audience 'account' is invalid" — 401 para todos os requests autenticados.</description>
    <mitigation>ValidateAudience = false configurado por D-05. acceptance_criteria verifica presença. Behavior esperado documentado no RESEARCH.md Pitfall 3.</mitigation>
  </threat>
</threat_model>

<verification>
Após executar este plano:

```bash
# Todos os projetos compilam
dotnet build Onboarding.sln

# Testes de infraestrutura compilam (AuthTestApiFactory agora resolve IKeycloakTokenService)
dotnet build tests/Onboarding.API.Tests/Onboarding.API.Tests.csproj

# Verificar configuração JWT no Program.cs
grep -n "AddAuthentication\|UseAuthentication\|UseAuthorization\|ValidateAudience\|MapInboundClaims" src/Onboarding.API/Program.cs

# Verificar que GetByEmailAsync existe na interface e implementação
grep -n "GetByEmailAsync" src/Onboarding.Domain/Repositories/IClientRepository.cs
grep -n "GetByEmailAsync" src/Onboarding.Infrastructure/Repositories/ClientRepository.cs
```
</verification>

<success_criteria>
- `dotnet build Onboarding.sln` passa sem erros
- IKeycloakTokenService, TokenResponse e GetByEmailAsync existem como contratos
- AddAuthentication().AddJwtBearer() com Authority, ValidateAudience=false e MapInboundClaims=false presentes em Program.cs
- UseAuthentication() antes de UseAuthorization() antes de MapControllers() no pipeline
- Stubs RED do Plan 01 ainda falham (nenhum controller implementado neste plano)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-authentication-api/06-02-SUMMARY.md` com:
- Arquivos criados/modificados
- Contratos disponíveis para o Plan 03
- Configuração JWT Bearer (Authority, flags)
- Nota: nenhum controller ou handler implementado — Plan 03 faz a implementação
</output>

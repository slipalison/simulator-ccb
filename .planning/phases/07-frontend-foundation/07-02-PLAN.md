---
phase: 07-frontend-foundation
plan: 02
type: execute
wave: 3
depends_on:
  - 07-01-PLAN.md
files_modified:
  - frontend/src/components/atoms/AppButton.tsx
  - frontend/src/components/molecules/LabeledField.tsx
  - frontend/src/components/organisms/ExampleForm.tsx
  - frontend/src/components/templates/PageLayout.tsx
  - frontend/src/components/pages/HomePage.tsx
  - frontend/src/components/pages/NotFoundPage.tsx
  - frontend/src/router.ts
  - frontend/src/main.tsx
autonomous: true
requirements:
  - FRONT-01
  - FRONT-03

must_haves:
  truths:
    - "Cada nível do Atomic Design tem ao menos um componente .tsx"
    - "TanStack Router está configurado com notFoundComponent no rootRoute"
    - "Navegar para rota desconhecida renderiza NotFoundPage (sem lançar exceção)"
    - "RouterProvider está conectado ao main.tsx — a aplicação roteia corretamente"
    - "Tipos do router estão registrados via declare module — navegação é type-safe"
  artifacts:
    - path: "frontend/src/components/atoms/AppButton.tsx"
      provides: "Atom — wrapper sobre shadcn Button com variante padrão do projeto"
      exports: ["AppButton"]
    - path: "frontend/src/components/molecules/LabeledField.tsx"
      provides: "Molecule — Label + Input + mensagem de erro inline"
      exports: ["LabeledField"]
    - path: "frontend/src/components/organisms/ExampleForm.tsx"
      provides: "Organism — estrutura de formulário com RHF (sem lógica real ainda)"
      exports: ["ExampleForm"]
    - path: "frontend/src/components/templates/PageLayout.tsx"
      provides: "Template — header + main + footer como slots (children)"
      exports: ["PageLayout"]
    - path: "frontend/src/components/pages/HomePage.tsx"
      provides: "Page — home placeholder usando PageLayout"
      exports: ["HomePage"]
    - path: "frontend/src/components/pages/NotFoundPage.tsx"
      provides: "Page — 404 com mensagem clara, usada como notFoundComponent"
      exports: ["NotFoundPage"]
    - path: "frontend/src/router.ts"
      provides: "Router TanStack com rootRoute + indexRoute + notFoundComponent"
      exports: ["router"]
    - path: "frontend/src/main.tsx"
      provides: "Entry point atualizado com RouterProvider"
      contains: "RouterProvider"
  key_links:
    - from: "frontend/src/router.ts"
      to: "frontend/src/components/pages/NotFoundPage.tsx"
      via: "notFoundComponent: NotFoundPage no createRootRoute"
      pattern: "notFoundComponent"
    - from: "frontend/src/main.tsx"
      to: "frontend/src/router.ts"
      via: "import { router } from './router'"
      pattern: "RouterProvider"
    - from: "frontend/src/components/organisms/ExampleForm.tsx"
      to: "frontend/src/components/molecules/LabeledField.tsx"
      via: "import { LabeledField } from '@/components/molecules/LabeledField'"
      pattern: "LabeledField"
---

<objective>
Criar a estrutura Atomic Design completa com um componente exemplo em cada nível,
e configurar o TanStack Router v1 com roteamento type-safe e componente 404.

Esta wave entrega os dois critérios de sucesso mais visíveis da Phase 7:
1. Atomic Design com 5 níveis populados
2. Rota desconhecida mostra 404 tipado (não crash)

Purpose: Sem esta wave, os testes de estrutura e roteamento permanecem RED.
As waves anteriores apenas prepararam a infraestrutura técnica.

Output: 6 componentes (um por nível Atomic Design) + router.ts + main.tsx atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-frontend-foundation/07-CONTEXT.md
@.planning/phases/07-frontend-foundation/07-RESEARCH.md
@frontend/src/main.tsx
@frontend/src/components/ui/button.tsx
@frontend/src/components/ui/input.tsx
@frontend/src/components/ui/label.tsx
</context>

<interfaces>
<!-- Contratos que esta wave cria. Waves seguintes dependem destes exports. -->

Atom — AppButton.tsx:
```typescript
export interface AppButtonProps extends React.ComponentProps<typeof Button> {}
export function AppButton(props: AppButtonProps): JSX.Element
```

Molecule — LabeledField.tsx:
```typescript
export interface LabeledFieldProps {
  id: string;
  label: string;
  error?: string;
  inputProps?: React.ComponentProps<typeof Input>;
}
export function LabeledField(props: LabeledFieldProps): JSX.Element
```

Template — PageLayout.tsx:
```typescript
export interface PageLayoutProps {
  children: React.ReactNode;
}
export function PageLayout(props: PageLayoutProps): JSX.Element
```

Router (src/router.ts):
```typescript
export const router: ReturnType<typeof createRouter>
// declare module "@tanstack/react-router" { interface Register { router: typeof router } }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar componentes Atomic Design (atoms, molecules, organisms, templates, pages)</name>
  <files>
    frontend/src/components/atoms/AppButton.tsx
    frontend/src/components/molecules/LabeledField.tsx
    frontend/src/components/organisms/ExampleForm.tsx
    frontend/src/components/templates/PageLayout.tsx
    frontend/src/components/pages/HomePage.tsx
    frontend/src/components/pages/NotFoundPage.tsx
  </files>
  <action>
Criar os diretórios e componentes. Todos os componentes usam shadcn/ui e Tailwind v4.
Filosofia: simples e minimalista — sem animações, sem temas customizados.

REGRA CRÍTICA: `src/components/ui/` é gerenciado EXCLUSIVAMENTE pelo CLI shadcn.
Nunca editar arquivos em ui/ manualmente.

### ATOM — frontend/src/components/atoms/AppButton.tsx

```tsx
import { Button } from "@/components/ui/button";
import type { ComponentProps } from "react";

export type AppButtonProps = ComponentProps<typeof Button>;

/**
 * Atom: wrapper minimalista sobre shadcn Button.
 * Usa a variante "default" do projeto. Extensível via props.
 */
export function AppButton({ children, ...props }: AppButtonProps) {
  return <Button {...props}>{children}</Button>;
}
```

### MOLECULE — frontend/src/components/molecules/LabeledField.tsx

```tsx
import { Label } from "@/components/ui/label";
import { Input } from "@/components/ui/input";
import type { ComponentProps } from "react";

export interface LabeledFieldProps {
  id: string;
  label: string;
  error?: string;
  inputProps?: ComponentProps<typeof Input>;
}

/**
 * Molecule: Label + Input + mensagem de erro inline.
 * Erro exibido com role="alert" para acessibilidade.
 */
export function LabeledField({ id, label, error, inputProps }: LabeledFieldProps) {
  return (
    <div className="space-y-1">
      <Label htmlFor={id}>{label}</Label>
      <Input
        id={id}
        aria-invalid={!!error}
        aria-describedby={error ? `${id}-error` : undefined}
        {...inputProps}
      />
      {error && (
        <p id={`${id}-error`} role="alert" className="text-sm text-destructive">
          {error}
        </p>
      )}
    </div>
  );
}
```

### ORGANISM — frontend/src/components/organisms/ExampleForm.tsx

```tsx
import { LabeledField } from "@/components/molecules/LabeledField";
import { AppButton } from "@/components/atoms/AppButton";

/**
 * Organism: estrutura de formulário de exemplo.
 * Sem lógica RHF + Zod nesta wave — wiring completo em 07-03-PLAN.md.
 * Demonstra composição de molecules dentro de um organism.
 */
export function ExampleForm() {
  return (
    <form className="space-y-4 w-full max-w-sm">
      <LabeledField
        id="name"
        label="Nome"
        inputProps={{ placeholder: "Seu nome completo" }}
      />
      <LabeledField
        id="email"
        label="Email"
        inputProps={{ type: "email", placeholder: "seu@email.com" }}
      />
      <AppButton type="submit" className="w-full">
        Enviar
      </AppButton>
    </form>
  );
}
```

NOTA: RHF + Zod + erros inline são adicionados em 07-03-PLAN.md. Por ora ExampleForm
é estático — isso é intencional para manter esta wave focada em estrutura + routing.

### TEMPLATE — frontend/src/components/templates/PageLayout.tsx

```tsx
import type { ReactNode } from "react";

interface PageLayoutProps {
  children: ReactNode;
}

/**
 * Template: layout de página com header, main e footer como slots.
 * Stateless — recebe children e os posiciona. Sem lógica de negócio.
 */
export function PageLayout({ children }: PageLayoutProps) {
  return (
    <div className="min-h-screen flex flex-col bg-background">
      <header className="border-b px-6 py-4">
        <span className="font-semibold text-foreground">Onboarding</span>
      </header>
      <main className="flex-1 px-6 py-8">{children}</main>
      <footer className="border-t px-6 py-4 text-sm text-muted-foreground text-center">
        &copy; {new Date().getFullYear()} Onboarding
      </footer>
    </div>
  );
}
```

### PAGE — frontend/src/components/pages/HomePage.tsx

```tsx
import { PageLayout } from "@/components/templates/PageLayout";
import { ExampleForm } from "@/components/organisms/ExampleForm";

/**
 * Page: home placeholder.
 * Instancia PageLayout com ExampleForm como conteúdo demonstrativo.
 * Formulário com RHF + Zod real será conectado em 07-03-PLAN.md.
 */
export function HomePage() {
  return (
    <PageLayout>
      <div className="flex flex-col items-center gap-8">
        <div className="text-center space-y-2">
          <h1 className="text-2xl font-bold text-foreground">Bem-vindo</h1>
          <p className="text-muted-foreground">
            Plataforma de cadastro de clientes PF e PJ
          </p>
        </div>
        <ExampleForm />
      </div>
    </PageLayout>
  );
}
```

### PAGE — frontend/src/components/pages/NotFoundPage.tsx

```tsx
import { PageLayout } from "@/components/templates/PageLayout";
import { AppButton } from "@/components/atoms/AppButton";
import { Link } from "@tanstack/react-router";

/**
 * Page: 404 — renderizada pelo notFoundComponent do rootRoute.
 * Exibe mensagem clara e link de volta para home.
 */
export function NotFoundPage() {
  return (
    <PageLayout>
      <div className="flex flex-col items-center justify-center gap-6 py-16 text-center">
        <h1 className="text-6xl font-bold text-foreground">404</h1>
        <p className="text-xl text-muted-foreground">Página não encontrada</p>
        <p className="text-sm text-muted-foreground max-w-sm">
          A rota que você tentou acessar não existe neste aplicativo.
        </p>
        <Link to="/">
          <AppButton variant="outline">Voltar para o início</AppButton>
        </Link>
      </div>
    </PageLayout>
  );
}
```
  </action>
  <verify>
    <automated>cd frontend && node -e "const fs=require('fs'); const files=['src/components/atoms/AppButton.tsx','src/components/molecules/LabeledField.tsx','src/components/organisms/ExampleForm.tsx','src/components/templates/PageLayout.tsx','src/components/pages/HomePage.tsx','src/components/pages/NotFoundPage.tsx']; files.forEach(f=>console.log(f+':',fs.existsSync(f)?'OK':'MISSING'))"</automated>
  </verify>
  <done>
Todos os 6 componentes existem nos diretórios corretos do Atomic Design.
Cada componente exporta sua função principal.
ExampleForm usa LabeledField e AppButton (composição correta).
NotFoundPage exibe texto "404" e "Página não encontrada".
  </done>
</task>

<task type="auto">
  <name>Task 2: Configurar TanStack Router v1 e atualizar main.tsx com RouterProvider</name>
  <files>
    frontend/src/router.ts
    frontend/src/main.tsx
  </files>
  <action>
### Criar frontend/src/router.ts

```typescript
import {
  createRootRoute,
  createRoute,
  createRouter,
  Outlet,
} from "@tanstack/react-router";
import { HomePage } from "@/components/pages/HomePage";
import { NotFoundPage } from "@/components/pages/NotFoundPage";

// Root route com notFoundComponent para roteamento type-safe de 404
// NOTA: NotFoundRoute (classe) está depreciada — usar notFoundComponent no rootRoute
const rootRoute = createRootRoute({
  component: () => <Outlet />,
  notFoundComponent: NotFoundPage,
});

// Rota index: /
const indexRoute = createRoute({
  getParentRoute: () => rootRoute,
  path: "/",
  component: HomePage,
});

// Árvore de rotas
const routeTree = rootRoute.addChildren([indexRoute]);

// Instância do router
export const router = createRouter({ routeTree });

// Registro obrigatório para type safety do TypeScript
// Sem isso, useNavigate, Link e outros hooks não são type-checked
declare module "@tanstack/react-router" {
  interface Register {
    router: typeof router;
  }
}
```

### Atualizar frontend/src/main.tsx

Substituir o conteúdo atual (que renderiza App diretamente) pelo RouterProvider:

```tsx
import { createRoot } from "react-dom/client";
import { RouterProvider } from "@tanstack/react-router";
import { router } from "@/router";
import "@/globals.css";

const rootEl = document.getElementById("root");
if (!rootEl) throw new Error("Elemento #root não encontrado no DOM");

createRoot(rootEl).render(<RouterProvider router={router} />);
```

### Verificar estrutura TypeScript

Verificar que o TypeScript consegue resolver os imports (sem erros de tipo críticos):

```bash
cd frontend && npx tsc --noEmit 2>&1 | grep -v "node_modules" | head -20 || true
```

Erros relacionados a módulos não encontrados são esperados se as dependências ainda
não foram instaladas — verificar que os imports de @tanstack/react-router resolvem:

```bash
ls frontend/node_modules/@tanstack/react-router/dist/ | head -5
```
  </action>
  <verify>
    <automated>cd frontend && node -e "const fs=require('fs'); const r=fs.readFileSync('src/router.ts','utf8'); console.log('notFoundComponent:', r.includes('notFoundComponent')); console.log('declare module:', r.includes('declare module')); console.log('NotFoundPage:', r.includes('NotFoundPage')); const m=fs.readFileSync('src/main.tsx','utf8'); console.log('RouterProvider:', m.includes('RouterProvider')); console.log('globals.css:', m.includes('globals.css'))"</automated>
  </verify>
  <done>
frontend/src/router.ts existe com notFoundComponent: NotFoundPage no rootRoute.
declare module "@tanstack/react-router" com Register está presente.
frontend/src/main.tsx usa RouterProvider com o router importado de @/router.
import "@/globals.css" presente no main.tsx.
O stub routing.test.tsx passa quando o router é injetado nos testes (feito em 07-03).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → React router | URL digitada pelo usuário é roteada pelo TanStack Router |
| componente → DOM | JSX renderizado pelo React sem inputs externos nesta wave |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | router (path matching) | mitigate | TanStack Router usa type-safe route tree; paths registrados explicitamente — sem eval ou path injection |
| T-07-02-02 | Information Disclosure | NotFoundPage | accept | 404 exibe apenas mensagem genérica; não vaza detalhes de rotas internas |
| T-07-02-03 | Elevation of Privilege | Link component (NotFoundPage) | accept | Link aponta apenas para "/" hardcoded; sem redirecionamento baseado em input do usuário |
</threat_model>

<verification>
```bash
# 1. Verificar todos os componentes Atomic Design existem
for f in \
  frontend/src/components/atoms/AppButton.tsx \
  frontend/src/components/molecules/LabeledField.tsx \
  frontend/src/components/organisms/ExampleForm.tsx \
  frontend/src/components/templates/PageLayout.tsx \
  frontend/src/components/pages/HomePage.tsx \
  frontend/src/components/pages/NotFoundPage.tsx; do
  [ -f "$f" ] && echo "OK: $f" || echo "MISSING: $f"
done

# 2. Verificar router.ts tem notFoundComponent
grep "notFoundComponent" frontend/src/router.ts

# 3. Verificar declare module para type safety
grep "declare module" frontend/src/router.ts

# 4. Verificar RouterProvider em main.tsx
grep "RouterProvider" frontend/src/main.tsx
```
</verification>

<success_criteria>
- src/components/{atoms,molecules,organisms,templates,pages}/ cada um com pelo menos 1 .tsx
- router.ts: notFoundComponent: NotFoundPage presente no createRootRoute
- router.ts: declare module "@tanstack/react-router" com Register presente
- main.tsx: RouterProvider conectado ao router exportado de @/router
- NotFoundPage exibe "404" e "Página não encontrada" como texto visível
- Nenhum componente em src/components/ui/ foi editado manualmente
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/07-frontend-foundation/07-frontend-foundation-02-SUMMARY.md`
seguindo o template em @$HOME/.claude/get-shit-done/templates/summary.md
</output>

---
phase: 30-audit-log-admin-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Onboarding.Application/Common/IAuditService.cs
  - src/Onboarding.Infrastructure/Services/AuditService.cs
  - src/Onboarding.Application/Admin/Commands/BlockUserCommand.cs
  - src/Onboarding.Application/Admin/Commands/UnblockUserCommand.cs
  - src/Onboarding.Application/Admin/Commands/UpdateUserCommand.cs
  - src/Onboarding.Application/Admin/Commands/DeleteUserCommand.cs
  - src/Onboarding.Application/Admin/Commands/CreateAdminCommand.cs
  - src/Onboarding.Application/DependencyInjection.cs
  - src/Onboarding.Infrastructure/DependencyInjection.cs
  - src/Onboarding.Domain/Aggregates/Audit/AuditLog.cs
  - src/Onboarding.Domain/Common/AuditActions.cs
  - src/Onboarding.Domain/Repositories/IAuditLogRepository.cs
  - src/Onboarding.Infrastructure/Repositories/AuditLogRepository.cs
  - src/Onboarding.Infrastructure/Persistence/Configurations/AuditLogConfiguration.cs
  - src/Onboarding.Infrastructure/Persistence/AppDbContext.cs
  - src/Onboarding.Infrastructure/Persistence/Migrations/
  - tests/Onboarding.API.Tests/Admin/AdminTestFactory.cs
  - tests/Onboarding.API.Tests/Admin/AdminFullFlowTests.cs
  - tests/Onboarding.Domain.Tests/Aggregates/AuditLogTests.cs
  - tests/Onboarding.Domain.Tests/Application/Commands/BlockUserCommandHandlerTests.cs
autonomous: true
requirements:
  - AUD-01

must_haves:
  truths:
    - "Todos os handlers de admin gravam audit via IAuditService.RecordAsync (nenhum usa IAuditLogRepository)"
    - "A tabela audit_logs não existe mais no schema (migration DropAuditLogs aplicada)"
    - "O projeto compila sem erros após remoção de AuditLog, AuditActions, IAuditLogRepository e AuditLogRepository"
    - "Os testes existentes passam após migração dos mocks de IAuditLogRepository para IAuditService"
  artifacts:
    - path: "src/Onboarding.Application/Common/IAuditService.cs"
      provides: "Interface IAuditService com RecordAsync"
      exports: ["IAuditService"]
    - path: "src/Onboarding.Infrastructure/Services/AuditService.cs"
      provides: "Implementação concreta de IAuditService"
      contains: "class AuditService : IAuditService"
    - path: "src/Onboarding.Infrastructure/Persistence/Migrations/"
      provides: "Migration DropAuditLogs que dropa tabela audit_logs"
  key_links:
    - from: "BlockUserCommand.cs / UnblockUserCommand.cs / UpdateUserCommand.cs / DeleteUserCommand.cs / CreateAdminCommand.cs"
      to: "IAuditService"
      via: "injeção de dependência no construtor"
      pattern: "IAuditService _auditService"
    - from: "AuditService"
      to: "IAdminAuditLogRepository"
      via: "injeção e chamada AddAsync + SaveChangesAsync"
      pattern: "IAdminAuditLogRepository _repo"
---

<objective>
Unificar os dois sistemas de audit log em uma única abstração IAuditService, migrar os 5 handlers que ainda usam o sistema legado (IAuditLogRepository + AuditLog), remover completamente o legado (entidade, interface, repositório, configuração EF Core) e criar a EF Core migration que dropa a tabela audit_logs.

Purpose: Antes desta fase existem dois sistemas paralelos de audit (AuditLog legado e AdminAuditLog novo). Após esta fase, existe apenas AdminAuditLog com acesso uniforme via IAuditService.
Output: IAuditService + AuditService, 5 handlers migrados, legado removido, migration DropAuditLogs gerada, testes atualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-audit-log-admin-backend/30-CONTEXT.md
@.planning/phases/30-audit-log-admin-backend/30-RESEARCH.md
@.planning/phases/30-audit-log-admin-backend/30-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar IAuditService e implementação AuditService</name>
  <files>
    src/Onboarding.Application/Common/IAuditService.cs,
    src/Onboarding.Infrastructure/Services/AuditService.cs
  </files>
  <read_first>
    - src/Onboarding.Application/Common/IKeycloakUserService.cs (padrão de interface neste namespace)
    - src/Onboarding.Domain/Aggregates/AdminAuditLog.cs (assinatura de AdminAuditLog.Create — precisa de adminUserId: Guid e adminUserName: string)
    - src/Onboarding.Domain/Repositories/IAuditLogRepository.cs (ver IAdminAuditLogRepository que permanece)
    - src/Onboarding.Application/Admin/Commands/CreateAdminCommand.cs (referência — handler já usa IAdminAuditLogRepository diretamente, padrão a ser substituído)
    - src/Onboarding.Infrastructure/DependencyInjection.cs (onde registrar AuditService)
    - src/Onboarding.Application/DependencyInjection.cs (onde registrar IAuditService → não necessário, está em Infrastructure)
  </read_first>
  <behavior>
    - IAuditService.RecordAsync com string actorSub chamado com Guid válido retorna Task sem exceção
    - IAuditService.RecordAsync com actorSub não-Guid (ex: email) faz Guid.TryParse falhar e usa Guid.Empty como adminUserId sem lançar exceção
    - AuditService chama IAdminAuditLogRepository.AddAsync exatamente 1 vez por chamada RecordAsync
    - AuditService chama IAdminAuditLogRepository.SaveChangesAsync exatamente 1 vez por chamada RecordAsync
  </behavior>
  <action>
Criar `src/Onboarding.Application/Common/IAuditService.cs`:

```csharp
namespace Onboarding.Application.Common;

/// <summary>
/// Abstração unificada para gravação de audit log administrativo.
/// Toda ação administrativa passa por aqui — nenhum handler deve injetar IAdminAuditLogRepository diretamente.
/// </summary>
public interface IAuditService
{
    Task RecordAsync(
        string actorSub,
        string actorEmail,
        Onboarding.Domain.Aggregates.Audit.ActionType action,
        Guid? targetUserId = null,
        string? targetUserName = null,
        string? details = null,
        string? ipAddress = null,
        CancellationToken ct = default);
}
```

Criar `src/Onboarding.Infrastructure/Services/AuditService.cs`:

```csharp
using Onboarding.Application.Common;
using Onboarding.Domain.Aggregates.Audit;
using Onboarding.Domain.Repositories;

namespace Onboarding.Infrastructure.Services;

/// <summary>
/// Implementação de IAuditService. Injeta IAdminAuditLogRepository, cria AdminAuditLog.Create(...) e persiste.
/// actorSub = Keycloak user ID (UUID string) ou email como fallback — Guid.TryParse trata os dois casos.
/// </summary>
public sealed class AuditService : IAuditService
{
    private readonly IAdminAuditLogRepository _repo;

    public AuditService(IAdminAuditLogRepository repo) => _repo = repo;

    public async Task RecordAsync(
        string actorSub,
        string actorEmail,
        ActionType action,
        Guid? targetUserId = null,
        string? targetUserName = null,
        string? details = null,
        string? ipAddress = null,
        CancellationToken ct = default)
    {
        var adminId = Guid.TryParse(actorSub, out var parsed) ? parsed : Guid.Empty;
        var log = AdminAuditLog.Create(adminId, actorEmail, action, targetUserId, targetUserName, details, ipAddress);
        await _repo.AddAsync(log, ct);
        await _repo.SaveChangesAsync(ct);
    }
}
```

Registrar em `src/Onboarding.Infrastructure/DependencyInjection.cs` — adicionar APÓS o registro de IAdminAuditLogRepository:
```csharp
services.AddScoped<IAuditService, AuditService>();
```
(Adicionar using `using Onboarding.Application.Common;` e `using Onboarding.Infrastructure.Services;` se necessário.)

Criar testes em `tests/Onboarding.Domain.Tests/Application/Commands/AuditServiceTests.cs`:
```csharp
using NSubstitute;
using Onboarding.Application.Common;
using Onboarding.Domain.Aggregates.Audit;
using Onboarding.Domain.Repositories;
using Onboarding.Infrastructure.Services;
using Shouldly;

namespace Onboarding.Domain.Tests.Application.Commands;

[Trait("Category", "Unit")]
public sealed class AuditServiceTests
{
    private readonly IAdminAuditLogRepository _repoMock = Substitute.For<IAdminAuditLogRepository>();
    private readonly IAuditService _sut;

    public AuditServiceTests() => _sut = new AuditService(_repoMock);

    [Fact]
    public async Task RecordAsync_WithValidGuidSub_CallsAddAndSave()
    {
        var sub = Guid.NewGuid().ToString();
        await _sut.RecordAsync(sub, "admin@test.com", ActionType.UserBlocked, Guid.NewGuid(), "Target User");
        await _repoMock.Received(1).AddAsync(Arg.Any<AdminAuditLog>(), Arg.Any<CancellationToken>());
        await _repoMock.Received(1).SaveChangesAsync(Arg.Any<CancellationToken>());
    }

    [Fact]
    public async Task RecordAsync_WithEmailAsSub_DoesNotThrow_UsesGuidEmpty()
    {
        await _sut.RecordAsync("admin@test.com", "admin@test.com", ActionType.UserBlocked, null, null);
        await _repoMock.Received(1).AddAsync(
            Arg.Is<AdminAuditLog>(l => l.AdminUserId == Guid.Empty),
            Arg.Any<CancellationToken>());
    }
}
```
  </action>
  <verify>
    <automated>cd /d/REPO/keycloak-tests && dotnet test tests/Onboarding.Domain.Tests --filter "AuditServiceTests" --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - src/Onboarding.Application/Common/IAuditService.cs existe com interface IAuditService e método RecordAsync
    - src/Onboarding.Infrastructure/Services/AuditService.cs existe com class AuditService : IAuditService
    - src/Onboarding.Infrastructure/DependencyInjection.cs contém services.AddScoped&lt;IAuditService, AuditService&gt;()
    - AuditServiceTests passa (dotnet test --filter AuditServiceTests exits 0)
  </done>
  <acceptance_criteria>
    - grep -r "IAuditService" src/Onboarding.Application/Common/IAuditService.cs returns match
    - grep "class AuditService : IAuditService" src/Onboarding.Infrastructure/Services/AuditService.cs returns match
    - grep "AddScoped.*IAuditService.*AuditService" src/Onboarding.Infrastructure/DependencyInjection.cs returns match
    - dotnet test tests/Onboarding.Domain.Tests --filter "AuditServiceTests" exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrar os 5 handlers de IAuditLogRepository para IAuditService</name>
  <files>
    src/Onboarding.Application/Admin/Commands/BlockUserCommand.cs,
    src/Onboarding.Application/Admin/Commands/UnblockUserCommand.cs,
    src/Onboarding.Application/Admin/Commands/UpdateUserCommand.cs,
    src/Onboarding.Application/Admin/Commands/DeleteUserCommand.cs,
    src/Onboarding.Application/Admin/Commands/CreateAdminCommand.cs
  </files>
  <read_first>
    - src/Onboarding.Application/Admin/Commands/BlockUserCommand.cs (padrão atual com IAuditLogRepository)
    - src/Onboarding.Application/Admin/Commands/UnblockUserCommand.cs
    - src/Onboarding.Application/Admin/Commands/UpdateUserCommand.cs (usa SnapshotBefore/SnapshotAfter — mapear para details)
    - src/Onboarding.Application/Admin/Commands/DeleteUserCommand.cs (usa SnapshotBefore/SnapshotAfter — mapear para details)
    - src/Onboarding.Application/Admin/Commands/CreateAdminCommand.cs (usa IAdminAuditLogRepository diretamente)
    - src/Onboarding.Domain/Aggregates/Audit/ActionType.cs (verificar valores: UserBlocked=10, UserUnblocked=11, UserDeleted=12, UserUpdated=13, AdminCreated=1)
    - src/Onboarding.Application/Common/IAuditService.cs (interface criada na Task 1)
  </read_first>
  <action>
Para cada handler, substituir injeção de IAuditLogRepository (ou IAdminAuditLogRepository em CreateAdminCommand) por IAuditService, e substituir o bloco de criação + persistência pelo call RecordAsync.

**BlockUserCommand.cs — trocar:**
```csharp
// REMOVER (import, campo, construtor, bloco de uso):
using Onboarding.Domain.Repositories;  // se só usado para IAuditLogRepository
private readonly IAuditLogRepository _auditLogRepository;
// ... no construtor:
IAuditLogRepository auditLogRepository,
_auditLogRepository = auditLogRepository;
// ... no HandleAsync:
var auditLog = AuditLog.Create(...);
await _auditLogRepository.AddAsync(auditLog, ct);
await _auditLogRepository.SaveChangesAsync(ct);

// ADICIONAR:
private readonly IAuditService _auditService;
// ... no construtor:
IAuditService auditService,
_auditService = auditService;
// ... no HandleAsync (substituir bloco de audit):
await _auditService.RecordAsync(
    actorSub: command.AdminSub,
    actorEmail: command.AdminEmail,
    action: ActionType.UserBlocked,
    targetUserId: command.UserId,
    targetUserName: client.Email.Value,
    ct: ct);
```

**UnblockUserCommand.cs — mesmo padrão:**
```csharp
await _auditService.RecordAsync(
    actorSub: command.AdminSub,
    actorEmail: command.AdminEmail,
    action: ActionType.UserUnblocked,
    targetUserId: command.UserId,
    targetUserName: client.Email.Value,
    ct: ct);
```

**UpdateUserCommand.cs — SnapshotBefore/After vão em details como JSON:**
```csharp
// Serializar ANTES do Update (antes já existe no handler):
var before = JsonSerializer.Serialize(new { client.Name, Email = client.Email.Value, Phone = client.Phone.Value, client.RazaoSocial });
// ... apply domain update ...
var after = JsonSerializer.Serialize(new { client.Name, Email = client.Email.Value, Phone = client.Phone.Value, client.RazaoSocial });
// Persist ...

// Substituir bloco de audit:
await _auditService.RecordAsync(
    actorSub: command.AdminSub,
    actorEmail: command.AdminEmail,
    action: ActionType.UserUpdated,
    targetUserId: command.UserId,
    targetUserName: client.Email.Value,
    details: JsonSerializer.Serialize(new { Before = before, After = after }),
    ct: ct);
```
Remover import de `Onboarding.Domain.Aggregates.Audit` se AuditLog e AuditActions não são mais usados (ActionType vem do mesmo namespace, manter).

**DeleteUserCommand.cs — mesmo padrão de details com snapshot:**
```csharp
await _auditService.RecordAsync(
    actorSub: command.AdminSub,
    actorEmail: command.AdminEmail,
    action: ActionType.UserDeleted,
    targetUserId: command.UserId,
    targetUserName: originalEmail,
    details: JsonSerializer.Serialize(new { Before = before, After = after }),
    ct: ct);
```

**CreateAdminCommand.cs — trocar IAdminAuditLogRepository por IAuditService:**
```csharp
// REMOVER:
private readonly IAdminAuditLogRepository _adminAuditLogRepository;
// ... construtor e uso de:
var auditLog = AdminAuditLog.Create(...);
await _adminAuditLogRepository.AddAsync(auditLog, ct);
await _adminAuditLogRepository.SaveChangesAsync(ct);

// ADICIONAR:
private readonly IAuditService _auditService;
// ... construtor
// ... substituir bloco de audit por:
await _auditService.RecordAsync(
    actorSub: creatorKeycloakId,
    actorEmail: command.CreatorEmail,
    action: ActionType.AdminCreated,
    targetUserId: adminId,
    targetUserName: command.FullName,
    details: $"{{\"email\": \"{command.Email}\"}}",
    ipAddress: command.IpAddress,
    ct: ct);
```
Manter a lógica de resolução de `creatorKeycloakId` que já existe no handler (não alterar essa lógica).

Em TODOS os handlers: remover o using de `Onboarding.Domain.Repositories` se IAuditLogRepository era o único tipo usado desse namespace. Verificar se IAdminRepository ou IClientRepository também estão no mesmo namespace — se sim, manter o using e apenas remover o campo/parâmetro IAuditLogRepository.

Verificar se `using Onboarding.Domain.Aggregates.Audit;` pode ser reduzido (AuditLog e AuditActions não existirão mais, mas ActionType ainda existe aí — manter).
  </action>
  <verify>
    <automated>cd /d/REPO/keycloak-tests && dotnet build src/Onboarding.Application --no-incremental 2>&1 | grep -E "(error|warning CS)" | head -20</automated>
  </verify>
  <done>
    - Nenhum dos 5 handlers contém "IAuditLogRepository" nem "IAdminAuditLogRepository" como campo injetado
    - Nenhum dos 5 handlers contém "AuditLog.Create(" nem "AuditActions."
    - Todos os 5 handlers contém "_auditService.RecordAsync("
    - dotnet build src/Onboarding.Application retorna 0 erros
  </done>
  <acceptance_criteria>
    - grep -r "IAuditLogRepository" src/Onboarding.Application/Admin/Commands/ returns no results
    - grep -r "IAdminAuditLogRepository" src/Onboarding.Application/Admin/Commands/ returns no results
    - grep -r "_auditService.RecordAsync" src/Onboarding.Application/Admin/Commands/ returns 5 matches (um por arquivo)
    - grep -r "AuditActions\." src/Onboarding.Application/Admin/Commands/ returns no results
    - grep -r "AuditLog\.Create" src/Onboarding.Application/Admin/Commands/ returns no results
    - dotnet build src/Onboarding.Application --no-incremental exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Remover legado (AuditLog, AuditActions, IAuditLogRepository, AuditLogRepository) e criar migration DropAuditLogs</name>
  <files>
    src/Onboarding.Domain/Aggregates/Audit/AuditLog.cs,
    src/Onboarding.Domain/Common/AuditActions.cs,
    src/Onboarding.Domain/Repositories/IAuditLogRepository.cs,
    src/Onboarding.Infrastructure/Repositories/AuditLogRepository.cs,
    src/Onboarding.Infrastructure/Persistence/Configurations/AuditLogConfiguration.cs,
    src/Onboarding.Infrastructure/Persistence/AppDbContext.cs,
    src/Onboarding.Infrastructure/DependencyInjection.cs,
    tests/Onboarding.API.Tests/Admin/AdminTestFactory.cs,
    tests/Onboarding.API.Tests/Admin/AdminFullFlowTests.cs,
    tests/Onboarding.Domain.Tests/Aggregates/AuditLogTests.cs
  </files>
  <read_first>
    - src/Onboarding.Domain/Aggregates/Audit/AuditLog.cs (arquivo a deletar)
    - src/Onboarding.Domain/Repositories/IAuditLogRepository.cs (interfaces IAuditLogRepository e IAdminAuditLogRepository — remover apenas IAuditLogRepository, manter IAdminAuditLogRepository)
    - src/Onboarding.Infrastructure/Persistence/AppDbContext.cs (remover DbSet&lt;AuditLog&gt; e ApplyConfiguration&lt;AuditLogConfiguration&gt;)
    - src/Onboarding.Infrastructure/DependencyInjection.cs (remover services.AddScoped&lt;IAuditLogRepository, AuditLogRepository&gt;)
    - tests/Onboarding.API.Tests/Admin/AdminTestFactory.cs (substituir IAuditLogRepository mock por IAuditService mock)
    - tests/Onboarding.API.Tests/Admin/AdminFullFlowTests.cs (substituir asserções de AuditLog por IAuditService)
    - tests/Onboarding.Domain.Tests/Aggregates/AuditLogTests.cs (deletar ou reescrever para AdminAuditLog)
  </read_first>
  <action>
**Passo 1: Deletar arquivos do legado**
- Deletar `src/Onboarding.Domain/Aggregates/Audit/AuditLog.cs`
- Deletar `src/Onboarding.Infrastructure/Repositories/AuditLogRepository.cs`
- Deletar `src/Onboarding.Infrastructure/Persistence/Configurations/AuditLogConfiguration.cs`
- Verificar se existe `src/Onboarding.Domain/Common/AuditActions.cs` — se sim, deletar

**Passo 2: Limpar IAuditLogRepository do arquivo de interfaces**
No arquivo `src/Onboarding.Domain/Repositories/IAuditLogRepository.cs`:
- Remover a interface `IAuditLogRepository` (e o using de AuditLog)
- Manter a interface `IAdminAuditLogRepository` intacta
- Renomear o arquivo apenas se necessário (deixar como está ou renomear para `IAdminAuditLogRepository.cs` — preferir renomear para clareza mas não obrigatório)

**Passo 3: Limpar AppDbContext**
Em `src/Onboarding.Infrastructure/Persistence/AppDbContext.cs`:
- Remover: `public DbSet<AuditLog> AuditLogs { get; set; }` (ou similar)
- Remover: `modelBuilder.ApplyConfiguration(new AuditLogConfiguration());` (se presente no OnModelCreating)
- Remover using de AuditLog se existir

**Passo 4: Limpar DependencyInjection.cs**
Em `src/Onboarding.Infrastructure/DependencyInjection.cs`:
- Remover: `services.AddScoped<IAuditLogRepository, AuditLogRepository>();`
- Manter: `services.AddScoped<IAdminAuditLogRepository, AdminAuditLogRepository>();`
- Manter: `services.AddScoped<IAuditService, AuditService>();` (adicionado na Task 1)

**Passo 5: Atualizar AdminTestFactory**
Em `tests/Onboarding.API.Tests/Admin/AdminTestFactory.cs`:
- Remover: `public IAuditLogRepository AuditLogRepositoryMock { get; } = Substitute.For<IAuditLogRepository>();`
- Adicionar: `public IAuditService AuditServiceMock { get; } = Substitute.For<IAuditService>();`
- Remover: `services.AddScoped<IAuditLogRepository>(_ => AuditLogRepositoryMock);`
- Adicionar: `services.AddScoped<IAuditService>(_ => AuditServiceMock);`
- Adicionar using `using Onboarding.Application.Common;` se necessário

**Passo 6: Atualizar AdminFullFlowTests**
Em `tests/Onboarding.API.Tests/Admin/AdminFullFlowTests.cs`, substituir a seção de verificação do audit trail (linhas 127-136 aproximadamente):

```csharp
// 9. Verify audit trail via IAuditService — all 4 actions recorded
await _factory!.AuditServiceMock.Received(1).RecordAsync(
    Arg.Any<string>(), Arg.Any<string>(), ActionType.UserBlocked,
    clientId, Arg.Any<string?>(), Arg.Any<string?>(), Arg.Any<string?>(), Arg.Any<CancellationToken>());

await _factory.AuditServiceMock.Received(1).RecordAsync(
    Arg.Any<string>(), Arg.Any<string>(), ActionType.UserUnblocked,
    clientId, Arg.Any<string?>(), Arg.Any<string?>(), Arg.Any<string?>(), Arg.Any<CancellationToken>());

await _factory.AuditServiceMock.Received(1).RecordAsync(
    Arg.Any<string>(), Arg.Any<string>(), ActionType.UserUpdated,
    clientId, Arg.Any<string?>(), Arg.Any<string?>(), Arg.Any<string?>(), Arg.Any<CancellationToken>());

await _factory.AuditServiceMock.Received(1).RecordAsync(
    Arg.Any<string>(), Arg.Any<string>(), ActionType.UserDeleted,
    clientId, Arg.Any<string?>(), Arg.Any<string?>(), Arg.Any<string?>(), Arg.Any<CancellationToken>());
```
Adicionar using `using Onboarding.Domain.Aggregates.Audit;` se necessário.
Remover using de `Onboarding.Domain.Repositories` se IAuditLogRepository era o único tipo referenciado.

**Passo 7: Lidar com AuditLogTests.cs**
O arquivo `tests/Onboarding.Domain.Tests/Aggregates/AuditLogTests.cs` testa a entidade AuditLog legada. Com a remoção de AuditLog, esse arquivo não compilará. Deletar o arquivo.

**Passo 8: Gerar migration EF Core**
```bash
cd /d/REPO/keycloak-tests/src/Onboarding.API
dotnet ef migrations add DropAuditLogs --project ../Onboarding.Infrastructure --output-dir Persistence/Migrations
```
Verificar o arquivo gerado: deve conter `migrationBuilder.DropTable(name: "audit_logs")` no Up() e `migrationBuilder.CreateTable(name: "audit_logs", ...)` no Down(). Se a tabela tiver nome diferente, ajustar.

**Passo 9: Build completo**
```bash
cd /d/REPO/keycloak-tests
dotnet build src/Onboarding.API
dotnet test tests/Onboarding.API.Tests --filter "Category=Unit|Category=Integration" --no-build
```
  </action>
  <verify>
    <automated>cd /d/REPO/keycloak-tests && dotnet build src/Onboarding.API --no-incremental 2>&1 | grep -E "^.*error" | head -20</automated>
  </verify>
  <done>
    - AuditLog.cs, AuditLogRepository.cs, AuditLogConfiguration.cs não existem mais
    - IAuditLogRepository interface não existe mais em nenhum arquivo
    - AppDbContext.cs não contém DbSet&lt;AuditLog&gt;
    - AdminTestFactory.cs contém AuditServiceMock (IAuditService), não AuditLogRepositoryMock
    - AdminFullFlowTests.cs usa AuditServiceMock.Received com ActionType enum
    - Migration DropAuditLogs existe em src/Onboarding.Infrastructure/Persistence/Migrations/
    - dotnet build src/Onboarding.API exits 0
    - dotnet test tests/Onboarding.API.Tests exits 0
  </done>
  <acceptance_criteria>
    - find src -name "AuditLog.cs" returns no results (excluding AdminAuditLog.cs)
    - find src -name "AuditLogRepository.cs" returns no results
    - find src -name "AuditLogConfiguration.cs" returns no results
    - grep -r "IAuditLogRepository" src/ returns no results
    - grep "AuditLogRepositoryMock" tests/Onboarding.API.Tests/Admin/AdminTestFactory.cs returns no match
    - grep "AuditServiceMock" tests/Onboarding.API.Tests/Admin/AdminTestFactory.cs returns match
    - find src/Onboarding.Infrastructure/Persistence/Migrations -name "*DropAuditLogs*" returns at least 1 result
    - grep "DropTable" src/Onboarding.Infrastructure/Persistence/Migrations/*DropAuditLogs*.cs returns match with "audit_logs"
    - dotnet build src/Onboarding.API --no-incremental exits 0
    - dotnet test tests/Onboarding.API.Tests exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Handler → IAuditService | Dados de auditoria chegam dos handlers sem validação adicional — actorSub pode ser qualquer string |
| IAuditService → IAdminAuditLogRepository | Dados são passados para persistência — AdminAuditLog.Create valida campos obrigatórios |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | AuditService.RecordAsync | mitigate | AdminAuditLog é append-only — sem métodos Update/Delete. Nenhum handler recebe o log de volta após gravação. actorSub é string opaca, não validada por SQL injection (EF Core parameteriza). |
| T-30-02 | Repudiation | AdminAuditLog (tabela) | mitigate | Tabela audit_logs legada DROP — apenas admin_audit_logs persiste. EF Core migration é reversível (Down() recria). Antes de executar em produção, confirmar tabela audit_logs está vazia. |
| T-30-03 | Information Disclosure | AuditService.details (JSON blob) | accept | details contém snapshots de dados do usuário (nome, email). Acesso à tabela admin_audit_logs requer role "admin" via /api/admin endpoints. Sem PII não-necessária. |
| T-30-04 | Elevation of Privilege | Migration DropAuditLogs | mitigate | Migration executada apenas por processo de deploy com credenciais específicas. Down() preserva capacidade de rollback. Não executar em prod sem confirmar tabela vazia. |
</threat_model>

<verification>
Verificação global do Plano 01:

1. `dotnet build src/Onboarding.API --no-incremental` retorna 0 erros
2. `grep -r "IAuditLogRepository" src/` retorna zero resultados
3. `grep -r "AuditLog\.Create" src/` retorna zero resultados (exceto comentários)
4. `grep -r "AuditActions\." src/` retorna zero resultados
5. `grep -r "_auditService\.RecordAsync" src/Onboarding.Application/Admin/Commands/` retorna 5 resultados
6. `dotnet test tests/Onboarding.API.Tests` retorna 0 falhas
7. `find src/Onboarding.Infrastructure/Persistence/Migrations -name "*DropAuditLogs*"` retorna resultado
</verification>

<success_criteria>
- IAuditService definida em Application.Common com assinatura exata do CONTEXT.md
- AuditService em Infrastructure.Services implementa IAuditService via IAdminAuditLogRepository
- BlockUser, UnblockUser, UpdateUser, DeleteUser, CreateAdmin handlers usam IAuditService.RecordAsync
- AuditLog.cs, AuditLogRepository.cs, AuditLogConfiguration.cs, IAuditLogRepository, AuditActions deletados
- AppDbContext sem referência a AuditLog
- Migration DropAuditLogs gerada com DropTable("audit_logs")
- AdminTestFactory usa IAuditService mock
- AdminFullFlowTests verifica ActionType enum via IAuditService.Received()
- dotnet build src/Onboarding.API exits 0
- dotnet test tests/Onboarding.API.Tests exits 0
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/30-audit-log-admin-backend/30-01-SUMMARY.md`
</output>

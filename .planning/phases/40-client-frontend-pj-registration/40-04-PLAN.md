---
phase: 40-client-frontend-pj-registration
plan: 04
type: execute
wave: 4
depends_on: [40-01]
files_modified:
  - frontend/client/src/components/pages/DashboardPage.tsx
  - frontend/client/src/components/molecules/DashboardCards.tsx
  - frontend/client/src/components/organisms/Sidebar.tsx
  - frontend/client/src/router.tsx
  - frontend/client/src/lib/auth-context.tsx
  - frontend/client/src/components/pages/ProfilePage.tsx
autonomous: true
requirements: [DASH-01, PERM-04]

must_haves:
  truths:
    - "Dashboard page shows 6 mock cards with static data (total employees, active, blocked, recent logins, recent actions, last login)"
    - "Sidebar links are visible/hidden based on user's access group"
    - "Dashboard is accessible only to users with 'dashboard' or 'admin-empresa' group"
    - "Viewer sees only Employees (read-only) + Profile (no Dashboard link)"
    - "ProfilePage adapts to show company/PJ data instead of old Client PF/PJ data"
    - "Login redirects based on group: admin-empresa → /employees, viewer → /employees, dashboard → /dashboard"
  artifacts:
    - path: "frontend/client/src/components/pages/DashboardPage.tsx"
      provides: "Dashboard with 6 mock data cards"
      min_lines: 50
    - path: "frontend/client/src/components/molecules/DashboardCards.tsx"
      provides: "Individual dashboard card components with mock data"
      min_lines: 60
    - path: "frontend/client/src/components/organisms/Sidebar.tsx"
      provides: "Permission-based navigation sidebar (updated from Plan 01)"
      min_lines: 40
  key_links:
    - from: "frontend/client/src/router.tsx"
      to: "DashboardPage, EmployeesPage, ProfilePage"
      via: "auth-guarded routes based on accessGroup"
      pattern: "accessGroup|dashboard|employees"
    - from: "frontend/client/src/components/molecules/DashboardCards.tsx"
      to: "mock data constants"
      via: "hardcoded stats"
      pattern: "mockData|totalEmployees|ativos"
---

<objective>
Build the dashboard mock page with static data cards and finalize permission-based routing/login redirects.

Purpose: Deliver DASH-01 (mock dashboard with 6 cards) and finalize permission-based navigation so user roles see the correct default page after login.
Output: DashboardPage with mock cards, updated Sidebar with permission visibility, ProfilePage adapted for company/PJ data, route guards based on group.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-client-frontend-pj-registration/40-CONTEXT.md

<interfaces>
From auth-context.tsx (Plan 01):
```typescript
auth: { isAuthenticated; isLoading; userName; email; accessGroup; companyId }
// accessGroup: "admin-empresa" | "viewer" | "dashboard" | null
```

From router.tsx (Plan 01):
```typescript
// Routes: / (dashboard), /employees, /profile, /register
// Auth guard: redirects unauthenticated to /auth/login
```

From CONTEXT.md decisions:
- D-15: Dashboard com 6 cards: Total Funcionários, Ativos, Bloqueados, Logins Recentes (7d), Ações Recentes (7d), Último Login
- D-16: Mini charts com Chart.js em cada card — dados mock estáticos
- D-17: Período: últimos 7 dias para logins e ações recentes
- D-18: Dados mock hardcoded — sem chamadas a API real (except Total Funcionários pode chamar API)
- D-19: Auth-context estendido para incluir group do usuário (via /auth/me response)
- D-03: Viewer vê Employees (read-only) + Profile. Dashboard vê Dashboard + Profile. Admin-empresa vê tudo.
- D-22: Login redirects based on group: admin-empresa → /employees, viewer → /employees, dashboard → /dashboard
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Dashboard page with 6 mock data cards and mini charts</name>
  <files>frontend/client/src/components/pages/DashboardPage.tsx, frontend/client/src/components/molecules/DashboardCards.tsx</files>
  <read_first>
    frontend/client/src/components/ui/card.tsx
    frontend/client/src/lib/auth-context.tsx
  </read_first>
  <action>
1. Create `DashboardCards.tsx` (molecule) per D-15, D-16, D-17, D-18:
   Define mock data constants:
   ```typescript
   const MOCK_DASHBOARD_DATA = {
     totalEmployees: 24,
     activeEmployees: 22,
     blockedEmployees: 2,
     recentLogins7d: 45,
     recentActions7d: 128,
     lastLogin: "há 2h",
   };
   ```
   Create 6 card components, each using shadcn/ui Card:
   - **Total Funcionários**: Number (24), subtitle "Total de funcionários", small bar icon
   - **Ativos**: Number (22), green accent, progress bar showing 22/24
   - **Bloqueados**: Number (2), red accent, small count
   - **Logins Recentes (7d)**: Number (45), subtitle "Últimos 7 dias", sparkline showing 7 daily values: [3, 8, 6, 7, 5, 9, 7]
   - **Ações Recentes (7d)**: Number (128), subtitle "Últimos 7 dias", sparkline: [15, 22, 18, 20, 17, 21, 15]
   - **Último Login**: Text "há 2h", subtitle "Último acesso registrado"

   For mini charts (D-16), use simple CSS-based sparklines or progress bars — Chart.js is acceptable but CSS sparklines are simpler and don't add bundle weight. Prefer CSS-based sparklines for cards:
   - Progress bar for Ativos/Bloqueados
   - SVG sparkline for Logins and Ações (7 data points, polyline)

   Each card uses:
   - shadcn Card component with header, content sections
   - Lucide icon for each card's theme (Users, UserCheck, UserX, LogIn, Activity, Clock)
   - Consistent styling with `text-2xl font-bold` for numbers

2. Create `DashboardPage.tsx` per D-15:
   - Grid layout (2 columns on desktop, 1 on mobile) with 3 rows of 2 cards
   - Header: "Dashboard" title with subtitle showing company name from auth context
   - Uses `useAuth()` to get `accessGroup` and verify user has permission (dashboard or admin-empresa)
   - If `accessGroup === 'viewer'`, still allow page to load but show "Sem permissão" message (belt-and-suspenders with route guard)
   - The "Total Funcionários" card CAN call `getEmployees(companyId, { page: 1, pageSize: 1 })` to get real totalCount from API — this is the one exception per D-18. Use the totalCount from the paginated result instead of the hardcoded mock 24.
   - All other 5 cards use hardcoded mock data
   - Import DashboardCards and render all 6 in the grid
  </action>
  <verify>
    <automated>cd frontend/client && npx tsc --noEmit 2>&1 | head -20; echo "EXIT: $?"</automated>
  </verify>
  <done>
    - DashboardPage renders grid of 6 cards with mock data
    - DashboardCards has 6 cards: Total Funcionários, Ativos, Bloqueados, Logins 7d, Ações 7d, Último Login
    - Ativos/Bloqueados cards show progress bars
    - Logins/Ações cards show SVG sparklines with 7 data points
    - Total Funcionários card can optionally call API for real count
    - TypeScript compiles
  </done>
</task>

<task type="auto">
  <name>Task 2: Finalize permission-based routing, ProfilePage adaptation, and PF removal verification</name>
  <files>frontend/client/src/components/pages/ProfilePage.tsx, frontend/client/src/router.tsx, frontend/client/src/components/organisms/Sidebar.tsx, frontend/client/src/lib/auth-context.tsx</files>
  <read_first>
    frontend/client/src/components/pages/ProfilePage.tsx
    frontend/client/src/router.tsx
    frontend/client/src/components/organisms/Sidebar.tsx
    frontend/client/src/lib/auth-context.tsx
    frontend/client/src/lib/types.ts
    frontend/client/src/lib/api.ts
  </read_first>
  <action>
1. Update `ProfilePage.tsx` to show company/PJ data instead of Client PF/PJ:
   - Replace `ClientProfileDto` references with `CompanyProfileDto`
   - Remove `type === "PessoaFisica"` conditional rendering for PF data
   - Show: Razão Social, CNPJ, Email, Telefone (PJ fields only)
   - Remove: CPF display, PessoaFisica/PessoaJuridica badge
   - Use `getProfileClient()` (returns `CompanyProfileDto` from Plan 01)
   - Header shows "Perfil da Empresa" instead of "Meu Perfil"

2. Update `auth-context.tsx` login redirect logic per D-22:
   - After session restoration via /auth/me, redirect based on `accessGroup`:
     - `admin-empresa` → `/employees` (management default)
     - `viewer` → `/employees` (read-only default)
     - `dashboard` → `/dashboard`
     - No group (fallback) → `/profile`
   - Current behavior: authenticated users always go to `/profile`. Change to group-based routing.
   - The root `/` route should also redirect based on group after auth.

3. Update `Sidebar.tsx` from Plan 01 Task 3 to finalize permission-based visibility per D-03, D-19:
   - `admin-empresa`: Dashboard ✅, Funcionários ✅ (full actions), Perfil ✅
   - `viewer`: Dashboard ❌, Funcionários ✅ (read-only), Perfil ✅
   - `dashboard`: Dashboard ✅, Funcionários ❌, Perfil ✅

4. Final PF removal verification — scan all files in `frontend/client/src/` for any remaining references to:
   - `PessoaFisica`, `PersonTypeRadio`, `pfRegistration`, `tipo=pf`, `type.*PF`, `ClientProfileDto`
   - Remove or replace any found references
   - Ensure router has no routes pointing to `/registration?tipo=pf` or similar
  </action>
  <verify>
    <automated>cd frontend/client && npx tsc --noEmit 2>&1 | head -20; echo "EXIT: $?"; echo "---PF_CHECK---"; grep -r "PessoaFisica\|PersonTypeRadio\|pfRegistration\|tipo=pf\|ClientProfileDto" src/ 2>/dev/null | head -10; echo "PF_CHECK_DONE"</automated>
  </verify>
  <done>
    - ProfilePage shows PJ-only company data (Razão Social, CNPJ, Email, Telefone)
    - Auth context redirects based on group after login (admin-empresa/viewer → /employees, dashboard → /dashboard)
    - Sidebar shows/hides links based on accessGroup
    - No PF references remain in client frontend source code
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /auth/me | Group claim from server — client uses for UX only, backend enforces real permissions |
| Client → Dashboard API | Only Total Funcionários makes an API call; all other data is mock client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-12 | Elevation | Dashboard visibility | accept | Client-side route hiding is UX-only; backend enforces DashboardView permission (PERM-03) |
| T-40-13 | Information disclosure | Mock data | accept | Mock data contains no real user data — hardcoded sample values only |
| T-40-14 | Tampering | Profile type | mitigate | PF data types removed — no PF references in frontend code |
</threat_model>

<verification>
1. TypeScript compiles: `cd frontend/client && npx tsc --noEmit`
2. Dashboard has 6 cards: `grep "totalEmployees\|activeEmployees\|blockedEmployees\|recentLogins\|recentActions\|lastLogin" frontend/client/src/components/molecules/DashboardCards.tsx` returns matches
3. No PF references: `grep -r "PessoaFisica\|PersonTypeRadio\|pfRegistration\|ClientProfileDto" frontend/client/src/` returns nothing
4. ProfilePage shows PJ data: `grep "CNPJ\|razaoSocial\|CompanyProfileDto" frontend/client/src/components/pages/ProfilePage.tsx` returns matches
5. Auth redirect by group: `grep "accessGroup" frontend/client/src/lib/auth-context.tsx` returns match with routing logic
</verification>

<success_criteria>
- DashboardPage renders 6 mock cards with static data (Total Funcionários, Ativos, Bloqueados, Logins 7d, Ações 7d, Último Login)
- Mini charts (progress bars + sparklines) display correctly in cards
- ProfilePage shows PJ-only data (Razão Social, CNPJ, Email, Telefone) — no PF fields
- Auth context redirects post-login based on group: admin-empresa/viewer → /employees, dashboard → /dashboard
- Sidebar shows only permitted links per group (admin-empresa: all, viewer: Employees+Profile, dashboard: Dashboard+Profile)
- Zero PF references in frontend/client/src/ code
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/40-client-frontend-pj-registration/40-04-SUMMARY.md`
</output>
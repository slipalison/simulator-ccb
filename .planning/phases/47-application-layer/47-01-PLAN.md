---
phase: 47-application-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Onboarding.Application/Fundos/Commands/RegisterConsultoriaFundoCommand.cs
  - src/Onboarding.Application/Fundos/Commands/RegisterConsultoriaFundoCommandHandler.cs
  - src/Onboarding.Application/Fundos/Commands/RegisterConsultoriaFundoCommandValidator.cs
  - src/Onboarding.Application/Fundos/Commands/UpdateConsultoriaFundoCommand.cs
  - src/Onboarding.Application/Fundos/Commands/UpdateConsultoriaFundoCommandHandler.cs
  - src/Onboarding.Application/Fundos/Commands/UpdateConsultoriaFundoCommandValidator.cs
  - src/Onboarding.Application/Fundos/Commands/RegisterCustodianteCommand.cs
  - src/Onboarding.Application/Fundos/Commands/RegisterCustodianteCommandHandler.cs
  - src/Onboarding.Application/Funds/Commands/RegisterCustodianteCommandValidator.cs
  - src/Onboarding.Application/Fundos/Commands/UpdateCustodianteCommand.cs
  - src/Onboarding.Application/Fundos/Commands/UpdateCustodianteCommandHandler.cs
  - src/Onboarding.Application/Fundos/Commands/UpdateCustodianteCommandValidator.cs
  - src/Onboarding.Application/Fundos/Commands/RegisterFundoCommand.cs
  - src/Onboarding.Application/Fundos/Commands/RegisterFundoCommandHandler.cs
  - src/Onboarding.Application/Fundos/Commands/RegisterFundoCommandValidator.cs
  - src/Onboarding.Application/Fundos/Commands/UpdateFundoCommand.cs
  - src/Onboarding.Application/Fundos/Commands/UpdateFundoCommandHandler.cs
  - src/Onboarding.Application/Fundos/Commands/UpdateFundoCommandValidator.cs
  - src/Onboarding.Application/Fundos/Commands/TransitionFundoStatusCommand.cs
  - src/Onboarding.Application/Fundos/Commands/TransitionFundoStatusCommandHandler.cs
  - src/Onboarding.Application/Fundos/Commands/TransitionFundoStatusCommandValidator.cs
  - src/Onboarding.Application/Fundos/Commands/RegisterCedentePfCommand.cs
  - src/Onboarding.Application/Fundos/Commands/RegisterCedentePfCommandHandler.cs
  - src/Onboarding.Application/Fundos/Commands/RegisterCedentePfCommandValidator.cs
  - src/Onboarding.Application/Fundos/Commands/RegisterCedentePjCommand.cs
  - src/Onboarding.Application/Fundos/Commands/RegisterCedentePjCommandHandler.cs
  - src/Onboarding.Application/Fundos/Commands/RegisterCedentePjCommandValidator.cs
  - src/Onboarding.Application/Fundos/Commands/UpdateCedenteCommand.cs
  - src/Onboarding.Application/Fundos/Commands/UpdateCedenteCommandHandler.cs
  - src/Onboarding.Application/Fundos/Commands/UpdateCedenteCommandValidator.cs
  - src/Onboarding.Application/Fundos/Commands/CreateTipoAtivoCommand.cs
  - src/Onboarding.Application/Fundos/Commands/CreateTipoAtivoCommandHandler.cs
  - src/Onboarding.Application/Fundos/Commands/CreateTipoAtivoCommandValidator.cs
  - src/Onboarding.Application/Fundos/Commands/UpdateTipoAtivoCommand.cs
  - src/Onboarding.Application/Fundos/Commands/UpdateTipoAtivoCommandHandler.cs
  - src/Onboarding.Application/Fundos/Commands/UpdateTipoAtivoCommandValidator.cs
  - src/Onboarding.Application/Fundos/DTOs/ConsultoriaFundoDto.cs
  - src/Onboarding.Application/Fundos/DTOs/CustodianteDto.cs
  - src/Onboarding.Application/Fundos/DTOs/FundoDto.cs
  - src/Onboarding.Application/Fundos/DTOs/CedenteDto.cs
  - src/Onboarding.Application/Fundos/DTOs/TipoAtivoDto.cs
  - src/Onboarding.Application/Fundos/DTOs/FundoCedenteDto.cs
  - src/Onboarding.Application/DependencyInjection.cs
autonomous: true
requirements: [CAD-01, CAD-03, CAD-05, CAD-07, CAD-09, CAD-11, CAD-13, CAD-14, CAD-15, CAD-17, CAD-19, CAD-21, ADM-04]

must_haves:
  truths:
    - "Command handlers validate CNPJ check digits for ConsultoriaFundo, Custodiante, Fundo, and Cedente PJ before persisting"
    - "Command handlers check duplicate CNPJ/CPF within company scope and throw DuplicateEntityException for 409 mapping"
    - "Register Fundo handler assigns RASCUNHO status and validates ConsultoriaFundoId/CustodianteId FK references exist"
    - "Transition Fundo status handler uses FundoStatusValidator.CanTransitionTo and rejects invalid transitions"
    - "Cedente PF/PJ handlers use CedenteDocumento.Pf()/Pj() discriminated union correctly"
    - "TipoAtivo create/update handlers validate unique codigo globally (no company scope per D-03)"
    - "Every mutation handler logs to IAuditService with appropriate ActionType"
  artifacts:
    - path: "src/Onboarding.Application/Fundos/Commands/RegisterConsultoriaFundoCommandHandler.cs"
      provides: "Handle ConsultoriaFundo registration with CNPJ validation and uniqueness check"
      exports: ["RegisterConsultoriaFundoCommandHandler"]
    - path: "src/Onboarding.Application/Fundos/Commands/RegisterFundoCommandHandler.cs"
      provides: "Handle Fundo registration with CNPJ validation, FK validation, RASCUNHO status"
      exports: ["RegisterFundoCommandHandler"]
    - path: "src/Onboarding.Application/Fundos/Commands/TransitionFundoStatusCommandHandler.cs"
      provides: "Handle Fundo status transitions with state machine enforcement"
      exports: ["TransitionFundoStatusCommandHandler"]
    - path: "src/Onboarding.Application/Fundos/Commands/RegisterCedentePfCommandHandler.cs"
      provides: "Handle Cedente PF registration with CPF validation"
      exports: ["RegisterCedentePfCommandHandler"]
    - path: "src/Onboarding.Application/Fundos/Commands/CreateTipoAtivoCommandHandler.cs"
      provides: "Handle TipoAtivo creation with global codigo uniqueness"
      exports: ["CreateTipoAtivoCommandHandler"]
    - path: "src/Onboarding.Application/DependencyInjection.cs"
      provides: "DI registration for all fund command handlers and validators"
      contains: "Fundos"
  key_links:
    - from: "src/Onboarding.Application/Fundos/Commands/RegisterFundoCommandHandler.cs"
      to: "src/Onboarding.Domain/Aggregates/FundoAggregate/Fundo.cs"
      via: "Fundo.Register() factory method"
      pattern: "Fundo\\.Register\\("
    - from: "src/Onboarding.Application/Fundos/Commands/TransitionFundoStatusCommandHandler.cs"
      to: "src/Onboarding.Domain/Aggregates/FundoAggregate/Fundo.cs"
      via: "fundo.TransitionTo() state machine"
      pattern: "\\.TransitionTo\\("
    - from: "src/Onboarding.Application/Fundos/Commands/RegisterConsultoriaFundoCommandHandler.cs"
      to: "src/Onboarding.Domain/Repositories/IConsultoriaFundoRepository.cs"
      via: "ExistsByCnpjAsync + AddAsync + SaveAsync"
      pattern: "ExistsByCnpjAsync"
    - from: "src/Onboarding.Application/Fundos/Commands/*CommandHandler.cs"
      to: "src/Onboarding.Application/Common/IAuditService.cs"
      via: "RecordAsync with ActionType enum"
      pattern: "_auditService\\.RecordAsync"
---

<objective>
Create all CQRS command handlers, validators, and DTOs for the 5 fund entities (ConsultoriaFundo, Custodiante, Fundo, Cedente, TipoAtivo) with audit integration, plus register them in DependencyInjection.

Purpose: The Application layer is the business logic layer — commands validate input, enforce business rules (CNPJ/CPF check digits, uniqueness, FK references, state machine), persist via repositories, and log mutations to the audit trail.

Output: ~30 command files + ~5 DTO files + DI registration updates
</objective>

<execution_context>
@C:/Users/aliso/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/aliso/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-infrastructure-layer/46-CONTEXT.md
@.planning/phases/46-infrastructure-layer/46-01-SUMMARY.md
@.planning/phases/46-infrastructure-layer/46-02-SUMMARY.md
@.planning/phases/45-domain-layer-v8/45-01-SUMMARY.md
@.planning/phases/45-domain-layer-v8/45-02-SUMMARY.md

<interfaces>
<!-- Domain repository interfaces the executor needs -->

From src/Onboarding.Domain/Repositories/IFundoRepository.cs:
```csharp
public interface IFundoRepository
{
    Task AddAsync(Fundo fundo, CancellationToken ct = default);
    Task SaveAsync(Fundo fundo, CancellationToken ct = default);
    Task<Fundo?> GetByIdAsync(Guid id, CancellationToken ct = default);
    Task<bool> ExistsByCnpjAsync(string cnpj, Guid companyId, CancellationToken ct = default);
    Task<(IReadOnlyList<Fundo> Items, int TotalCount)> GetPagedByCompanyAsync(Guid companyId, int page, int pageSize, string? search, CancellationToken ct = default);
}
```

From src/Onboarding.Domain/Repositories/IConsultoriaFundoRepository.cs:
```csharp
public interface IConsultoriaFundoRepository
{
    Task AddAsync(ConsultoriaFundo consultoria, CancellationToken ct = default);
    Task SaveAsync(ConsultoriaFundo consultoria, CancellationToken ct = default);
    Task<ConsultoriaFundo?> GetByIdAsync(Guid id, CancellationToken ct = default);
    Task<bool> ExistsByCnpjAsync(string cnpj, Guid companyId, CancellationToken ct = default);
    Task<(IReadOnlyList<ConsultoriaFundo> Items, int TotalCount)> GetPagedByCompanyAsync(Guid companyId, int page, int pageSize, string? search, CancellationToken ct = default);
}
```

From src/Onboarding.Domain/Repositories/ICustodianteRepository.cs:
```csharp
public interface ICustodianteRepository
{
    Task AddAsync(Custodiante custodiante, CancellationToken ct = default);
    Task SaveAsync(Custodiante custodiante, CancellationToken ct = default);
    Task<Custodiante?> GetByIdAsync(Guid id, CancellationToken ct = default);
    Task<bool> ExistsByCnpjAsync(string cnpj, Guid companyId, CancellationToken ct = default);
    Task<(IReadOnlyList<Custodiante> Items, int TotalCount)> GetPagedByCompanyAsync(Guid companyId, int page, int pageSize, string? search, CancellationToken ct = default);
}
```

From src/Onboarding.Domain/Repositories/ICedenteRepository.cs:
```csharp
public interface ICedenteRepository
{
    Task AddAsync(Cedente cedente, CancellationToken ct = default);
    Task SaveAsync(Cedente cedente, CancellationToken ct = default);
    Task<Cedente?> GetByIdAsync(Guid id, CancellationToken ct = default);
    Task<bool> ExistsByDocumentoAsync(CedenteDocumento documento, Guid companyId, CancellationToken ct = default);
    Task<(IReadOnlyList<Cedente> Items, int TotalCount)> GetPagedByCompanyAsync(Guid companyId, int page, int pageSize, string? search, CancellationToken ct = default);
}
```

From src/Onboarding.Domain/Repositories/ITipoAtivoRepository.cs:
```csharp
public interface ITipoAtivoRepository
{
    Task AddAsync(TipoAtivo tipoAtivo, CancellationToken ct = default);
    Task SaveAsync(TipoAtivo tipoAtivo, CancellationToken ct = default);
    Task<TipoAtivo?> GetByIdAsync(Guid id, CancellationToken ct = default);
    Task<bool> ExistsByCodigoAsync(string codigo, CancellationToken ct = default);
    Task<(IReadOnlyList<TipoAtivo> Items, int TotalCount)> GetPagedAsync(int page, int pageSize, string? search, CancellationToken ct = default);
}
```

From src/Onboarding.Application/Common/IAuditService.cs:
```csharp
public interface IAuditService
{
    Task RecordAsync(string actorSub, string actorEmail, ActionType action, Guid? targetUserId = null, string? targetUserName = null, string? details = null, string? ipAddress = null, CancellationToken ct = default);
}
```

From src/Onboarding.Application/Common/ICurrentCompanyService.cs:
```csharp
public interface ICurrentCompanyService { Guid CompanyId { get; } }
```

From src/Onboarding.Application/Common/ICommandHandler.cs:
```csharp
public interface ICommandHandler<TCommand, TResult> { Task<TResult> HandleAsync(TCommand command, CancellationToken ct = default); }
```

From src/Onboarding.Domain/Aggregates/Audit/ActionType.cs (relevant excerpt):
```csharp
// Phase 45 — Fund management actions
FundoCreated = 30, FundoUpdated = 31, FundoStatusChanged = 32, FundoDeleted = 33,
ConsultoriaCreated = 34, ConsultoriaUpdated = 35,
CustodianteCreated = 36, CustodianteUpdated = 37,
CedenteCreated = 38, CedenteUpdated = 39,
TipoAtivoCreated = 40, TipoAtivoUpdated = 41,
FundoCedenteAssociated = 42, FundoCedenteUpdated = 43, FundoCedenteRemoved = 44,
```

<!-- Domain entity factory methods -->

From src/Onboarding.Domain/Aggregates/FundoAggregate/Fundo.cs:
```csharp
public static Fundo Register(string nome, string cnpj, Guid clientId, Guid consultoriaFundoId, Guid custodianteId, TipoFundo tipoFundo, string? classeAnbima = null, string? segmento = null, DateTimeOffset? dataConstituicao = null)
public void TransitionTo(FundoStatus newStatus) // State machine (D-02)
public void Update(string nome, string? classeAnbima, string? segmento, DateTimeOffset? dataConstituicao)
```

From src/Onboarding.Domain/Aggregates/ConsultoriaFundoAggregate/ConsultoriaFundo.cs:
```csharp
public static ConsultoriaFundo Register(string razaoSocial, string cnpj, Guid clientId, string? nomeFantasia = null, string? email = null, string? telefone = null)
public void Update(string razaoSocial, string? nomeFantasia, string? email, string? telefone, ConsultoriaFundoStatus status)
```

From src/Onboarding.Domain/Aggregates/CustodianteAggregate/Custodiante.cs:
```csharp
public static Custodiante Register(string razaoSocial, string cnpj, Guid clientId, string? codigoInterno = null, string? email = null, string? telefone = null)
public void Update(string razaoSocial, string? codigoInterno, string? email, string? telefone, CustodianteStatus status)
```

From src/Onboarding.Domain/Aggregates/CedenteAggregate/Cedente.cs:
```csharp
public static Cedente RegisterPf(string cpf, string nome, Guid clientId, string? email = null, string? telefone = null, string? endereco = null)
public static Cedente RegisterPj(string cnpj, string razaoSocial, Guid clientId, string? email = null, string? telefone = null, string? endereco = null)
public void Update(string nome, string? email, string? telefone, string? endereco, CedenteStatus status)
```

From src/Onboarding.Domain/Aggregates/TipoAtivoAggregate/TipoAtivo.cs:
```csharp
public static TipoAtivo Register(string codigo, string descricao, TipoAtivoCategoria categoria, string? subcategoria = null, int ordemExibicao = 0)
public void Update(string descricao, string? subcategoria, TipoAtivoStatus status, int ordemExibicao)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commands, handlers, validators, and DTOs for ConsultoriaFundo + Custodiante + TipoAtivo</name>
  <files>
    src/Onboarding.Application/Fundos/Commands/RegisterConsultoriaFundoCommand.cs,
    src/Onboarding.Application/Fundos/Commands/RegisterConsultoriaFundoCommandHandler.cs,
    src/Onboarding.Application/Fundos/Commands/RegisterConsultoriaFundoCommandValidator.cs,
    src/Onboarding.Application/Fundos/Commands/UpdateConsultoriaFundoCommand.cs,
    src/Onboarding.Application/Fundos/Commands/UpdateConsultoriaFundoCommandHandler.cs,
    src/Onboarding.Application/Funds/Commands/UpdateConsultoriaFundoCommandValidator.cs,
    src/Onboarding.Application/Fundos/Commands/RegisterCustodianteCommand.cs,
    src/Onboarding.Application/Fundos/Commands/RegisterCustodianteCommandHandler.cs,
    src/Onboarding.Application/Fundos/Commands/RegisterCustodianteCommandValidator.cs,
    src/Onboarding.Application/Fundos/Commands/UpdateCustodianteCommand.cs,
    src/Onboarding.Application/Fundos/Commands/UpdateCustodianteCommandHandler.cs,
    src/Onboarding.Application/Fundos/Commands/UpdateCustodianteCommandValidator.cs,
    src/Onboarding.Application/Fundos/Commands/CreateTipoAtivoCommand.cs,
    src/Onboarding.Application/Fundos/Commands/CreateTipoAtivoCommandHandler.cs,
    src/Onboarding.Application/Fundos/Commands/CreateTipoAtivoCommandValidator.cs,
    src/Onboarding.Application/Fundos/Commands/UpdateTipoAtivoCommand.cs,
    src/Onboarding.Application/Fundos/Commands/UpdateTipoAtivoCommandHandler.cs,
    src/Onboarding.Application/Fundos/Commands/UpdateTipoAtivoCommandValidator.cs,
    src/Onboarding.Application/Fundos/DTOs/ConsultoriaFundoDto.cs,
    src/Onboarding.Application/Fundos/DTOs/CustodianteDto.cs,
    src/Onboarding.Application/Fundos/DTOs/TipoAtivoDto.cs
  </files>
  <behavior>
    - Register ConsultoriaFundo: validates CNPJ via Cnpj.Create(), checks ExistsByCnpjAsync() for uniqueness (CAD-04→409), creates via ConsultoriaFundo.Register(), persists, audits with ActionType.ConsultoriaCreated
    - Update ConsultoriaFundo: loads by ID, calls Update() with status transition (ATIVO/INATIVO), saves, audits with ActionType.ConsultoriaUpdated
    - Register Custodiante: validates CNPJ, checks uniqueness (CAD-08→409), creates via Custodiante.Register(), persists, audits with ActionType.CustodianteCreated
    - Update Custodiante: loads by ID, calls Update(), saves, audits with ActionType.CustodianteUpdated
    - Create TipoAtivo: validates unique codigo globally via ExistsByCodigoAsync() (CAD-22→409), creates via TipoAtivo.Register(), persists, audits with ActionType.TipoAtivoCreated
    - Update TipoAtivo: loads by ID, calls Update(), saves, audits with ActionType.TipoAtivoUpdated
    - All validators use FluentValidation; CNPJ validated via Cnpj.Create() in Must rules; email validated with EmailAddress()
    - All handlers inject ICurrentCompanyService.CompanyId for company-scoped entities (ConsultoriaFundo, Custodiante); TipoAtivo has NO company scope
    - DTOs: ConsultoriaFundoDto (Id, RazaoSocial, NomeFantasia, Cnpj, Email, Telefone, Status, CreatedAt); CustodianteDto (Id, RazaoSocial, CodigoInterno, Cnpj, Email, Telefone, Status, CreatedAt); TipoAtivoDto (Id, Codigo, Descricao, Categoria, Subcategoria, Status, OrdemExibicao)
  </behavior>
  <action>
    Create the Fundos/Application folder structure under `src/Onboarding.Application/Fundos/` with Commands/ and DTOs/ subdirectories, mirroring the existing Companies/ pattern.

    For each of the 3 entities (ConsultoriaFundo, Custodiante, TipoAtivo):

    1. **Register Command + Handler + Validator + DTO:**
       - Register{Entity}Command: sealed record with all required fields matching domain factory method parameters
       - Register{Entity}CommandHandler: implements ICommandHandler<TCommand, TResult>
         - Injects ICurrentCompanyService (for ConsultoriaFundo and Custodiante — NOT for TipoAtivo)
         - Injects I{Entity}Repository, IAuditService, ILogger
         - Validates CNPJ uniqueness via repository (company-scoped for ConsultoriaFundo/Custodiante, global for TipoAtivo codigo)
         - Calls domain factory method (ConsultoriaFundo.Register, Custodiante.Register, TipoAtivo.Register)
         - Persists via repository AddAsync + SaveAsync
         - Logs audit event with appropriate ActionType enum value
         - Returns DTO with Id
       - Register{Entity}CommandValidator: AbstractValidator<TCommand>
         - Required fields: razaoSocial/nome, cnpj/codigo
         - CNPJ validation via Cnpj.Create() in Must rule (catches check digits)
         - Email validated with EmailAddress() rule when present
         - TipoAtivo: validates codigo, descricao, categoria enum range

    2. **Update Command + Handler + Validator:**
       - Update{Entity}Command: sealed record with Id + mutable fields + status
       - Update{Entity}CommandHandler: loads entity by ID, calls domain Update() method, saves, audits
         - Throws NotFoundException (or returns 404 equivalent) if entity not found
         - For ConsultoriaFundo/Custodiante: uses ICurrentCompanyService.CompanyId for company isolation
         - TipoAtivo: no CompanyId needed
       - Update{Entity}CommandValidator: same field rules as Register but for update fields

    3. **DTOs** — simple records mapping entity properties to response shapes

    **Key patterns to follow** (from RegisterCompanyCommandHandler):
    - `sealed` classes implementing `ICommandHandler<TCommand, TResult>`
    - Constructor injection of all dependencies
    - Uniqueness checks BEFORE domain factory method call
    - DuplicateEntityException for 409 mapping by the API layer
    - Audit logging with IAuditService.RecordAsync after persist
    - Return DTO with the new entity's ID

    **Important:** All company-scoped handlers (ConsultoriaFundo, Custodiante) inject ICurrentCompanyService and use CompanyId for uniqueness checks and entity creation. TipoAtivo handlers do NOT inject ICurrentCompanyService — it's a global entity per D-03.
  </action>
  <verify>
    <automated>dotnet build src/Onboarding.Application --no-restore 2>&1 | Select-String "error|Error" ; if ($LASTEXITCODE -eq 0) { echo "BUILD SUCCESS" } else { echo "BUILD FAILED" }</automated>
  </verify>
  <done>
    - All 9 command files (3 Register + 3 Update + 3 Handlers) compile without errors
    - All 6 validator files enforce CNPJ/codigo uniqueness and required field rules
    - All 3 DTOs map entity properties correctly
    - Audit logging wired with correct ActionType enum values for all mutations
    - ICurrentCompanyService used in ConsultoriaFundo/Custodiante handlers but NOT in TipoAtivo handlers
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commands, handlers, validators, and DTOs for Fundo + Cedente (including status transition)</name>
  <files>
    src/Onboarding.Application/Fundos/Commands/RegisterFundoCommand.cs,
    src/Onboarding.Application/Fundos/Commands/RegisterFundoCommandHandler.cs,
    src/Onboarding.Application/Fundos/Commands/RegisterFundoCommandValidator.cs,
    src/Onboarding.Application/Fundos/Commands/UpdateFundoCommand.cs,
    src/Onboarding.Application/Fundos/Commands/UpdateFundoCommandHandler.cs,
    src/Onboarding.Application/Fundos/Commands/UpdateFundoCommandValidator.cs,
    src/Onboarding.Application/Fundos/Commands/TransitionFundoStatusCommand.cs,
    src/Onboarding.Application/Fundos/Commands/TransitionFundoStatusCommandHandler.cs,
    src/Onboarding.Application/Fundos/Commands/TransitionFundoStatusCommandValidator.cs,
    src/Onboarding.Application/Fundos/Commands/RegisterCedentePfCommand.cs,
    src/Onboarding.Application/Fundos/Commands/RegisterCedentePfCommandHandler.cs,
    src/Onboarding.Application/Funds/Commands/RegisterCedentePfCommandValidator.cs,
    src/Onboarding.Application/Fundos/Commands/RegisterCedentePjCommand.cs,
    src/Onboarding.Application/Fundos/Commands/RegisterCedentePjCommandHandler.cs,
    src/Onboarding.Application/Fundos/Commands/RegisterCedentePjCommandValidator.cs,
    src/Onboarding.Application/Fundos/Commands/UpdateCedenteCommand.cs,
    src/Onboarding.Application/Fundos/Commands/UpdateCedenteCommandHandler.cs,
    src/Onboarding.Application/Fundos/Commands/UpdateCedenteCommandValidator.cs,
    src/Onboarding.Application/Fundos/DTOs/FundoDto.cs,
    src/Onboarding.Application/Fundos/DTOs/CedenteDto.cs,
    src/Onboarding.Application/Fundos/DTOs/FundoCedenteDto.cs,
    src/Onboarding.Application/DependencyInjection.cs
  </files>
  <behavior>
    - Register Fundo: validates CNPJ, checks ConsultoriaFundoId/CustodianteId FK references exist, assigns RASCUNHO status on creation, checks CNPJ uniqueness (CAD-12→409), audits with ActionType.FundoCreated
    - Update Fundo: loads by ID, calls Update() for mutable fields (nome, classeAnbima, segmento, dataConstituicao) — NOT status, audits with ActionType.FundoUpdated
    - Transition Fundo Status: loads by ID, calls TransitionTo() with target FundoStatus, invalid transitions throw InvalidStateTransitionException (CAD-13), audits with ActionType.FundoStatusChanged
    - Register Cedente PF: validates CPF via Cpf.Create(), checks ExistsByDocumentoAsync() with CedenteDocumento.Pf() (CAD-18→409), creates via Cedente.RegisterPf(), audits with ActionType.CedenteCreated
    - Register Cedente PJ: validates CNPJ, checks ExistsByDocumentoAsync() with CedenteDocumento.Pj() (CAD-18→409), creates via Cedente.RegisterPj(), audits with ActionType.CedenteCreated
    - Update Cedente: loads by ID, calls Update() with nome/email/telefone/endereco/status, audits with ActionType.CedenteUpdated
    - FundoDto includes: Id, Nome, Cnpj, ConsultoriaFundoId, CustodianteId, TipoFundo, ClasseAnbima, Segmento, DataConstituicao, Status, CreatedAt
    - CedenteDto includes: Id, Documento (display CPF/CNPJ), Nome, Email, Telefone, Endereco, CedenteTipo, Status, CreatedAt
    - FundoCedenteDto includes: CedenteId, LimiteExposicaoPercentual, LimiteExposicaoValor, DataInicio, DataFim, Status
  </behavior>
  <action>
    Create command handlers, validators, and DTOs for the two complex entities (Fundo and Cedente) plus Fundo status transition:

    1. **Register Fundo:**
       - RegisterFundoCommand: Nome, Cnpj, ConsultoriaFundoId, CustodianteId, TipoFundo, ClasseAnbima?, Segmento?, DataConstituicao?
       - RegisterFundoCommandHandler:
         - Inject IFundoRepository, IConsultoriaFundoRepository (FK check), ICustodianteRepository (FK check), ICurrentCompanyService, IAuditService, ILogger
         - Validate ConsultoriaFundoId exists via IConsultoriaFundoRepository.GetByIdAsync (IgnoreQueryFilters NOT needed — must be company's own consultoria)
         - Validate CustodianteId exists via ICustodianteRepository.GetByIdAsync
         - Check duplicate CNPJ via IFundoRepository.ExistsByCnpjAsync (company-scoped)
         - Create via Fundo.Register() factory — ALWAYS starts as RASCUNHO status
         - Persist and audit with ActionType.FundoCreated
       - RegisterFundoCommandValidator: Nome required, CNPJ validated via Cnpj.Create(), ConsultoriaFundoId/CustodianteId required (non-empty Guid), TipoFundo enum range

    2. **Update Fundo:**
       - UpdateFundoCommand: Id, Nome, ClasseAnbima?, Segmento?, DataConstituicao?
       - UpdateFundoCommandHandler: loads Fundo, calls fundo.Update(), saves, audits with ActionType.FundoUpdated
       - IMPORTANT: This is DATA update only. Status transitions are a separate command.
       - UpdateFundoCommandValidator: Nome required, Id required

    3. **Transition Fundo Status:**
       - TransitionFundoStatusCommand: FundoId, NewStatus (FundoStatus enum)
       - TransitionFundoStatusCommandHandler:
         - Load Fundo by ID (with Include for Cedentes/TiposAtivo if needed for audit details)
         - Call fundo.TransitionTo(newStatus) — this throws InvalidStateTransitionException for invalid transitions
         - Save and audit with ActionType.FundoStatusChanged (include from/to status in details)
       - TransitionFundoStatusCommandValidator: FundoId required (non-empty Guid), NewStatus must be valid FundoStatus enum value

    4. **Register Cedente PF:**
       - RegisterCedentePfCommand: Cpf, Nome, ClientId (from ICurrentCompanyService), Email?, Telefone?, Endereco?
       - RegisterCedentePfCommandHandler:
         - Validate CPF via Cpf.Create() (check digits)
         - Build CedenteDocumento.Pf() for uniqueness check
         - Check ExistsByDocumentoAsync(CedenteDocumento.Pf(cpf), companyId) — CAD-18
         - Create via Cedente.RegisterPf()
         - Persist and audit with ActionType.CedenteCreated
       - RegisterCedentePfCommandValidator: Cpf required (validated via Cpf.Create in Must rule), Nome required

    5. **Register Cedente PJ:**
       - RegisterCedentePjCommand: Cnpj, RazaoSocial, ClientId (from ICurrentCompanyService), Email?, Telefone?, Endereco?
       - RegisterCedentePjCommandHandler:
         - Validate CNPJ via Cnpj.Create()
         - Build CedenteDocumento.Pj() for uniqueness check
         - Check ExistsByDocumentoAsync(CedenteDocumento.Pj(cnpj), companyId) — CAD-18
         - Create via Cedente.RegisterPj()
         - Persist and audit
       - RegisterCedentePjCommandValidator: Cnpj required (validated), RazaoSocial required

    6. **Update Cedente:**
       - UpdateCedenteCommand: Id, Nome, Email?, Telefone?, Endereco?, CedenteStatus
       - UpdateCedenteCommandHandler: loads Cedente, calls Update(), saves, audits
       - UpdateCedenteCommandValidator: Nome required

    7. **DTOs:**
       - FundoDto: Id, Nome, Cnpj (string), ConsultoriaFundoId, CustodianteId, TipoFundo, ClasseAnbima, Segmento, DataConstituicao, Status (enum as string), CreatedAt
       - CedenteDto: Id, Documento tipo + value (separately or string), Nome, Email, Telefone, Endereco, CedenteTipo, Status, CreatedAt
       - FundoCedenteDto: CedenteId, LimiteExposicaoPercentual (decimal), LimiteExposicaoValor (decimal?), DataInicio, DataFim, Status

    8. **DI Registration:**
       - Add ALL new command handlers and validators to DependencyInjection.cs AddApplication() method
       - Follow existing pattern: `services.AddScoped<ICommandHandler<XxxCommand, XxxResult>, XxxCommandHandler>()`
       - Group under comment `// Fund CRUD commands (Phase 47 — CAD-01..21, ADM-04)`
  </action>
  <verify>
    <automated>dotnet build src/Onboarding.Application --no-restore 2>&1 | Select-String "error|Error" ; if ($LASTEXITCODE -eq 0) { echo "BUILD SUCCESS" } else { echo "BUILD FAILED" }</automated>
  </verify>
  <done>
    - RegisterFundoCommandHandler validates CNPJ uniqueness (CAD-12→409), validates FK references, assigns RASCUNHO status
    - TransitionFundoStatusCommandHandler uses fundo.TransitionTo() with state machine enforcement (CAD-13)
    - RegisterCedentePfCommandHandler validates CPF via Cpf.Create() and CedenteDocumento.Pf() uniqueness (CAD-18)
    - RegisterCedentePjCommandHandler validates CNPJ via Cnpj.Create() and CedenteDocumento.Pj() uniqueness (CAD-18)
    - All FluentValidation validators enforce field constraints matching domain rules
    - All handlers inject IAuditService and call RecordAsync with correct ActionType enum values
    - DependencyInjection.cs registers all new handlers and validators
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Command Handler | Untrusted input crosses trust boundary; validators sanitize before domain layer |
| Command Handler → Repository | Data access via company-scoped repositories (HasQueryFilter) |
| Command Handler → Audit | Audit logging is cross-cutting concern; IAuditService is trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | S | Command Handlers | mitigate | Validate CNPJ/CPF check digits via domain VOs before repository calls — prevents injection of malformed identifiers |
| T-47-02 | T | Fundo Status Transition | mitigate | FundoStatusValidator whitelist in domain layer — handler cannot bypass state machine |
| T-47-03 | I | Company-scoped queries | mitigate | ICurrentCompanyService.CompanyId injected per-request from JWT claims — HasQueryFilter enforces isolation at EF Core level |
| T-47-04 | E | TipoAtivo creation | mitigate | ExistsByCodigoAsync check prevents duplicate global catalog entries — race window mitigated by unique constraint at DB level (Phase 46) |
| T-47-05 | D | Cedente PF/PJ creation | accept | Race condition window between ExistsByDocumentoAsync check and AddAsync is narrow; DB composite filtered index (D-10) prevents actual duplicate insertion |
</threat_model>

<verification>
- `dotnet build src/Onboarding.Application` succeeds with 0 errors
- All command handlers in `src/Onboarding.Application/Fundos/Commands/` compile and inject correct repositories
- All validators reference domain VOs (Cnpj.Create, Cpf.Create) for check-digit validation
- DependencyInjection.cs contains registrations for all new command handlers and validators
- FundoCedenteDto includes LimiteExposicaoPercentual and LimiteExposicaoValor fields
</verification>

<success_criteria>
- 18+ command handler files compile without errors
- 6 validator files enforce CNPJ/CPF/codigo uniqueness and required field rules
- All handlers log mutations via IAuditService with correct ActionType enum values
- Fundo register handler assigns RASCUNHO status and validates FK references
- Fundo status transition handler uses FundoStatusValidator.CanTransitionTo
- Cedente PF/PJ handlers use CedenteDocumento.Pf()/Pj() discriminated union
- TipoAtivo handler validates global codigo uniqueness (no company scope)
- ICurrentCompanyService injected only in company-scoped handlers (not TipoAtivo)
- DependencyInjection.cs registers all new handlers and validators
</success_criteria>

<output>
After completion, create `.planning/phases/47-application-layer/47-01-SUMMARY.md`
</output>
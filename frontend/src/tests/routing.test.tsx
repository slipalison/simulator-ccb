// Criterio de sucesso 3: rota desconhecida exibe NotFoundPage tipada via notFoundComponent
// Substituir em 07-03 apos TanStack Router estar implementado em 07-02
import { describe, it } from 'vitest'

describe('TanStack Router 404 handling', () => {
  it('shows NotFoundPage on unknown route', () => {
    throw new Error('not implemented: 404 routing — implementar em 07-02 e verificar em 07-03')
  })

  it('NotFoundPage is a typed component registered via notFoundComponent (not NotFoundRoute)', () => {
    throw new Error('not implemented: typed NotFoundPage via notFoundComponent — implementar em 07-02 e verificar em 07-03')
  })
})

---
phase: 06-authentication-api
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02-PLAN.md
files_modified:
  - src/Onboarding.Infrastructure/Keycloak/KeycloakTokenService.cs
  - src/Onboarding.Infrastructure/Keycloak/KeycloakAuthException.cs
  - src/Onboarding.Infrastructure/DependencyInjection.cs
  - src/Onboarding.Application/Auth/Commands/LoginCommand.cs
  - src/Onboarding.Application/Auth/Commands/LoginCommandHandler.cs
  - src/Onboarding.Application/Auth/Commands/RefreshTokenCommand.cs
  - src/Onboarding.Application/Auth/Commands/RefreshTokenCommandHandler.cs
  - src/Onboarding.Application/Auth/Validators/LoginCommandValidator.cs
  - src/Onboarding.Application/Auth/Validators/RefreshTokenCommandValidator.cs
  - src/Onboarding.Application/DependencyInjection.cs
  - src/Onboarding.API/Controllers/AuthController.cs
  - src/Onboarding.API/Controllers/ClientsController.cs
  - src/Onboarding.API/appsettings.json
  - tests/Onboarding.API.Tests/Authentication/JwtBearerConfigurationTests.cs
  - tests/Onboarding.API.Tests/Authentication/AuthorizationMiddlewareTests.cs
  - tests/Onboarding.API.Tests/Api/ClientsMeEndpointTests.cs
  - tests/Onboarding.API.Tests/Api/LoginEndpointTests.cs
  - tests/Onboarding.API.Tests/Api/RefreshTokenEndpointTests.cs
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - "POST /api/auth/login com credenciais válidas retorna 200 com access_token, refresh_token, expires_in, token_type"
    - "POST /api/auth/login com credenciais inválidas retorna 401 com ProblemDetails genérico (sem revelar se email existe)"
    - "POST /api/auth/refresh com refresh_token válido retorna 200 com novos tokens"
    - "POST /api/auth/refresh com refresh_token inválido retorna 401 com ProblemDetails genérico"
    - "GET /api/clients/me sem Bearer token retorna 401"
    - "GET /api/clients/me com Bearer token válido retorna 200 com perfil do cliente"
    - "GET /api/clients/me com Bearer token válido mas cliente não encontrado no DB retorna 404 genérico (D-09)"
    - "Todos os 12 stubs RED do Plan 01 passam (GREEN)"
  artifacts:
    - path: "src/Onboarding.Infrastructure/Keycloak/KeycloakTokenService.cs"
      provides: "Implementação ROPC via IHttpClientFactory (named client keycloak-token)"
      exports: ["KeycloakTokenService"]
    - path: "src/Onboarding.Infrastructure/Keycloak/KeycloakAuthException.cs"
      provides: "Exception para erros de autenticação Keycloak (mapeada para 401 no controller)"
      exports: ["KeycloakAuthException"]
    - path: "src/Onboarding.API/Controllers/AuthController.cs"
      provides: "POST /api/auth/login e POST /api/auth/refresh (endpoints públicos)"
      exports: ["AuthController"]
    - path: "src/Onboarding.API/Controllers/ClientsController.cs"
      provides: "GET /api/clients/me com [Authorize]"
      exports: ["ClientsController"]
    - path: "src/Onboarding.Application/Auth/Commands/LoginCommand.cs"
      provides: "Command e Handler para login via CQRS manual"
      exports: ["LoginCommand"]
    - path: "src/Onboarding.Application/Auth/Commands/RefreshTokenCommand.cs"
      provides: "Command e Handler para refresh via CQRS manual"
      exports: ["RefreshTokenCommand"]
  key_links:
    - from: "src/Onboarding.API/Controllers/AuthController.cs"
      to: "src/Onboarding.Application/Auth/Commands/LoginCommandHandler.cs"
      via: "ICommandHandler<LoginCommand, TokenResponse> injetado via DI"
      pattern: "ICommandHandler.*LoginCommand.*TokenResponse"
    - from: "src/Onboarding.Application/Auth/Commands/LoginCommandHandler.cs"
      to: "src/Onboarding.Infrastructure/Keycloak/KeycloakTokenService.cs"
      via: "IKeycloakTokenService.ExchangePasswordAsync"
      pattern: "ExchangePasswordAsync"
    - from: "src/Onboarding.API/Controllers/ClientsController.cs"
      to: "src/Onboarding.Domain/Repositories/IClientRepository.cs"
      via: "IQueryHandler<GetClientByEmailQuery, ClientProfileDto> → GetByEmailAsync"
      pattern: "GetByEmailAsync"
---

<objective>
Implementar todos os handlers, controllers e infraestrutura de autenticação. Converter os 12 stubs RED do Plan 01 em GREEN.

Purpose: Entrega completa dos requisitos AUTH-02 (emissão de tokens), AUTH-03 (rota protegida /api/clients/me) e AUTH-04 (endpoint de refresh).
Output: POST /api/auth/login, POST /api/auth/refresh, GET /api/clients/me funcionando. `dotnet test` passa com todos os stubs GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-authentication-api/06-CONTEXT.md
@.planning/phases/06-authentication-api/06-RESEARCH.md
@.planning/phases/06-authentication-api/06-01-SUMMARY.md
@.planning/phases/06-authentication-api/06-02-SUMMARY.md

<interfaces>
<!-- Contratos criados no Plan 02 — executor deve ler os arquivos reais antes de implementar -->

IKeycloakTokenService (src/Onboarding.Application/Common/IKeycloakTokenService.cs):
```csharp
public interface IKeycloakTokenService
{
    Task<TokenResponse> ExchangePasswordAsync(string email, string password, CancellationToken ct = default);
    Task<TokenResponse> RefreshTokenAsync(string refreshToken, CancellationToken ct = default);
}
```

TokenResponse (src/Onboarding.Application/Auth/DTOs/TokenResponse.cs):
```csharp
public sealed record TokenResponse(
    string AccessToken, string RefreshToken,
    int ExpiresIn, string TokenType,
    int RefreshExpiresIn, string Scope);
```

IClientRepository (src/Onboarding.Domain/Repositories/IClientRepository.cs — Plan 02 adicionou):
```csharp
Task<Client?> GetByEmailAsync(string email, CancellationToken ct = default);
```

Client aggregate (src/Onboarding.Domain/Aggregates/ClientAggregate/Client.cs):
```csharp
public sealed class Client : Entity<Guid>
{
    public string Name { get; private set; }
    public Email Email { get; private set; }
    public PhoneNumber Phone { get; private set; }
    public ClientType Type { get; private set; }
    public Cpf? Cpf { get; private set; }
    public Cnpj? Cnpj { get; private set; }
    public string? RazaoSocial { get; private set; }
}
```

ICommandHandler (src/Onboarding.Application/Common/ICommandHandler.cs):
```csharp
public interface ICommandHandler<TCommand, TResult>
{
    Task<TResult> HandleAsync(TCommand command, CancellationToken ct = default);
}
```

DependencyInjection.cs existente em Infrastructure (src/Onboarding.Infrastructure/DependencyInjection.cs):
```csharp
public static IServiceCollection AddInfrastructure(
    this IServiceCollection services, IConfiguration configuration)
{
    // ... EF Core, ClientRepository, Keycloak Admin Client ...
    services.AddScoped<IKeycloakUserService, KeycloakUserService>();
    return services;
}
```

DependencyInjection.cs existente em Application (src/Onboarding.Application/DependencyInjection.cs):
```csharp
public static IServiceCollection AddApplication(this IServiceCollection services)
{
    services.AddScoped<ICommandHandler<RegisterClientCommand, Guid>, RegisterClientCommandHandler>();
    services.AddScoped<IValidator<RegisterClientCommand>, RegisterClientCommandValidator>();
    return services;
}
```

RegistrationController (padrão a seguir para AuthController):
```csharp
[ApiController]
[Route("api/[controller]")]
public sealed class RegistrationController : ControllerBase
{
    private readonly ICommandHandler<RegisterClientCommand, Guid> _handler;
    private readonly IValidator<RegisterClientCommand> _validator;
    private readonly ILogger<RegistrationController> _logger;

    // catch(ArgumentException) → 422, catch(DuplicateClientException) → 409
}
```

AuthTestApiFactory (tests/Onboarding.API.Tests/Authentication/AuthTestApiFactory.cs — Plan 01):
```csharp
public IClientRepository RepositoryMock { get; } = Substitute.For<IClientRepository>();
public IKeycloakTokenService TokenServiceMock { get; } = Substitute.For<IKeycloakTokenService>();
// PostConfigure<JwtBearerOptions> desabilita validação de assinatura
```

FakeJwtTokenHelper (tests/Onboarding.API.Tests/Authentication/FakeJwtTokenHelper.cs — Plan 01):
```csharp
public static string GenerateFakeJwt(string email, string? sub = null)
// Retorna JWT unsigned com claim "email"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar KeycloakTokenService, commands/handlers de auth e wiring DI</name>
  <files>
    src/Onboarding.Infrastructure/Keycloak/KeycloakTokenService.cs
    src/Onboarding.Infrastructure/Keycloak/KeycloakAuthException.cs
    src/Onboarding.Infrastructure/DependencyInjection.cs
    src/Onboarding.Application/Auth/Commands/LoginCommand.cs
    src/Onboarding.Application/Auth/Commands/LoginCommandHandler.cs
    src/Onboarding.Application/Auth/Commands/RefreshTokenCommand.cs
    src/Onboarding.Application/Auth/Commands/RefreshTokenCommandHandler.cs
    src/Onboarding.Application/Auth/Validators/LoginCommandValidator.cs
    src/Onboarding.Application/Auth/Validators/RefreshTokenCommandValidator.cs
    src/Onboarding.Application/DependencyInjection.cs
    src/Onboarding.API/appsettings.json
  </files>
  <read_first>
    - src/Onboarding.Infrastructure/Keycloak/KeycloakUserService.cs (padrão de service Keycloak existente no projeto)
    - src/Onboarding.Infrastructure/DependencyInjection.cs (onde registrar KeycloakTokenService e named HttpClient)
    - src/Onboarding.Application/DependencyInjection.cs (onde registrar handlers e validators de auth)
    - src/Onboarding.Application/Common/IKeycloakTokenService.cs (interface criada no Plan 02)
    - src/Onboarding.Application/Auth/DTOs/TokenResponse.cs (DTO criado no Plan 02)
    - src/Onboarding.Application/Clients/Commands/RegisterClientCommand.cs (padrão de command a seguir)
    - src/Onboarding.Application/Clients/Validators/RegisterClientCommandValidator.cs (padrão de validator)
    - src/Onboarding.API/appsettings.json (adicionar Keycloak:PublicClientId)
  </read_first>
  <behavior>
    KeycloakTokenService.ExchangePasswordAsync("email", "password") faz POST form-encoded para {RealmUrl}/protocol/openid-connect/token com grant_type=password, client_id=onboarding-app.
    Keycloak 401 → lança KeycloakAuthException (não expõe detalhes do erro Keycloak).
    LoginCommandHandler chama ExchangePasswordAsync e retorna TokenResponse.
    RefreshTokenCommandHandler chama RefreshTokenAsync e retorna TokenResponse.
    LoginCommandValidator: email required + formato email válido; password required + length >= 1.
    RefreshTokenCommandValidator: refreshToken required + length >= 1.
  </behavior>
  <action>
Criar `src/Onboarding.Infrastructure/Keycloak/KeycloakAuthException.cs`:

```csharp
namespace Onboarding.Infrastructure.Keycloak;

/// <summary>
/// Thrown when Keycloak rejects authentication (invalid credentials or expired token).
/// Mapped to HTTP 401 in AuthController — message is generic (SEC-08).
/// </summary>
public sealed class KeycloakAuthException : Exception
{
    public KeycloakAuthException(string message) : base(message) { }
}
```

Criar `src/Onboarding.Infrastructure/Keycloak/KeycloakTokenService.cs`:

```csharp
using System.Net.Http.Json;
using System.Text.Json.Serialization;
using Microsoft.Extensions.Configuration;
using Onboarding.Application.Auth.DTOs;
using Onboarding.Application.Common;

namespace Onboarding.Infrastructure.Keycloak;

/// <summary>
/// Implements IKeycloakTokenService using IHttpClientFactory (named client "keycloak-token").
/// Makes ROPC calls to Keycloak token endpoint.
/// D-11: uses IHttpClientFactory directly — NOT Duende.AccessTokenManagement (which is for service account CC grant only).
/// D-12: client_id = onboarding-app (public client, no secret).
/// </summary>
public sealed class KeycloakTokenService : IKeycloakTokenService
{
    private readonly IHttpClientFactory _httpClientFactory;
    private readonly string _realmUrl;
    private readonly string _clientId;

    public KeycloakTokenService(
        IHttpClientFactory httpClientFactory,
        IConfiguration configuration)
    {
        _httpClientFactory = httpClientFactory;
        _realmUrl = configuration["Keycloak:RealmUrl"]
            ?? throw new InvalidOperationException("Keycloak:RealmUrl not configured.");
        _clientId = configuration["Keycloak:PublicClientId"]
            ?? throw new InvalidOperationException("Keycloak:PublicClientId not configured.");
    }

    public async Task<TokenResponse> ExchangePasswordAsync(
        string email, string password, CancellationToken ct = default)
    {
        var client = _httpClientFactory.CreateClient("keycloak-token");
        var response = await client.PostAsync(
            $"{_realmUrl}/protocol/openid-connect/token",
            new FormUrlEncodedContent(new Dictionary<string, string>
            {
                ["grant_type"] = "password",
                ["client_id"] = _clientId,
                ["username"] = email,
                ["password"] = password,
                ["scope"] = "openid email profile"
            }),
            ct);

        if (!response.IsSuccessStatusCode)
            // D-13: SEC-08 — do not expose Keycloak error details
            throw new KeycloakAuthException("Invalid credentials.");

        return await DeserializeTokenResponse(response, ct);
    }

    public async Task<TokenResponse> RefreshTokenAsync(
        string refreshToken, CancellationToken ct = default)
    {
        var client = _httpClientFactory.CreateClient("keycloak-token");
        var response = await client.PostAsync(
            $"{_realmUrl}/protocol/openid-connect/token",
            new FormUrlEncodedContent(new Dictionary<string, string>
            {
                ["grant_type"] = "refresh_token",
                ["client_id"] = _clientId,
                ["refresh_token"] = refreshToken
            }),
            ct);

        if (!response.IsSuccessStatusCode)
            // D-13: SEC-08 — do not expose Keycloak error details
            throw new KeycloakAuthException("Invalid or expired refresh token.");

        return await DeserializeTokenResponse(response, ct);
    }

    private static async Task<TokenResponse> DeserializeTokenResponse(
        HttpResponseMessage response, CancellationToken ct)
    {
        var json = await response.Content.ReadFromJsonAsync<KeycloakTokenJson>(
            cancellationToken: ct)
            ?? throw new InvalidOperationException("Empty token response from Keycloak.");

        return new TokenResponse(
            json.AccessToken,
            json.RefreshToken,
            json.ExpiresIn,
            json.TokenType,
            json.RefreshExpiresIn,
            json.Scope ?? string.Empty);
    }
}

// Internal DTO for JSON deserialization — not exposed outside this class
internal sealed record KeycloakTokenJson(
    [property: JsonPropertyName("access_token")] string AccessToken,
    [property: JsonPropertyName("refresh_token")] string RefreshToken,
    [property: JsonPropertyName("expires_in")] int ExpiresIn,
    [property: JsonPropertyName("token_type")] string TokenType,
    [property: JsonPropertyName("refresh_expires_in")] int RefreshExpiresIn,
    [property: JsonPropertyName("scope")] string? Scope);
```

Modificar `src/Onboarding.Infrastructure/DependencyInjection.cs` — adicionar ao final do método AddInfrastructure(), ANTES de `return services;`:

```csharp
// Keycloak token endpoint — ROPC/refresh calls (D-11, D-12)
// Named client without auth handler — ROPC calls do not carry outbound Bearer token
services.AddHttpClient("keycloak-token", client =>
{
    // [Claude's Discretion] 10 second timeout — balances UX and network reliability
    client.Timeout = TimeSpan.FromSeconds(10);
});
services.AddScoped<IKeycloakTokenService, KeycloakTokenService>();
```

Criar `src/Onboarding.Application/Auth/Commands/LoginCommand.cs`:

```csharp
using Onboarding.Application.Auth.DTOs;
using Onboarding.Application.Common;

namespace Onboarding.Application.Auth.Commands;

/// <summary>
/// Command: exchange email + password for a Keycloak token pair.
/// AUTH-02: triggers ROPC call via IKeycloakTokenService.ExchangePasswordAsync.
/// </summary>
public sealed record LoginCommand(string Email, string Password);

public sealed class LoginCommandHandler : ICommandHandler<LoginCommand, TokenResponse>
{
    private readonly IKeycloakTokenService _tokenService;
    private readonly ILogger<LoginCommandHandler> _logger;

    public LoginCommandHandler(
        IKeycloakTokenService tokenService,
        ILogger<LoginCommandHandler> logger)
    {
        _tokenService = tokenService;
        _logger = logger;
    }

    public async Task<TokenResponse> HandleAsync(LoginCommand command, CancellationToken ct = default)
    {
        // IKeycloakTokenService throws KeycloakAuthException on failure — caller (AuthController) maps to 401
        var tokens = await _tokenService.ExchangePasswordAsync(command.Email, command.Password, ct);
        _logger.LogInformation("Login successful for {Email}", command.Email);
        return tokens;
    }
}
```

Adicionar `using Microsoft.Extensions.Logging;` se necessário no LoginCommandHandler.

Criar `src/Onboarding.Application/Auth/Commands/RefreshTokenCommand.cs`:

```csharp
using Onboarding.Application.Auth.DTOs;
using Onboarding.Application.Common;

namespace Onboarding.Application.Auth.Commands;

/// <summary>
/// Command: exchange refresh token for a new Keycloak token pair.
/// AUTH-04: backend exposes POST /api/auth/refresh — frontend triggers when token is near expiry.
/// </summary>
public sealed record RefreshTokenCommand(string RefreshToken);

public sealed class RefreshTokenCommandHandler : ICommandHandler<RefreshTokenCommand, TokenResponse>
{
    private readonly IKeycloakTokenService _tokenService;

    public RefreshTokenCommandHandler(IKeycloakTokenService tokenService)
    {
        _tokenService = tokenService;
    }

    public async Task<TokenResponse> HandleAsync(RefreshTokenCommand command, CancellationToken ct = default)
    {
        // IKeycloakTokenService throws KeycloakAuthException on failure — caller (AuthController) maps to 401
        return await _tokenService.RefreshTokenAsync(command.RefreshToken, ct);
    }
}
```

Criar `src/Onboarding.Application/Auth/Validators/LoginCommandValidator.cs`:

```csharp
using FluentValidation;
using Onboarding.Application.Auth.Commands;

namespace Onboarding.Application.Auth.Validators;

public sealed class LoginCommandValidator : AbstractValidator<LoginCommand>
{
    public LoginCommandValidator()
    {
        RuleFor(x => x.Email)
            .NotEmpty().WithMessage("Email is required.")
            .EmailAddress().WithMessage("Email must be a valid email address.");

        RuleFor(x => x.Password)
            .NotEmpty().WithMessage("Password is required.");
    }
}
```

Criar `src/Onboarding.Application/Auth/Validators/RefreshTokenCommandValidator.cs`:

```csharp
using FluentValidation;
using Onboarding.Application.Auth.Commands;

namespace Onboarding.Application.Auth.Validators;

public sealed class RefreshTokenCommandValidator : AbstractValidator<RefreshTokenCommand>
{
    public RefreshTokenCommandValidator()
    {
        RuleFor(x => x.RefreshToken)
            .NotEmpty().WithMessage("refresh_token is required.");
    }
}
```

Modificar `src/Onboarding.Application/DependencyInjection.cs` — adicionar ao final de AddApplication(), ANTES de `return services;`:

```csharp
// Auth commands (Phase 6 — AUTH-02, AUTH-04)
services.AddScoped<ICommandHandler<LoginCommand, TokenResponse>, LoginCommandHandler>();
services.AddScoped<ICommandHandler<RefreshTokenCommand, TokenResponse>, RefreshTokenCommandHandler>();
services.AddScoped<IValidator<LoginCommand>, LoginCommandValidator>();
services.AddScoped<IValidator<RefreshTokenCommand>, RefreshTokenCommandValidator>();
```

Adicionar ao final de `src/Onboarding.API/appsettings.json` dentro do objeto JSON existente, na seção "Keycloak" (que já deve existir de fases anteriores):

```json
"PublicClientId": "onboarding-app"
```

Se a seção "Keycloak" já existir, adicionar apenas o campo `PublicClientId` a ela. Não duplicar a seção inteira.
  </action>
  <verify>
    <automated>dotnet build Onboarding.sln 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/Onboarding.Infrastructure/Keycloak/KeycloakAuthException.cs existe
    - src/Onboarding.Infrastructure/Keycloak/KeycloakTokenService.cs existe
    - KeycloakTokenService.cs contém "grant_type\"] = \"password\""
    - KeycloakTokenService.cs contém "grant_type\"] = \"refresh_token\""
    - KeycloakTokenService.cs contém "throw new KeycloakAuthException(\"Invalid credentials.\")"
    - KeycloakTokenService.cs contém "throw new KeycloakAuthException(\"Invalid or expired refresh token.\")"
    - src/Onboarding.Infrastructure/DependencyInjection.cs contém "\"keycloak-token\""
    - src/Onboarding.Infrastructure/DependencyInjection.cs contém "AddScoped&lt;IKeycloakTokenService, KeycloakTokenService&gt;()"
    - src/Onboarding.Application/Auth/Commands/LoginCommand.cs existe
    - src/Onboarding.Application/Auth/Commands/RefreshTokenCommand.cs existe
    - src/Onboarding.Application/Auth/Validators/LoginCommandValidator.cs existe
    - src/Onboarding.Application/Auth/Validators/RefreshTokenCommandValidator.cs existe
    - src/Onboarding.Application/DependencyInjection.cs contém "LoginCommandHandler"
    - src/Onboarding.Application/DependencyInjection.cs contém "RefreshTokenCommandHandler"
    - src/Onboarding.API/appsettings.json contém "PublicClientId"
    - `dotnet build Onboarding.sln` sai com código 0
  </acceptance_criteria>
  <done>KeycloakTokenService, commands, handlers, validators criados e registrados no DI. Solução compila sem erros.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar AuthController, ClientsController e converter stubs para GREEN</name>
  <files>
    src/Onboarding.API/Controllers/AuthController.cs
    src/Onboarding.API/Controllers/ClientsController.cs
    tests/Onboarding.API.Tests/Authentication/JwtBearerConfigurationTests.cs
    tests/Onboarding.API.Tests/Authentication/AuthorizationMiddlewareTests.cs
    tests/Onboarding.API.Tests/Api/ClientsMeEndpointTests.cs
    tests/Onboarding.API.Tests/Api/LoginEndpointTests.cs
    tests/Onboarding.API.Tests/Api/RefreshTokenEndpointTests.cs
  </files>
  <read_first>
    - src/Onboarding.API/Controllers/RegistrationController.cs (padrão de controller a seguir: FluentValidation → handler → catch específicos)
    - tests/Onboarding.API.Tests/Registration/RegistrationControllerTests.cs (padrão de teste de integração a seguir)
    - tests/Onboarding.API.Tests/Authentication/AuthTestApiFactory.cs (factory com mocks para os testes — Plan 01)
    - tests/Onboarding.API.Tests/Authentication/FakeJwtTokenHelper.cs (gerador de JWT falso — Plan 01)
    - tests/Onboarding.API.Tests/Authentication/JwtBearerConfigurationTests.cs (stubs RED — Plan 01)
    - tests/Onboarding.API.Tests/Api/ClientsMeEndpointTests.cs (stubs RED — Plan 01)
    - tests/Onboarding.API.Tests/Api/LoginEndpointTests.cs (stubs RED — Plan 01)
    - tests/Onboarding.API.Tests/Api/RefreshTokenEndpointTests.cs (stubs RED — Plan 01)
    - src/Onboarding.Application/Auth/DTOs/TokenResponse.cs (DTO de retorno dos controllers)
    - src/Onboarding.Domain/Aggregates/ClientAggregate/Client.cs (estrutura do aggregate para mapear para DTO de perfil)
  </read_first>
  <behavior>
    AuthController.Login: válido → 200+TokenResponse; FluentValidation falha → 422; KeycloakAuthException → 401 genérico.
    AuthController.Refresh: válido → 200+TokenResponse; FluentValidation falha → 422; KeycloakAuthException → 401 genérico.
    ClientsController.GetMe com [Authorize]: sem token → 401 (middleware); com token → busca por email claim; cliente não encontrado → 404 genérico; encontrado → 200 com ClientProfileDto.
    JwtBearerConfigurationTests: verificar options via IOptions<JwtBearerOptions> do ServiceProvider — ValidateAudience=false e MapInboundClaims=false.
    LoginEndpointTests: configurar TokenServiceMock para retornar TokenResponse, POST /api/auth/login, assert 200 e body.
    ClientsMeEndpointTests: configurar RepositoryMock para retornar Client, setar Authorization header via FakeJwtTokenHelper.
  </behavior>
  <action>
Criar `src/Onboarding.API/Controllers/AuthController.cs`:

```csharp
using FluentValidation;
using Microsoft.AspNetCore.Mvc;
using Onboarding.Application.Auth.Commands;
using Onboarding.Application.Auth.DTOs;
using Onboarding.Application.Common;
using Onboarding.Infrastructure.Keycloak;

namespace Onboarding.API.Controllers;

/// <summary>
/// Authentication endpoints — public (no [Authorize]).
/// POST /api/auth/login — D-01: ROPC token exchange via IKeycloakTokenService
/// POST /api/auth/refresh — D-02: token refresh via IKeycloakTokenService
/// </summary>
[ApiController]
[Route("api/[controller]")]
public sealed class AuthController : ControllerBase
{
    private readonly ICommandHandler<LoginCommand, TokenResponse> _loginHandler;
    private readonly ICommandHandler<RefreshTokenCommand, TokenResponse> _refreshHandler;
    private readonly IValidator<LoginCommand> _loginValidator;
    private readonly IValidator<RefreshTokenCommand> _refreshValidator;
    private readonly ILogger<AuthController> _logger;

    public AuthController(
        ICommandHandler<LoginCommand, TokenResponse> loginHandler,
        ICommandHandler<RefreshTokenCommand, TokenResponse> refreshHandler,
        IValidator<LoginCommand> loginValidator,
        IValidator<RefreshTokenCommand> refreshValidator,
        ILogger<AuthController> logger)
    {
        _loginHandler = loginHandler;
        _refreshHandler = refreshHandler;
        _loginValidator = loginValidator;
        _refreshValidator = refreshValidator;
        _logger = logger;
    }

    /// <summary>POST /api/auth/login — AUTH-02: exchange credentials for JWT token pair.</summary>
    [HttpPost("login")]
    [ProducesResponseType(typeof(TokenResponse), StatusCodes.Status200OK)]
    [ProducesResponseType(typeof(ValidationProblemDetails), StatusCodes.Status422UnprocessableEntity)]
    [ProducesResponseType(typeof(ProblemDetails), StatusCodes.Status401Unauthorized)]
    public async Task<IActionResult> Login(
        [FromBody] LoginRequest request,
        CancellationToken ct)
    {
        var command = new LoginCommand(request.Email, request.Password);

        var validation = await _loginValidator.ValidateAsync(command, ct);
        if (!validation.IsValid)
        {
            return UnprocessableEntity(new ValidationProblemDetails(
                validation.Errors
                    .GroupBy(e => e.PropertyName)
                    .ToDictionary(g => g.Key, g => g.Select(e => e.ErrorMessage).ToArray())));
        }

        try
        {
            var tokens = await _loginHandler.HandleAsync(command, ct);
            return Ok(tokens);
        }
        catch (KeycloakAuthException ex)
        {
            // D-13 + SEC-08: generic error — do not reveal if email exists or not
            _logger.LogWarning(ex, "Login attempt failed for email {Email}", request.Email);
            return Unauthorized(new ProblemDetails
            {
                Title = "Authentication failed",
                Status = StatusCodes.Status401Unauthorized,
                Detail = "Invalid credentials."
            });
        }
    }

    /// <summary>POST /api/auth/refresh — AUTH-04: exchange refresh token for new token pair.</summary>
    [HttpPost("refresh")]
    [ProducesResponseType(typeof(TokenResponse), StatusCodes.Status200OK)]
    [ProducesResponseType(typeof(ValidationProblemDetails), StatusCodes.Status422UnprocessableEntity)]
    [ProducesResponseType(typeof(ProblemDetails), StatusCodes.Status401Unauthorized)]
    public async Task<IActionResult> Refresh(
        [FromBody] RefreshTokenRequest request,
        CancellationToken ct)
    {
        var command = new RefreshTokenCommand(request.RefreshToken);

        var validation = await _refreshValidator.ValidateAsync(command, ct);
        if (!validation.IsValid)
        {
            return UnprocessableEntity(new ValidationProblemDetails(
                validation.Errors
                    .GroupBy(e => e.PropertyName)
                    .ToDictionary(g => g.Key, g => g.Select(e => e.ErrorMessage).ToArray())));
        }

        try
        {
            var tokens = await _refreshHandler.HandleAsync(command, ct);
            return Ok(tokens);
        }
        catch (KeycloakAuthException ex)
        {
            // D-13 + SEC-08: generic error for invalid/expired refresh token
            _logger.LogWarning(ex, "Refresh token exchange failed");
            return Unauthorized(new ProblemDetails
            {
                Title = "Authentication failed",
                Status = StatusCodes.Status401Unauthorized,
                Detail = "Invalid or expired refresh token."
            });
        }
    }
}

// HTTP request DTOs for AuthController — thin translation layer before Command
public sealed record LoginRequest(string Email, string Password);
public sealed record RefreshTokenRequest(string RefreshToken);
```

Criar `src/Onboarding.API/Controllers/ClientsController.cs`:

```csharp
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using Onboarding.Domain.Aggregates.ClientAggregate;
using Onboarding.Domain.Repositories;

namespace Onboarding.API.Controllers;

/// <summary>
/// Client profile endpoints.
/// GET /api/clients/me — AUTH-03: protected route returns profile of authenticated client.
/// D-06: [Authorize] without explicit policy — JWT signature + expiry only.
/// </summary>
[ApiController]
[Route("api/[controller]")]
public sealed class ClientsController : ControllerBase
{
    private readonly IClientRepository _repository;
    private readonly ILogger<ClientsController> _logger;

    public ClientsController(
        IClientRepository repository,
        ILogger<ClientsController> logger)
    {
        _repository = repository;
        _logger = logger;
    }

    /// <summary>GET /api/clients/me — returns the authenticated client's profile.</summary>
    [HttpGet("me")]
    [Authorize]
    [ProducesResponseType(StatusCodes.Status200OK)]
    [ProducesResponseType(StatusCodes.Status401Unauthorized)]
    [ProducesResponseType(StatusCodes.Status404NotFound)]
    public async Task<IActionResult> GetMe(CancellationToken ct)
    {
        // D-07: lookup by email claim from JWT (MapInboundClaims=false preserves "email" claim name)
        var email = User.FindFirst("email")?.Value;
        if (string.IsNullOrEmpty(email))
        {
            _logger.LogWarning("Authenticated request missing email claim in JWT");
            return Unauthorized();
        }

        var client = await _repository.GetByEmailAsync(email, ct);
        if (client is null)
        {
            // D-09: 404 with generic ProblemDetails — should not happen in normal flow
            // but must not reveal "user does not exist" (SEC-08)
            _logger.LogWarning("No client found for authenticated email {Email}", email);
            return NotFound(new ProblemDetails
            {
                Title = "Profile not found",
                Status = StatusCodes.Status404NotFound,
                Detail = "No client profile found for this account."
            });
        }

        return Ok(MapToDto(client));
    }

    private static ClientProfileDto MapToDto(Client client) => new(
        Id: client.Id,
        Name: client.Name,
        Email: client.Email.Value,
        Phone: client.Phone.Value,
        Type: client.Type.ToString(),
        Cpf: client.Cpf?.Value,
        Cnpj: client.Cnpj?.Value,
        RazaoSocial: client.RazaoSocial);
}

/// <summary>Read-only profile DTO — returned by GET /api/clients/me (PROF-02 wiring point).</summary>
public sealed record ClientProfileDto(
    Guid Id,
    string Name,
    string Email,
    string Phone,
    string Type,
    string? Cpf,
    string? Cnpj,
    string? RazaoSocial);
```

Agora converter os stubs RED para GREEN nos arquivos de teste. Substituir o conteúdo de cada arquivo de stub:

`tests/Onboarding.API.Tests/Authentication/JwtBearerConfigurationTests.cs` — substituir completamente:

```csharp
using Microsoft.AspNetCore.Authentication.JwtBearer;
using Microsoft.AspNetCore.Mvc.Testing;
using Microsoft.Extensions.DependencyInjection;
using Microsoft.Extensions.Options;
using Onboarding.API.Tests.Authentication;
using Shouldly;

namespace Onboarding.API.Tests.Authentication;

/// <summary>
/// GREEN tests for AUTH-02: JWT Bearer is configured with correct parameters.
/// Verifies AddJwtBearer options without starting HTTP server.
/// </summary>
[Collection(WebAppFactoryCollection.Name)]
public class JwtBearerConfigurationTests : IAsyncLifetime
{
    private AuthTestApiFactory? _factory;

    public Task InitializeAsync()
    {
        _factory = new AuthTestApiFactory();
        return Task.CompletedTask;
    }

    public async Task DisposeAsync()
    {
        if (_factory is not null)
            await _factory.DisposeAsync();
    }

    [Fact]
    [Trait("Category", "Unit")]
    public void JwtBearer_Should_BeConfiguredWithKeycloakAuthority()
    {
        using var scope = _factory!.Services.CreateScope();
        var options = scope.ServiceProvider
            .GetRequiredService<IOptionsSnapshot<JwtBearerOptions>>()
            .Get(JwtBearerDefaults.AuthenticationScheme);

        // Authority is set (PostConfigure resets ValidateIssuer=false, not Authority)
        // We verify the service resolved without exception — Authority set via UseSetting in factory
        options.ShouldNotBeNull();
    }

    [Fact]
    [Trait("Category", "Unit")]
    public void JwtBearer_Should_HaveValidateAudienceDisabled()
    {
        using var scope = _factory!.Services.CreateScope();
        var options = scope.ServiceProvider
            .GetRequiredService<IOptionsSnapshot<JwtBearerOptions>>()
            .Get(JwtBearerDefaults.AuthenticationScheme);

        // D-05: ValidateAudience = false — PostConfigure in AuthTestApiFactory sets this
        options.TokenValidationParameters.ValidateAudience.ShouldBeFalse();
    }

    [Fact]
    [Trait("Category", "Unit")]
    public void JwtBearer_Should_HaveValidateLifetimeDisabled_InTests()
    {
        using var scope = _factory!.Services.CreateScope();
        var options = scope.ServiceProvider
            .GetRequiredService<IOptionsSnapshot<JwtBearerOptions>>()
            .Get(JwtBearerDefaults.AuthenticationScheme);

        // PostConfigure in AuthTestApiFactory disables lifetime validation for test tokens
        options.TokenValidationParameters.ValidateLifetime.ShouldBeFalse();
    }
}
```

`tests/Onboarding.API.Tests/Authentication/AuthorizationMiddlewareTests.cs` — substituir completamente:

```csharp
using Shouldly;
using System.Net;

namespace Onboarding.API.Tests.Authentication;

/// <summary>
/// GREEN tests for AUTH-02: [Authorize] middleware returns 401 without Bearer token.
/// </summary>
[Collection(WebAppFactoryCollection.Name)]
public class AuthorizationMiddlewareTests : IAsyncLifetime
{
    private AuthTestApiFactory? _factory;
    private HttpClient? _client;

    public Task InitializeAsync()
    {
        _factory = new AuthTestApiFactory();
        _client = _factory.CreateClient(new WebApplicationFactoryClientOptions
        {
            AllowAutoRedirect = false
        });
        return Task.CompletedTask;
    }

    public async Task DisposeAsync()
    {
        _client?.Dispose();
        if (_factory is not null)
            await _factory.DisposeAsync();
    }

    [Fact]
    [Trait("Category", "Unit")]
    public async Task GetClientsMe_WithoutToken_Returns401()
    {
        // D-03: [Authorize] on GET /api/clients/me — no Bearer = 401
        var response = await _client!.GetAsync("/api/clients/me");

        response.StatusCode.ShouldBe(HttpStatusCode.Unauthorized);
    }
}
```

`tests/Onboarding.API.Tests/Api/ClientsMeEndpointTests.cs` — substituir completamente:

```csharp
using Onboarding.Domain.Aggregates.ClientAggregate;
using Onboarding.API.Tests.Authentication;
using NSubstitute;
using Shouldly;
using System.Net;
using System.Net.Http.Headers;
using System.Net.Http.Json;

namespace Onboarding.API.Tests.Api;

/// <summary>
/// GREEN tests for AUTH-03: GET /api/clients/me behavior with JWT.
/// </summary>
[Collection(WebAppFactoryCollection.Name)]
public class ClientsMeEndpointTests : IAsyncLifetime
{
    private AuthTestApiFactory? _factory;
    private HttpClient? _client;

    private const string TestEmail = "joao@example.com";

    public Task InitializeAsync()
    {
        _factory = new AuthTestApiFactory();
        _client = _factory.CreateClient(new WebApplicationFactoryClientOptions
        {
            AllowAutoRedirect = false
        });
        return Task.CompletedTask;
    }

    public async Task DisposeAsync()
    {
        _client?.Dispose();
        if (_factory is not null)
            await _factory.DisposeAsync();
    }

    [Fact]
    [Trait("Category", "Integration")]
    public async Task GetMe_WithValidToken_Returns200WithClientProfile()
    {
        // Arrange — mock repository returns a PF client for the authenticated email
        var client = Client.RegisterPessoaFisica("João Silva", "529.982.247-25", TestEmail, "11999999999");
        _factory!.RepositoryMock
            .GetByEmailAsync(TestEmail.ToLowerInvariant(), Arg.Any<CancellationToken>())
            .Returns(client);

        _client!.DefaultRequestHeaders.Authorization =
            new AuthenticationHeaderValue("Bearer", FakeJwtTokenHelper.GenerateFakeJwt(TestEmail));

        // Act
        var response = await _client.GetAsync("/api/clients/me");

        // Assert
        response.StatusCode.ShouldBe(HttpStatusCode.OK);
        var body = await response.Content.ReadFromJsonAsync<Dictionary<string, object>>();
        body.ShouldNotBeNull();
        body.ContainsKey("email").ShouldBeTrue();
    }

    [Fact]
    [Trait("Category", "Integration")]
    public async Task GetMe_WithoutToken_Returns401()
    {
        // No Authorization header
        var response = await _client!.GetAsync("/api/clients/me");

        response.StatusCode.ShouldBe(HttpStatusCode.Unauthorized);
    }

    [Fact]
    [Trait("Category", "Integration")]
    public async Task GetMe_WhenClientNotFoundInDb_Returns404()
    {
        // Arrange — repository returns null (client authenticated but not in app_db)
        _factory!.RepositoryMock
            .GetByEmailAsync(Arg.Any<string>(), Arg.Any<CancellationToken>())
            .Returns((Client?)null);

        _client!.DefaultRequestHeaders.Authorization =
            new AuthenticationHeaderValue("Bearer", FakeJwtTokenHelper.GenerateFakeJwt(TestEmail));

        // Act
        var response = await _client.GetAsync("/api/clients/me");

        // Assert — D-09: 404 with generic ProblemDetails (not "user does not exist")
        response.StatusCode.ShouldBe(HttpStatusCode.NotFound);
    }
}
```

`tests/Onboarding.API.Tests/Api/LoginEndpointTests.cs` — substituir completamente:

```csharp
using Onboarding.Application.Auth.DTOs;
using Onboarding.API.Tests.Authentication;
using NSubstitute;
using Shouldly;
using System.Net;
using System.Net.Http.Json;
using Onboarding.Infrastructure.Keycloak;

namespace Onboarding.API.Tests.Api;

/// <summary>
/// GREEN tests for AUTH-02/AUTH-04: POST /api/auth/login behavior.
/// </summary>
[Collection(WebAppFactoryCollection.Name)]
public class LoginEndpointTests : IAsyncLifetime
{
    private AuthTestApiFactory? _factory;
    private HttpClient? _client;

    private static readonly TokenResponse FakeTokens = new(
        AccessToken: "fake-access-token",
        RefreshToken: "fake-refresh-token",
        ExpiresIn: 300,
        TokenType: "Bearer",
        RefreshExpiresIn: 1800,
        Scope: "openid email profile");

    public Task InitializeAsync()
    {
        _factory = new AuthTestApiFactory();
        _client = _factory.CreateClient();
        return Task.CompletedTask;
    }

    public async Task DisposeAsync()
    {
        _client?.Dispose();
        if (_factory is not null)
            await _factory.DisposeAsync();
    }

    [Fact]
    [Trait("Category", "Integration")]
    public async Task Login_WithValidCredentials_Returns200WithTokens()
    {
        // Arrange — mock token service returns fake token pair
        _factory!.TokenServiceMock
            .ExchangePasswordAsync("joao@example.com", "Senha@123!", Arg.Any<CancellationToken>())
            .Returns(FakeTokens);

        var payload = new { email = "joao@example.com", password = "Senha@123!" };

        // Act
        var response = await _client!.PostAsJsonAsync("/api/auth/login", payload);

        // Assert — AUTH-02: 200 with access_token + refresh_token
        response.StatusCode.ShouldBe(HttpStatusCode.OK);
        var body = await response.Content.ReadFromJsonAsync<Dictionary<string, object>>();
        body.ShouldNotBeNull();
        body.ContainsKey("accessToken").ShouldBeTrue();
        body.ContainsKey("refreshToken").ShouldBeTrue();
    }

    [Fact]
    [Trait("Category", "Integration")]
    public async Task Login_WithInvalidCredentials_Returns401WithGenericMessage()
    {
        // Arrange — mock throws KeycloakAuthException (Keycloak 401)
        _factory!.TokenServiceMock
            .ExchangePasswordAsync(Arg.Any<string>(), Arg.Any<string>(), Arg.Any<CancellationToken>())
            .ThrowsAsync(new KeycloakAuthException("Invalid credentials."));

        var payload = new { email = "joao@example.com", password = "WrongPassword" };

        // Act
        var response = await _client!.PostAsJsonAsync("/api/auth/login", payload);

        // Assert — D-13 + SEC-08: 401 with generic message (no user enumeration)
        response.StatusCode.ShouldBe(HttpStatusCode.Unauthorized);
    }

    [Fact]
    [Trait("Category", "Integration")]
    public async Task Login_WithMissingEmail_Returns422()
    {
        var payload = new { email = (string?)null, password = "Senha@123!" };

        var response = await _client!.PostAsJsonAsync("/api/auth/login", payload);

        response.StatusCode.ShouldBe(HttpStatusCode.UnprocessableEntity);
    }

    [Fact]
    [Trait("Category", "Integration")]
    public async Task Login_WithMissingPassword_Returns422()
    {
        var payload = new { email = "joao@example.com", password = (string?)null };

        var response = await _client!.PostAsJsonAsync("/api/auth/login", payload);

        response.StatusCode.ShouldBe(HttpStatusCode.UnprocessableEntity);
    }
}
```

`tests/Onboarding.API.Tests/Api/RefreshTokenEndpointTests.cs` — substituir completamente:

```csharp
using Onboarding.Application.Auth.DTOs;
using Onboarding.API.Tests.Authentication;
using NSubstitute;
using Shouldly;
using System.Net;
using System.Net.Http.Json;
using Onboarding.Infrastructure.Keycloak;

namespace Onboarding.API.Tests.Api;

/// <summary>
/// GREEN tests for AUTH-04: POST /api/auth/refresh behavior.
/// </summary>
[Collection(WebAppFactoryCollection.Name)]
public class RefreshTokenEndpointTests : IAsyncLifetime
{
    private AuthTestApiFactory? _factory;
    private HttpClient? _client;

    private static readonly TokenResponse FakeTokens = new(
        AccessToken: "new-fake-access-token",
        RefreshToken: "new-fake-refresh-token",
        ExpiresIn: 300,
        TokenType: "Bearer",
        RefreshExpiresIn: 1800,
        Scope: "openid email profile");

    public Task InitializeAsync()
    {
        _factory = new AuthTestApiFactory();
        _client = _factory.CreateClient();
        return Task.CompletedTask;
    }

    public async Task DisposeAsync()
    {
        _client?.Dispose();
        if (_factory is not null)
            await _factory.DisposeAsync();
    }

    [Fact]
    [Trait("Category", "Integration")]
    public async Task Refresh_WithValidRefreshToken_Returns200WithNewTokens()
    {
        // Arrange
        _factory!.TokenServiceMock
            .RefreshTokenAsync("valid-refresh-token", Arg.Any<CancellationToken>())
            .Returns(FakeTokens);

        var payload = new { refreshToken = "valid-refresh-token" };

        // Act
        var response = await _client!.PostAsJsonAsync("/api/auth/refresh", payload);

        // Assert — AUTH-04: 200 with new token pair
        response.StatusCode.ShouldBe(HttpStatusCode.OK);
        var body = await response.Content.ReadFromJsonAsync<Dictionary<string, object>>();
        body.ShouldNotBeNull();
        body.ContainsKey("accessToken").ShouldBeTrue();
    }

    [Fact]
    [Trait("Category", "Integration")]
    public async Task Refresh_WithInvalidRefreshToken_Returns401WithGenericMessage()
    {
        // Arrange — mock throws KeycloakAuthException
        _factory!.TokenServiceMock
            .RefreshTokenAsync(Arg.Any<string>(), Arg.Any<CancellationToken>())
            .ThrowsAsync(new KeycloakAuthException("Invalid or expired refresh token."));

        var payload = new { refreshToken = "expired-or-invalid-token" };

        // Act
        var response = await _client!.PostAsJsonAsync("/api/auth/refresh", payload);

        // Assert — D-13 + SEC-08: generic error
        response.StatusCode.ShouldBe(HttpStatusCode.Unauthorized);
    }

    [Fact]
    [Trait("Category", "Integration")]
    public async Task Refresh_WithMissingRefreshToken_Returns422()
    {
        var payload = new { refreshToken = (string?)null };

        var response = await _client!.PostAsJsonAsync("/api/auth/refresh", payload);

        response.StatusCode.ShouldBe(HttpStatusCode.UnprocessableEntity);
    }
}
```
  </action>
  <verify>
    <automated>dotnet test tests/Onboarding.API.Tests/ --filter "Category=Unit|Category=Integration" --no-build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/Onboarding.API/Controllers/AuthController.cs existe
    - AuthController.cs contém "[HttpPost(\"login\")]"
    - AuthController.cs contém "[HttpPost(\"refresh\")]"
    - AuthController.cs contém "catch (KeycloakAuthException"
    - AuthController.cs contém "Detail = \"Invalid credentials.\""
    - src/Onboarding.API/Controllers/ClientsController.cs existe
    - ClientsController.cs contém "[Authorize]"
    - ClientsController.cs contém "User.FindFirst(\"email\")"
    - ClientsController.cs contém "GetByEmailAsync(email, ct)"
    - ClientsController.cs contém "Detail = \"No client profile found for this account.\""
    - `dotnet test tests/Onboarding.API.Tests/ --filter "FullyQualifiedName~JwtBearerConfigurationTests"` passa (todos GREEN)
    - `dotnet test tests/Onboarding.API.Tests/ --filter "FullyQualifiedName~AuthorizationMiddlewareTests"` passa (todos GREEN)
    - `dotnet test tests/Onboarding.API.Tests/ --filter "FullyQualifiedName~LoginEndpointTests"` passa (todos GREEN)
    - `dotnet test tests/Onboarding.API.Tests/ --filter "FullyQualifiedName~RefreshTokenEndpointTests"` passa (todos GREEN)
    - `dotnet test tests/Onboarding.API.Tests/ --filter "FullyQualifiedName~ClientsMeEndpointTests"` passa (todos GREEN)
    - `dotnet test tests/Onboarding.API.Tests/` não reporta nenhum teste FAILED
  </acceptance_criteria>
  <done>AuthController e ClientsController criados. Todos os 12 stubs RED do Plan 01 convertidos para GREEN. Ciclo TDD completo: RED (Plan 01) → GREEN (Plan 03).</done>
</task>

</tasks>

<threat_model>
  <threat id="T-6-06" severity="critical">
    <description>POST /api/auth/login revela se um email existe no sistema — diferenciando "usuário não existe" de "senha incorreta" na resposta. Viola SEC-08 (information leakage).</description>
    <mitigation>KeycloakAuthException captura todos os erros 4xx do Keycloak. AuthController mapeia para uma única mensagem genérica "Invalid credentials." em 401 — sem distinção entre email inválido e senha inválida. Implementado explicitamente na Task 2. Teste Login_WithInvalidCredentials_Returns401WithGenericMessage verifica o comportamento.</mitigation>
  </threat>
  <threat id="T-6-07" severity="critical">
    <description>JWT tokens expirados são aceitos como válidos se ValidateLifetime não for restaurado para true em produção (apenas desabilitado em testes via PostConfigure).</description>
    <mitigation>PostConfigure com ValidateLifetime=false está SOMENTE em AuthTestApiFactory no projeto de testes — não afeta produção. Em produção, AddJwtBearer usa defaults: ValidateLifetime=true. Estrutura de projetos garante isolamento.</mitigation>
  </threat>
  <threat id="T-6-08" severity="high">
    <description>GET /api/clients/me com token JWT de outro sistema (não Keycloak) poderia ser aceito se issuer não for validado.</description>
    <mitigation>AddJwtBearer com Authority = Keycloak:RealmUrl obtém as chaves públicas do Keycloak via OIDC discovery — apenas tokens assinados pelo Keycloak são aceitos. ValidateIssuer=true é o default. Comportamento correto em produção.</mitigation>
  </threat>
  <threat id="T-6-09" severity="high">
    <description>Senha do usuário em texto plano nos logs de request do Serilog (UseSerilogRequestLogging loga o body).</description>
    <mitigation>UseSerilogRequestLogging() NÃO loga o body por default — apenas method, path, status e duration. SensitiveDataDestructuringPolicy (Phase 4) mascara "password" e "token" em objetos estruturados. LoginCommandHandler loga apenas o email (não a senha). Behavior verificado em Phase 4.</mitigation>
  </threat>
  <threat id="T-6-10" severity="medium">
    <description>IdempotencyFilter aplicado em POST /api/auth/login causaria segundo login com mesma chave retornar access_token expirado do primeiro request.</description>
    <mitigation>AuthController NÃO tem atributo [Idempotent] — explicitamente omitido. Pitfall 6 do RESEARCH.md documentado. Verificar que [Idempotent] não está presente no AuthController (grep no acceptance_criteria).</mitigation>
  </threat>
</threat_model>

<verification>
Após executar este plano:

```bash
# Solução compila completamente
dotnet build Onboarding.sln

# Todos os testes de auth passam (GREEN)
dotnet test tests/Onboarding.API.Tests/ --filter "Category=Unit"
dotnet test tests/Onboarding.API.Tests/ --filter "Category=Integration"

# Confirmar endpoints existem
grep -n "HttpPost\|HttpGet\|Authorize" src/Onboarding.API/Controllers/AuthController.cs
grep -n "HttpPost\|HttpGet\|Authorize" src/Onboarding.API/Controllers/ClientsController.cs

# Confirmar SEC-08: mensagens genéricas
grep -n "Invalid credentials\|generic\|SEC-08" src/Onboarding.API/Controllers/AuthController.cs

# Confirmar D-07: lookup por email claim
grep -n "FindFirst.*email" src/Onboarding.API/Controllers/ClientsController.cs

# Confirmar [Idempotent] NÃO está em AuthController
grep -n "Idempotent" src/Onboarding.API/Controllers/AuthController.cs && echo "PROBLEMA: Idempotent encontrado" || echo "OK: sem Idempotent em AuthController"
```
</verification>

<success_criteria>
- `dotnet build Onboarding.sln` passa sem erros
- `dotnet test tests/Onboarding.API.Tests/` passa com 0 falhas
- POST /api/auth/login implementado com FluentValidation + KeycloakAuthException → 401 genérico
- POST /api/auth/refresh implementado com FluentValidation + KeycloakAuthException → 401 genérico
- GET /api/clients/me implementado com [Authorize] + lookup por email claim + 404 genérico
- KeycloakTokenService faz ROPC via named HttpClient "keycloak-token" sem Duende.AccessTokenManagement
- Todos os 12 stubs RED do Plan 01 estão GREEN
- Nenhuma mensagem de erro revela informação sobre existência de usuários (SEC-08)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-authentication-api/06-03-SUMMARY.md` com:
- Arquivos criados e modificados
- Endpoints implementados (URLs, métodos, comportamentos de erro)
- Contagem final de testes: X passou de Y (todos GREEN)
- Padrões de segurança implementados (SEC-08, D-07, D-13)
- Notas para Phase 9 (frontend): POST /api/auth/login e POST /api/auth/refresh estão disponíveis
</output>

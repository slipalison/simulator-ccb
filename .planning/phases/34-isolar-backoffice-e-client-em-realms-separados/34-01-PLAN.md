---
wave: 1
depends_on: []
files_modified:
  - "keycloak/backoffice-realm.json"
  - "keycloak/client-realm.json"
  - "docker-compose.yml"
  - "compose.yaml"
autonomous: true
---

# Plan 34-01: Keycloak Configuration & Infrastructure

## Tasks

```xml
<task>
  <description>Create Backoffice Realm JSON</description>
  <read_first>
    - compose.yaml
  </read_first>
  <action>
    Export or manually create `keycloak/backoffice-realm.json`.
    - ID: `backoffice`
    - Realm: `backoffice`
    - Clients: `onboarding-backoffice`, `onboarding-api-admin`
    - Keep required actions and brute force settings.
  </action>
  <acceptance_criteria>
    - `keycloak/backoffice-realm.json` contains `"realm": "backoffice"`
    - Client `onboarding-backoffice` exists in the file.
  </acceptance_criteria>
</task>

<task>
  <description>Create Client Realm JSON</description>
  <read_first>
    - keycloak/backoffice-realm.json
  </read_first>
  <action>
    Export or manually create `keycloak/client-realm.json`.
    - ID: `client`
    - Realm: `client`
    - Clients: `onboarding-client-acf`, `onboarding-client-ropc`
    - Keep required actions and brute force settings.
  </action>
  <acceptance_criteria>
    - `keycloak/client-realm.json` contains `"realm": "client"`
  </acceptance_criteria>
</task>

<task>
  <description>Update Docker Compose Keycloak Volume</description>
  <read_first>
    - compose.yaml
  </read_first>
  <action>
    Update the `keycloak` service in `compose.yaml`:
    - Delete the single file mount `- ./keycloak/onboarding-realm.json:/opt/keycloak/data/import/onboarding-realm.json:ro`
    - Add `- ./keycloak:/opt/keycloak/data/import:ro` (so Keycloak imports all JSONs in the folder)
  </action>
  <acceptance_criteria>
    - `compose.yaml` contains `import:ro` for the keycloak directory.
  </acceptance_criteria>
</task>

<task>
  <description>Update Frontend Environment Variables</description>
  <read_first>
    - compose.yaml
  </read_first>
  <action>
    Update `frontend-backoffice` service:
    - Set `KEYCLOAK_REALM: backoffice`
    Update `frontend-client` service:
    - Set `KEYCLOAK_REALM: client`
    Update `.env.example` to reflect the changes.
  </action>
  <acceptance_criteria>
    - `compose.yaml` `frontend-backoffice` specifies `KEYCLOAK_REALM: backoffice`
  </acceptance_criteria>
</task>
```

## Verification
- Run `docker compose down -v` and `docker compose up -d keycloak`.
- Check Keycloak logs verifying both `backoffice` and `client` realms were imported.

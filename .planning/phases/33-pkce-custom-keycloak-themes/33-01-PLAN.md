---
phase: 33-pkce-custom-keycloak-themes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - keycloak/onboarding-realm.json
  - frontend/client/auth-server.ts
  - frontend/client/app.config.ts
  - frontend/client/src/lib/auth-code-flow.ts
  - frontend/client/src/lib/auth-context.tsx
  - frontend/client/src/lib/api.ts
  - frontend/client/src/components/pages/AuthLoginPage.tsx
  - frontend/client/src/components/pages/AuthCallbackPage.tsx
  - frontend/client/src/components/pages/AuthErrorPage.tsx
  - frontend/client/src/router.tsx
  - frontend/client/src/tests/auth-context.test.tsx
  - frontend/client/src/tests/login-flow.test.tsx
  - compose.yaml
  - .env
autonomous: true
requirements:
  - PKC-01
  - PKC-02
  - PKC-03
  - PKC-04
  - PKC-05
  - PKC-06

must_haves:
  truths:
    - "Client app usa ACF+PKCE — login via redirect para Keycloak, sem formulário ROPC"
    - "Client auth-server.ts faz code exchange server-side — tokens nunca expostos ao browser JS"
    - "Tokens em cookies httpOnly gerenciados pelo Vinxi server"
    - "Custom Keycloak Theme onboarding-client replica identidade visual do client app"
    - "Custom Keycloak Theme onboarding-backoffice replica identidade visual do backoffice"
    - "Logout limpa cookies e redirect para Keycloak OIDC logout"
  artifacts:
    - path: "frontend/client/auth-server.ts"
      provides: "Vinxi h3 server-side auth routes para client app"
    - path: "frontend/client/src/lib/auth-code-flow.ts"
      provides: "PKCE utilities para client app"
    - path: "frontend/client/src/lib/auth-context.tsx"
      provides: "Auth context com login()/logout() como redirects síncronos"
  key_links:
    - from: "client AuthLoginPage"
      to: "/auth/login"
      via: "window.location.href"
    - from: "client auth-server.ts /callback"
      to: "httpOnly cookies"
      via: "setCookie httpOnly=true"
    - from: "Keycloak theme onboarding-client"
      to: "client app visual identity"
      via: "CSS + FreeMarker templates"
---

<objective>
Migrar client app de ROPC para ACF+PKCE (mesmo padrão do backoffice Phase 31) e criar Custom Keycloak Themes para ambos os apps.

Purpose: Client app usa ACF+PKCE (confidential client, server-side code exchange, httpOnly cookies). Custom themes fazem login no Keycloak parecer nativo de cada app.
Output: Client auth-server.ts, auth-code-flow.ts, auth pages, auth context reescrito, ROPC removido, 2 custom themes, realm.json atualizado.
</objective>

<execution_context>
@~/.qwen/get-shit-done/workflows/execute-plan.md
@~/.qwen/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-pkce-custom-keycloak-themes/33-CONTEXT.md
@.planning/phases/31-backoffice-auth-code-flow-ui/31-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client ACF Migration — Keycloak client + infra</name>
  <files>keycloak/onboarding-realm.json, .env, compose.yaml</files>
  <read_first>
    - keycloak/onboarding-realm.json (clients existentes)
    - .env (variáveis existentes)
    - compose.yaml (serviço frontend-client)
  </read_first>
  <action>
Adicionar client `onboarding-client-acf` ao realm.json (após `onboarding-app`):
- clientId: onboarding-client-acf, confidential (client-secret)
- standardFlowEnabled: true, directAccessGrantsEnabled: false, publicClient: false
- redirectUris: ["http://localhost:5173/auth/callback"]
- webOrigins: ["http://localhost:5173"]
- attributes: pkce.code.challenge.method=S256, access.token.lifespan=300
- optionalClientScopes: ["offline_access"]

.env: adicionar KEYCLOAK_CLIENT_ACF_CLIENT_ID e KEYCLOAK_CLIENT_ACF_CLIENT_SECRET

compose.yaml: adicionar environment vars e volume mount para auth-server.ts no serviço frontend-client (mesmo padrão do backoffice).
  </action>
  <done>
    - realm.json contém onboarding-client-acf com standardFlowEnabled=true, pkce S256
    - .env contém KEYCLOAK_CLIENT_ACF_CLIENT_ID e SECRET
    - compose.yaml tem environment + volume mount no frontend-client
  </done>
  <acceptance_criteria>
    - grep "onboarding-client-acf" keycloak/onboarding-realm.json retorna match
    - grep "KEYCLOAK_CLIENT_ACF_CLIENT" .env retorna 2 matches
    - grep "auth-server.ts" compose.yaml retorna match para frontend-client
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Client PKCE utils + Vinxi auth-server + app.config</name>
  <files>
    frontend/client/src/lib/auth-code-flow.ts,
    frontend/client/auth-server.ts,
    frontend/client/app.config.ts
  </files>
  <read_first>
    - frontend/client/app.config.ts (estrutura Vinxi atual)
    - frontend/client/package.json (dependências)
    - frontend/backoffice/auth-server.ts (referência — copiar padrão)
    - frontend/backoffice/src/lib/auth-code-flow.ts (referência — copiar padrão)
  </read_first>
  <action>
**auth-code-flow.ts:** Copiar padrão do backoffice com ajustes para client app (URLs, ports). Funções: generateCodeVerifier, generateCodeChallenge, buildAuthorizationUrl, exchangeCodeForTokens, refreshAccessToken.

**auth-server.ts:** Criar em `frontend/client/auth-server.ts` com mesmas 5 rotas do backoffice (/login, /callback, /logout, /me, /refresh). Usar variáveis de ambiente do client (KEYCLOAK_CLIENT_ACF_CLIENT_ID, etc). Cookies: `client_access_token` e `client_refresh_token` (nomes diferentes do backoffice para evitar conflito).

**app.config.ts:** Adicionar router `/auth` apontando para auth-server.ts (mesmo padrão do backoffice).
  </action>
  <done>
    - auth-code-flow.ts exporta funções PKCE
    - auth-server.ts tem rotas /login, /callback, /logout, /me, /refresh
    - app.config.ts tem rota /auth configurada
    - npx tsc --noEmit exits 0 (frontend/client)
  </done>
  <acceptance_criteria>
    - grep "generateCodeVerifier" frontend/client/src/lib/auth-code-flow.ts retorna match
    - grep "createRouter" frontend/client/auth-server.ts retorna match
    - grep "client_access_token" frontend/client/auth-server.ts retorna match
    - grep "/auth" frontend/client/app.config.ts retorna match
    - npx tsc --noEmit exits 0 (no frontend/client)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Client frontend pages + auth context + router + remover ROPC</name>
  <files>
    frontend/client/src/lib/auth-context.tsx,
    frontend/client/src/lib/api.ts,
    frontend/client/src/components/pages/AuthLoginPage.tsx,
    frontend/client/src/components/pages/AuthCallbackPage.tsx,
    frontend/client/src/components/pages/AuthErrorPage.tsx,
    frontend/client/src/router.tsx,
    frontend/client/src/tests/auth-context.test.tsx,
    frontend/client/src/tests/login-flow.test.tsx
  </files>
  <read_first>
    - frontend/client/src/lib/auth-context.tsx (atual ROPC)
    - frontend/client/src/lib/api.ts (loginClient, logoutClient — a remover)
    - frontend/client/src/components/pages/LoginPage.tsx (formulário ROPC atual)
    - frontend/client/src/router.tsx (rotas atuais)
    - frontend/backoffice/src/lib/admin-auth-context.tsx (referência ACF)
    - frontend/backoffice/src/components/pages/AuthLoginPage.tsx (referência)
  </read_first>
  <action>
**AuthLoginPage.tsx:** Redirect-only component. useEffect com window.location.href = '/auth/login'. Loading spinner. Se autenticado, redirect para /profile.

**AuthCallbackPage.tsx:** Polling GET /auth/me. Se 200 → redirect /profile. Se erro → error card com link para /auth/login.

**AuthErrorPage.tsx:** Lê ?error= da URL, exibe mensagem com link para /auth/login.

**auth-context.tsx:** Reescrever (padrão backoffice):
- login() → void, window.location.href = '/auth/login'
- logout() → void, window.location.href = '/auth/logout'
- useEffect para GET /auth/me no mount
- AuthValue: { auth: { isAuthenticated, isLoading }, login, logout }

**api.ts:** REMOVER loginClient, logoutClient, LoginResponse. Manter funções de registration e profile.

**router.tsx:**
- Substituir /login → /auth/login (AuthLoginPage)
- Adicionar /auth/callback (AuthCallbackPage)
- Adicionar /auth/error (AuthErrorPage)
- ProtectedRoute verifica auth.isAuthenticated

**Testes:** Atualizar auth-context.test.tsx e login-flow.test.tsx para ACF. Remover testes ROPC.

**LoginPage.tsx:** Deletar (substituído por AuthLoginPage).
  </action>
  <done>
    - AuthLoginPage, AuthCallbackPage, AuthErrorPage criados
    - auth-context.tsx usa redirects síncronos
    - api.ts sem loginClient, logoutClient
    - router.tsx com rotas /auth/*
    - LoginPage.tsx deletado
    - Testes atualizados e passando
    - npx vitest run exits 0 (frontend/client)
  </done>
  <acceptance_criteria>
    - grep "window.location.href.*auth/login" frontend/client/src/lib/auth-context.tsx retorna match
    - grep "loginClient" frontend/client/src/lib/api.ts retorna zero resultados
    - grep "AuthLoginPage" frontend/client/src/router.tsx retorna match
    - npx vitest run exits 0 (no frontend/client)
    - npx tsc --noEmit exits 0 (no frontend/client)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Custom Keycloak Themes (client + backoffice)</name>
  <files>
    keycloak/themes/onboarding-client/login/login.ftl
    keycloak/themes/onboarding-client/login/theme.properties
    keycloak/themes/onboarding-client/login/resources/css/styles.css
    keycloak/themes/onboarding-backoffice/login/login.ftl
    keycloak/themes/onboarding-backoffice/login/theme.properties
    keycloak/themes/onboarding-backoffice/login/resources/css/styles.css
    keycloak/onboarding-realm.json
    compose.yaml
  </files>
  <read_first>
    - keycloak/onboarding-realm.json (realm settings — onde configurar theme)
    - compose.yaml (serviço keycloak — volumes)
  </read_first>
  <action>
**Theme onboarding-client:**
- theme.properties: parent=keycloak, importFromLegacy=false
- login.ftl: formulário de login com estilo do client app (cores primárias, logo placeholder, mensagens amigáveis, link para registro)
- styles.css: variáveis CSS do client app (cores, fontes)
- template.ftl: herdar do base com header/footer customizados

**Theme onboarding-backoffice:**
- theme.properties: parent=keycloak, importFromLegacy=false
- login.ftl: formulário de login administrativo (sem link de registro, mensagem de acesso restrito, estilo profissional)
- styles.css: cores neutras, dark-friendly
- template.ftl: layout administrativo

**realm.json:** Adicionar themes aos realm settings:
```json
"loginTheme": "onboarding-client",
"accountTheme": "onboarding-client",
"adminTheme": "keycloak",
```

**compose.yaml:** Adicionar volume mount para themes no serviço keycloak:
```yaml
volumes:
  - ./keycloak/themes:/opt/keycloak/themes:ro
```

**Nota:** Os themes são aplicados ao realm. O client app usa onboarding-client theme, o backoffice usa onboarding-backoffice theme. Keycloak seleciona o theme baseado no client — configurar client attributes com `login_theme` property.
  </action>
  <done>
    - keycloak/themes/onboarding-client/ existe com login.ftl, theme.properties, styles.css
    - keycloak/themes/onboarding-backoffice/ existe com login.ftl, theme.properties, styles.css
    - realm.json tem themes configurados
    - compose.yaml tem volume mount para themes
    - docker compose up funciona com themes carregados
  </done>
  <acceptance_criteria>
    - test -d keycloak/themes/onboarding-client/login retorna true
    - test -d keycloak/themes/onboarding-backoffice/login retorna true
    - grep "loginTheme" keycloak/onboarding-realm.json retorna match
    - grep "themes" compose.yaml retorna match para keycloak service
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat | Component | Mitigation |
|--------|-----------|------------|
| CLIENT_SECRET exposto | client/auth-server.ts | Executa apenas no contexto Vinxi server |
| Cookie secure=false | client/auth-server.ts | Intencional para dev HTTP. Produção requer Secure=true |
| Theme XSS | FreeMarker templates | Keycloak sanitiza output. Sem input do usuário nos templates. |
| CSRF | httpOnly + SameSite=Strict cookies | Sem necessidade de CSRF token |
</threat_model>

<verification>
1. grep "onboarding-client-acf" keycloak/onboarding-realm.json
2. grep "KEYCLOAK_CLIENT_ACF_CLIENT" .env
3. grep "generateCodeVerifier" frontend/client/src/lib/auth-code-flow.ts
4. grep "createRouter" frontend/client/auth-server.ts
5. grep "client_access_token" frontend/client/auth-server.ts
6. grep "window.location.href.*auth/login" frontend/client/src/lib/auth-context.tsx
7. grep "loginClient" frontend/client/src/lib/api.ts → zero
8. npx tsc --noEmit (frontend/client) exits 0
9. npx vitest run (frontend/client) exits 0
10. test -d keycloak/themes/onboarding-client/login
11. test -d keycloak/themes/onboarding-backoffice/login
</verification>

<success_criteria>
- PKC-01: Client app usa ACF+PKCE — login via redirect, code exchange server-side
- PKC-02: Backoffice usa ACF+PKCE (já feito na Phase 31) — verificado
- PKC-03: Custom theme onboarding-client — login page no Keycloak replica visual do client app
- PKC-04: Custom theme onboarding-backoffice — login page no Keycloak replica visual do backoffice
- PKC-05: 2FA TOTP funciona nativamente via Keycloak — sem código custom nas apps
- PKC-06: UPDATE_PASSWORD funciona nativamente via Keycloak — sem código custom nas apps
- Zero referências a loginClient/logoutClient no client app
- Todos os testes passando em ambos frontends
- Themes carregados no Keycloak via compose.yaml
</success_criteria>

<output>
Após execução, criar 33-01-SUMMARY.md com status, changes, deviations, commits, verification results.
</output>

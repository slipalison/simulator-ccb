---
phase: 08-registration-ui
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/router.tsx
  - frontend/src/components/pages/RegistrationPage.tsx
  - frontend/src/components/molecules/RegistrationTypeSelector.tsx
autonomous: true

# Goal-backward verification (derived during planning, verified after execution)
must_haves:
  truths:
    - "User can navigate to /registration route"
    - "Registration page shows PF and PJ type options"
    - "Selecting PF leads to PF form, selecting PJ leads to PJ form"
  artifacts:
    - path: "frontend/src/components/pages/RegistrationPage.tsx"
      provides: "Registration page with type selector"
      min_lines: 30
    - path: "frontend/src/components/molecules/RegistrationTypeSelector.tsx"
      provides: "PF/PJ type selection component"
      exports: ["RegistrationTypeSelector"]
    - path: "frontend/src/router.tsx"
      provides: "/registration route registered"
      contains: "RegistrationPage"
  key_links:
    - from: "frontend/src/router.tsx"
      to: "frontend/src/components/pages/RegistrationPage.tsx"
      via: "import + createRoute"
      pattern: "RegistrationPage"
    - from: "RegistrationPage.tsx"
      to: "RegistrationTypeSelector.tsx"
      via: "import + render"
      pattern: "RegistrationTypeSelector"
---

<objective>
Create the registration entry point with PF/PJ type selection and routing infrastructure.

Purpose: Users need a clear entry point to registration where they choose between Pessoa Física and Pessoa Jurídica before filling the form. This establishes the routing and selection UX that subsequent forms depend on.
Output: RegistrationPage with type selector, /registration route wired, type-safe navigation.
</objective>

<execution_context>
@~/.qwen/get-shit-done/workflows/execute-plan.md
@~/.qwen/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Prior phase summaries — RegistrationPage will replace ExampleForm in router
@.planning/phases/07-frontend-foundation/07-frontend-foundation-02-SUMMARY.md
@.planning/phases/07-frontend-foundation/07-frontend-foundation-03-SUMMARY.md

# Existing router — add /registration route alongside existing index route
@frontend/src/router.tsx

# Existing components to compose with:
@frontend/src/components/templates/PageLayout.tsx
@frontend/src/components/ui/card.tsx
@frontend/src/components/ui/button.tsx
@frontend/src/lib/utils.ts

# Requirements this plan addresses:
- REG-01: Formulário de cadastro PF (entry point)
- REG-02: Formulário de cadastro PJ (entry point)
- FRONT-01: Atomic Design (molecule + page components)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RegistrationTypeSelector molecule</name>
  <files>frontend/src/components/molecules/RegistrationTypeSelector.tsx</files>
  <action>
    Create a molecule component that presents two mutually exclusive options: "Pessoa Física" and "Pessoa Jurídica".
    
    Implementation:
    - Use shadcn/ui Card component from @/components/ui/card for each option
    - Each card shows: icon (use emoji or simple SVG), title ("Pessoa Física" / "Pessoa Jurídica"), description ("CPF" / "CNPJ")
    - Clicking a card calls an onSelect callback with the selected type ('PF' | 'PJ')
    - Use Tailwind for layout: grid grid-cols-1 md:grid-cols-2 gap-4
    - Hover state: ring-2 ring-primary cursor-pointer
    - Selected state: ring-2 ring-primary bg-primary/5
    - Export: `export type RegistrationType = 'PF' | 'PJ'` and `export function RegistrationTypeSelector({ onSelect }: { onSelect: (type: RegistrationType) => void })`
    
    What to avoid:
    - Don't import icons from external libraries — use simple text/emoji or inline SVG
    - Don't add form logic here — this is purely a selection component
  </action>
  <verify>npx tsc --noEmit passes in frontend/ directory</verify>
  <done>RegistrationTypeSelector renders two selectable cards, onSelect callback fires with correct type</done>
</task>

<task type="auto">
  <name>Task 2: Create RegistrationPage and wire to router</name>
  <files>frontend/src/components/pages/RegistrationPage.tsx, frontend/src/router.tsx</files>
  <action>
    Create RegistrationPage that uses RegistrationTypeSelector to present the choice, then conditionally renders a placeholder for the selected form type.
    
    RegistrationPage.tsx:
    - Import PageLayout, RegistrationTypeSelector
    - State: `const [selectedType, setSelectedType] = useState<RegistrationType | null>(null)`
    - If no selection: show title "Criar sua conta" + RegistrationTypeSelector
    - If PF selected: show "Cadastro — Pessoa Física" + placeholder text "Formulário PF em breve"
    - If PJ selected: show "Cadastro — Pessoa Jurídica" + placeholder text "Formulário PJ em breve"
    - Include a "Voltar" button when a type is selected to go back to the selection screen
    - Wrap in PageLayout with title
    
    router.tsx:
    - Add import: `import { RegistrationPage } from "@/components/pages/RegistrationPage"`
    - Add route: `const registrationRoute = createRoute({ getParentRoute: () => rootRoute, path: '/registration', component: RegistrationPage })`
    - Add to tree: `const routeTree = rootRoute.addChildren([indexRoute, registrationRoute])`
    
    What to avoid:
    - Don't remove or modify the existing indexRoute
    - Don't create the actual forms yet — placeholders only in this plan
  </action>
  <verify>
    1. npx tsc --noEmit passes in frontend/
    2. Navigate to http://localhost:5173/registration shows selection screen
    3. Clicking PF/PJ shows corresponding placeholder
  </verify>
  <done>/registration route exists, shows type selector, clicking type shows placeholder with back button</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npx tsc --noEmit` passes
- [ ] `cd frontend && npm run build` succeeds
- [ ] RegistrationPage renders with title "Criar sua conta"
- [ ] RegistrationTypeSelector shows two cards (PF and PJ)
- [ ] Clicking PF shows "Cadastro — Pessoa Física" placeholder
- [ ] Clicking PJ shows "Cadastro — Pessoa Jurídica" placeholder
- [ ] "Voltar" button returns to selection screen
- [ ] /registration route is type-safe (useNavigate type-checks)
</verification>

<success_criteria>

- All tasks completed
- TypeScript compiles without errors
- /registration route is accessible and shows type selector
- PF/PJ selection works with correct state transitions
- No regression: existing / route still works
- All files follow Atomic Design conventions (molecule in molecules/, page in pages/)
</success_criteria>

<output>
After completion, create `.planning/phases/08-registration-ui/08-01-SUMMARY.md` with:
- Components created (RegistrationTypeSelector, RegistrationPage)
- Route added (/registration)
- Build verification status
- Any deviations from plan
</output>

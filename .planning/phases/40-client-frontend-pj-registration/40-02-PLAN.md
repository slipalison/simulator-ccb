---
phase: 40-client-frontend-pj-registration
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - frontend/client/src/components/molecules/RegistrationForm.tsx
  - frontend/client/src/components/molecules/TermsDialog.tsx
  - frontend/client/src/components/pages/RegisterPage.tsx
  - frontend/client/src/lib/validation-schemas.ts
autonomous: true
requirements: [REG-01, REG-05]

must_haves:
  truths:
    - "Registration form is a 2-step wizard (Step 1: company data, Step 2: access data + terms)"
    - "No PF/PJ type selector exists anywhere in the UI"
    - "Terms acceptance checkbox is required to submit — form cannot be submitted without it"
    - "CNPJ validation uses modulo-11 check digit algorithm"
    - "Password UX has 5-level strength meter + show/hide + confirm password"
    - "Successful registration redirects to login (ACF redirect)"
  artifacts:
    - path: "frontend/client/src/components/molecules/RegistrationForm.tsx"
      provides: "PJ-only 2-step wizard registration form"
      min_lines: 100
    - path: "frontend/client/src/components/molecules/TermsDialog.tsx"
      provides: "Terms of Use modal with mock text"
      min_lines: 40
    - path: "frontend/client/src/components/pages/RegisterPage.tsx"
      provides: "Full page wrapper for RegistrationForm"
      min_lines: 30
  key_links:
    - from: "frontend/client/src/components/molecules/RegistrationForm.tsx"
      to: "POST /api/companies/registration"
      via: "registerCompany function"
      pattern: "registerCompany"
    - from: "frontend/client/src/components/molecules/RegistrationForm.tsx"
      to: "validation-schemas.ts"
      via: "companyDataSchema, companyAccessSchema"
      pattern: "companyDataSchema|companyAccessSchema"
---

<objective>
Build the PJ-only registration wizard with terms acceptance, replacing the existing PF/PJ registration form entirely.

Purpose: Deliver the core registration flow (D-05 through D-08): 2-step wizard for PJ data, CNPJ validation, password UX, and mandatory terms acceptance checkbox.
Output: RegistrationForm (wizard), TermsDialog, RegisterPage — no PF references anywhere.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-client-frontend-pj-registration/40-CONTEXT.md

<interfaces>
<!-- From Plan 01 — contracts this plan consumes -->

From auth-context.tsx (extended in Plan 01):
```typescript
auth: { isAuthenticated: boolean; isLoading: boolean; userName: string | null; email: string | null; accessGroup: string | null; companyId: string | null; }
```

From api.ts (added in Plan 01):
```typescript
export interface RegisterCompanyRequest {
  razaoSocial: string; cnpj: string; email: string; phone: string;
  password: string; termsAccepted: boolean; termsVersion: string;
}
export async function registerCompany(data: RegisterCompanyRequest): Promise<{ id: string }>
```

From validation-schemas.ts (Plan 01):
```typescript
export const companyDataSchema = z.object({ ... }); // Step 1: razaoSocial + cnpj
export const companyAccessSchema = z.object({ ... }); // Step 2: email, phone, password, confirmPassword, termsAccepted
```

From existing PasswordStrengthMeter.tsx:
```typescript
// Reusable 5-level strength meter + show/hide toggle
// Already used in current RegistrationForm
```

From existing PasswordField.tsx:
```typescript
// Reusable password input with show/hide toggle
```

From Phase 38 CompaniesController POST /api/companies/registration:
```typescript
// Request body: { razaoSocial, cnpj, email, phone, password, termsAccepted: true, termsVersion: "1.0" }
// Response: 201 { id: "..." } | 409 CNPJ duplicate | 422 validation error
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TermsDialog component and RegisterPage</name>
  <files>frontend/client/src/components/molecules/TermsDialog.tsx, frontend/client/src/components/pages/RegisterPage.tsx</files>
  <read_first>
    frontend/client/src/components/ui/dialog.tsx
    frontend/client/src/components/molecules/RegistrationForm.tsx
  </read_first>
  <action>
1. Create `TermsDialog.tsx` (molecule) per D-07:
   - Uses shadcn/ui Dialog component
   - Props: `open: boolean`, `onOpenChange: (open: boolean) => void`
   - Content: "Termos de Uso — Versão 1.0" heading, mock terms text (3-4 paragraphs of Lorem-like placeholder text about data usage, privacy, LGPD compliance)
   - "Li e concordo com os Termos de Uso" button that calls `onOpenChange(false)`
   - Scrollable content area with max-height
   - Import from `@/components/ui/dialog`

2. Create `RegisterPage.tsx` (page) as full-page wrapper:
   - Centers `RegistrationForm` vertically and horizontally
   - Shows product logo/branding above the form
   - "Já tem uma conta? Entrar" link → `/auth/login`
   - Clean, professional layout (same visual style as current login page)
   - No sidebar visible (registration is pre-auth)
  </action>
  <verify>
    <automated>cd frontend/client && npx tsc --noEmit 2>&1 | head -20; echo "EXIT: $?"</automated>
  </verify>
  <done>
    - TermsDialog component renders with mock terms text and close action
    - RegisterPage centers RegistrationForm with branding and login link
    - TypeScript compiles
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite RegistrationForm as PJ-only 2-step wizard with terms acceptance</name>
  <files>frontend/client/src/components/molecules/RegistrationForm.tsx, frontend/client/src/components/molecules/PersonTypeRadio.tsx, frontend/client/src/lib/validation-schemas.ts</files>
  <read_first>
    frontend/client/src/components/molecules/RegistrationForm.tsx
    frontend/client/src/components/molecules/PasswordStrengthMeter.tsx
    frontend/client/src/components/molecules/PasswordField.tsx
    frontend/client/src/lib/validation-schemas.ts
    frontend/client/src/lib/api.ts
  </read_first>
  <action>
1. DELETE `PersonTypeRadio.tsx` entirely (per D-06). This file was already targeted in Plan 01 Task 2 but confirm deletion.

2. Completely rewrite `RegistrationForm.tsx` as a 2-step wizard per D-05, D-06, D-07, D-08:

   **Step 1 — "Dados da Empresa":**
   - Fields: Razão Social (text input), CNPJ (text input with mask `XX.XXX.XXX/XXXX-XX`)
   - CNPJ input: apply mask on blur, strip mask on submit, validate with modulo-11 algorithm
   - Uses `companyDataSchema` for validation
   - "Continuar" button → validates Step 1, on success shows Step 2
   - "Voltar" link on Step 2 → goes back to Step 1

   **Step 2 — "Dados de Acesso":**
   - Fields: Email (email input), Telefone (phone input with mask), Senha (password + strength meter), Confirmar Senha (password confirmation), Aceito os Termos de Uso (checkbox)
   - Password UX (D-08): 5-level strength meter (`PasswordStrengthMeter`), show/hide toggle (`PasswordField`), confirm password validation
   - Terms checkbox (D-07): required checkbox with link "Termos de Uso" → opens `TermsDialog` modal. Checkbox must be `true` (z.literal(true)) to submit.
   - Uses `companyAccessSchema` for validation
   - "Cadastrar" button → validates Step 2, calls `registerCompany()` API
   - Loading state on submit button
   - Error handling: 409 → "CNPJ já cadastrado", 422 → display field errors, 503 → "Serviço temporariamente indisponível"

3. Wizard state management: Use React state (`step: 1 | 2`) with smooth transitions between steps. Animate step change with CSS transitions.

4. After successful registration (201 response), redirect to `/` which triggers ACF login flow per existing pattern. Do NOT auto-login — the ACF flow handles authentication.

5. In `validation-schemas.ts`, verify that `pfRegistrationSchema`, `PfRegistrationData`, `registrationSchema`, `RegistrationData` are all removed. Only `companyDataSchema`, `companyAccessSchema`, `editEmployeeSchema`, `loginSchema`, `passwordSchema`, and validator functions (`validateCpf`, `validateCnpj`) should remain.
  </action>
  <verify>
    <automated>cd frontend/client && npx tsc --noEmit 2>&1 | head -20; echo "EXIT: $?"; echo "---"; grep -r "PersonTypeRadio\|pfRegistration\|PessoaFisica\|tipo=pf\|personType" src/ 2>/dev/null | head -5; echo "PF_CHECK_DONE"</automated>
  </verify>
  <done>
    - RegistrationForm is a 2-step wizard (Step 1: company data, Step 2: access + terms)
    - CNPJ input has mask and modulo-11 validation
    - Password UX: strength meter, show/hide, confirm password mismatch validation
    - Terms checkbox is required (z.literal(true)) — form cannot submit without it
    - TermsDialog opens when clicking "Termos de Uso" link
    - PersonTypeRadio.tsx deleted
    - No PF references in RegistrationForm or validation schemas
    - Successful registration redirects to ACF login
    - TypeScript compiles
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/companies/registration | Untrusted input — validated server-side with FluentValidation |
| Browser → Keycloak ACF | Auth redirect — no credentials in client code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-05 | Tampering | CNPJ field | mitigate | Client-side CNPJ validation (modulo-11) + server-side FluentValidation — defense in depth |
| T-40-06 | Repudiation | Terms acceptance | mitigate | termsAccepted boolean + termsVersion sent to backend — acceptance recorded per D-07 |
| T-40-07 | Information disclosure | PF removal | mitigate | All PF references removed — no PF data in form, schemas, or API calls |
</threat_model>

<verification>
1. TypeScript compiles: `cd frontend/client && npx tsc --noEmit`
2. No PF references: `grep -r "PersonTypeRadio\|pfRegistration\|PessoaFisica" frontend/client/src/` returns nothing
3. Wizard works: Step 1 validates companyData, Step 2 validates companyAccess
4. Terms checkbox required: schema uses `z.literal(true)` — form cannot submit with checkbox false
5. Registration API call: form submits `registerCompany()` with RJ payload
</verification>

<success_criteria>
- RegistrationForm is a 2-step wizard (company data → access data + terms)
- CNPJ validated with modulo-11 check digit on client (mask applied on blur)
- Password UX: 5-level strength meter + show/hide + confirm validation
- Terms checkbox mandatory (z.literal(true)) with modal link to TermsDialog
- No PF/PJ type selector or PF-specific code anywhere
- PersonTypeRadio.tsx deleted
- Successful registration redirects to ACF login (POST → 201 → /)
</success_criteria>

<output>
After completion, create `.planning/phases/40-client-frontend-pj-registration/40-02-SUMMARY.md`
</output>
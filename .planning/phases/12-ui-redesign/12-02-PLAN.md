---
phase: 12
phase_name: ui-redesign
plan: 02
plan_name: login-registration-redesign
created: "2026-04-08"
status: pending
must_haves:
  - "UI-03: LoginPage redesign com shadcn Card, Form, Input, Button"
  - "UI-04: RegistrationPage redesign com shadcn RadioGroup, Form, Input, PasswordStrength"
artifacts:
  - ".planning/PHASE-12-UI-REDESIGN.md"
  - "frontend/src/components/pages/LoginPage.tsx"
  - "frontend/src/components/molecules/RegistrationForm.tsx"
  - "frontend/src/components/molecules/PasswordField.tsx"
  - "frontend/src/components/molecules/LoginForm.tsx"
key_links:
  - "PHASE-12-UI-REDESIGN.md: UI-03 com mockup completo da LoginPage"
  - "PHASE-12-UI-REDESIGN.md: UI-04 com mockup completo da RegistrationPage"
  - "Phase 11: RegistrationForm.tsx e PasswordField.tsx existentes — modificar, não recriar"
autonomous: true
---

# Phase 12 — Plan 02: LoginPage + RegistrationPage Redesign

## Objective

Redesenhar completamente as telas de Login e Registration usando shadcn/ui, mantendo toda a lógica existente (autenticação, validação, auto-login) mas com visual profissional e acessível.

**Requisitos atendidos:** UI-03, UI-04

## Execution Context

### Current State
- **shadcn/ui:** Já instalado (Plan 01) — componentes em `src/components/ui/`
- **Theme infrastructure:** Já pronta (Plan 01) — ThemeProvider, ThemeToggle
- **LoginPage.tsx:** Existe com Tailwind custom, RHF + Zod, ROPC login
- **RegistrationForm.tsx:** Existe com PersonTypeRadio, Zod superRefine, auto-login, PasswordStrengthMeter
- **PasswordField.tsx:** Existe com Eye/EyeOff toggle (lucide-react)
- **LoginForm.tsx:** Existe como molecule com RHF + Zod
- **PasswordStrengthMeter.tsx:** Existe — manter (não é componente shadcn)

### Dependencies
- **Plan 01:** Deve ser executado primeiro (shadcn setup + theme infra)
- **Phase 11:** Formulários existentes funcionais — redesign é visual, não funcional

### Technical Approach
```
LoginPage:
1. Substituir container custom por shadcn Card
2. Substituir inputs custom por shadcn Input
3. Substituir botão custom por shadcn Button
4. Usar shadcn Form (RHF wrapper) para campos
5. Manter lógica de login (auth-context, ROPC) intacta
6. Adicionar loading state no botão (spinner)
7. Estilizar Alert de erro com shadcn Alert

RegistrationForm:
1. Substituir RadioGroup custom por shadcn RadioGroup
2. Usar shadcn Form + Input para todos os campos
3. Integrar PasswordField com shadcn Input
4. Integrar PasswordStrengthMeter (manter componente custom)
5. Confirm password com shadcn Input
6. Manter lógica de auto-login intacta
7. Adicionar Toast de sucesso no registro
8. Estilizar erros com shadcn Alert
```

### Design Tokens (do PHASE-12-UI-REDESIGN.md)
- Border radius: `--radius: 0.5rem` (8px)
- Font: system font stack (shadcn default)
- Spacing: Tailwind spacing (4, 8, 12, 16, 24, 32px)

## Tasks

### Task 12.2.1: TDD Stubs RED — Redesigned Forms

**Objective:** Criar testes para as formas redesignadas (renderização, validação, interações)

**Actions:**

1. **Criar `frontend/src/tests/login-form-redesign.test.tsx`:**
   ```tsx
   describe('LoginForm (shadcn redesign)', () => {
     it('renders with shadcn Card container', () => { /* stub - fail */ });
     it('renders email input with shadcn Input', () => { /* stub - fail */ });
     it('renders password field with show/hide toggle', () => { /* stub - fail */ });
     it('renders submit button with shadcn Button', () => { /* stub - fail */ });
     it('shows loading spinner when submitting', () => { /* stub - fail */ });
     it('shows error alert on invalid credentials', () => { /* stub - fail */ });
     it('shows "Forgot password?" link', () => { /* stub - fail */ });
     it('shows "Create account" link', () => { /* stub - fail */ });
   });
   ```

2. **Criar `frontend/src/tests/registration-form-redesign.test.tsx`:**
   ```tsx
   describe('RegistrationForm (shadcn redesign)', () => {
     it('renders with shadcn Card container', () => { /* stub - fail */ });
     it('renders PF/PJ radio group with shadcn RadioGroup', () => { /* stub - fail */ });
     it('shows PF fields (nome, cpf) when PF selected', () => { /* stub - fail */ });
     it('shows PJ fields (razaoSocial, cnpj) when PJ selected', () => { /* stub - fail */ });
     it('renders password field with strength meter', () => { /* stub - fail */ });
     it('renders confirm password field', () => { /* stub - fail */ });
     it('shows password match indicator', () => { /* stub - fail */ });
     it('shows loading spinner when submitting', () => { /* stub - fail */ });
     it('shows toast on successful registration', () => { /* stub - fail */ });
   });
   ```

3. **Executar testes — devem falhar (TDD RED):**
   ```bash
   cd frontend && npm test
   ```

**Output:**
- 17 stub tests falhando (8 login + 9 registration)
- Estrutura de testes para redesigned forms

**Success Criteria:**
- [ ] 17 testes falhando (nenhum passando — garantindo que detectam ausência)
- [ ] Testes importam componentes que serão modificados

---

### Task 12.2.2: LoginPage Redesign

**Objective:** Redesenhar LoginPage com shadcn/ui mantendo toda a lógica existente

**Actions:**

1. **Modificar `frontend/src/components/pages/LoginPage.tsx`:**
   ```tsx
   import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"
   import { Alert, AlertDescription } from "@/components/ui/alert"
   import { LoginForm } from "../molecules/LoginForm"
   import { Link } from "@tanstack/react-router"
   import { ThemeToggle } from "../atoms/ThemeToggle"

   export function LoginPage() {
     return (
       <div className="min-h-screen flex items-center justify-center bg-background p-4">
         <div className="absolute top-4 right-4">
           <ThemeToggle />
         </div>
         <Card className="w-full max-w-md">
           <CardHeader className="space-y-1">
             <CardTitle className="text-2xl text-center">Bem-vindo de volta!</CardTitle>
             <CardDescription className="text-center">
               Entre com suas credenciais para acessar sua conta
             </CardDescription>
           </CardHeader>
           <CardContent>
             <LoginForm />
             <div className="mt-4 space-y-2 text-center text-sm">
               <Link to="/forgot-password" className="block text-muted-foreground hover:text-foreground">
                 Esqueceu a senha?
               </Link>
               <div className="text-muted-foreground">
                 Não tem conta?{" "}
                 <Link to="/register" className="text-primary hover:underline">
                   Criar conta →
                 </Link>
               </div>
             </div>
           </CardContent>
         </Card>
       </div>
     )
   }
   ```

2. **Modificar `frontend/src/components/molecules/LoginForm.tsx`:**
   ```tsx
   import { useForm } from "react-hook-form"
   import { zodResolver } from "@hookform/resolvers/zod"
   import * as z from "zod"
   import { Button } from "@/components/ui/button"
   import { Input } from "@/components/ui/input"
   import {
     Form,
     FormControl,
     FormField,
     FormItem,
     FormLabel,
     FormMessage,
   } from "@/components/ui/form"
   import { Alert, AlertDescription } from "@/components/ui/alert"
   import { PasswordField } from "./PasswordField"
   import { useAuth } from "../../lib/auth-context"
   import { useState } from "react"
   import { useNavigate } from "@tanstack/react-router"
   import { Loader2 } from "lucide-react"

   const loginSchema = z.object({
     email: z.string().email("Email inválido"),
     password: z.string().min(1, "Senha é obrigatória"),
   })

   export function LoginForm() {
     const { login } = useAuth()
     const navigate = useNavigate()
     const [error, setError] = useState<string | null>(null)
     const [isSubmitting, setIsSubmitting] = useState(false)

     const form = useForm<z.infer<typeof loginSchema>>({
       resolver: zodResolver(loginSchema),
       defaultValues: { email: "", password: "" },
     })

     async function onSubmit(values: z.infer<typeof loginSchema>) {
       setIsSubmitting(true)
       setError(null)
       try {
         await login(values.email, values.password)
         navigate({ to: "/profile" })
       } catch (err: any) {
         setError("Email ou senha inválidos")
       } finally {
         setIsSubmitting(false)
       }
     }

     return (
       <Form {...form}>
         <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-4">
           <FormField
             control={form.control}
             name="email"
             render={({ field }) => (
               <FormItem>
                 <FormLabel>Email</FormLabel>
                 <FormControl>
                   <Input placeholder="seu@email.com" {...field} />
                 </FormControl>
                 <FormMessage />
               </FormItem>
             )}
           />

           <FormField
             control={form.control}
             name="password"
             render={({ field }) => (
               <FormItem>
                 <FormLabel>Senha</FormLabel>
                 <FormControl>
                   <PasswordField value={field.value} onChange={field.onChange} />
                 </FormControl>
                 <FormMessage />
               </FormItem>
             )}
           />

           {error && (
             <Alert variant="destructive">
               <AlertDescription>{error}</AlertDescription>
             </Alert>
           )}

           <Button type="submit" className="w-full" disabled={isSubmitting}>
             {isSubmitting && <Loader2 className="mr-2 h-4 w-4 animate-spin" />}
             Entrar
           </Button>
         </form>
       </Form>
     )
   }
   ```

3. **Executar testes de login — devem passar (TDD GREEN):**
   ```bash
   cd frontend && npm test -- login-form-redesign
   ```

**Output:**
- `LoginPage.tsx` redesign completo com shadcn Card
- `LoginForm.tsx` redesign com shadcn Form, Input, Button
- Loading state com spinner
- Error alert com shadcn Alert
- Links para forgot password e registration
- 8 testes de login passando

**Success Criteria:**
- [ ] LoginPage renderiza centralizada com Card
- [ ] ThemeToggle visível no canto superior direito
- [ ] Email input com label e placeholder
- [ ] Password field com show/hide toggle
- [ ] Submit button com texto "Entrar"
- [ ] Loading spinner durante submit
- [ ] Error alert com "Email ou senha inválidos"
- [ ] Link "Esqueceu a senha?" → `/forgot-password`
- [ ] Link "Criar conta →" → `/register`
- [ ] 8 testes de login passando
- [ ] Lógica de login (auth-context) intacta e funcional

---

### Task 12.2.3: RegistrationPage Redesign

**Objective:** Redesenhar RegistrationForm com shadcn/ui mantendo lógica de PF/PJ, auto-login, password strength

**Actions:**

1. **Modificar `frontend/src/components/molecules/RegistrationForm.tsx`:**
   ```tsx
   import { useForm } from "react-hook-form"
   import { zodResolver } from "@hookform/resolvers/zod"
   import * as z from "zod"
   import { Button } from "@/components/ui/button"
   import { Input } from "@/components/ui/input"
   import {
     Form,
     FormControl,
     FormField,
     FormItem,
     FormLabel,
     FormMessage,
   } from "@/components/ui/form"
   import { RadioGroup, RadioGroupItem } from "@/components/ui/radio-group"
   import { Alert, AlertDescription } from "@/components/ui/alert"
   import { PasswordField } from "./PasswordField"
   import { PasswordStrengthMeter } from "./PasswordStrengthMeter"
   import { registerClient } from "../../lib/api"
   import { useAuth } from "../../lib/auth-context"
   import { useNavigate } from "@tanstack/react-router"
   import { useState } from "react"
   import { Loader2 } from "lucide-react"

   // Manter schema Zod existente com superRefine para PF/PJ
   const registrationSchema = z.object({
     personType: z.enum(["pf", "pj"]),
     nome: z.string().min(1, "Nome é obrigatório").optional(),
     razaoSocial: z.string().min(1, "Razão Social é obrigatória").optional(),
     cpf: z.string().regex(/^\d{11}$/, "CPF inválido").optional(),
     cnpj: z.string().regex(/^[A-Z0-9]{14}$/, "CNPJ inválido").optional(),
     email: z.string().email("Email inválido"),
     telefone: z.string().min(10, "Telefone inválido"),
     password: z.string().min(8, "Senha deve ter no mínimo 8 caracteres"),
     confirmPassword: z.string(),
   }).superRefine((data, ctx) => {
     // Validação condicional PF/PJ
     if (data.personType === "pf" && !data.nome) {
       ctx.addIssue({ code: z.ZodIssueCode.custom, message: "Nome é obrigatório", path: ["nome"] })
     }
     if (data.personType === "pj" && !data.razaoSocial) {
       ctx.addIssue({ code: z.ZodIssueCode.custom, message: "Razão Social é obrigatória", path: ["razaoSocial"] })
     }
     if (data.password !== data.confirmPassword) {
       ctx.addIssue({ code: z.ZodIssueCode.custom, message: "As senhas não coincidem", path: ["confirmPassword"] })
     }
   })

   export function RegistrationForm() {
     const { login } = useAuth()
     const navigate = useNavigate()
     const [error, setError] = useState<string | null>(null)
     const [isSubmitting, setIsSubmitting] = useState(false)
     const [personType, setPersonType] = useState<"pf" | "pj">("pf")

     const form = useForm<z.infer<typeof registrationSchema>>({
       resolver: zodResolver(registrationSchema),
       defaultValues: {
         personType: "pf",
         nome: "",
         razaoSocial: "",
         cpf: "",
         cnpj: "",
         email: "",
         telefone: "",
         password: "",
         confirmPassword: "",
       },
     })

     async function onSubmit(values: z.infer<typeof registrationSchema>) {
       setIsSubmitting(true)
       setError(null)
       try {
         await registerClient({
           personType: values.personType,
           nome: values.nome,
           razaoSocial: values.razaoSocial,
           cpf: values.cpf,
           cnpj: values.cnpj,
           email: values.email,
           telefone: values.telefone,
           password: values.password,
         })

         // Auto-login
         try {
           await login(values.email, values.password)
           navigate({ to: "/profile" })
         } catch {
           navigate({ to: "/login", state: { message: "Conta criada! Faça login." } })
         }
       } catch (err: any) {
         setError(err.message || "Erro ao criar conta")
       } finally {
         setIsSubmitting(false)
       }
     }

     return (
       <Form {...form}>
         <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-6">
           {/* Person Type */}
           <FormField
             control={form.control}
             name="personType"
             render={({ field }) => (
               <FormItem>
                 <FormLabel>Tipo de pessoa</FormLabel>
                 <FormControl>
                   <RadioGroup
                     onValueChange={(v) => {
                       field.onChange(v)
                       setPersonType(v as "pf" | "pj")
                     }}
                     defaultValue={field.value}
                     className="flex gap-4"
                   >
                     <FormItem className="flex items-center space-x-2 space-y-0">
                       <FormControl>
                         <RadioGroupItem value="pf" />
                       </FormControl>
                       <FormLabel className="font-normal">Pessoa Física</FormLabel>
                     </FormItem>
                     <FormItem className="flex items-center space-x-2 space-y-0">
                       <FormControl>
                         <RadioGroupItem value="pj" />
                       </FormControl>
                       <FormLabel className="font-normal">Pessoa Jurídica</FormLabel>
                     </FormItem>
                   </RadioGroup>
                 </FormControl>
                 <FormMessage />
               </FormItem>
             )}
           />

           {/* PF Fields */}
           {personType === "pf" && (
             <FormField
               control={form.control}
               name="nome"
               render={({ field }) => (
                 <FormItem>
                   <FormLabel>Nome completo</FormLabel>
                   <FormControl>
                     <Input placeholder="João da Silva" {...field} />
                   </FormControl>
                   <FormMessage />
                 </FormItem>
               )}
             />
           )}

           {personType === "pf" && (
             <FormField
               control={form.control}
               name="cpf"
               render={({ field }) => (
                 <FormItem>
                   <FormLabel>CPF</FormLabel>
                   <FormControl>
                     <Input placeholder="000.000.000-00" {...field} />
                   </FormControl>
                   <FormMessage />
                 </FormItem>
               )}
             />
           )}

           {/* PJ Fields */}
           {personType === "pj" && (
             <FormField
               control={form.control}
               name="razaoSocial"
               render={({ field }) => (
                 <FormItem>
                   <FormLabel>Razão Social</FormLabel>
                   <FormControl>
                     <Input placeholder="Empresa LTDA" {...field} />
                   </FormControl>
                   <FormMessage />
                 </FormItem>
               )}
             />
           )}

           {personType === "pj" && (
             <FormField
               control={form.control}
               name="cnpj"
               render={({ field }) => (
                 <FormItem>
                   <FormLabel>CNPJ</FormLabel>
                   <FormControl>
                     <Input placeholder="00.000.000/0000-00" {...field} />
                   </FormControl>
                   <FormMessage />
                 </FormItem>
               )}
             />
           )}

           {/* Common Fields */}
           <FormField
             control={form.control}
             name="email"
             render={({ field }) => (
               <FormItem>
                 <FormLabel>Email</FormLabel>
                 <FormControl>
                   <Input type="email" placeholder="seu@email.com" {...field} />
                 </FormControl>
                 <FormMessage />
               </FormItem>
             )}
           />

           <FormField
             control={form.control}
             name="telefone"
             render={({ field }) => (
               <FormItem>
                 <FormLabel>Telefone</FormLabel>
                 <FormControl>
                   <Input placeholder="(00) 00000-0000" {...field} />
                 </FormControl>
                 <FormMessage />
               </FormItem>
             )}
           />

           {/* Password */}
           <FormField
             control={form.control}
             name="password"
             render={({ field }) => (
               <FormItem>
                 <FormLabel>Senha</FormLabel>
                 <FormControl>
                   <PasswordField value={field.value} onChange={field.onChange} />
                 </FormControl>
                 <FormMessage />
               </FormItem>
             )}
           />
           <PasswordStrengthMeter password={form.watch("password")} />

           {/* Confirm Password */}
           <FormField
             control={form.control}
             name="confirmPassword"
             render={({ field }) => (
               <FormItem>
                 <FormLabel>Confirmar senha</FormLabel>
                 <FormControl>
                   <PasswordField value={field.value} onChange={field.onChange} />
                 </FormControl>
                 <FormMessage />
               </FormItem>
             )}
           />
           {form.watch("confirmPassword") && form.watch("password") === form.watch("confirmPassword") && (
             <p className="text-sm text-green-600 dark:text-green-400">✓ As senhas coincidem</p>
           )}

           {/* Error */}
           {error && (
             <Alert variant="destructive">
               <AlertDescription>{error}</AlertDescription>
             </Alert>
           )}

           {/* Submit */}
           <Button type="submit" className="w-full" disabled={isSubmitting}>
             {isSubmitting && <Loader2 className="mr-2 h-4 w-4 animate-spin" />}
             Criar conta
           </Button>
         </form>
       </Form>
     )
   }
   ```

2. **Modificar `frontend/src/components/pages/RegistrationPage.tsx` (se existir):**
   Envolver RegistrationForm com shadcn Card:
   ```tsx
   import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"
   import { RegistrationForm } from "../molecules/RegistrationForm"
   import { Link } from "@tanstack/react-router"
   import { ThemeToggle } from "../atoms/ThemeToggle"

   export function RegistrationPage() {
     return (
       <div className="min-h-screen flex items-center justify-center bg-background p-4">
         <div className="absolute top-4 right-4">
           <ThemeToggle />
         </div>
         <Card className="w-full max-w-lg">
           <CardHeader className="space-y-1">
             <CardTitle className="text-2xl text-center">Criar sua conta</CardTitle>
             <CardDescription className="text-center">
               Preencha seus dados para se cadastrar
             </CardDescription>
           </CardHeader>
           <CardContent>
             <RegistrationForm />
             <div className="mt-4 text-center text-sm text-muted-foreground">
               Já tem conta?{" "}
               <Link to="/login" className="text-primary hover:underline">
                 Fazer login →
               </Link>
             </div>
           </CardContent>
         </Card>
       </div>
     )
   }
   ```

3. **Modificar `frontend/src/components/molecules/PasswordField.tsx`:**
   Integrar com shadcn Input:
   ```tsx
   import { useState } from "react"
   import { Eye, EyeOff } from "lucide-react"
   import { Input } from "@/components/ui/input"
   import { Button } from "@/components/ui/button"

   interface PasswordFieldProps {
     value: string
     onChange: (value: string) => void
     label?: string
   }

   export function PasswordField({ value, onChange, label }: PasswordFieldProps) {
     const [showPassword, setShowPassword] = useState(false)

     return (
       <div className="relative">
         <Input
           type={showPassword ? "text" : "password"}
           value={value}
           onChange={(e) => onChange(e.target.value)}
           placeholder="••••••••"
           className="pr-10"
         />
         <Button
           type="button"
           variant="ghost"
           size="sm"
           className="absolute right-0 top-0 h-full px-3 py-2 hover:bg-transparent"
           onClick={() => setShowPassword(!showPassword)}
         >
           {showPassword ? (
             <EyeOff className="h-4 w-4" />
           ) : (
             <Eye className="h-4 w-4" />
           )}
         </Button>
       </div>
     )
   }
   ```

4. **Executar testes de registration — devem passar (TDD GREEN):**
   ```bash
   cd frontend && npm test -- registration-form-redesign
   ```

5. **Executar todos os testes frontend:**
   ```bash
   cd frontend && npm test
   ```

**Output:**
- `RegistrationForm.tsx` redesign completo com shadcn Form, RadioGroup, Input
- `RegistrationPage.tsx` com shadcn Card container
- `PasswordField.tsx` integrado com shadcn Input
- Loading state com spinner
- Toast de sucesso no registro
- Error alert com shadcn Alert
- Password match indicator
- 9 testes de registration passando

**Success Criteria:**
- [ ] RegistrationPage renderiza centralizada com Card
- [ ] ThemeToggle visível no canto superior direito
- [ ] Radio group PF/PJ estilizado (não radio nativo)
- [ ] Campos PF (nome, cpf) mostram quando PF selecionado
- [ ] Campos PJ (razaoSocial, cnpj) mostram quando PJ selecionado
- [ ] Transição entre PF/PJ é suave
- [ ] Password field com show/hide toggle
- [ ] PasswordStrengthMeter visível abaixo do campo senha
- [ ] Confirm password field com match indicator
- [ ] Submit button com texto "Criar conta"
- [ ] Loading spinner durante submit
- [ ] Error alert genérico em caso de falha
- [ ] Auto-login após registro (lógica mantida)
- [ ] Link "Já tem conta? Fazer login →" → `/login`
- [ ] 9 testes de registration passando
- [ ] Total frontend: 78 existing + 17 new = 95 tests passing

---

## Verification

### Automated Tests
```bash
cd frontend && npm test
# Expected: 17 redesign tests passing (8 login + 9 registration)
# Total frontend: 78 existing + 17 new = 95 tests
```

### Manual Checks
```bash
cd frontend && npm run dev
```

1. Navigate to http://localhost:5173/login
   - Verify: Card centered, theme toggle top-right, clean layout
   - Toggle dark mode → verify: Card adapts, colors change smoothly

2. Navigate to http://localhost:5173/register
   - Verify: Card with PF/PJ radio group, conditional fields
   - Switch PF ↔ PJ → verify: Smooth field transitions
   - Type password → verify: Strength meter updates in real-time
   - Type confirm password → verify: Match indicator appears

3. Submit invalid data → verify: Inline validation errors with shadcn FormMessage

### Build Check
```bash
cd frontend && npm run build
# Expected: No errors
```

## Success Criteria (Overall Plan)

This plan is complete when ALL of the following are TRUE:

1. ✅ LoginPage redesenhada com shadcn Card, Form, Input, Button
2. ✅ ThemeToggle visível no canto superior direito da LoginPage
3. ✅ Loading spinner no botão "Entrar" durante submit
4. ✅ Error alert com shadcn Alert para credenciais inválidas
5. ✅ Links "Esqueceu a senha?" e "Criar conta" presentes
6. ✅ RegistrationPage redesenhada com shadcn Card, RadioGroup, Form
7. ✅ Radio group PF/PJ estilizado (não radio buttons nativos)
8. ✅ Campos condicionais com transição suave
9. ✅ PasswordStrengthMeter integrado ao form
10. ✅ Confirm password com match indicator
11. ✅ Loading spinner no botão "Criar conta" durante submit
12. ✅ Auto-login após registro mantido
13. ✅ 17 novos testes passando (8 login + 9 registration)
14. ✅ `npm run build` sem erros

## Output

- **Files modified:**
  - `frontend/src/components/pages/LoginPage.tsx` — shadcn Card container
  - `frontend/src/components/molecules/LoginForm.tsx` — shadcn Form + Input + Button
  - `frontend/src/components/pages/RegistrationPage.tsx` — shadcn Card container
  - `frontend/src/components/molecules/RegistrationForm.tsx` — shadcn Form + RadioGroup + Input
  - `frontend/src/components/molecules/PasswordField.tsx` — shadcn Input integration

- **Files created:**
  - `frontend/src/tests/login-form-redesign.test.tsx` — 8 tests
  - `frontend/src/tests/registration-form-redesign.test.tsx` — 9 tests

- **Tests:** 17 new tests passing

- **Preserved:**
  - All auth logic (ROPC, auto-login) — intact
  - All validation (Zod schemas, superRefine) — intact
  - Password strength meter — maintained, only restyled
  - Password field toggle — maintained, only restyled

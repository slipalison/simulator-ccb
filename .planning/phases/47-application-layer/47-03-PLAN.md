---
phase: 47-application-layer
plan: 03
type: execute
wave: 3
depends_on: ["47-01", "47-02"]
files_modified:
  - tests/Onboarding.Application.Tests/Fundos/Commands/RegisterConsultoriaFundoCommandHandlerTests.cs
  - tests/Onboarding.Application.Tests/Fundos/Commands/RegisterCustodianteCommandHandlerTests.cs
  - tests/Onboarding.Application.Tests/Fundos/Commands/RegisterFundoCommandHandlerTests.cs
  - tests/Onboarding.Application.Tests/Fundos/Commands/RegisterCedentePfCommandHandlerTests.cs
  - tests/Onboarding.Application.Tests/Fundos/Commands/RegisterCedentePjCommandHandlerTests.cs
  - tests/Onboarding.Application.Tests/Fundos/Commands/CreateTipoAtivoCommandHandlerTests.cs
  - tests/Onboarding.Application.Tests/Fundos/Commands/TransitionFundoStatusCommandHandlerTests.cs
  - tests/Onboarding.Application.Tests/Fundos/Commands/UpdateCedenteCommandHandlerTests.cs
  - tests/Onboarding.Application.Tests/Fundos/Queries/ListFundoQueryHandlerTests.cs
  - tests/Onboarding.Application.Tests/Fundos/Queries/ListTipoAtivoQueryHandlerTests.cs
  - tests/Onboarding.Application.Tests/Fundos/Validators/RegisterConsultoriaFundoCommandValidatorTests.cs
  - tests/Onboarding.Application.Tests/Fundos/Validators/RegisterFundoCommandValidatorTests.cs
  - tests/Onboarding.Application.Tests/Fundos/Validators/RegisterCedentePfCommandValidatorTests.cs
  - tests/Onboarding.Application.Tests/Fundos/Validators/CreateTipoAtivoCommandValidatorTests.cs
autonomous: true
requirements: [CAD-01, CAD-02, CAD-03, CAD-05, CAD-06, CAD-07, CAD-09, CAD-10, CAD-11, CAD-13, CAD-14, CAD-15, CAD-16, CAD-17, CAD-19, CAD-20, CAD-21, ADM-04]

must_haves:
  truths:
    - "All command handlers validate CNPJ/CPF check digits and reject invalid formats"
    - "All command handlers enforce uniqueness constraints (CNPJ per company, CPF/CNPJ per company, codigo global)"
    - "Fundo status transition tests validate state machine rules (RASCUNHO→ATIVO, ATIVO↔SUSPENSO, etc.)"
    - "Cedente PF/PJ polymorphic creation routes to correct CedenteDocumento variant"
    - "TipoAtivo handlers work without company scope (global entity)"
    - "Audit logging tests verify IAuditService.RecordAsync called with correct ActionType"
  artifacts:
    - path: "tests/Onboarding.Application.Tests/Fundos/Commands/RegisterFundoCommandHandlerTests.cs"
      provides: "Handler tests for Fundo registration with CNPJ validation, FK validation, RASCUNHO status"
      min_lines: 50
    - path: "tests/Onboarding.Application.Tests/Fundos/Commands/TransitionFundoStatusCommandHandlerTests.cs"
      provides: "Handler tests for Fundo status state machine transitions"
      min_lines: 80
    - path: "tests/Onboarding.Application.Tests/Fundos/Commands/RegisterCedentePfCommandHandlerTests.cs"
      provides: "Handler tests for Cedente PF registration with CPF validation"
      min_lines: 40
    - path: "tests/Onboarding.Application.Tests/Fundos/Commands/RegisterCedentePjCommandHandlerTests.cs"
      provides: "Handler tests for Cedente PJ registration with CNPJ validation"
      min_lines: 40
    - path: "tests/Onboarding.Application.Tests/Fundos/Queries/ListFundoQueryHandlerTests.cs"
      provides: "Handler tests for paginated fundo listing with company isolation"
      min_lines: 30
  key_links:
    - from: "tests/Onboarding.Application.Tests/Fundos/Commands/TransitionFundoStatusCommandHandlerTests.cs"
      to: "src/Onboarding.Domain/Aggregates/FundoAggregate/FundoStatus.cs"
      via: "FundoStatusValidator.CanTransitionTo white-list transitions tested"
      pattern: "FundoStatus\\."
    - from: "tests/Onboarding.Application.Tests/Fundos/Commands/RegisterCedentePfCommandHandlerTests.cs"
      to: "src/Onboarding.Domain/ValueObjects/Cpf.cs"
      via: "Cpf.Create() validation tested"
      pattern: "Cpf\\.Create"
---

<objective>
Comprehensive unit tests for all 5 fund entity command handlers, query handlers, and validators. Verify CNPJ/CPF validation, uniqueness checks, state machine transitions, Cedente PF/PJ routing, TipoAtivo global scope, and audit logging.

Purpose: Achieve 80%+ coverage on the Application layer handlers for fund entities. Tests must verify all business rules specified in success criteria (CAD-01 through CAD-21) and ensure audit trail is recorded (ADM-04).

Output: ~14 test files covering commands, queries, and validators
</objective>

<execution_context>
@C:/Users/aliso/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/aliso/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-application-layer/47-01-PLAN.md
@.planning/phases/47-application-layer/47-02-PLAN.md

<interfaces>
<!-- Test infrastructure: xUnit + Shouldly + NSubstitute (from STACK.md) -->

From src/Onboarding.Application/Common/ICommandHandler.cs:
```csharp
public interface ICommandHandler<TCommand, TResult> { Task<TResult> HandleAsync(TCommand command, CancellationToken ct = default); }
```

From src/Onboarding.Application/Common/IQueryHandler.cs:
```csharp
public interface IQueryHandler<TQuery, TResult> { Task<TResult> HandleAsync(TQuery query, CancellationToken ct = default); }
```

From src/Onboarding.Domain/Aggregates/FundoAggregate/FundoStatus.cs:
```csharp
// Valid transitions per D-02:
// RASCUNHO → ATIVO
// ATIVO → SUSPENSO, EM_LIQUIDACAO
// SUSPENSO → ATIVO
// EM_LIQUIDACAO → ENCERRADO
// All other transitions throw InvalidStateTransitionException
```

From src/Onboarding.Domain/Exceptions/DuplicateEntityException.cs:
```csharp
public class DuplicateEntityException : DomainException { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests for command handlers, query handlers, and validators</name>
  <files>
    tests/Onboarding.Application.Tests/Fundos/Commands/RegisterConsultoriaFundoCommandHandlerTests.cs,
    tests/Onboarding.Application.Tests/Fundos/Commands/RegisterCustodianteCommandHandlerTests.cs,
    tests/Onboarding.Application.Tests/Fundos/Commands/RegisterFundoCommandHandlerTests.cs,
    tests/Onboarding.Application.Tests/Fundos/Commands/RegisterCedentePfCommandHandlerTests.cs,
    tests/Onboarding.Application.Tests/Fundos/Commands/RegisterCedentePjCommandHandlerTests.cs,
    tests/Onboarding.Application.Tests/Fundos/Commands/CreateTipoAtivoCommandHandlerTests.cs,
    tests/Onboarding.Application.Tests/Fundos/Commands/TransitionFundoStatusCommandHandlerTests.cs,
    tests/Onboarding.Application.Tests/Fundos/Commands/UpdateCedenteCommandHandlerTests.cs,
    tests/Onboarding.Application.Tests/Fundos/Queries/ListFundoQueryHandlerTests.cs,
    tests/Onboarding.Application.Tests/Fundos/Queries/ListTipoAtivoQueryHandlerTests.cs,
    tests/Onboarding.Application.Tests/Fundos/Validators/RegisterConsultoriaFundoCommandValidatorTests.cs,
    tests/Onboarding.Application.Tests/Fundos/Validators/RegisterFundoCommandValidatorTests.cs,
    tests/Onboarding.Application.Tests/Fundos/Validators/RegisterCedentePfCommandValidatorTests.cs,
    tests/Onboarding.Application.Tests/Fundos/Validators/CreateTipoAtivoCommandValidatorTests.cs
  </files>
  <behavior>
    - RegisterConsultoriaFundo: valid CNPJ creates consultoria, duplicate CNPJ throws, invalid CNPJ fails validation
    - RegisterCustodiante: valid CNPJ creates custodiante, duplicate CNPJ throws
    - RegisterFundo: valid input creates fundo with RASCUNHO status, invalid FK references throw, duplicate CNPJ throws
    - TransitionFundoStatus: valid transitions succeed (RASCUNHO→ATIVO, ATIVO→SUSPENSO, ATIVO→EM_LIQUIDACAO, EM_LIQUIDACAO→ENCERRADO, SUSPENSO→ATIVO), invalid transitions throw InvalidStateTransitionException (ENCERRADO→ATIVO, RASCUNHO→SUSPENSO, etc.)
    - RegisterCedentePf: valid CPF creates cedente, duplicate CPF throws, invalid CPF fails validation
    - RegisterCedentePj: valid CNPJ creates cedente, duplicate CNPJ throws
    - UpdateCedente: valid update modifies fields, not-found throws
    - CreateTipoAtivo: valid input creates tipo ativo, duplicate codigo throws, no company scope needed
    - ListFundo: returns paginated results filtered by CompanyId from ICurrentCompanyService
    - ListTipoAtivo: returns paginated results with no company filter (global scope)
    - Validators: CNPJ check-digit validation, CPF check-digit validation, required field rules, email format
    - Audit: all mutation handlers call IAuditService.RecordAsync with correct ActionType
  </behavior>
  <action>
    Create the Fundos test directory structure:

    `tests/Onboarding.Application.Tests/Fundos/Commands/`
    `tests/Onboarding.Application.Tests/Fundos/Queries/`
    `tests/Onboarding.Application.Tests/Fundos/Validators/`

    Using xUnit + Shouldly + NSubstitute (per STACK.md), write unit tests for each handler and validator.

    **Command Handler Test Pattern (per Registration handler):**
    ```csharp
    public class RegisterXxxCommandHandlerTests
    {
        private readonly IXxxRepository _repository;
        private readonly ICurrentCompanyService _companyService;
        private readonly IAuditService _auditService;
        private readonly RegisterXxxCommandHandler _sut;

        // Constructor: Substitute.For<>() all interfaces, instantiate handler

        [Fact]
        public async Task HandleAsync_WithValidData_CreatesAndPersistsEntity() { ... }

        [Fact]
        public async Task HandleAsync_WithDuplicateCnpj_ThrowsDuplicateEntityException() { ... }

        [Fact]
        public async Task HandleAsync_RecordsAuditLog() { ... }
    }
    ```

    **Key test cases to implement:**

    1. **RegisterConsultoriaFundoCommandHandlerTests:**
       - Valid data creates consultoria with ATIVO status
       - Duplicate CNPJ within same company throws DuplicateEntityException (CAD-04)
       - Audit logged with ActionType.ConsultoriaCreated

    2. **RegisterCustodianteCommandHandlerTests:**
       - Valid data creates custodiante with ATIVO status
       - Duplicate CNPJ within same company throws DuplicateEntityException (CAD-08)
       - Audit logged with ActionType.CustodianteCreated

    3. **RegisterFundoCommandHandlerTests:**
       - Valid data creates fundo with RASCUNHO status (CAD-09)
       - Invalid ConsultoriaFundoId FK throws (consultoria not found)
       - Invalid CustodianteId FK throws (custodiante not found)
       - Duplicate CNPJ within same company throws DuplicateEntityException (CAD-12)
       - Audit logged with ActionType.FundoCreated

    4. **TransitionFundoStatusCommandHandlerTests:**
       - RASCUNHO → ATIVO succeeds
       - ATIVO → SUSPENSO succeeds
       - SUSPENSO → ATIVO succeeds
       - ATIVO → EM_LIQUIDACAO succeeds
       - EM_LIQUIDACAO → ENCERRADO succeeds
       - ATIVO ↔ SUSPENSO (bidirectional) succeeds
       - ENCERRADO → ATIVO throws InvalidStateTransitionException (CAD-13)
       - RASCUNHO → SUSPENSO throws InvalidStateTransitionException
       - SUSPENSO → ENCERRADO throws InvalidStateTransitionException
       - Audit logged with ActionType.FundoStatusChanged (include from/to status)

    5. **RegisterCedentePfCommandHandlerTests:**
       - Valid CPF creates cedente with CedenteDocumento.Pf variant
       - Invalid CPF check digits fail validation (CAD-14)
       - Duplicate CPF within same company throws DuplicateEntityException (CAD-18)

    6. **RegisterCedentePjCommandHandlerTests:**
       - Valid CNPJ creates cedente with CedenteDocumento.Pj variant
       - Duplicate CNPJ within same company throws DuplicateEntityException (CAD-18)

    7. **UpdateCedenteCommandHandlerTests:**
       - Updates nome, email, telefone, endereco, status fields
       - Entity not found throws appropriate exception

    8. **CreateTipoAtivoCommandHandlerTests:**
       - Valid data creates tipo ativo globally (no CompanyId)
       - Duplicate codigo throws DuplicateEntityException (CAD-22)
       - Audit logged with ActionType.TipoAtivoCreated
       - No ICurrentCompanyService injection (D-03/TEN-03)

    9. **ListFundoQueryHandlerTests:**
       - Returns PaginatedResult filtered by CompanyId from ICurrentCompanyService
       - Search parameter filters by nome/CNPJ

    10. **ListTipoAtivoQueryHandlerTests:**
        - Returns PaginatedResult with NO company filter (global)
        - Search parameter filters by descricao/codigo

    **Validator Test Cases:**

    11. **RegisterConsultoriaFundoCommandValidatorTests:**
        - RazaoSocial required
        - CNPJ validated via Cnpj.Create() (check-digit failures)
        - Email format validated when provided

    12. **RegisterFundoCommandValidatorTests:**
        - Nome required
        - CNPJ validated
        - ConsultoriaFundoId required (non-empty Guid)
        - CustodianteId required (non-empty Guid)
        - TipoFundo must be valid enum value

    13. **RegisterCedentePfCommandValidatorTests:**
        - CPF validated via Cpf.Create() (check-digit failures)
        - Nome required

    14. **CreateTipoAtivoCommandValidatorTests:**
        - Codigo required
        - Descricao required
        - Categoria must be valid enum value

    **NSubstitute mocking pattern:**
    - Repository: `Substitute.For<IXxxRepository>()` — setup ExistsByCnpjAsync/ExistsByDocumentoAsync to return false for valid flow, true for duplicate flow
    - ICurrentCompanyService: `Substitute.For<ICurrentCompanyService>()` — setup CompanyId to return a test Guid
    - IAuditService: `Substitute.For<IAuditService>()` — verify RecordAsync was called
  </action>
  <verify>
    <automated>dotnet test tests/Onboarding.Application.Tests --filter="FullyQualifiedName~Fundos" --no-restore 2>&1 | Select-Object -Last 20</automated>
  </verify>
  <done>
    - All command handler tests pass: valid creation, duplicate detection, FK validation, audit logging
    - TransitionFundoStatus tests cover all valid/invalid state machine transitions per CAD-13
    - Cedente PF/PJ tests verify correct CedenteDocumento variant creation
    - TipoAtivo handler tests verify no ICurrentCompanyService (global scope per D-03/TEN-03)
    - All validator tests pass: required fields, CNPJ/CPF check digits, email format
    - Query handler tests verify company-scoped vs global isolation
    - 80%+ code coverage on Fundos Application layer handlers
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Handler | Unit tests mock repositories and services — no real DB or HTTP calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-08 | E | Test coverage | mitigate | Tests verify both happy paths and edge cases (duplicate, invalid state, not-found) to prevent regression |
</threat_model>

<verification>
- `dotnet test tests/Onboarding.Application.Tests --filter="FullyQualifiedName~Fundos"` — all tests pass
- TransitionFundoStatusHandlerTests cover ≥6 valid transitions and ≥3 invalid transitions
- RegisterCedentePf/Pj tests verify CedenteDocumento.Pf/Pj variant creation
- CreateTipoAtivo tests confirm no CompanyId parameter in repository calls
- All validators test CNPJ/CPF check-digit rejection and required field rules
</verification>

<success_criteria>
- 80%+ code coverage on Fundos Application layer handlers
- All handler tests verify business rules per CAD-01 through CAD-21
- Fundo status transition tests validate state machine rules (CAD-13)
- Cedente PF/PJ polymorphic creation correctly routes to CedenteDocumento variants
- TipoAtivo handlers work without company scope (D-03/TEN-03)
- Audit logging verified via mock assertions for all mutation handlers (ADM-04)
- All validator tests confirm CNPJ/CPF check digits and required field rules
</success_criteria>

<output>
After completion, create `.planning/phases/47-application-layer/47-03-SUMMARY.md`
</output>
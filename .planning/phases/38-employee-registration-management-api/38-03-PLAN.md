---
phase: 38-employee-registration-management-api
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Onboarding.Application/Admin/Commands/BlockEmployeeCommand.cs
  - src/Onboarding.Application/Admin/Commands/UnblockEmployeeCommand.cs
  - src/Onboarding.Application/Admin/Commands/DeleteEmployeeCommand.cs
  - src/Onboarding.Application/Admin/Queries/GetPaginatedEmployeesQuery.cs
  - src/Onboarding.Application/Admin/Queries/GetEmployeeDetailsQuery.cs
  - src/Onboarding.API/Controllers/AdminUserController.cs
  - tests/Onboarding.Application.Tests/Admin/GetPaginatedEmployeesHandlerTests.cs
  - tests/Onboarding.Application.Tests/Admin/GetEmployeeDetailsHandlerTests.cs
  - tests/Onboarding.Application.Tests/Admin/BlockEmployeeHandlerTests.cs
  - tests/Onboarding.Application.Tests/Admin/DeleteEmployeeHandlerTests.cs
autonomous: true
requirements: [REG-03, MGMT-01, MGMT-02, MGMT-03, MGMT-04, MGMT-05]

must_haves:
  truths:
    - "BackOffice admin can list employees across ALL companies with companyId filter"
    - "BackOffice admin can block/unblock any employee from any company"
    - "BackOffice admin can view employee details regardless of company"
    - "BackOffice admin can LGPD-delete any employee regardless of company"
  artifacts:
    - path: "src/Onboarding.Application/Admin/Queries/GetPaginatedEmployeesQuery.cs"
      provides: "Implemented handler for paginated employee listing across companies"
      contains: "GetPaginatedEmployeesHandler"
    - path: "src/Onboarding.Application/Admin/Commands/BlockEmployeeCommand.cs"
      provides: "Implemented handler for blocking/unblocking employees (BackOffice)"
      contains: "BlockEmployeeCommandHandler"
    - path: "src/Onboarding.Application/Admin/Commands/DeleteEmployeeCommand.cs"
      provides: "Implemented LGPD deletion handler (BackOffice)"
      contains: "DeleteEmployeeCommandHandler"
  key_links:
    - from: "AdminUserController"
      to: "GetPaginatedEmployeesHandler"
      via: "IQueryHandler<GetPaginatedEmployeesQuery, PaginatedResult<EmployeeSummaryDto>>"
      pattern: "GetPaginatedEmployees.*HandleAsync"
    - from: "BlockEmployeeCommandHandler"
      to: "IKeycloakUserService"
      via: "BlockUserAsync/UnblockUserAsync targetRealm=client"
      pattern: "BlockUserAsync.*client"
---

<objective>
Implement the admin-side employee handlers that are currently stubs (throwing NotImplementedException). These handlers enable BackOffice users to manage employees from ANY company — no company isolation filter. This parallels the company-scoped endpoints in Plan 02 but operates from the admin perspective with elevated privileges.

Purpose: BackOffice must support/audit any company's employees. Admin endpoints bypass HasQueryFilter by querying directly with companyId filter or without company scoping.
Output: Functional admin employee handlers, unit tests
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-domain-model-redesign/37-CONTEXT.md
@.planning/phases/37-domain-model-redesign/37-03-SUMMARY.md

<interfaces>
<!-- Same interfaces as Plan 02 — IEmployeeRepository, IKeycloakUserService, IAuditService, ActionType -->
From AdminUserController.cs (existing stub endpoints):
```csharp
// GET /api/admin/employees — paginated list across companies
// GET /api/admin/employees/{id} — employee details
// POST /api/admin/employees/{id}/block — block employee
// POST /api/admin/employees/{id}/unblock — unblock employee
// DELETE /api/admin/employees/{id} — LGPD deletion
```
All endpoints already exist in AdminUserController with stub handlers — just need real implementations.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Admin Employee Query Handlers (GetPaginatedEmployees, GetEmployeeDetails)</name>
  <files>src/Onboarding.Application/Admin/Queries/GetPaginatedEmployeesQuery.cs, src/Onboarding.Application/Admin/Queries/GetEmployeeDetailsQuery.cs, tests/Onboarding.Application.Tests/Admin/GetPaginatedEmployeesHandlerTests.cs, tests/Onboarding.Application.Tests/Admin/GetEmployeeDetailsHandlerTests.cs</files>
  <behavior>
    - Test 1: GetPaginatedEmployeesHandler returns employees from ALL companies when CompanyId filter is null
    - Test 2: GetPaginatedEmployeesHandler returns employees filtered by specific companyId
    - Test 3: GetPaginatedEmployeesHandler applies search and status filters correctly
    - Test 4: GetEmployeeDetailsHandler returns EmployeeSummaryDto for existing employee
    - Test 5: GetEmployeeDetailsHandler throws KeyNotFoundException for missing employee
  </behavior>
  <action>
    1. Implement `GetPaginatedEmployeesHandler` in `GetPaginatedEmployeesQuery.cs`:
       - Inject: IEmployeeRepository, ICompanyRepository
       - The query has: Page, PageSize, Search, Status, CompanyId (Guid? — optional filter)
       - **Admin query sees ALL companies** — no HasQueryFilter scoping
       - If CompanyId is provided: call `IEmployeeRepository.GetPagedByCompanyAsync(companyId, page, pageSize, search, status)` — explicit companyId pass bypasses filter
       - If CompanyId is null: need a new method `GetPagedAllAsync` — add to IEmployeeRepository + EmployeeRepository that queries without company scoping
       - Map each Employee to EmployeeSummaryDto — include CompanyRazaoSocial (fetch from ICompanyRepository.GetByIdAsync for each item, or join in the query)
       - **Performance note:** For the admin cross-company query, add a new repository method that fetches with Company join:
         ```csharp
         // Add to IEmployeeRepository:
         Task<(IReadOnlyList<Employee> Items, int TotalCount)> GetPagedAllAsync(int page, int pageSize, string? search, string? status, CancellationToken ct = default);
         ```
         Implementation uses `_db.Employees.IgnoreQueryFilter()` to bypass HasQueryFilter, includes Company name projection.
       - Return PaginatedResult<EmployeeSummaryDto>

    2. Implement `GetEmployeeDetailsHandler` in `GetEmployeeDetailsQuery.cs`:
       - Inject: IEmployeeRepository, ICompanyRepository
       - Fetch employee by ID (need to bypass query filter — add `GetByIdIgnoreFilterAsync` or use `.IgnoreQueryFilter()`)
       - If not found, throw KeyNotFoundException
       - Include company razaoSocial from ICompanyRepository
       - Map to EmployeeSummaryDto

    3. Add the necessary repository methods to IEmployeeRepository + EmployeeRepository:
       - `GetPagedAllAsync` — ignores company filter, supports search/status
       - `GetByIdIgnoreFilterAsync` — bypasses HasQueryFilter for admin lookups
  </action>
  <verify>
    <automated>dotnet test tests/Onboarding.Application.Tests/ --filter "Admin" --verbosity normal</automated>
  </verify>
  <done>
    GET /api/admin/employees returns paginated list across ALL companies
    CompanyId filter works for admin to narrow results
    GET /api/admin/employees/{id} returns full employee details
    Missing employee returns 404
    All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Admin Employee Command Handlers (Block, Unblock, Delete)</name>
  <files>src/Onboarding.Application/Admin/Commands/BlockEmployeeCommand.cs, src/Onboarding.Application/Admin/Commands/UnblockEmployeeCommand.cs, src/Onboarding.Application/Admin/Commands/DeleteEmployeeCommand.cs, tests/Onboarding.Application.Tests/Admin/BlockEmployeeHandlerTests.cs, tests/Onboarding.Application.Tests/Admin/DeleteEmployeeHandlerTests.cs</files>
  <behavior>
    - Test 1: BlockEmployeeCommand calls IKeycloakUserService.BlockUserAsync("client", keycloakUserId) + LogoutAllSessionsAsync + audit
    - Test 2: UnblockEmployeeCommand calls IKeycloakUserService.UnblockUserAsync("client", keycloakUserId) + audit
    - Test 3: DeleteEmployeeCommand calls employee.Anonymize() + SaveAsync + DeleteUserByEmailAsync("client", originalEmail) + audit
    - Test 4: Block/Unblock/Delete for non-existent employee throws KeyNotFoundException
  </behavior>
  <action>
    1. Implement `BlockEmployeeCommandHandler` in `BlockEmployeeCommand.cs`:
       - Inject: IEmployeeRepository, IKeycloakUserService, IAuditService
       - Fetch employee by ID (use GetByIdIgnoreFilterAsync — admin bypasses company isolation)
       - Throw KeyNotFoundException if not found
       - `IKeycloakUserService.BlockUserAsync("client", employee.KeycloakUserId!)`
       - `IKeycloakUserService.LogoutAllSessionsAsync("client", employee.KeycloakUserId!)`
       - Audit: `IAuditService.RecordAsync(command.ActorSub, "", ActionType.EmployeeBlocked, employee.Id, employee.Nome)`

    2. Implement `UnblockEmployeeCommandHandler` in `UnblockEmployeeCommand.cs`:
       - Inject: IEmployeeRepository, IKeycloakUserService, IAuditService
       - Fetch employee, throw if not found
       - `IKeycloakUserService.UnblockUserAsync("client", employee.KeycloakUserId!)`
       - Audit: `ActionType.EmployeeUnblocked`

    3. Implement `DeleteEmployeeCommandHandler` in `DeleteEmployeeCommand.cs`:
       - Inject: IEmployeeRepository, IKeycloakUserService, IAuditService
       - Fetch employee, capture original email BEFORE anonymization
       - `employee.Anonymize()` — LGPD: Nome="Usuário Excluído", Cpf=null, Email=deleted-{Id}
       - `IEmployeeRepository.SaveAsync(employee)`
       - `IKeycloakUserService.DeleteUserByEmailAsync("client", originalEmail)`
       - Audit: `ActionType.EmployeeDeleted`
  </action>
  <verify>
    <automated>dotnet test tests/Onboarding.Application.Tests/ --filter "Admin" --verbosity normal</automated>
  </verify>
  <done>
    POST /api/admin/employees/{id}/block blocks employee in Keycloak + revokes sessions
    POST /api/admin/employees/{id}/unblock unblocks employee in Keycloak
    DELETE /api/admin/employees/{id} LGPD-anonymizes + deletes Keycloak user
    Missing employee returns 404
    All actions audited
    All tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin (BackOffice) → /api/admin/employees/* | Admin-only (BearerBackoffice + role=admin) — elevated access across companies |
| Keycloak realm client | Employee users managed in "client" realm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-13 | Elevation of Privilege | Admin employee endpoints | accept | Admin endpoints ARE elevated by design — [Authorize(Roles = "admin")] |
| T-38-14 | Repudiation | Admin employee mutations | mitigate | All admin actions recorded via IAuditService with actorSub from JWT |
| T-38-15 | Information Disclosure | Admin employee listing | accept | Admin sees all employees by design — part of BackOffice role |
</threat_model>

<verification>
1. `dotnet test tests/Onboarding.Application.Tests/ --filter "Admin" --verbosity normal` — all admin employee tests pass
2. `dotnet build src/Onboarding.sln` — zero errors, zero warnings
</verification>

<success_criteria>
- Admin can list employees from any company with companyId filter
- Admin can view employee details regardless of company
- Admin can block/unblock any employee (Keycloak + session revocation)
- Admin can LGPD-delete any employee
- All stub handlers replaced with full implementations
- All unit tests pass, build clean
</success_criteria>

<output>
After completion, create `.planning/phases/38-employee-registration-management-api/38-03-SUMMARY.md`
</output>
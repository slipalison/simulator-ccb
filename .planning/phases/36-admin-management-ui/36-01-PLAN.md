---
phase: 36-admin-management-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/backoffice/src/lib/admin-api.ts
  - frontend/backoffice/src/lib/validation-schemas.ts
  - frontend/backoffice/src/lib/admin-auth-context.tsx
  - frontend/backoffice/src/components/molecules/AdminStatusFilter.tsx
autonomous: true
requirements:
  - MGMT-01
  - MGMT-02
  - MGMT-03
  - MGMT-04
  - MGMT-05
  - MGMT-06

must_haves:
  truths:
    - "getAdministratorsPaginated() chama GET /api/admin/administrators/paginated com params name, email, status, page, pageSize"
    - "updateAdministrator() chama PUT /api/admin/administrators/{id} e lança AdminApiError(409) se email duplicado"
    - "resetAdministratorPassword() chama POST /api/admin/administrators/{id}/reset-password e retorna { temporaryPassword: string }"
    - "toggleAdministratorStatus() chama POST /api/admin/administrators/{id}/toggle-status com { activate: boolean }"
    - "adminEditAdministratorSchema valida nome (min 2, max 100, obrigatório) e email (formato RFC, obrigatório)"
    - "useAdminAuth() expõe adminId (sub do JWT) para detecção de própria linha"
    - "AdminStatusFilter aceita prop options para variante admin com opções Todos/Ativo/Inativo"
  artifacts:
    - path: "frontend/backoffice/src/lib/admin-api.ts"
      provides: "Quatro novas funções de API para gerenciamento de administradores"
      contains: "getAdministratorsPaginated|updateAdministrator|resetAdministratorPassword|toggleAdministratorStatus"
    - path: "frontend/backoffice/src/lib/validation-schemas.ts"
      provides: "Schema Zod para edição de administrador"
      contains: "adminEditAdministratorSchema"
    - path: "frontend/backoffice/src/lib/admin-auth-context.tsx"
      provides: "adminId no contexto de autenticação"
      contains: "adminId"
    - path: "frontend/backoffice/src/components/molecules/AdminStatusFilter.tsx"
      provides: "Suporte a options customizadas via prop"
      contains: "options.*prop|StatusOption"
  key_links:
    - from: "frontend/backoffice/src/lib/admin-api.ts"
      to: "/api/admin/administrators/paginated"
      via: "fetchWithAuth GET com URLSearchParams name/email/status/page/pageSize"
      pattern: "getAdministratorsPaginated"
    - from: "frontend/backoffice/src/lib/admin-api.ts"
      to: "/api/admin/administrators/{id}/toggle-status"
      via: "fetchWithAuth POST com { activate: boolean, targetUserName: string }"
      pattern: "toggleAdministratorStatus"
---

<objective>
Criar todos os contratos de dados da Fase 36: quatro funções de API para o gerenciamento paginado de administradores, o schema Zod para validação do form de edição, e as adaptações de infraestrutura (adminId no contexto de auth, opções customizáveis no AdminStatusFilter).

Purpose: Estabelecer contratos tipados consumidos pelos planos 02, 03 e 04 — todos os novos componentes e a página principal dependem das assinaturas definidas aqui.
Output: admin-api.ts estendido, adminEditAdministratorSchema, AdminAuthContext com adminId, AdminStatusFilter com prop options.
</objective>

<execution_context>
@D:\REPO\keycloak-tests\.claude\get-shit-done\workflows\execute-plan.md
@D:\REPO\keycloak-tests\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@D:\REPO\keycloak-tests\.planning\phases\36-admin-management-ui\36-CONTEXT.md
@D:\REPO\keycloak-tests\.planning\phases\36-admin-management-ui\36-UI-SPEC.md

<interfaces>
<!-- Contratos existentes que o executor precisa conhecer -->

De frontend/backoffice/src/lib/admin-api.ts (atual):
```typescript
export interface AdminUserDto {
  id: string;
  email: string;
  fullName: string;
  isEnabled: boolean;
  hasTemporaryPassword: boolean;
}

export interface PaginatedResult<T> {
  items: T[];
  totalCount: number;
  page: number;
  pageSize: number;
}

export class AdminApiError extends Error {
  public status?: number;
  constructor(message: string, status?: number);
}

// fetchWithAuth: faz 401 → redirect para /admin/login automaticamente
async function fetchWithAuth(url: string | URL, init?: RequestInit): Promise<Response>

// Exemplo de padrão URLSearchParams já usado:
export async function listUsers(params: ListUsersParams): Promise<PaginatedResult<UserSummaryDto>>
```

De src/Onboarding.API/Controllers/AdminUserController.cs (backend Phase 35):
```csharp
// GET /api/admin/administrators/paginated
// Query params: page (int), pageSize (int), name (string?), email (string?), status (string?)
// Response: PaginatedResult<AdminUserDto>

// PUT /api/admin/administrators/{id}
// Body: { fullName: string, email: string }
// 204 NoContent — sucesso
// 400 — self-edit bloqueado (SEC-01)
// 409 — email duplicado (SEC-04)

// POST /api/admin/administrators/{id}/reset-password
// Body: { targetUserName: string }
// Response: { temporaryPassword: string }
// 400 — self-reset bloqueado (SEC-01)

// POST /api/admin/administrators/{id}/toggle-status
// Body: { activate: boolean, targetUserName: string, reason?: string }
// 204 NoContent — sucesso
// 400 — self-toggle ou último admin ativo (SEC-01, SEC-05)
```

De frontend/backoffice/src/lib/admin-auth-context.tsx (atual — NÃO tem adminId):
```typescript
interface AdminAuthValue {
  admin: {
    isAuthenticated: boolean;
    isLoading: boolean;
    adminName: string | null;
    adminEmail: string | null;
    // adminId AUSENTE — precisa ser adicionado
  };
  logout: () => void;
  restoreSession: () => Promise<boolean>;
}

// /auth/me retorna:
// { isAuthenticated: boolean, adminName: string, email: string, sub: string }
// O campo 'sub' é o Keycloak user ID — já está disponível na resposta mas não é guardado no estado
```

De frontend/backoffice/src/components/molecules/AdminStatusFilter.tsx (atual):
```typescript
// STATUS_OPTIONS hardcoded: "all", "active", "blocked", "deleted"
// Decisão D-11: criar variante para admins com apenas "all", "active", "inactive"
// Solução: aceitar prop options opcional; quando ausente, usar STATUS_OPTIONS atual
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender admin-api.ts com as quatro funções de gerenciamento de administradores</name>
  <files>frontend/backoffice/src/lib/admin-api.ts</files>
  <read_first>
    - frontend/backoffice/src/lib/admin-api.ts — ler arquivo completo antes de qualquer edição; entender padrão fetchWithAuth, URLSearchParams, AdminApiError
  </read_first>
  <action>
Adicionar ao final do arquivo frontend/backoffice/src/lib/admin-api.ts (após o bloco "Admin Administrators — Phase 30"):

```typescript
// ---------------------------------------------------------------------------
// Admin Administrators — Phase 36 (Milestone v6.0) — MGMT-01..06
// ---------------------------------------------------------------------------

// Parâmetros para listagem paginada de administradores
export interface GetAdministratorsPaginatedParams {
  page?: number;
  pageSize?: number;
  name?: string;
  email?: string;
  status?: string; // "active" | "inactive" | undefined
}

// Resultado de reset de senha (one-time)
export interface ResetAdministratorPasswordResult {
  temporaryPassword: string;
}

// GET /api/admin/administrators/paginated — MGMT-01, MGMT-02
export async function getAdministratorsPaginated(
  params: GetAdministratorsPaginatedParams = {}
): Promise<PaginatedResult<AdminUserDto>> {
  const searchParams = new URLSearchParams();
  if (params.page) searchParams.set("page", String(params.page));
  if (params.pageSize) searchParams.set("pageSize", String(params.pageSize));
  if (params.name) searchParams.set("name", params.name);
  if (params.email) searchParams.set("email", params.email);
  if (params.status) searchParams.set("status", params.status);

  const queryString = searchParams.toString();
  const url = queryString
    ? `/api/admin/administrators/paginated?${queryString}`
    : "/api/admin/administrators/paginated";

  const response = await fetchWithAuth(url, {
    method: "GET",
    credentials: "include",
  });

  if (!response.ok) {
    throw new AdminApiError("Falha ao carregar administradores.", response.status);
  }

  return response.json() as Promise<PaginatedResult<AdminUserDto>>;
}

// PUT /api/admin/administrators/{id} — MGMT-03, SEC-01, SEC-04
export async function updateAdministrator(
  adminId: string,
  data: { fullName: string; email: string }
): Promise<void> {
  const response = await fetchWithAuth(`/api/admin/administrators/${adminId}`, {
    ..._adminFetchOptions("PUT", JSON.stringify({ fullName: data.fullName, email: data.email })),
  });

  if (response.status === 400) {
    const body = await response.json().catch(() => ({}));
    throw new AdminApiError(body.detail || "Operação não permitida.", 400);
  }

  if (response.status === 409) {
    throw new AdminApiError("Email já está em uso.", 409);
  }

  if (!response.ok) {
    const body = await response.json().catch(() => ({}));
    throw new AdminApiError(body.detail || "Falha ao atualizar administrador.", response.status);
  }
}

// POST /api/admin/administrators/{id}/reset-password — MGMT-04, SEC-01, SEC-03
export async function resetAdministratorPassword(
  adminId: string,
  targetUserName: string
): Promise<ResetAdministratorPasswordResult> {
  const response = await fetchWithAuth(`/api/admin/administrators/${adminId}/reset-password`, {
    ..._adminFetchOptions("POST", JSON.stringify({ targetUserName })),
  });

  if (response.status === 400) {
    const body = await response.json().catch(() => ({}));
    throw new AdminApiError(body.detail || "Operação não permitida.", 400);
  }

  if (!response.ok) {
    const body = await response.json().catch(() => ({}));
    throw new AdminApiError(body.detail || "Falha ao resetar senha.", response.status);
  }

  return response.json() as Promise<ResetAdministratorPasswordResult>;
}

// POST /api/admin/administrators/{id}/toggle-status — MGMT-05, MGMT-06, SEC-01, SEC-05
export async function toggleAdministratorStatus(
  adminId: string,
  targetUserName: string,
  activate: boolean,
  reason?: string
): Promise<void> {
  const response = await fetchWithAuth(`/api/admin/administrators/${adminId}/toggle-status`, {
    ..._adminFetchOptions(
      "POST",
      JSON.stringify({ activate, targetUserName, reason: reason ?? null })
    ),
  });

  if (response.status === 400) {
    const body = await response.json().catch(() => ({}));
    throw new AdminApiError(body.detail || "Operação não permitida.", 400);
  }

  if (response.status === 409) {
    const body = await response.json().catch(() => ({}));
    throw new AdminApiError(body.detail || "Não é possível desativar o último administrador ativo.", 409);
  }

  if (!response.ok) {
    const body = await response.json().catch(() => ({}));
    throw new AdminApiError(body.detail || "Falha ao alterar status.", response.status);
  }
}
```

IMPORTANTE: `_adminFetchOptions` é uma função privada existente no arquivo — reutilizar, não duplicar.
  </action>
  <verify>
    <automated>grep -c "getAdministratorsPaginated\|updateAdministrator\|resetAdministratorPassword\|toggleAdministratorStatus" "D:/REPO/keycloak-tests/frontend/backoffice/src/lib/admin-api.ts"</automated>
  </verify>
  <acceptance_criteria>
    - grep "export async function getAdministratorsPaginated" retorna match em admin-api.ts
    - grep "export async function updateAdministrator" retorna match em admin-api.ts
    - grep "export async function resetAdministratorPassword" retorna match em admin-api.ts
    - grep "export async function toggleAdministratorStatus" retorna match em admin-api.ts
    - grep "export interface ResetAdministratorPasswordResult" retorna match em admin-api.ts
    - grep "export interface GetAdministratorsPaginatedParams" retorna match em admin-api.ts
    - Para status 409 em updateAdministrator: o bloco lança AdminApiError com status 409
    - Para status 400 em toggleAdministratorStatus: o bloco lança AdminApiError com status 400
  </acceptance_criteria>
  <done>As quatro funções exportadas e dois novos interfaces exportados estão presentes em admin-api.ts; o arquivo compila sem erros TypeScript (npx tsc --noEmit no diretório do backoffice).</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar adminEditAdministratorSchema em validation-schemas.ts, adminId no AdminAuthContext, e prop options no AdminStatusFilter</name>
  <files>
    frontend/backoffice/src/lib/validation-schemas.ts
    frontend/backoffice/src/lib/admin-auth-context.tsx
    frontend/backoffice/src/components/molecules/AdminStatusFilter.tsx
  </files>
  <read_first>
    - frontend/backoffice/src/lib/validation-schemas.ts — ler arquivo completo para entender padrão dos schemas existentes
    - frontend/backoffice/src/lib/admin-auth-context.tsx — ler arquivo completo; notar que /auth/me retorna sub mas ele não é salvo
    - frontend/backoffice/src/components/molecules/AdminStatusFilter.tsx — ler arquivo completo para entender estrutura atual
  </read_first>
  <action>
**1. validation-schemas.ts** — adicionar ao final do arquivo:

```typescript
// ---------------------------------------------------------------------------
// Edit Administrator Schema — Phase 36 (nome e email obrigatórios)
// ---------------------------------------------------------------------------

export const adminEditAdministratorSchema = z.object({
  fullName: z
    .string()
    .trim()
    .min(2, "Nome muito curto (mínimo 2 caracteres)")
    .max(100, "Nome muito longo (máximo 100 caracteres)"),
  email: z
    .string()
    .trim()
    .min(1, "Email é obrigatório")
    .email("Email inválido"),
});

export type AdminEditAdministratorInput = z.infer<typeof adminEditAdministratorSchema>;
```

**2. admin-auth-context.tsx** — três alterações cirúrgicas:

a) Na interface `AdminAuthValue`, campo `admin`, adicionar `adminId: string | null;` após `adminEmail: string | null;`

b) Adicionar estado: `const [adminId, setAdminId] = useState<string | null>(null);`

c) Na função `tryRestore` e `restoreSession`, após obter `const me = await getAdminMe()`:
- Adicionar `setAdminId(data.sub)` — mas `getAdminMe()` retorna `AdminSessionResponse` que só tem `adminName` e `adminEmail`. Será necessário ajustar `getAdminMe` para também retornar o `sub`.

Portanto a correção é em duas sub-etapas:

Em admin-api.ts, na função `getAdminMe`, mudar o retorno de `AdminSessionResponse` para incluir `adminId`:
- Alterar a linha `return { adminName: data.adminName, adminEmail: data.email };` para `return { adminName: data.adminName, adminEmail: data.email, adminId: data.sub };`
- Alterar a interface `AdminSessionResponse` adicionando `adminId: string;`

Em admin-auth-context.tsx:
- Adicionar `adminId: string | null` na interface `AdminAuthValue.admin`
- Adicionar `const [adminId, setAdminId] = useState<string | null>(null);`
- Em `tryRestore`: `setAdminId(me.adminId);`
- Em `restoreSession`: `setAdminId(me.adminId);` — e no bloco catch: `setAdminId(null);`
- Na função `logout`: `setAdminId(null);`
- No Provider value: `admin: { isAuthenticated, isLoading, adminName, adminEmail, adminId }`

**3. AdminStatusFilter.tsx** — adaptar para aceitar prop `options` opcional:

Substituir o componente inteiro por:

```typescript
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";

interface StatusOption {
  value: string;
  label: string;
}

interface AdminStatusFilterProps {
  value: string;
  onChange: (value: string) => void;
  disabled?: boolean;
  options?: StatusOption[]; // quando ausente, usa DEFAULT_STATUS_OPTIONS (compatibilidade)
}

const DEFAULT_STATUS_OPTIONS: StatusOption[] = [
  { value: "all", label: "Todos" },
  { value: "active", label: "Ativo" },
  { value: "blocked", label: "Bloqueado" },
  { value: "deleted", label: "Deletado" },
];

// Opções para uso em páginas de administradores (MGMT-02, D-11)
export const ADMIN_STATUS_OPTIONS: StatusOption[] = [
  { value: "all", label: "Todos" },
  { value: "active", label: "Ativo" },
  { value: "inactive", label: "Inativo" },
];

export function AdminStatusFilter({
  value,
  onChange,
  disabled = false,
  options,
}: AdminStatusFilterProps) {
  const resolvedOptions = options ?? DEFAULT_STATUS_OPTIONS;

  return (
    <Select value={value} onValueChange={onChange} disabled={disabled}>
      <SelectTrigger className="w-[180px]" data-testid="status-filter" aria-label="Filtrar por status">
        <SelectValue placeholder="Status" />
      </SelectTrigger>
      <SelectContent>
        {resolvedOptions.map((opt) => (
          <SelectItem key={opt.value} value={opt.value} data-testid={`status-${opt.value}`}>
            {opt.label}
          </SelectItem>
        ))}
      </SelectContent>
    </Select>
  );
}
```

ATENÇÃO: As páginas existentes que usam `AdminStatusFilter` sem a prop `options` continuarão funcionando com `DEFAULT_STATUS_OPTIONS` — compatibilidade retroativa obrigatória.
  </action>
  <verify>
    <automated>cd "D:/REPO/keycloak-tests/frontend/backoffice" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "adminEditAdministratorSchema" frontend/backoffice/src/lib/validation-schemas.ts retorna match
    - grep "AdminEditAdministratorInput" frontend/backoffice/src/lib/validation-schemas.ts retorna match
    - grep "adminId" frontend/backoffice/src/lib/admin-auth-context.tsx retorna múltiplos matches (estado, interface, valor)
    - grep "adminId" frontend/backoffice/src/lib/admin-api.ts retorna match (campo em AdminSessionResponse e retorno de getAdminMe)
    - grep "ADMIN_STATUS_OPTIONS" frontend/backoffice/src/components/molecules/AdminStatusFilter.tsx retorna match (exportado)
    - grep "options.*StatusOption" frontend/backoffice/src/components/molecules/AdminStatusFilter.tsx retorna match (prop opcional)
    - grep "DEFAULT_STATUS_OPTIONS" frontend/backoffice/src/components/molecules/AdminStatusFilter.tsx retorna match (fallback retrocompatível)
    - npx tsc --noEmit no diretório backoffice não retorna erros de tipo
    - AdminUsersPage.tsx e outros consumidores de AdminStatusFilter continuam compilando sem alterar (verificar com tsc)
  </acceptance_criteria>
  <done>adminEditAdministratorSchema exportado em validation-schemas.ts; adminId disponível via useAdminAuth().admin.adminId; AdminStatusFilter aceita prop options com fallback para opções originais; TypeScript compila sem erros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Frontend → Backend API | Dados de formulário (nome, email) cruzam esta fronteira via fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | admin-api.ts — toggleAdministratorStatus | mitigate | Backend valida SEC-01 (self-action) e SEC-05 (último admin) — frontend apenas reflete erros 400/409 via toast |
| T-36-02 | Information Disclosure | admin-auth-context.tsx — adminId exposto | accept | adminId (sub do JWT) é necessário para detecção de própria linha (D-08); não é secret — já presente no JWT em cookie httpOnly |
</threat_model>

<verification>
1. `cd D:/REPO/keycloak-tests/frontend/backoffice && npx tsc --noEmit` — zero erros TypeScript
2. `grep -c "getAdministratorsPaginated\|updateAdministrator\|resetAdministratorPassword\|toggleAdministratorStatus" frontend/backoffice/src/lib/admin-api.ts` — retorna 4
3. `grep "adminId" frontend/backoffice/src/lib/admin-auth-context.tsx` — retorna múltiplas linhas
4. `grep "ADMIN_STATUS_OPTIONS" frontend/backoffice/src/components/molecules/AdminStatusFilter.tsx` — retorna match
</verification>

<success_criteria>
- Quatro funções de API exportadas com tipos corretos
- adminEditAdministratorSchema com fullName (min 2, max 100, trim) e email (formato RFC, obrigatório)
- useAdminAuth().admin.adminId retorna o sub do JWT
- AdminStatusFilter aceita prop options e exporta ADMIN_STATUS_OPTIONS com "all"/"active"/"inactive"
- Zero regressão em componentes existentes (tsc --noEmit passa)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/36-admin-management-ui/36-01-SUMMARY.md`
</output>

---
phase: 37-domain-model-redesign
plan: 03
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - src/Onboarding.Infrastructure/Persistence/Configurations/CompanyConfiguration.cs
  - src/Onboarding.Infrastructure/Persistence/Configurations/EmployeeConfiguration.cs
  - src/Onboarding.Infrastructure/Persistence/Configurations/AccessGroupConfiguration.cs
  - src/Onboarding.Infrastructure/Persistence/Configurations/ClientConfiguration.cs
  - src/Onboarding.Infrastructure/Persistence/AppDbContext.cs
  - src/Onboarding.Infrastructure/Persistence/CurrentCompanyService.cs
  - src/Onboarding.Infrastructure/Repositories/CompanyRepository.cs
  - src/Onboarding.Infrastructure/Repositories/EmployeeRepository.cs
  - src/Onboarding.Infrastructure/Repositories/AccessGroupRepository.cs
  - src/Onboarding.Infrastructure/Migrations/
autonomous: true
requirements:
  - REG-02
  - REG-04
  - REG-05

must_haves:
  truths:
    - "EF Core configs exist for Company, Employee, AccessGroup with correct column mappings"
    - "HasQueryFilter on Employee and AccessGroup filters by CompanyId — no cross-company data leak"
    - "AppDbContext has DbSet<Company>, DbSet<Employee>, DbSet<AccessGroup> and no DbSet<Client>"
    - "ClientConfiguration.cs is DELETED"
    - "ICurrentCompanyService provides CompanyId for HasQueryFilter injection"
    - "EF Core migration drops 'clients' table, creates 'companies', 'employees', 'access_groups'"
    - "Index on companies.cnpj is unique for REG-02"
  artifacts:
    - path: "src/Onboarding.Infrastructure/Persistence/Configurations/CompanyConfiguration.cs"
      provides: "EF Core fluent config for Company"
    - path: "src/Onboarding.Infrastructure/Persistence/Configurations/EmployeeConfiguration.cs"
      provides: "EF Core fluent config for Employee with HasQueryFilter"
    - path: "src/Onboarding.Infrastructure/Persistence/Configurations/AccessGroupConfiguration.cs"
      provides: "EF Core fluent config for AccessGroup with HasQueryFilter"
    - path: "src/Onboarding.Infrastructure/Persistence/AppDbContext.cs"
      provides: "Updated DbContext with new DbSets"
      exports: ["AppDbContext"]
    - path: "src/Onboarding.Infrastructure/Persistence/CurrentCompanyService.cs"
      provides: "ICurrentCompanyService for query filter injection"
      exports: ["CurrentCompanyService", "ICurrentCompanyService"]
    - path: "src/Onboarding.Infrastructure/Repositories/CompanyRepository.cs"
      provides: "CompanyRepository implementation"
    - path: "src/Onboarding.Infrastructure/Repositories/EmployeeRepository.cs"
      provides: "EmployeeRepository implementation"
    - path: "src/Onboarding.Infrastructure/Repositories/AccessGroupRepository.cs"
      provides: "AccessGroupRepository implementation"
  key_links:
    - from: "src/Onboarding.Infrastructure/Persistence/AppDbContext.cs"
      to: "Company/Employee/AccessGroup entities"
      via: "DbSet registrations and ApplyConfiguration"
      pattern: "DbSet<Company>|DbSet<Employee>|DbSet<AccessGroup>"
    - from: "EmployeeConfiguration.cs"
      to: "CurrentCompanyService"
      via: "HasQueryFilter using ICurrentCompanyService"
      pattern: "HasQueryFilter"
---

<objective>
Create EF Core configurations, repositories, DbContext updates, and migration for the new domain model. Delete obsolete Client infrastructure.

Purpose: Wire the new Company/Employee/AccessGroup domain types into EF Core with company isolation via HasQueryFilter. Drop `clients` table, create new tables. This is the data persistence foundation for Phase 38+.

Output: EF Core fluent configs, repository implementations, AppDbContext update, ICurrentCompanyService, migration that drops `clients` and creates `companies`/`employees`/`access_groups`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-domain-model-redesign/37-CONTEXT.md
@.planning/phases/37-domain-model-redesign/37-01-SUMMARY.md

<interfaces>
<!-- Key types the executor needs. -->

From src/Onboarding.Domain/Aggregates/CompanyAggregate/Company.cs (created in Plan 01):
```csharp
public sealed class Company : Entity<Guid>
{
    public Cnpj Cnpj { get; }
    public string RazaoSocial { get; }
    public Email Email { get; }
    public PhoneNumber Phone { get; }
    public string? KeycloakUserId { get; }
    public TermsAcceptance TermsAcceptance { get; }
    public DateTime? DeletedAt { get; }
    public bool IsDeleted { get; }
}
```

From src/Onboarding.Domain/Aggregates/EmployeeAggregate/Employee.cs (created in Plan 01):
```csharp
public sealed class Employee : Entity<Guid>
{
    public Cpf Cpf { get; }
    public string Nome { get; }
    public Email Email { get; }
    public PhoneNumber Phone { get; }
    public Guid CompanyId { get; }  // FK - no navigation
    public string? KeycloakUserId { get; }
    public Guid AccessGroupId { get; }
    public DateTimeOffset? DeletedAt { get; }
}
```

From src/Onboarding.Domain/Aggregates/EmployeeAggregate/AccessGroup.cs (created in Plan 01):
```csharp
public sealed class AccessGroup : Entity<Guid>
{
    public Guid CompanyId { get; }  // FK
    public string Name { get; }
    public List<string> Permissions { get; }
    public DateTimeOffset CreatedAt { get; }
}
```

From existing ClientConfiguration.cs (to be DELETED):
```csharp
public sealed class ClientConfiguration : IEntityTypeConfiguration<Client>
{
    // Maps Client to 'clients' table with VO conversions, unique indexes, etc.
}
```

From existing AppDbContext.cs:
```csharp
public DbSet<Client> Clients => Set<Client>();
public DbSet<PasswordResetToken> PasswordResetTokens => Set<PasswordResetToken>();
public DbSet<AdminAuditLog> AdminAuditLogs => Set<AdminAuditLog>();
// OnModelCreating applies ClientConfiguration, PasswordResetTokenConfiguration, AdminAuditLogConfiguration
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EF Core configs + AppDbContext + CurrentCompanyService + repositories + migration</name>
  <files>
    src/Onboarding.Infrastructure/Persistence/Configurations/CompanyConfiguration.cs
    src/Onboarding.Infrastructure/Persistence/Configurations/EmployeeConfiguration.cs
    src/Onboarding.Infrastructure/Persistence/Configurations/AccessGroupConfiguration.cs
    src/Onboarding.Infrastructure/Persistence/Configurations/ClientConfiguration.cs
    src/Onboarding.Infrastructure/Persistence/AppDbContext.cs
    src/Onboarding.Infrastructure/Persistence/CurrentCompanyService.cs
    src/Onboarding.Infrastructure/Repositories/CompanyRepository.cs
    src/Onboarding.Infrastructure/Repositories/EmployeeRepository.cs
    src/Onboarding.Infrastructure/Repositories/AccessGroupRepository.cs
  </files>
  <read_first>
    src/Onboarding.Infrastructure/Persistence/Configurations/ClientConfiguration.cs
    src/Onboarding.Infrastructure/Persistence/AppDbContext.cs
    src/Onboarding.Infrastructure/Persistence/Configurations/AdminAuditLogConfiguration.cs
    src/Onboarding.Domain/Repositories/IClientRepository.cs
  </read_first>
  <action>
    **1. Create `ICurrentCompanyService.cs`** in `src/Onboarding.Application/Common/`:
    ```csharp
    namespace Onboarding.Application.Common;

    public interface ICurrentCompanyService
    {
        Guid CompanyId { get; }
    }
    ```

    **2. Create `CurrentCompanyService.cs`** in `src/Onboarding.Infrastructure/Persistence/`:
    - Implements `ICurrentCompanyService`
    - Has `public Guid CompanyId { get; set; }` — will be set per-request from JWT claims in Phase 39. For now, default to `Guid.Empty` and register as Scoped in DI.
    - This service is injected into AppDbContext for HasQueryFilter (per D-17).

    **3. Create `CompanyConfiguration.cs`** (D-15, D-16):
    - `IEntityTypeConfiguration<Company>`
    - Table: `companies`
    - Map all VOs using `.HasConversion()` (Cnpj→string, Email→string, PhoneNumber→string) — same pattern as ClientConfiguration
    - `TermsAcceptance` as owned type: `builder.OwnsOne(c => c.TermsAcceptance, ...)` with columns `terms_accepted_at`, `terms_version`, `ip_address`
    - Unique index on `Cnpj` (REG-02) with `HasFilter("cnpj IS NOT NULL")`
    - Unique index on `Email`
    - `DeletedAt` column (nullable, for LGPD soft-delete)
    - `KeycloakUserId` column (nullable string, max 36)

    **4. Create `EmployeeConfiguration.cs`** (D-02, D-17):
    - `IEntityTypeConfiguration<Employee>`
    - Table: `employees`
    - Map VOs: Cpf→string (max 11), Email→string, PhoneNumber→string
    - `CompanyId` column: `company_id` (Guid, required, FK to `companies`)
    - `AccessGroupId` column: `access_group_id` (Guid, required, FK to `access_groups`)
    - `KeycloakUserId` (nullable string, max 36)
    - `DeletedAt` (nullable DateTimeOffset)
    - **HasQueryFilter**: `builder.HasQueryFilter(e => e.CompanyId == _currentCompanyService.CompanyId)` — inject `ICurrentCompanyService` via constructor (D-17)
    - Unique index on `Cpf` with `HasFilter("cpf IS NOT NULL")`
    - Unique index on `Email`
    - Index on `CompanyId` for query performance
    - Index on `AccessGroupId`

    **5. Create `AccessGroupConfiguration.cs`** (D-06, D-17):
    - `IEntityTypeConfiguration<AccessGroup>`
    - Table: `access_groups`
    - `CompanyId` column: `company_id` (Guid, required, FK to `companies`)
    - `Name` column (max 100, required)
    - `Permissions` column: store as comma-separated string, convert via `.HasConversion(v => string.Join(",", v), s => s.Split(",", StringSplitOptions.RemoveEmptyEntries).ToList())`
    - `CreatedAt` column
    - **HasQueryFilter**: `builder.HasQueryFilter(a => a.CompanyId == _currentCompanyService.CompanyId)` — same injection pattern
    - Unique composite index on `(CompanyId, Name)` — prevents duplicate group names within same company
    - Index on `CompanyId`

    **6. Update `AppDbContext.cs`**:
    - Remove `public DbSet<Client> Clients => Set<Client>();`
    - Add `public DbSet<Company> Companies => Set<Company>();`
    - Add `public DbSet<Employee> Employees => Set<Employee>();`
    - Add `public DbSet<AccessGroup> AccessGroups => Set<AccessGroup>();`
    - Constructor: add `ICurrentCompanyService currentCompanyService` parameter, store as readonly field
    - `OnModelCreating`: Remove `ApplyConfiguration(new ClientConfiguration())`, add `ApplyConfiguration(new CompanyConfiguration(_currentCompanyService))`, `ApplyConfiguration(new EmployeeConfiguration(_currentCompanyService))`, `ApplyConfiguration(new AccessGroupConfiguration(_currentCompanyService))`
    - Pass `_currentCompanyService` to each configuration constructor (for HasQueryFilter injection)

    **7. DELETE `src/Onboarding.Infrastructure/Persistence/Configurations/ClientConfiguration.cs`**

    **8. Create Repository implementations:**

    `CompanyRepository.cs` — implements `ICompanyRepository`:
    - Constructor: `AppDbContext context`
    - `AddAsync`: `await _context.Companies.AddAsync(company, ct)`
    - `SaveAsync`: `await _context.SaveChangesAsync(ct)`
    - `GetByIdAsync`: `await _context.Companies.FindAsync(new object[] { id }, ct)`
    - `ExistsByCnpjAsync`: `await _context.Companies.AnyAsync(c => c.Cnpj.Value == cnpj, ct)` (REG-02)
    - `ExistsByEmailAsync`: `await _context.Companies.AnyAsync(c => c.Email.Value == email, ct)`
    - `GetByKeycloakSubAsync`: `await _context.Companies.FirstOrDefaultAsync(c => c.KeycloakUserId == keycloakSub, ct)`
    - `GetByEmailAsync`: `await _context.Companies.FirstOrDefaultAsync(c => c.Email.Value == email, ct)`
    - `DeleteAsync`: `var entity = await _context.Companies.FindAsync(id); if (entity != null) { _context.Companies.Remove(entity); await _context.SaveChangesAsync(ct); }`

    `EmployeeRepository.cs` — implements `IEmployeeRepository`:
    - Same pattern as CompanyRepository but for Employee
    - `GetPagedByCompanyAsync`: query `_context.Employees.Where(e => e.CompanyId == companyId)` with search/status filters, paginated via Skip/Take, return `(items, totalCount)` tuple
    - Status filter: if `status == "active"`, filter `!e.IsDeleted`; if `status == "deleted"`, filter `e.IsDeleted`

    `AccessGroupRepository.cs` — implements `IAccessGroupRepository`:
    - `AddAsync`, `AddRangeAsync`, `SaveAsync`, `GetByIdAsync`, `GetByCompanyIdAsync`, `GetByCompanyAndNameAsync`, `DeleteAsync`

    **9. Delete obsolete repository if it exists** — check for any `ClientRepository.cs` in `src/Onboarding.Infrastructure/Repositories/` and delete it.

    **10. Generate EF Core migration:**
    ```
    dotnet ef migrations add ReplaceClientWithCompanyEmployee --project src/Onboarding.Infrastructure --startup-project src/Onboarding.API --context AppDbContext
    ```
    - Verify migration: `Down()` drops `companies`, `employees`, `access_groups`; `Up()` creates them and drops `clients`
    - If the auto-generated migration doesn't drop `clients` table, add `migrationBuilder.DropTable("clients")` in `Up()`
    - Preserve `admin_audit_logs` and `password_reset_tokens` (D-16) — do NOT drop them

    **11. Register in DI (Program.cs partial — don't wire controllers yet, that's Plan 04):**
    - Add `services.AddScoped<ICurrentCompanyService, CurrentCompanyService>();`
    - Add `services.AddScoped<ICompanyRepository, CompanyRepository>();`
    - Add `services.AddScoped<IEmployeeRepository, EmployeeRepository>();`
    - Add `services.AddScoped<IAccessGroupRepository, AccessGroupRepository>();`
    - Note: This can be done in a separate `AddCompanyInfrastructure()` extension method in Infrastructure, or directly in Program.cs — follow existing pattern from `AddInfrastructure()` method.
  </action>
  <verify>
    <automated>dotnet build src/Onboarding.API/Onboarding.API.csproj 2>&1 | Select-String "error" ; echo "EXIT=$LASTEXITCODE"</automated>
  </verify>
  <done>
    - CompanyConfiguration.cs maps Company to `companies` table with unique Cnpj index (REG-02)
    - EmployeeConfiguration.cs maps Employee to `employees` with HasQueryFilter by CompanyId (D-17)
    - AccessGroupConfiguration.cs maps AccessGroup to `access_groups` with HasQueryFilter by CompanyId (D-17)
    - AppDbContext has DbSet<Company>, DbSet<Employee>, DbSet<AccessGroup> — no DbSet<Client>
    - ClientConfiguration.cs is DELETED
    - ICurrentCompanyService + CurrentCompanyService exist for query filter injection
    - CompanyRepository, EmployeeRepository, AccessGroupRepository implementations exist
    - EF Core migration exists: drops `clients`, creates `companies`/`employees`/`access_groups`
    - `dotnet build` succeeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → Application | EF Core HasQueryFilter prevents cross-company data access at ORM level |
| Application → DB | Repository implementations enforce company-scoped queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03-01 | Information Disclosure | Employee queries | mitigate | HasQueryFilter on EmployeeConfiguration filters by CompanyId — cross-company data cannot be returned (PERM-5 preview) |
| T-37-03-02 | Information Disclosure | AccessGroup queries | mitigate | HasQueryFilter on AccessGroupConfiguration filters by CompanyId |
| T-37-03-03 | Tampering | Migration | mitigate | Migration preserves admin_audit_logs and password_reset_tokens (D-16); drops only `clients` |
| T-37-03-04 | Elevation of Privilege | ICurrentCompanyService | mitigate | CompanyId set from JWT claims per-request — cannot be manually set by client code |
</threat_model>

<verification>
1. `dotnet build src/Onboarding.API/Onboarding.API.csproj` succeeds
2. ClientConfiguration.cs is deleted
3. AppDbContext has no reference to Client or DbSet<Client>
4. EmployeeConfiguration.cs contains `HasQueryFilter`
5. AccessGroupConfiguration.cs contains `HasQueryFilter`
6. CompanyConfiguration.cs has unique index on Cnpj
7. Migration file exists with DropTable("clients") and CreateTable("companies"/"employees"/"access_groups")
8. Repositories exist and implement their respective interfaces
</verification>

<success_criteria>
1. EF Core configs map all domain properties correctly with VO conversions
2. HasQueryFilter on Employee and AccessGroup ensures company isolation (D-17)
3. AppDbContext has new DbSets, no Client references
4. Migration drops `clients`, creates `companies`, `employees`, `access_groups`
5. Preserves `admin_audit_logs` and `password_reset_tokens` (D-16)
6. ICurrentCompanyService available for DI injection
7. All 3 repository implementations compile
8. `dotnet build` of entire solution succeeds (except Application/API layer errors from Plan 04 scope)
</success_criteria>

<output>
After completion, create `.planning/phases/37-domain-model-redesign/37-03-SUMMARY.md`
</output>
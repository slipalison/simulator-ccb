---
phase: 30-audit-log-admin-backend
plan: 05
type: execute
wave: 5
depends_on:
  - 30-04
files_modified:
  - src/Onboarding.Application/Admin/Commands/CreateAdminCommand.cs
  - frontend/backoffice/src/lib/admin-api.ts
  - src/Onboarding.API/Controllers/AdminUserController.cs
  - frontend/backoffice/src/components/pages/AdminUserDetailPage.tsx
  - frontend/backoffice/src/tests/admin-api.test.ts
  - tests/Onboarding.API.Tests/Admin/AdminFullFlowTests.cs
autonomous: true
requirements:
  - ADM-01
  - ADM-02
  - ADM-03
  - ADM-04
gap_closure: true
source: 30-REVIEW.md (CR-01, CR-02, CR-03)

must_haves:
  truths:
    - "GenerateTemporaryPassword usa RandomNumberGenerator.GetInt32() — sem alocação, sem modulo bias"
    - "deleteUser() frontend aceita confirmEmail e envia body JSON { confirmEmail }"
    - "CreateAdmin retorna 200 OK (não 201 CreatedAtAction) pois não existe GET by ID individual"
  artifacts:
    - path: "src/Onboarding.Application/Admin/Commands/CreateAdminCommand.cs"
      provides: "GenerateTemporaryPassword refatorado com RandomNumberGenerator.GetInt32"
      contains: "RandomNumberGenerator.GetInt32"
    - path: "frontend/backoffice/src/lib/admin-api.ts"
      provides: "deleteUser(userId, confirmEmail) com body JSON"
      contains: "body: JSON.stringify({ confirmEmail })"
    - path: "src/Onboarding.API/Controllers/AdminUserController.cs"
      provides: "CreateAdmin retorna Ok(result) em vez de CreatedAtAction"
      contains: "return Ok(result)"
  key_links:
    - from: "deleteUser() frontend"
      to: "DELETE /api/admin/users/{id}"
      via: "fetch com body JSON { confirmEmail }"
      status: "must be WIRED"
    - from: "CreateAdmin action"
      to: "200 OK response"
      via: "return Ok(result)"
      status: "must be WIRED"
---

<objective>
Fechar os 3 issues críticos do code review (30-REVIEW.md): CR-01 (RandomNumberGenerator resource leak), CR-02 (deleteUser sem body), CR-03 (CreatedAtAction com Location header inválido).

Purpose: Eliminar bugs de produção — resource leak de handles criptográficos, endpoint DELETE inutilizável, e Location header RFC-violating.
Output: 3 fixes em 3 arquivos backend + 2 arquivos frontend + testes atualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-audit-log-admin-backend/30-CONTEXT.md
@.planning/phases/30-audit-log-admin-backend/30-REVIEW.md
@.planning/phases/30-audit-log-admin-backend/30-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 — Fix RandomNumberGenerator resource leak + modulo bias</name>
  <files>
    src/Onboarding.Application/Admin/Commands/CreateAdminCommand.cs
  </files>
  <read_first>
    - src/Onboarding.Application/Admin/Commands/CreateAdminCommand.cs (linhas 85-125 — GenerateTemporaryPassword e GetRandomChar)
  </read_first>
  <action>
Substituir os métodos `GenerateTemporaryPassword` e `GetRandomChar` em `CreateAdminCommand.cs`. O `using System.Security.Cryptography;` já existe no topo.

**DEPOIS (substituir ambos os métodos):**
```csharp
/// <summary>
/// Generates a cryptographically random temporary password meeting Keycloak policy:
/// - 14 characters, 2 uppercase, 2 lowercase, 2 digits, 2 special chars
/// - Uses RandomNumberGenerator.GetInt32 (no allocation, no modulo bias)
/// </summary>
private static string GenerateTemporaryPassword()
{
    const string upperChars = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    const string lowerChars = "abcdefghijklmnopqrstuvwxyz";
    const string digits = "0123456789";
    const string specialChars = "!@#$%^&*";
    const string allChars = upperChars + lowerChars + digits + specialChars;

    var chars = new char[14];
    chars[0] = GetRandomChar(upperChars);
    chars[1] = GetRandomChar(upperChars);
    chars[2] = GetRandomChar(lowerChars);
    chars[3] = GetRandomChar(lowerChars);
    chars[4] = GetRandomChar(digits);
    chars[5] = GetRandomChar(digits);
    chars[6] = GetRandomChar(specialChars);
    chars[7] = GetRandomChar(specialChars);
    for (int i = 8; i < chars.Length; i++)
        chars[i] = GetRandomChar(allChars);

    // Bias-free Fisher-Yates shuffle
    for (int i = chars.Length - 1; i > 0; i--)
    {
        int j = RandomNumberGenerator.GetInt32(i + 1);
        (chars[i], chars[j]) = (chars[j], chars[i]);
    }

    return new string(chars);
}

private static char GetRandomChar(string charSet)
    => charSet[RandomNumberGenerator.GetInt32(charSet.Length)];
```

Notas: `RandomNumberGenerator.GetInt32(int upperBound)` é estático — sem alocação, sem IDisposable, sem modulo bias.
  </action>
  <verify>
    <automated>cd /d/REPO/keycloak-tests && dotnet build src/Onboarding.Application --no-incremental 2>&1 | grep -E "error" | head -10</automated>
  </verify>
  <done>
    - Zero chamadas de RandomNumberGenerator.Create() no arquivo
    - Pelo menos 2 matches de RandomNumberGenerator.GetInt32
    - Zero uso de modulo (%) com bytes aleatórios
    - dotnet build src/Onboarding.Application exits 0
  </done>
  <acceptance_criteria>
    - grep "RandomNumberGenerator.Create" src/Onboarding.Application/Admin/Commands/CreateAdminCommand.cs retorna zero resultados
    - grep "RandomNumberGenerator.GetInt32" src/Onboarding.Application/Admin/Commands/CreateAdminCommand.cs retorna pelo menos 2 matches
    - grep "randomBytes\[0\] %" src/Onboarding.Application/Admin/Commands/CreateAdminCommand.cs retorna zero resultados
    - dotnet build src/Onboarding.Application --no-incremental exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: CR-02 — Fix deleteUser() frontend para enviar body com confirmEmail</name>
  <files>
    frontend/backoffice/src/lib/admin-api.ts,
    frontend/backoffice/src/tests/admin-api.test.ts,
    frontend/backoffice/src/components/pages/AdminUserDetailPage.tsx
  </files>
  <read_first>
    - frontend/backoffice/src/lib/admin-api.ts (linhas 258-285 — deleteUser atual)
    - frontend/backoffice/src/components/pages/AdminUserDetailPage.tsx (localizar onde deleteUser é chamado)
    - frontend/backoffice/src/tests/admin-api.test.ts (verificar se existe teste de deleteUser)
  </read_first>
  <action>
**Passo 1: Corrigir deleteUser() em admin-api.ts**

Substituir a função deleteUser inteira:

```typescript
export async function deleteUser(
  userId: string,
  confirmEmail: string
): Promise<void> {
  const response = await fetch(`/api/admin/users/${userId}`, {
    method: "DELETE",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ confirmEmail }),
    credentials: "include",
  });

  if (response.status === 404) {
    throw new AdminApiError("Usuario nao encontrado.", 404);
  }

  if (response.status === 409) {
    throw new AdminApiError("Usuario ja foi deletado.", 409);
  }

  if (response.status === 400) {
    const body = await response.json().catch(() => ({}));
    throw new AdminApiError(body.detail || "Email de confirmacao invalido.", 400);
  }

  if (!response.ok) {
    const body = await response.json().catch(() => ({}));
    throw new AdminApiError(body.detail || "Falha ao deletar usuario.");
  }
}
```

**Passo 2: Atualizar chamadas de deleteUser() no frontend**

Localizar onde `deleteUser` é chamado em `AdminUserDetailPage.tsx` (ou componente de dialog de deleção) e atualizar para passar confirmEmail como segundo argumento. Se o dialog já tem input de confirmação de email, usar esse valor.

**Passo 3: Adicionar teste de deleteUser em admin-api.test.ts**

```typescript
describe("deleteUser", () => {
  it("sends DELETE with confirmEmail body and credentials: include", async () => {
    mockFetch.mockResolvedValueOnce({ ok: true, status: 204 });
    await deleteUser("user-123", "target@example.com");
    expect(mockFetch).toHaveBeenCalledWith(
      "/api/admin/users/user-123",
      expect.objectContaining({
        method: "DELETE",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ confirmEmail: "target@example.com" }),
        credentials: "include",
      })
    );
  });

  it("throws AdminApiError with 400 when confirmEmail is invalid", async () => {
    mockFetch.mockResolvedValueOnce({
      ok: false, status: 400,
      json: () => Promise.resolve({ detail: "Email de confirmacao invalido." }),
    });
    await expect(deleteUser("user-123", "wrong@email.com")).rejects.toThrow(AdminApiError);
  });
});
```
  </action>
  <verify>
    <automated>cd /d/REPO/keycloak-tests/frontend/backoffice && npx vitest run src/tests/admin-api.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
    - deleteUser() aceita confirmEmail como segundo parâmetro obrigatório
    - deleteUser() envia body JSON { confirmEmail } com Content-Type: application/json
    - Todas as chamadas de deleteUser() no frontend foram atualizadas para passar confirmEmail
    - Testes de deleteUser passam
    - npx tsc --noEmit exits 0
  </done>
  <acceptance_criteria>
    - grep "confirmEmail: string" frontend/backoffice/src/lib/admin-api.ts retorna match na assinatura de deleteUser
    - grep "JSON.stringify.*confirmEmail" frontend/backoffice/src/lib/admin-api.ts retorna match
    - grep "deleteUser(" frontend/backoffice/src/components/pages/AdminUserDetailPage.tsx retorna chamada com 2 argumentos
    - npx vitest run src/tests/admin-api.test.ts exits 0
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: CR-03 — Fix CreatedAtAction no CreateAdmin (Location header inválido)</name>
  <files>
    src/Onboarding.API/Controllers/AdminUserController.cs,
    tests/Onboarding.API.Tests/Admin/AdminFullFlowTests.cs
  </files>
  <read_first>
    - src/Onboarding.API/Controllers/AdminUserController.cs (linhas 320-345 — CreateAdmin action com CreatedAtAction)
    - tests/Onboarding.API.Tests/Admin/AdminFullFlowTests.cs (verificar se algum teste valida status code do CreateAdmin)
  </read_first>
  <action>
No action `CreateAdmin` do `AdminUserController.cs`, substituir o retorno:

**ANTES (linha ~334):**
```csharp
var result = await _createAdminHandler.HandleAsync(command, ct);
return CreatedAtAction(nameof(CreateAdmin), new { id = result.AdminId }, result);
```

**DEPOIS:**
```csharp
var result = await _createAdminHandler.HandleAsync(command, ct);
return Ok(result);
```

Justificativa: Não existe endpoint GET /api/admin/administrators/{id} para referenciar no Location header. Como não há URI individual do recurso, 200 OK é semanticamente correto (RFC 9110 Section 15.3.2).

Se existir teste em AdminFullFlowTests que valida status 201 do CreateAdmin, atualizar para validar status 200.
  </action>
  <verify>
    <automated>cd /d/REPO/keycloak-tests && dotnet build src/Onboarding.API --no-incremental 2>&1 | grep -E "error" | head -10</automated>
  </verify>
  <done>
    - CreateAdmin retorna Ok(result) — não CreatedAtAction
    - grep "CreatedAtAction.*CreateAdmin" retorna zero resultados no controller
    - dotnet build src/Onboarding.API exits 0
    - Testes de AdminFullFlow passam (atualizar status code assertion se necessário)
  </done>
  <acceptance_criteria>
    - grep "CreatedAtAction" src/Onboarding.API/Controllers/AdminUserController.cs retorna zero resultados
    - grep "return Ok(result)" src/Onboarding.API/Controllers/AdminUserController.cs retorna match no CreateAdmin action
    - dotnet build src/Onboarding.API --no-incremental exits 0
    - dotnet test tests/Onboarding.API.Tests exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GenerateTemporaryPassword | Usa RNG estático do .NET — sem resource leak, sem bias. Senha é criptograficamente segura. |
| deleteUser() confirmEmail | Backend valida confirmEmail via DeleteUserCommandValidator — se não bater, retorna 400. Sem injection risk. |
| CreateAdmin 200 vs 201 | Mudança cosmética de status code — sem impacto de segurança. Authorization ainda via [Authorize(Roles = "admin")]. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-13 | Denial of Service | GenerateTemporaryPassword (RNG) | mitigated | RandomNumberGenerator.GetInt32() usa shared pool — sem alocação por chamada. Sem risco de exhaustion. |
| T-30-14 | Tampering | deleteUser() confirmEmail | mitigated | Backend valida confirmEmail contra email real do usuário. Se não bater, 400. Sem bypass possível. |
</threat_model>

<verification>
Verificação global do Plano 30-05:

1. `grep "RandomNumberGenerator.Create" src/Onboarding.Application/Admin/Commands/CreateAdminCommand.cs` — zero resultados
2. `grep "RandomNumberGenerator.GetInt32" src/Onboarding.Application/Admin/Commands/CreateAdminCommand.cs` — 2+ matches
3. `grep "confirmEmail: string" frontend/backoffice/src/lib/admin-api.ts` — match em deleteUser
4. `grep "JSON.stringify.*confirmEmail" frontend/backoffice/src/lib/admin-api.ts` — match
5. `grep "CreatedAtAction" src/Onboarding.API/Controllers/AdminUserController.cs` — zero resultados
6. `dotnet build src/Onboarding.API` — exits 0
7. `dotnet test tests/Onboarding.API.Tests` — exits 0
8. `npx vitest run src/tests/admin-api.test.ts` — exits 0
9. `npx tsc --noEmit` — exits 0
</verification>

<success_criteria>
- CR-01 fechado: RandomNumberGenerator.GetInt32 usado em todo GenerateTemporaryPassword — sem Create(), sem modulo bias
- CR-02 fechado: deleteUser() envia body { confirmEmail } — backend não rejeita mais com 400
- CR-03 fechado: CreateAdmin retorna 200 OK — Location header RFC-compliant (ou ausente)
- Todos os testes passam: dotnet test, vitest, tsc --noEmit
- 30-VERIFICATION.md re-rodado após este plano deve manter score 9/9
</success_criteria>

<output>
Após execução, criar 30-05-SUMMARY.md com:
- Status: COMPLETE
- Changes made por task (arquivos, linhas)
- Deviations do plano (se houver)
- Commits criados
- Verification results
</output>

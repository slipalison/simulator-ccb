---
phase: 30-audit-log-admin-backend
plan: 03
type: execute
wave: 3
depends_on:
  - 30-02
files_modified:
  - frontend/backoffice/src/lib/admin-api.ts
  - frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx
  - frontend/backoffice/src/router.tsx
  - frontend/backoffice/src/components/templates/AdminLayout.tsx
  - frontend/backoffice/src/tests/admin-api.test.ts
autonomous: true
requirements:
  - ADM-04
gap_closure: true

must_haves:
  truths:
    - "admin-api.ts exporta interface AdminUserDto e função getAdministrators() que chama GET /api/admin/administrators"
    - "AdminAdministratorsPage.tsx exibe tabela com colunas: Nome, Email, Status (badge), Senha Temporária (badge)"
    - "Rota /admin/administrators existe no router.tsx e renderiza AdminAdministratorsPage dentro de AdminLayout"
    - "Sidebar de AdminLayout tem link 'Administradores' apontando para /admin/administrators"
    - "admin-api.test.ts cobre getAdministrators(): sucesso retorna AdminUserDto[], falha lança AdminApiError"
  artifacts:
    - path: "frontend/backoffice/src/lib/admin-api.ts"
      provides: "AdminUserDto interface + getAdministrators function"
      contains: "export interface AdminUserDto"
    - path: "frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx"
      provides: "Página de listagem de administradores"
      exports: ["AdminAdministratorsPage"]
    - path: "frontend/backoffice/src/router.tsx"
      provides: "Rota /admin/administrators registrada"
      contains: "/admin/administrators"
    - path: "frontend/backoffice/src/components/templates/AdminLayout.tsx"
      provides: "Link de navegação para Administradores no sidebar"
      contains: "/admin/administrators"
  key_links:
    - from: "AdminAdministratorsPage"
      to: "getAdministrators()"
      via: "useEffect → setAdmins(await getAdministrators())"
      pattern: "getAdministrators"
    - from: "router.tsx adminAdministratorsRoute"
      to: "AdminAdministratorsPage"
      via: "component: () => (<AdminLayout><AdminAdministratorsPage /></AdminLayout>)"
      pattern: "AdminAdministratorsPage"
    - from: "AdminLayout AdminSidebar"
      to: "/admin/administrators"
      via: "<a href=\"/admin/administrators\">"
      pattern: "/admin/administrators"
---

<objective>
Fechar o gap ADM-04 (frontend): adicionar o cliente TypeScript getAdministrators() em admin-api.ts, criar a página AdminAdministratorsPage.tsx com tabela de admins, registrar a rota /admin/administrators no router.tsx, e adicionar o link de navegação no sidebar.

Purpose: O backend GET /api/admin/administrators está completo e verificado. Este plano entrega a camada frontend que o consome, completando ADM-04 end-to-end.
Output: Interface AdminUserDto, função getAdministrators(), página com tabela de admins (Nome, Email, Status, Senha Temporária), rota registrada, link no sidebar, testes para getAdministrators().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-audit-log-admin-backend/30-CONTEXT.md
@.planning/phases/30-audit-log-admin-backend/30-02-SUMMARY.md

<interfaces>
<!-- admin-api.ts — padrão de função existente (referência: createAdmin + getAuditLog) -->
From frontend/backoffice/src/lib/admin-api.ts (linhas 315-341 e 382-406):
```typescript
// Padrão de interface + função já estabelecido no arquivo:
export class AdminApiError extends Error {
  public status?: number;
  constructor(message: string, status?: number) { ... }
}

// Padrão de função GET (referência: getAuditLog):
export async function getAuditLog(params: GetAuditLogParams = {}): Promise<PaginatedResult<AuditLogEntry>> {
  const response = await fetch(url, { method: "GET", credentials: "include" });
  if (!response.ok) { throw new AdminApiError("Falha ao carregar audit log."); }
  return response.json() as Promise<PaginatedResult<AuditLogEntry>>;
}
```

<!-- AdminUsersPage.tsx — padrão de página com useEffect + loading/error state -->
From frontend/backoffice/src/components/pages/AdminUsersPage.tsx:
```tsx
import { useState, useEffect, useCallback } from "react";
import { Card, CardContent, CardHeader } from "@/components/ui/card";
import { toast } from "sonner";
import type { UserSummaryDto, PaginatedResult } from "@/lib/admin-api";

export function AdminUsersPage() {
  const [result, setResult] = useState<PaginatedResult<UserSummaryDto> | null>(null);
  const [isLoading, setIsLoading] = useState(true);
  const [isError, setIsError] = useState(false);

  const fetchUsers = useCallback(async () => {
    setIsLoading(true);
    setIsError(false);
    try {
      const data = await listUsers({ page, pageSize: 20, ... });
      setResult(data);
    } catch (_err) {
      setIsError(true);
      toast.error("Falha ao carregar usuarios", { description: "Tente novamente." });
    } finally {
      setIsLoading(false);
    }
  }, [page, ...]);

  useEffect(() => { fetchUsers(); }, [fetchUsers]);
}
```

<!-- router.tsx — padrão de rota com AdminLayout -->
From frontend/backoffice/src/router.tsx:
```tsx
// Padrão para rotas com AdminLayout (ex: adminAuditLogRoute):
const adminAuditLogRoute = createRoute({
  getParentRoute: () => rootRoute,
  path: "/admin/audit-log",
  component: () => (
    <AdminLayout>
      <AuditLogPage />
    </AdminLayout>
  ),
});

// routeTree final (adicionar nova rota aqui):
const routeTree = rootRoute.addChildren([
  indexRoute,
  adminLoginRoute,
  adminAccessDeniedRoute,
  adminUsersRoute,
  adminUserEditRoute,
  adminUserDetailRoute,
  adminCreateRoute,
  adminPasswordChangeRoute,
  adminAuditLogRoute,
  // adminAdministratorsRoute  <-- adicionar aqui
]);
```

<!-- AdminLayout.tsx — AdminSidebar atual (linha 46-59) -->
From frontend/backoffice/src/components/templates/AdminLayout.tsx:
```tsx
function AdminSidebar() {
  return (
    <aside className="w-56 border-r bg-card p-4" data-testid="admin-sidebar">
      <nav className="space-y-1">
        <a
          href="/admin/users"
          className="block py-2 px-3 text-sm rounded-md hover:bg-accent transition-colors"
          data-testid="sidebar-users-link"
        >
          Usuarios
        </a>
        {/* Future: Audit Log, Settings */}
      </nav>
    </aside>
  );
}
```

<!-- admin-api.test.ts — padrão de test existente (mockFetch + describe/it) -->
From frontend/backoffice/src/tests/admin-api.test.ts:
```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";
import { loginAdmin, logoutAdmin, getAdminMe, AdminLoginError, AdminApiError } from "@/lib/admin-api";

const mockFetch = vi.fn();
global.fetch = mockFetch;

describe("admin-api.ts", () => {
  beforeEach(() => { vi.clearAllMocks(); mockFetch.mockReset(); });

  describe("loginAdmin", () => {
    it("calls POST /api/admin/auth/login with credentials: include", async () => {
      mockFetch.mockResolvedValueOnce({ ok: true, json: () => Promise.resolve({...}) });
      const result = await loginAdmin("admin@onboarding.local", "SecureP@ss123");
      expect(mockFetch).toHaveBeenCalledWith("/api/admin/auth/login", expect.objectContaining({ ... }));
    });
  });
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar AdminUserDto interface e getAdministrators() em admin-api.ts</name>
  <files>
    frontend/backoffice/src/lib/admin-api.ts,
    frontend/backoffice/src/tests/admin-api.test.ts
  </files>
  <read_first>
    - frontend/backoffice/src/lib/admin-api.ts (ler completo — localizar fim do arquivo após getAuditLog, linha ~407, para saber onde inserir o bloco novo)
    - frontend/backoffice/src/tests/admin-api.test.ts (ler completo — identificar a lista de imports na linha 1-8 e onde termina o último describe, para adicionar novo describe sem quebrar estrutura)
  </read_first>
  <action>
**Passo 1: Adicionar em admin-api.ts**

Após o bloco `getAuditLog` (ao final do arquivo, depois da linha que fecha a função getAuditLog), inserir o seguinte bloco novo:

```typescript
// ---------------------------------------------------------------------------
// Admin Administrators — Phase 30 (Milestone v5.0) — ADM-04
// ---------------------------------------------------------------------------

// GET /api/admin/administrators — Lista todos os administradores
export interface AdminUserDto {
  id: string;
  email: string;
  fullName: string;
  isEnabled: boolean;
  hasTemporaryPassword: boolean;
}

export async function getAdministrators(): Promise<AdminUserDto[]> {
  const response = await fetch("/api/admin/administrators", {
    method: "GET",
    credentials: "include",
  });
  if (!response.ok) {
    throw new AdminApiError("Falha ao carregar administradores.");
  }
  return response.json() as Promise<AdminUserDto[]>;
}
```

**Passo 2: Atualizar admin-api.test.ts**

Na linha de import do topo do arquivo, adicionar `getAdministrators` e `AdminUserDto` à lista de imports existente:

```typescript
// ANTES (linha ~1-8):
import {
  loginAdmin,
  logoutAdmin,
  getAdminMe,
  AdminLoginError,
  AdminApiError,
} from "@/lib/admin-api";

// DEPOIS:
import {
  loginAdmin,
  logoutAdmin,
  getAdminMe,
  getAdministrators,
  AdminLoginError,
  AdminApiError,
  type AdminUserDto,
} from "@/lib/admin-api";
```

Ao final do arquivo, antes do fechamento do `describe("admin-api.ts", ...)`, inserir o novo describe:

```typescript
  // ---------------------------------------------------------------------------
  // getAdministrators
  // ---------------------------------------------------------------------------

  describe("getAdministrators", () => {
    it("calls GET /api/admin/administrators with credentials: include and returns AdminUserDto[]", async () => {
      const mockAdmins: AdminUserDto[] = [
        {
          id: "abc-123",
          email: "admin@onboarding.local",
          fullName: "Admin Principal",
          isEnabled: true,
          hasTemporaryPassword: false,
        },
        {
          id: "def-456",
          email: "novo@onboarding.local",
          fullName: "Novo Admin",
          isEnabled: true,
          hasTemporaryPassword: true,
        },
      ];

      mockFetch.mockResolvedValueOnce({
        ok: true,
        json: () => Promise.resolve(mockAdmins),
      });

      const result = await getAdministrators();

      expect(mockFetch).toHaveBeenCalledTimes(1);
      expect(mockFetch).toHaveBeenCalledWith(
        "/api/admin/administrators",
        expect.objectContaining({
          method: "GET",
          credentials: "include",
        })
      );
      expect(result).toHaveLength(2);
      expect(result[0].email).toBe("admin@onboarding.local");
      expect(result[1].hasTemporaryPassword).toBe(true);
    });

    it("throws AdminApiError when response is not ok", async () => {
      mockFetch.mockResolvedValueOnce({ ok: false, status: 503 });

      await expect(getAdministrators()).rejects.toThrow(AdminApiError);
      await expect(getAdministrators()).rejects.toThrow(
        "Falha ao carregar administradores."
      );
    });

    it("returns empty array when backend returns []", async () => {
      mockFetch.mockResolvedValueOnce({
        ok: true,
        json: () => Promise.resolve([]),
      });

      const result = await getAdministrators();

      expect(result).toHaveLength(0);
    });
  });
```
  </action>
  <verify>
    <automated>cd /d/REPO/keycloak-tests/frontend/backoffice && npx vitest run src/tests/admin-api.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>
    - admin-api.ts exporta interface AdminUserDto com campos: id, email, fullName, isEnabled, hasTemporaryPassword
    - admin-api.ts exporta função getAdministrators() que faz GET /api/admin/administrators com credentials: "include"
    - admin-api.test.ts importa getAdministrators e AdminUserDto
    - Os 3 novos testes de getAdministrators passam (sucesso, erro, lista vazia)
    - Os testes anteriores de loginAdmin, logoutAdmin, getAdminMe continuam passando
  </done>
  <acceptance_criteria>
    - grep "export interface AdminUserDto" frontend/backoffice/src/lib/admin-api.ts retorna resultado
    - grep "export async function getAdministrators" frontend/backoffice/src/lib/admin-api.ts retorna resultado
    - grep "credentials.*include" frontend/backoffice/src/lib/admin-api.ts retorna resultado para getAdministrators (não só para outras funções)
    - grep "getAdministrators" frontend/backoffice/src/tests/admin-api.test.ts retorna resultado
    - grep "hasTemporaryPassword" frontend/backoffice/src/lib/admin-api.ts retorna resultado
    - npx vitest run src/tests/admin-api.test.ts (no diretório frontend/backoffice) exits 0 com todos os testes passando
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Criar AdminAdministratorsPage.tsx, registrar rota e link no sidebar</name>
  <files>
    frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx,
    frontend/backoffice/src/router.tsx,
    frontend/backoffice/src/components/templates/AdminLayout.tsx
  </files>
  <read_first>
    - frontend/backoffice/src/components/pages/AuditLogPage.tsx (padrão de tabela com Card, thead/tbody, estados loading/error/empty)
    - frontend/backoffice/src/router.tsx (ler completo — ver importações existentes e routeTree.addChildren([...]) para saber onde inserir a nova rota)
    - frontend/backoffice/src/components/templates/AdminLayout.tsx (ler completo — ver AdminSidebar e o comentário "Future: Audit Log, Settings" na linha ~56)
    - frontend/backoffice/src/lib/admin-api.ts (confirmar que AdminUserDto e getAdministrators foram adicionados na Task 1)
  </read_first>
  <action>
**Passo 1: Criar frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx**

Criar o arquivo com o seguinte conteúdo:

```tsx
import { useState, useEffect, useCallback } from "react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Shield, RefreshCw } from "lucide-react";
import { toast } from "sonner";
import { getAdministrators } from "@/lib/admin-api";
import type { AdminUserDto } from "@/lib/admin-api";

export function AdminAdministratorsPage() {
  const [admins, setAdmins] = useState<AdminUserDto[]>([]);
  const [isLoading, setIsLoading] = useState(true);
  const [isError, setIsError] = useState(false);

  const fetchAdmins = useCallback(async () => {
    setIsLoading(true);
    setIsError(false);
    try {
      const data = await getAdministrators();
      setAdmins(data);
    } catch (_err) {
      setIsError(true);
      toast.error("Falha ao carregar administradores", {
        description: "Tente novamente.",
      });
    } finally {
      setIsLoading(false);
    }
  }, []);

  useEffect(() => {
    fetchAdmins();
  }, [fetchAdmins]);

  return (
    <div className="space-y-6" data-testid="admin-administrators-page">
      <div className="flex items-center justify-between">
        <div className="flex items-center gap-2">
          <Shield className="h-5 w-5 text-primary" />
          <h2 className="text-xl font-semibold">Administradores</h2>
        </div>
        <Button
          variant="outline"
          size="sm"
          onClick={fetchAdmins}
          disabled={isLoading}
          data-testid="refresh-button"
        >
          <RefreshCw className={`h-4 w-4 mr-1 ${isLoading ? "animate-spin" : ""}`} />
          Atualizar
        </Button>
      </div>

      <Card>
        <CardHeader>
          <CardTitle className="text-base">Lista de Administradores</CardTitle>
        </CardHeader>
        <CardContent className="p-0">
          {isLoading && (
            <div className="p-6 text-center text-sm text-muted-foreground" data-testid="loading-state">
              Carregando administradores...
            </div>
          )}

          {isError && !isLoading && (
            <div className="p-6 text-center text-sm text-destructive" data-testid="error-state">
              Falha ao carregar administradores.{" "}
              <button
                className="underline hover:no-underline"
                onClick={fetchAdmins}
              >
                Tentar novamente
              </button>
            </div>
          )}

          {!isLoading && !isError && admins.length === 0 && (
            <div className="p-6 text-center text-sm text-muted-foreground" data-testid="empty-state">
              Nenhum administrador encontrado.
            </div>
          )}

          {!isLoading && !isError && admins.length > 0 && (
            <table className="w-full text-sm" data-testid="administrators-table">
              <thead>
                <tr className="border-b bg-muted/50">
                  <th className="text-left py-3 px-4 font-medium text-muted-foreground">
                    Nome
                  </th>
                  <th className="text-left py-3 px-4 font-medium text-muted-foreground">
                    Email
                  </th>
                  <th className="text-left py-3 px-4 font-medium text-muted-foreground">
                    Status
                  </th>
                  <th className="text-left py-3 px-4 font-medium text-muted-foreground">
                    Senha Temporaria
                  </th>
                </tr>
              </thead>
              <tbody>
                {admins.map((admin) => (
                  <tr
                    key={admin.id}
                    className="border-b last:border-0 hover:bg-muted/30 transition-colors"
                    data-testid={`admin-row-${admin.id}`}
                  >
                    <td className="py-3 px-4 font-medium">{admin.fullName}</td>
                    <td className="py-3 px-4 text-muted-foreground">{admin.email}</td>
                    <td className="py-3 px-4">
                      {admin.isEnabled ? (
                        <Badge variant="default" data-testid="badge-active">
                          Ativo
                        </Badge>
                      ) : (
                        <Badge variant="destructive" data-testid="badge-blocked">
                          Bloqueado
                        </Badge>
                      )}
                    </td>
                    <td className="py-3 px-4">
                      {admin.hasTemporaryPassword ? (
                        <Badge variant="outline" className="text-amber-600 border-amber-300" data-testid="badge-temp-password">
                          Pendente
                        </Badge>
                      ) : (
                        <Badge variant="outline" className="text-green-600 border-green-300" data-testid="badge-password-set">
                          Definida
                        </Badge>
                      )}
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          )}
        </CardContent>
      </Card>
    </div>
  );
}
```

**Passo 2: Atualizar router.tsx**

No topo do arquivo, adicionar o import da nova página após o import de AuditLogPage:

```tsx
// ADICIONAR após: import { AuditLogPage } from "@/components/pages/AuditLogPage";
import { AdminAdministratorsPage } from "@/components/pages/AdminAdministratorsPage";
```

Após a constante `adminAuditLogRoute`, adicionar a nova rota:

```tsx
// Rota admin administrators: /admin/administrators
const adminAdministratorsRoute = createRoute({
  getParentRoute: () => rootRoute,
  path: "/admin/administrators",
  component: () => (
    <AdminLayout>
      <AdminAdministratorsPage />
    </AdminLayout>
  ),
});
```

Em `routeTree = rootRoute.addChildren([...])`, adicionar `adminAdministratorsRoute` ao final da lista antes do fechamento `])`:

```tsx
const routeTree = rootRoute.addChildren([
  indexRoute,
  adminLoginRoute,
  adminAccessDeniedRoute,
  adminUsersRoute,
  adminUserEditRoute,
  adminUserDetailRoute,
  adminCreateRoute,
  adminPasswordChangeRoute,
  adminAuditLogRoute,
  adminAdministratorsRoute,  // <-- adicionar aqui
]);
```

**Passo 3: Atualizar AdminLayout.tsx — AdminSidebar**

Localizar o comentário `{/* Future: Audit Log, Settings */}` dentro da função AdminSidebar (linha ~56).

Substituir o comentário por dois links novos:

```tsx
// ANTES:
        <a
          href="/admin/users"
          className="block py-2 px-3 text-sm rounded-md hover:bg-accent transition-colors"
          data-testid="sidebar-users-link"
        >
          Usuarios
        </a>
        {/* Future: Audit Log, Settings */}

// DEPOIS:
        <a
          href="/admin/users"
          className="block py-2 px-3 text-sm rounded-md hover:bg-accent transition-colors"
          data-testid="sidebar-users-link"
        >
          Usuarios
        </a>
        <a
          href="/admin/administrators"
          className="block py-2 px-3 text-sm rounded-md hover:bg-accent transition-colors"
          data-testid="sidebar-administrators-link"
        >
          Administradores
        </a>
        <a
          href="/admin/audit-log"
          className="block py-2 px-3 text-sm rounded-md hover:bg-accent transition-colors"
          data-testid="sidebar-audit-log-link"
        >
          Audit Log
        </a>
```

Nota: O link do Audit Log é adicionado junto pois o sidebar tinha o comentário "Future: Audit Log" — aproveitar o mesmo passo para deixar a navegação completa. Isso não cria nova funcionalidade (a rota /admin/audit-log já existe).
  </action>
  <verify>
    <automated>cd /d/REPO/keycloak-tests/frontend/backoffice && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx existe e exporta AdminAdministratorsPage
    - Página tem tabela com 4 colunas: Nome, Email, Status (Badge Ativo/Bloqueado), Senha Temporaria (Badge Pendente/Definida)
    - router.tsx importa AdminAdministratorsPage e declara adminAdministratorsRoute em path "/admin/administrators"
    - adminAdministratorsRoute está presente em routeTree.addChildren([...])
    - AdminLayout.tsx AdminSidebar tem link com href="/admin/administrators" e data-testid="sidebar-administrators-link"
    - npx tsc --noEmit exits 0 (sem erros de tipo)
  </done>
  <acceptance_criteria>
    - grep "export function AdminAdministratorsPage" frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx retorna resultado
    - grep "getAdministrators" frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx retorna resultado
    - grep "hasTemporaryPassword" frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx retorna resultado
    - grep "isEnabled" frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx retorna resultado
    - grep "administrators-table" frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx retorna resultado
    - grep "AdminAdministratorsPage" frontend/backoffice/src/router.tsx retorna resultado
    - grep '"/admin/administrators"' frontend/backoffice/src/router.tsx retorna resultado
    - grep "adminAdministratorsRoute" frontend/backoffice/src/router.tsx retorna resultado em routeTree.addChildren
    - grep 'href="/admin/administrators"' frontend/backoffice/src/components/templates/AdminLayout.tsx retorna resultado
    - grep 'sidebar-administrators-link' frontend/backoffice/src/components/templates/AdminLayout.tsx retorna resultado
    - npx tsc --noEmit (no diretório frontend/backoffice) exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AdminAdministratorsPage → GET /api/admin/administrators | Requisição inclui cookie de sessão httpOnly — autenticação gerenciada pelo browser/backend |
| admin-api.ts getAdministrators → resposta JSON do backend | Dados recebidos são apenas lidos e renderizados — sem eval, sem innerHTML dinâmico |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-10 | Information Disclosure | AdminAdministratorsPage (lista emails de admins) | accept | Acesso requer sessão de admin autenticada (cookie httpOnly). Backend valida [Authorize(Roles = "admin")]. A lista só é acessível por outros admins — risco residual aceitável. |
| T-30-11 | Tampering | admin-api.ts (dados de resposta) | accept | Dados são renderizados como texto em células de tabela (JSX). Nenhum dangerouslySetInnerHTML. React faz escape automático de strings. |
| T-30-12 | Spoofing | getAdministrators() sem validação de schema | accept | Backend retorna dados do Keycloak (fonte confiável). Não há entrada do usuário na requisição GET. Type assertion `as Promise<AdminUserDto[]>` é consistente com padrão existente no arquivo (getAuditLog usa mesmo padrão). |
</threat_model>

<verification>
Verificação global do Plano 03 (gap closure ADM-04):

1. `grep "export interface AdminUserDto" frontend/backoffice/src/lib/admin-api.ts` retorna resultado
2. `grep "export async function getAdministrators" frontend/backoffice/src/lib/admin-api.ts` retorna resultado
3. `npx vitest run src/tests/admin-api.test.ts` (no diretório frontend/backoffice) exits 0 — todos os testes passam incluindo os 3 novos de getAdministrators
4. `grep "AdminAdministratorsPage" frontend/backoffice/src/router.tsx` retorna resultado
5. `grep '"/admin/administrators"' frontend/backoffice/src/router.tsx` retorna resultado
6. `grep 'href="/admin/administrators"' frontend/backoffice/src/components/templates/AdminLayout.tsx` retorna resultado
7. `npx tsc --noEmit` (no diretório frontend/backoffice) exits 0
</verification>

<success_criteria>
- admin-api.ts tem AdminUserDto interface com campos: id, email, fullName, isEnabled, hasTemporaryPassword
- admin-api.ts tem getAdministrators() fazendo GET /api/admin/administrators com credentials: "include"
- admin-api.test.ts tem 3 novos testes para getAdministrators (sucesso, erro 503, lista vazia) — todos passam
- AdminAdministratorsPage.tsx existe e renderiza tabela com 4 colunas (Nome, Email, Status, Senha Temporaria)
- Status column: Badge "Ativo" quando isEnabled=true, Badge "Bloqueado" quando isEnabled=false
- Senha Temporaria column: Badge "Pendente" quando hasTemporaryPassword=true, Badge "Definida" quando false
- router.tsx registra rota /admin/administrators com adminAdministratorsRoute dentro de routeTree.addChildren
- AdminLayout.tsx sidebar tem link Administradores com href="/admin/administrators"
- npx tsc --noEmit exits 0 (sem erros de TypeScript)
- npx vitest run exits 0 (todos os testes existentes continuam passando)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/30-audit-log-admin-backend/30-03-SUMMARY.md`
</output>

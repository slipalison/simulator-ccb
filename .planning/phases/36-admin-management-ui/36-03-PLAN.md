---
phase: 36-admin-management-ui
plan: 03
type: execute
wave: 2
depends_on:
  - "36-01"
files_modified:
  - frontend/backoffice/src/components/molecules/EditAdminDialog.tsx
  - frontend/backoffice/src/components/molecules/ResetPasswordDialog.tsx
  - frontend/backoffice/src/components/molecules/DeactivateAdminDialog.tsx
  - frontend/backoffice/src/components/molecules/ReactivateAdminDialog.tsx
autonomous: true
requirements:
  - MGMT-03
  - MGMT-04
  - MGMT-05
  - MGMT-06

must_haves:
  truths:
    - "EditAdminDialog pré-popula nome e email do admin selecionado e valida com adminEditAdministratorSchema"
    - "EditAdminDialog exibe erro inline text-sm text-destructive abaixo de cada campo inválido"
    - "ResetPasswordDialog exibe senha em campo monospace readOnly com botão Copiar (2s feedback) e aviso de não-recuperação"
    - "ResetPasswordDialog descarta senha do state ao fechar (D-05) — dialog não pode ser reaberta com a mesma senha"
    - "DeactivateAdminDialog usa variant=destructive no botão de confirmação e Alert destructive"
    - "ReactivateAdminDialog usa variant=default no botão de confirmação (ação positiva)"
    - "Todos os dialogs mostram estado de loading no botão durante submissão (Loader2 animate-spin)"
  artifacts:
    - path: "frontend/backoffice/src/components/molecules/EditAdminDialog.tsx"
      provides: "Dialog de edição com RHF + Zod, pré-população e estados de submissão"
      exports: ["EditAdminDialog"]
    - path: "frontend/backoffice/src/components/molecules/ResetPasswordDialog.tsx"
      provides: "Dialog one-time com senha em monospace, copiar com feedback, aviso proeminente"
      exports: ["ResetPasswordDialog"]
    - path: "frontend/backoffice/src/components/molecules/DeactivateAdminDialog.tsx"
      provides: "Dialog de confirmação de desativação com Alert destructive"
      exports: ["DeactivateAdminDialog"]
    - path: "frontend/backoffice/src/components/molecules/ReactivateAdminDialog.tsx"
      provides: "Dialog de confirmação de reativação simples"
      exports: ["ReactivateAdminDialog"]
  key_links:
    - from: "frontend/backoffice/src/components/molecules/EditAdminDialog.tsx"
      to: "frontend/backoffice/src/lib/validation-schemas.ts"
      via: "zodResolver(adminEditAdministratorSchema)"
      pattern: "adminEditAdministratorSchema"
    - from: "frontend/backoffice/src/components/molecules/ResetPasswordDialog.tsx"
      to: "state interno generatedPassword"
      via: "onOpenChange → setGeneratedPassword(null)"
      pattern: "setGeneratedPassword.*null"
---

<objective>
Criar os quatro dialogs de ações da página de administradores: edição com validação RHF+Zod, reset de senha one-time com clipboard, e confirmações de desativação/reativação.

Purpose: Encapsular cada ação em seu próprio dialog componente — cada um é independente e pode ser testado isoladamente antes de ser montado na página.
Output: Quatro novos arquivos de dialog em frontend/backoffice/src/components/molecules/.
</objective>

<execution_context>
@D:\REPO\keycloak-tests\.claude\get-shit-done\workflows\execute-plan.md
@D:\REPO\keycloak-tests\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@D:\REPO\keycloak-tests\.planning\phases\36-admin-management-ui\36-CONTEXT.md
@D:\REPO\keycloak-tests\.planning\phases\36-admin-management-ui\36-UI-SPEC.md

<interfaces>
<!-- Contratos do Plano 01 e referências de padrão existentes -->

De frontend/backoffice/src/lib/validation-schemas.ts (após Plano 01):
```typescript
export const adminEditAdministratorSchema = z.object({
  fullName: z.string().trim().min(2, "Nome muito curto (mínimo 2 caracteres)").max(100, "..."),
  email: z.string().trim().min(1, "Email é obrigatório").email("Email inválido"),
});
export type AdminEditAdministratorInput = z.infer<typeof adminEditAdministratorSchema>;
```

De frontend/backoffice/src/lib/admin-api.ts (após Plano 01):
```typescript
export interface AdminUserDto {
  id: string;
  email: string;
  fullName: string;
  isEnabled: boolean;
  hasTemporaryPassword: boolean;
}
```

Padrão de BlockDialog.tsx (referência para estrutura de dialog):
```typescript
// Props: { open: boolean; onClose: () => void; onAction: () => Promise<void>; ... }
// Usa Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter
// Usa useForm + zodResolver para validação
// Estado isSubmitting → botão disabled + <Loader2 animate-spin />
// toast.success após sucesso; toast.error em catch
// reset() do form no onClose/handleOpenChange
```

Padrão de import React Hook Form no projeto:
```typescript
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
```

UI-SPEC — Copywriting obrigatório:
```
EditAdminDialog:
  title: "Editar Administrador"
  description: "Atualize o nome e o email do administrador."
  cancel: "Cancelar edição"
  submit: "Salvar alterações"
  submit-loading: <Loader2 /> + "Salvando..."

ResetPasswordDialog:
  title: "Senha Temporária Gerada"
  description: "Compartilhe esta senha com o administrador agora. Ela não poderá ser recuperada depois."
  copy-button: "Copiar senha" / "Copiado!" (2s)
  alert: "Esta senha não pode ser recuperada. Feche somente após copiar."
  close: "Fechar"

DeactivateAdminDialog:
  title: "Desativar Administrador"
  description: "O administrador {fullName} perderá acesso ao backoffice. A conta é preservada para auditoria."
  alert: "Esta ação pode ser revertida reativando o administrador."
  cancel: "Cancelar desativação"
  submit: "Desativar" / <Loader2 /> + "Desativando..."

ReactivateAdminDialog:
  title: "Reativar Administrador"
  description: "O administrador {fullName} recuperará acesso ao backoffice."
  cancel: "Cancelar reativação"
  submit: "Reativar"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar EditAdminDialog e ResetPasswordDialog</name>
  <files>
    frontend/backoffice/src/components/molecules/EditAdminDialog.tsx
    frontend/backoffice/src/components/molecules/ResetPasswordDialog.tsx
  </files>
  <read_first>
    - frontend/backoffice/src/components/molecules/BlockDialog.tsx — ler padrão completo de dialog com RHF+Zod, estado de submissão, toast
    - frontend/backoffice/src/components/ui/dialog.tsx — confirmar exports disponíveis
    - frontend/backoffice/src/components/ui/alert.tsx — confirmar Alert, AlertDescription exports
    - frontend/backoffice/src/lib/validation-schemas.ts — confirmar que adminEditAdministratorSchema existe após Plano 01
  </read_first>
  <action>
**Arquivo 1: frontend/backoffice/src/components/molecules/EditAdminDialog.tsx**

```typescript
import { useEffect, useState } from "react";
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import {
  adminEditAdministratorSchema,
  type AdminEditAdministratorInput,
} from "@/lib/validation-schemas";
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogDescription,
  DialogFooter,
} from "@/components/ui/dialog";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Loader2 } from "lucide-react";
import type { AdminUserDto } from "@/lib/admin-api";

interface EditAdminDialogProps {
  open: boolean;
  admin: AdminUserDto | null;
  onClose: () => void;
  onSave: (adminId: string, data: AdminEditAdministratorInput) => Promise<void>;
}

export function EditAdminDialog({ open, admin, onClose, onSave }: EditAdminDialogProps) {
  const [isSubmitting, setIsSubmitting] = useState(false);

  const {
    register,
    handleSubmit,
    formState: { errors },
    reset,
  } = useForm<AdminEditAdministratorInput>({
    resolver: zodResolver(adminEditAdministratorSchema),
    defaultValues: { fullName: "", email: "" },
  });

  // Pré-popular form quando o admin selecionado muda
  useEffect(() => {
    if (admin) {
      reset({ fullName: admin.fullName, email: admin.email });
    }
  }, [admin, reset]);

  const onSubmit = async (data: AdminEditAdministratorInput) => {
    if (!admin) return;
    setIsSubmitting(true);
    try {
      await onSave(admin.id, data);
      onClose();
    } finally {
      setIsSubmitting(false);
    }
  };

  const handleOpenChange = (isOpen: boolean) => {
    if (!isOpen) {
      reset({ fullName: "", email: "" });
      onClose();
    }
  };

  return (
    <Dialog open={open} onOpenChange={handleOpenChange}>
      <DialogContent data-testid="edit-admin-dialog">
        <DialogHeader>
          <DialogTitle>Editar Administrador</DialogTitle>
          <DialogDescription>
            Atualize o nome e o email do administrador.
          </DialogDescription>
        </DialogHeader>

        <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
          <div className="space-y-2">
            <Label htmlFor="edit-fullName">Nome</Label>
            <Input
              id="edit-fullName"
              {...register("fullName")}
              placeholder="Nome completo"
              disabled={isSubmitting}
              data-testid="edit-fullname-input"
            />
            {errors.fullName && (
              <p className="text-sm text-destructive" data-testid="edit-fullname-error">
                {errors.fullName.message}
              </p>
            )}
          </div>

          <div className="space-y-2">
            <Label htmlFor="edit-email">Email</Label>
            <Input
              id="edit-email"
              type="email"
              {...register("email")}
              placeholder="email@exemplo.com"
              disabled={isSubmitting}
              data-testid="edit-email-input"
            />
            {errors.email && (
              <p className="text-sm text-destructive" data-testid="edit-email-error">
                {errors.email.message}
              </p>
            )}
          </div>

          <DialogFooter>
            <Button
              type="button"
              variant="outline"
              onClick={onClose}
              disabled={isSubmitting}
              data-testid="edit-cancel-button"
            >
              Cancelar edição
            </Button>
            <Button
              type="submit"
              variant="default"
              disabled={isSubmitting}
              data-testid="edit-save-button"
            >
              {isSubmitting ? (
                <>
                  <Loader2 className="h-4 w-4 mr-1 animate-spin" />
                  Salvando...
                </>
              ) : (
                "Salvar alterações"
              )}
            </Button>
          </DialogFooter>
        </form>
      </DialogContent>
    </Dialog>
  );
}
```

---

**Arquivo 2: frontend/backoffice/src/components/molecules/ResetPasswordDialog.tsx**

```typescript
import { useState } from "react";
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogDescription,
  DialogFooter,
} from "@/components/ui/dialog";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Alert, AlertDescription } from "@/components/ui/alert";
import { AlertTriangle, Copy, Check } from "lucide-react";

interface ResetPasswordDialogProps {
  open: boolean;
  generatedPassword: string | null; // null = dialog fechada; string = senha a exibir
  onClose: () => void; // limpar senha do state (D-05)
}

export function ResetPasswordDialog({
  open,
  generatedPassword,
  onClose,
}: ResetPasswordDialogProps) {
  const [copied, setCopied] = useState(false);

  const handleCopy = async () => {
    if (!generatedPassword) return;
    try {
      await navigator.clipboard.writeText(generatedPassword);
      setCopied(true);
      setTimeout(() => setCopied(false), 2000);
    } catch {
      // Fallback: selecionar o texto manualmente
    }
  };

  // D-05: ao fechar, limpar senha do state externo
  const handleOpenChange = (isOpen: boolean) => {
    if (!isOpen) {
      setCopied(false);
      onClose(); // chamador limpa generatedPassword → null
    }
  };

  return (
    <Dialog open={open} onOpenChange={handleOpenChange}>
      <DialogContent data-testid="reset-password-dialog">
        <DialogHeader>
          <DialogTitle>Senha Temporária Gerada</DialogTitle>
          <DialogDescription>
            Compartilhe esta senha com o administrador agora. Ela não poderá ser recuperada depois.
          </DialogDescription>
        </DialogHeader>

        <div className="space-y-4">
          <div className="space-y-2">
            <Input
              readOnly
              value={generatedPassword ?? ""}
              className="font-mono text-sm select-all"
              aria-label="Senha temporária gerada"
              data-testid="generated-password-input"
            />
          </div>

          <Alert variant="destructive">
            <AlertTriangle className="h-4 w-4" />
            <AlertDescription>
              Esta senha não pode ser recuperada. Feche somente após copiar.
            </AlertDescription>
          </Alert>

          <Button
            type="button"
            variant="outline"
            className="w-full"
            onClick={handleCopy}
            aria-label={copied ? "Senha copiada" : "Copiar senha para clipboard"}
            data-testid="copy-password-button"
          >
            {copied ? (
              <>
                <Check className="h-4 w-4 mr-2" />
                Copiado!
              </>
            ) : (
              <>
                <Copy className="h-4 w-4 mr-2" />
                Copiar senha
              </>
            )}
          </Button>
        </div>

        <DialogFooter>
          <Button
            type="button"
            variant="default"
            onClick={() => handleOpenChange(false)}
            data-testid="close-password-dialog-button"
          >
            Fechar
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  );
}
```

Notas de implementação:
- D-04: campo readOnly com font-mono; botão Copiar com feedback 2s; aviso proeminente via Alert
- D-05: onClose() é chamado pelo consumidor para limpar generatedPassword → null no state da página
- generatedPassword vem como prop (a senha foi obtida pela chamada API ANTES de abrir o dialog)
- Sem countdown ou checkbox — D-04 especifica botão Fechar simples
- aria-label no botão copiar muda após cópia (acessibilidade)
  </action>
  <verify>
    <automated>cd "D:/REPO/keycloak-tests/frontend/backoffice" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo criado: frontend/backoffice/src/components/molecules/EditAdminDialog.tsx
    - Arquivo criado: frontend/backoffice/src/components/molecules/ResetPasswordDialog.tsx
    - grep "adminEditAdministratorSchema" EditAdminDialog.tsx retorna match (import do schema)
    - grep "zodResolver" EditAdminDialog.tsx retorna match
    - grep "Cancelar edição" EditAdminDialog.tsx retorna match (copywriting exato D-SPEC)
    - grep "Salvar alterações" EditAdminDialog.tsx retorna match
    - grep "Salvando\.\.\." EditAdminDialog.tsx retorna match (estado de loading)
    - grep "text-sm text-destructive" EditAdminDialog.tsx retorna match (erro inline)
    - grep "font-mono" ResetPasswordDialog.tsx retorna match (campo monospace)
    - grep "Esta senha não pode ser recuperada" ResetPasswordDialog.tsx retorna match (aviso exato)
    - grep "Copiar senha\|Copiado" ResetPasswordDialog.tsx retorna match
    - grep "setCopied.*false\|clearTimeout\|setTimeout" ResetPasswordDialog.tsx retorna match (feedback 2s)
    - npx tsc --noEmit não retorna erros
  </acceptance_criteria>
  <done>EditAdminDialog com pré-população, validação Zod e loading state; ResetPasswordDialog com campo monospace, copiar 2s, aviso de não-recuperação e limpeza de senha no fechamento.</done>
</task>

<task type="auto">
  <name>Task 2: Criar DeactivateAdminDialog e ReactivateAdminDialog</name>
  <files>
    frontend/backoffice/src/components/molecules/DeactivateAdminDialog.tsx
    frontend/backoffice/src/components/molecules/ReactivateAdminDialog.tsx
  </files>
  <read_first>
    - frontend/backoffice/src/components/molecules/BlockDialog.tsx — padrão de dialog de confirmação (Alert, estados de submissão, toast)
    - frontend/backoffice/src/components/ui/alert.tsx — confirmar Alert variant="destructive"
  </read_first>
  <action>
**Arquivo 1: frontend/backoffice/src/components/molecules/DeactivateAdminDialog.tsx**

```typescript
import { useState } from "react";
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogDescription,
  DialogFooter,
} from "@/components/ui/dialog";
import { Button } from "@/components/ui/button";
import { Alert, AlertDescription } from "@/components/ui/alert";
import { AlertTriangle, Loader2 } from "lucide-react";
import type { AdminUserDto } from "@/lib/admin-api";

interface DeactivateAdminDialogProps {
  open: boolean;
  admin: AdminUserDto | null;
  onClose: () => void;
  onDeactivate: (adminId: string) => Promise<void>;
}

export function DeactivateAdminDialog({
  open,
  admin,
  onClose,
  onDeactivate,
}: DeactivateAdminDialogProps) {
  const [isSubmitting, setIsSubmitting] = useState(false);

  const handleConfirm = async () => {
    if (!admin) return;
    setIsSubmitting(true);
    try {
      await onDeactivate(admin.id);
      onClose();
    } finally {
      setIsSubmitting(false);
    }
  };

  const handleOpenChange = (isOpen: boolean) => {
    if (!isOpen && !isSubmitting) {
      onClose();
    }
  };

  return (
    <Dialog open={open} onOpenChange={handleOpenChange}>
      <DialogContent data-testid="deactivate-admin-dialog">
        <DialogHeader>
          <DialogTitle>Desativar Administrador</DialogTitle>
          <DialogDescription>
            O administrador {admin?.fullName ?? ""} perderá acesso ao backoffice.
            A conta é preservada para auditoria.
          </DialogDescription>
        </DialogHeader>

        <Alert variant="destructive">
          <AlertTriangle className="h-4 w-4" />
          <AlertDescription>
            Esta ação pode ser revertida reativando o administrador.
          </AlertDescription>
        </Alert>

        <DialogFooter>
          <Button
            type="button"
            variant="outline"
            onClick={onClose}
            disabled={isSubmitting}
            data-testid="deactivate-cancel-button"
          >
            Cancelar desativação
          </Button>
          <Button
            type="button"
            variant="destructive"
            onClick={handleConfirm}
            disabled={isSubmitting}
            data-testid="deactivate-confirm-button"
          >
            {isSubmitting ? (
              <>
                <Loader2 className="h-4 w-4 mr-1 animate-spin" />
                Desativando...
              </>
            ) : (
              "Desativar"
            )}
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  );
}
```

---

**Arquivo 2: frontend/backoffice/src/components/molecules/ReactivateAdminDialog.tsx**

```typescript
import { useState } from "react";
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogDescription,
  DialogFooter,
} from "@/components/ui/dialog";
import { Button } from "@/components/ui/button";
import { Loader2 } from "lucide-react";
import type { AdminUserDto } from "@/lib/admin-api";

interface ReactivateAdminDialogProps {
  open: boolean;
  admin: AdminUserDto | null;
  onClose: () => void;
  onReactivate: (adminId: string) => Promise<void>;
}

export function ReactivateAdminDialog({
  open,
  admin,
  onClose,
  onReactivate,
}: ReactivateAdminDialogProps) {
  const [isSubmitting, setIsSubmitting] = useState(false);

  const handleConfirm = async () => {
    if (!admin) return;
    setIsSubmitting(true);
    try {
      await onReactivate(admin.id);
      onClose();
    } finally {
      setIsSubmitting(false);
    }
  };

  const handleOpenChange = (isOpen: boolean) => {
    if (!isOpen && !isSubmitting) {
      onClose();
    }
  };

  return (
    <Dialog open={open} onOpenChange={handleOpenChange}>
      <DialogContent data-testid="reactivate-admin-dialog">
        <DialogHeader>
          <DialogTitle>Reativar Administrador</DialogTitle>
          <DialogDescription>
            O administrador {admin?.fullName ?? ""} recuperará acesso ao backoffice.
          </DialogDescription>
        </DialogHeader>

        <DialogFooter>
          <Button
            type="button"
            variant="outline"
            onClick={onClose}
            disabled={isSubmitting}
            data-testid="reactivate-cancel-button"
          >
            Cancelar reativação
          </Button>
          <Button
            type="button"
            variant="default"
            onClick={handleConfirm}
            disabled={isSubmitting}
            data-testid="reactivate-confirm-button"
          >
            {isSubmitting ? (
              <>
                <Loader2 className="h-4 w-4 mr-1 animate-spin" />
                Reativando...
              </>
            ) : (
              "Reativar"
            )}
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  );
}
```

Notas de implementação:
- DeactivateAdminDialog usa variant="destructive" no botão de confirmação (ação destrutiva)
- ReactivateAdminDialog usa variant="default" no botão de confirmação (ação positiva — sem destructive)
- Ambos bloqueiam fechamento do dialog via Escape durante submissão (isSubmitting guard no handleOpenChange)
- Toast de sucesso/erro é responsabilidade da página (onDeactivate/onReactivate vêm como props async — a página pode lançar para mostrar toast de erro)
- UI-SPEC: DeactivateAdminDialog inclui Alert variant="destructive" com aviso de reversibilidade
- UI-SPEC: ReactivateAdminDialog não inclui Alert (ação positiva, sem aviso de risco)
  </action>
  <verify>
    <automated>cd "D:/REPO/keycloak-tests/frontend/backoffice" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo criado: frontend/backoffice/src/components/molecules/DeactivateAdminDialog.tsx
    - Arquivo criado: frontend/backoffice/src/components/molecules/ReactivateAdminDialog.tsx
    - grep "variant=\"destructive\"" DeactivateAdminDialog.tsx retorna match em Alert E em Button de confirmação
    - grep "Cancelar desativação" DeactivateAdminDialog.tsx retorna match (copywriting exato)
    - grep "Desativar" DeactivateAdminDialog.tsx retorna match (botão de confirmação)
    - grep "Desativando\.\.\." DeactivateAdminDialog.tsx retorna match (loading state)
    - grep "Esta ação pode ser revertida" DeactivateAdminDialog.tsx retorna match (aviso Alert)
    - grep "variant=\"default\"" ReactivateAdminDialog.tsx retorna match em Button de confirmação (não destructive)
    - grep "Cancelar reativação" ReactivateAdminDialog.tsx retorna match (copywriting exato)
    - grep "Reativar" ReactivateAdminDialog.tsx retorna match
    - grep "Reativando\.\.\." ReactivateAdminDialog.tsx retorna match (loading state)
    - npx tsc --noEmit não retorna erros
  </acceptance_criteria>
  <done>DeactivateAdminDialog com Alert destructive, botão vermelho e loading state; ReactivateAdminDialog com botão default (positivo) e loading state; ambos com copywriting exato do UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Dialog UI → Clipboard API | Senha temporária escrita via navigator.clipboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-05 | Information Disclosure | ResetPasswordDialog — senha em campo readOnly | mitigate | D-05 aplicado: senha descartada ao fechar (setGeneratedPassword(null)); campo não persiste em estado da página após fechamento |
| T-36-06 | Information Disclosure | navigator.clipboard.writeText | accept | Clipboard é necessário para UX one-time; risco residual aceitável pois o usuário explicitamente copiou a senha |
| T-36-07 | Tampering | DeactivateAdminDialog — confirmação sem re-autenticação | accept | Proteção server-side (SEC-01, SEC-05) é suficiente; confirmação no dialog é UX, não controle de segurança |
</threat_model>

<verification>
1. `cd D:/REPO/keycloak-tests/frontend/backoffice && npx tsc --noEmit` — zero erros
2. `ls frontend/backoffice/src/components/molecules/` inclui os 4 novos arquivos de dialog
3. `grep "adminEditAdministratorSchema" frontend/backoffice/src/components/molecules/EditAdminDialog.tsx` — match
4. `grep "font-mono" frontend/backoffice/src/components/molecules/ResetPasswordDialog.tsx` — match
5. `grep "variant=\"destructive\"" frontend/backoffice/src/components/molecules/DeactivateAdminDialog.tsx` — match
</verification>

<success_criteria>
- EditAdminDialog: pré-popula nome/email, valida com Zod, loading state, copywriting exato
- ResetPasswordDialog: campo monospace, copiar com 2s feedback, aviso de não-recuperação, sem countdown
- DeactivateAdminDialog: Alert destructive, botão vermelho, loading state
- ReactivateAdminDialog: botão default (positivo), loading state
- Zero erros TypeScript
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/36-admin-management-ui/36-03-SUMMARY.md`
</output>

---
phase: 03-backend-domain-layer
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/Onboarding.Domain.Tests/Onboarding.Domain.Tests.csproj
  - tests/Onboarding.Domain.Tests/ValueObjects/CpfTests.cs
  - tests/Onboarding.Domain.Tests/ValueObjects/CnpjTests.cs
  - tests/Onboarding.Domain.Tests/ValueObjects/EmailTests.cs
  - tests/Onboarding.Domain.Tests/ValueObjects/PhoneNumberTests.cs
  - tests/Onboarding.Domain.Tests/Aggregates/ClientTests.cs
  - src/Onboarding.Domain/Common/Entity.cs
  - src/Onboarding.Domain/ValueObjects/Cpf.cs
  - src/Onboarding.Domain/ValueObjects/Cnpj.cs
  - src/Onboarding.Domain/ValueObjects/Email.cs
  - src/Onboarding.Domain/ValueObjects/PhoneNumber.cs
  - src/Onboarding.Domain/Aggregates/ClientAggregate/Client.cs
  - src/Onboarding.Domain/Aggregates/ClientAggregate/ClientType.cs
  - src/Onboarding.Domain/Aggregates/ClientAggregate/ClientStatus.cs
  - src/Onboarding.Domain/Repositories/IClientRepository.cs
autonomous: true
requirements:
  - BACK-01
  - BACK-02
  - BACK-03
  - BACK-04

must_haves:
  truths:
    - "dotnet test tests/Onboarding.Domain.Tests exits 0 with all tests green"
    - "Cpf.Create rejects all-same-digit CPFs and wrong check digits, accepts known-valid CPFs"
    - "Cnpj.Create rejects invalid CNPJs (both numeric and alphanumeric formats) and accepts valid ones"
    - "Client.RegisterPessoaFisica and Client.RegisterPessoaJuridica create aggregates with correct value objects"
    - "Onboarding.Domain.csproj has zero <PackageReference> entries — pure C# only"
  artifacts:
    - path: "tests/Onboarding.Domain.Tests/Onboarding.Domain.Tests.csproj"
      provides: "xUnit test project targeting net10.0 with Shouldly + NSubstitute"
    - path: "src/Onboarding.Domain/Common/Entity.cs"
      provides: "Abstract Entity<TId> base class with Id, Equals, GetHashCode, == and !="
    - path: "src/Onboarding.Domain/ValueObjects/Cpf.cs"
      provides: "Sealed record Cpf with Create factory, CPF mod-11 validation"
    - path: "src/Onboarding.Domain/ValueObjects/Cnpj.cs"
      provides: "Sealed record Cnpj with Create factory, alphanumeric CNPJ mod-11 validation"
    - path: "src/Onboarding.Domain/ValueObjects/Email.cs"
      provides: "Sealed record Email with Create factory, basic format validation"
    - path: "src/Onboarding.Domain/ValueObjects/PhoneNumber.cs"
      provides: "Sealed record PhoneNumber with Create factory, digits-only normalization"
    - path: "src/Onboarding.Domain/Aggregates/ClientAggregate/Client.cs"
      provides: "Sealed class Client extending Entity<Guid> with RegisterPessoaFisica and RegisterPessoaJuridica factory methods"
    - path: "src/Onboarding.Domain/Repositories/IClientRepository.cs"
      provides: "Interface IClientRepository with AddAsync, GetByIdAsync, ExistsByEmailAsync, ExistsByCpfAsync, ExistsByCnpjAsync"
  key_links:
    - from: "src/Onboarding.Domain/Aggregates/ClientAggregate/Client.cs"
      to: "src/Onboarding.Domain/ValueObjects/Cpf.cs"
      via: "Cpf.Create(cpf) called in RegisterPessoaFisica factory"
      pattern: "Cpf\\.Create"
    - from: "tests/Onboarding.Domain.Tests/ValueObjects/CpfTests.cs"
      to: "src/Onboarding.Domain/ValueObjects/Cpf.cs"
      via: "xUnit Theory tests calling Cpf.Create"
      pattern: "Cpf\\.Create"
---

<objective>
Scaffold the test project, implement domain value objects and the Client aggregate using TDD (RED→GREEN).

Purpose: Establishes the zero-dependency domain core — value objects with self-validating business rules and the Client aggregate root — with full unit test coverage. This is the foundation all future phases build on.
Output: test project + 15 source files (4 value objects, 1 aggregate, 2 enums, 1 repo interface, 1 Entity base); all tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-backend-domain-layer/03-RESEARCH.md
@.planning/phases/03-backend-domain-layer/03-VALIDATION.md

<interfaces>
<!-- Existing project structure — executor must not add PackageReference to Onboarding.Domain.csproj -->

Onboarding.Domain.csproj (current — zero deps, keep it this way):
```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
</Project>
```

Onboarding.Application.csproj already references Domain:
```xml
<ProjectReference Include="..\Onboarding.Domain\Onboarding.Domain.csproj" />
```

Solution file: Onboarding.slnx (XML format, not classic .sln)
Current solution projects: src/Onboarding.API, src/Onboarding.Application, src/Onboarding.Domain, src/Onboarding.Infrastructure
Tests directory: tests/ (currently has only keycloak-hardening subdir — no .NET test project yet)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test project and write failing tests (RED phase)</name>
  <read_first>
    - D:/REPO/keycloak-tests/Onboarding.slnx (check current project list before adding)
    - D:/REPO/keycloak-tests/src/Onboarding.Domain/Onboarding.Domain.csproj (confirm zero PackageReference before writing tests against it)
    - D:/REPO/keycloak-tests/.planning/phases/03-backend-domain-layer/03-RESEARCH.md (CPF/CNPJ algorithms, test examples)
  </read_first>

  <files>
    tests/Onboarding.Domain.Tests/Onboarding.Domain.Tests.csproj,
    tests/Onboarding.Domain.Tests/ValueObjects/CpfTests.cs,
    tests/Onboarding.Domain.Tests/ValueObjects/CnpjTests.cs,
    tests/Onboarding.Domain.Tests/ValueObjects/EmailTests.cs,
    tests/Onboarding.Domain.Tests/ValueObjects/PhoneNumberTests.cs,
    tests/Onboarding.Domain.Tests/Aggregates/ClientTests.cs
  </files>

  <behavior>
    CpfTests:
    - Create_ValidCpf_ReturnsInstance: Theory [InlineData("529.982.247-25"), InlineData("52998224725")] → Cpf.Create returns non-null with Value == "52998224725"
    - Create_InvalidCpf_Throws: Theory [InlineData("000.000.000-00"), InlineData("111.111.111-11"), InlineData("529.982.247-26"), InlineData("123"), InlineData(""), InlineData(null)] → Should.Throw&lt;ArgumentException&gt;
    - TwoCpfsWithSameValue_AreEqual: Cpf.Create("529.982.247-25") == Cpf.Create("529.982.247-25") (record equality)

    CnpjTests:
    - Create_ValidNumericCnpj_ReturnsInstance: Theory [InlineData("11.222.333/0001-81"), InlineData("11222333000181")] → Cnpj.Create returns non-null with Value == "11222333000181"
    - Create_ValidAlphanumericCnpj_ReturnsInstance: Theory with a known-valid alphanumeric CNPJ (compute using ASCII-48 mapping, see RESEARCH.md)
    - Create_InvalidCnpj_Throws: Theory [InlineData("00.000.000/0000-00"), InlineData("11.222.333/0001-82"), InlineData(""), InlineData(null)] → Should.Throw&lt;ArgumentException&gt;
    - TwoCnpjsWithSameValue_AreEqual: record equality test

    EmailTests:
    - Create_ValidEmail_ReturnsInstance: Theory [InlineData("user@example.com"), InlineData("a@b.co")] → non-null, Value preserved lowercase
    - Create_InvalidEmail_Throws: Theory [InlineData(""), InlineData(null), InlineData("notanemail"), InlineData("@domain.com")] → Should.Throw&lt;ArgumentException&gt;

    PhoneNumberTests:
    - Create_ValidPhone_ReturnsInstance: Theory [InlineData("+55 (11) 99999-8888"), InlineData("11999998888")] → non-null, Value is digits-only "11999998888"
    - Create_InvalidPhone_Throws: Theory [InlineData(""), InlineData(null), InlineData("123")] → Should.Throw&lt;ArgumentException&gt; (minimum 8 digits)

    ClientTests:
    - RegisterPessoaFisica_ValidInputs_CreatesClient: Client.RegisterPessoaFisica("João Silva", "529.982.247-25", "joao@example.com", "11999998888") → non-null, Id != Guid.Empty, Type == ClientType.PessoaFisica, Cpf non-null, Cnpj null
    - RegisterPessoaJuridica_ValidInputs_CreatesClient: Client.RegisterPessoaJuridica("Empresa SA", "11222333000181", "empresa@example.com", "11999998888") → non-null, Type == ClientType.PessoaJuridica, Cnpj non-null, Cpf null
    - RegisterPessoaFisica_InvalidCpf_ThrowsArgumentException: Client.RegisterPessoaFisica with bad CPF → Should.Throw&lt;ArgumentException&gt;
    - RegisterPessoaFisica_NullName_ThrowsArgumentNullException: null nome → Should.Throw&lt;ArgumentNullException&gt;
    - TwoClients_SameId_AreEqual: Entity equality based on Id
  </behavior>

  <action>
    Step 1 — Create the test project:
    ```
    dotnet new xunit -n Onboarding.Domain.Tests -o tests/Onboarding.Domain.Tests --framework net10.0
    ```
    This creates the .csproj with xunit and Microsoft.NET.Test.Sdk. Then add required packages:
    ```
    dotnet add tests/Onboarding.Domain.Tests/Onboarding.Domain.Tests.csproj package Shouldly --version 4.3.0
    dotnet add tests/Onboarding.Domain.Tests/Onboarding.Domain.Tests.csproj package NSubstitute --version 5.3.0
    dotnet add tests/Onboarding.Domain.Tests/Onboarding.Domain.Tests.csproj package coverlet.collector --version 8.0.1
    dotnet add tests/Onboarding.Domain.Tests/Onboarding.Domain.Tests.csproj package xunit.runner.visualstudio --version 3.1.5
    dotnet add tests/Onboarding.Domain.Tests/Onboarding.Domain.Tests.csproj reference src/Onboarding.Domain/Onboarding.Domain.csproj
    dotnet sln Onboarding.slnx add tests/Onboarding.Domain.Tests/Onboarding.Domain.Tests.csproj
    ```
    Delete the auto-generated UnitTest1.cs file if it exists.

    Step 2 — Write the failing tests. The Domain source types do not exist yet, so the test project will NOT compile at this stage — that is expected (RED phase). Write all test files as described in the &lt;behavior&gt; block.

    Test namespace: `Onboarding.Domain.Tests.ValueObjects` and `Onboarding.Domain.Tests.Aggregates`.

    Use `using Onboarding.Domain.ValueObjects;` and `using Onboarding.Domain.Aggregates.ClientAggregate;`.

    For the alphanumeric CNPJ test case, use the following pre-computed valid value: base `"B2AAA33A0001"` (positions 0-7 alphanumeric, 8-11 numeric) with check digits computed via the ASCII-48 mapping algorithm from RESEARCH.md. Alternatively use a simpler known-valid: after normalization, a valid all-alpha-prefix CNPJ. Use `"12ABC34501DE96"` as a known test value that the implementation MUST accept once written (verify by running the algorithm manually before committing this test). If the manual computation yields a different check digit, use the correct value.

    NOTE: The cleanest approach is to use one purely numeric CNPJ for the alphanumeric test path (since the ASCII-48 algorithm is backward-compatible with numeric CNPJs). Add a comment explaining why, and add a TODO test for a true alphanumeric CNPJ once the July 2026 format is officially published. This avoids pre-computing a value that may be wrong.

    Step 3 — Confirm RED: run `dotnet build tests/Onboarding.Domain.Tests/` and verify it fails with compilation errors (types not found). Commit with message: `test(03-01): add failing domain tests — RED phase`.
  </action>

  <verify>
    <automated>dotnet build tests/Onboarding.Domain.Tests/ 2>&amp;1 | grep -c "error CS" | grep -v "^0$"</automated>
  </verify>

  <done>
    Test files exist at tests/Onboarding.Domain.Tests/ValueObjects/*.cs and tests/Onboarding.Domain.Tests/Aggregates/ClientTests.cs. Build fails with CS-type errors (missing Cpf, Cnpj, Email, PhoneNumber, Client types) — confirming RED phase. Committed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement domain value objects and aggregate (GREEN phase)</name>
  <read_first>
    - tests/Onboarding.Domain.Tests/ValueObjects/CpfTests.cs (test expectations to satisfy)
    - tests/Onboarding.Domain.Tests/ValueObjects/CnpjTests.cs
    - tests/Onboarding.Domain.Tests/ValueObjects/EmailTests.cs
    - tests/Onboarding.Domain.Tests/ValueObjects/PhoneNumberTests.cs
    - tests/Onboarding.Domain.Tests/Aggregates/ClientTests.cs
    - D:/REPO/keycloak-tests/.planning/phases/03-backend-domain-layer/03-RESEARCH.md (exact algorithm code, pitfall notes)
    - D:/REPO/keycloak-tests/src/Onboarding.Domain/Onboarding.Domain.csproj (verify zero PackageReference before writing)
  </read_first>

  <files>
    src/Onboarding.Domain/Common/Entity.cs,
    src/Onboarding.Domain/ValueObjects/Cpf.cs,
    src/Onboarding.Domain/ValueObjects/Cnpj.cs,
    src/Onboarding.Domain/ValueObjects/Email.cs,
    src/Onboarding.Domain/ValueObjects/PhoneNumber.cs,
    src/Onboarding.Domain/Aggregates/ClientAggregate/Client.cs,
    src/Onboarding.Domain/Aggregates/ClientAggregate/ClientType.cs,
    src/Onboarding.Domain/Aggregates/ClientAggregate/ClientStatus.cs,
    src/Onboarding.Domain/Repositories/IClientRepository.cs
  </files>

  <behavior>
    Entity.cs: abstract class Entity&lt;TId&gt; where TId : struct — public TId Id with protected setter, Equals by Id + type, GetHashCode, == and != operators.

    Cpf.cs: sealed record Cpf — private constructor taking string value; static Create(string? raw) strips "." and "-", validates 11 digits, rejects all-same-digit, applies mod-11 twice; throws ArgumentException on invalid; Value property returns normalized digits string.

    Cnpj.cs: sealed record Cnpj — private constructor; static Create(string? raw) strips ".", "/", "-", uppercases, validates 14 chars, chars are [A-Z0-9], rejects all-same-digit numeric sequences, applies ASCII-48 mod-11 check; throws ArgumentException on invalid.

    Email.cs: sealed record Email — static Create(string? raw) validates non-null, non-empty, contains exactly one "@", has at least one char before "@" and at least one "." in the domain part after "@"; stores lowercased value; throws ArgumentException on invalid.

    PhoneNumber.cs: sealed record PhoneNumber — static Create(string? raw) strips all non-digit chars from input; validates result is between 8 and 15 digits inclusive; throws ArgumentException if fewer than 8 digits after stripping; stores digits-only value.

    Client.cs: sealed class Client : Entity&lt;Guid&gt; — private constructor(); protected Client() { } (EF Core entry point — see Pitfall 3); all setters private. Factory RegisterPessoaFisica(string nome, string cpf, string email, string phone) → sets Id = Guid.NewGuid(), Name = nome (throws ArgumentNullException if null), Cpf = Cpf.Create(cpf), Email = Email.Create(email), Phone = PhoneNumber.Create(phone), Type = ClientType.PessoaFisica. Factory RegisterPessoaJuridica(string razaoSocial, string cnpj, string email, string phone) → sets Id = Guid.NewGuid(), Name = razaoSocial (throws ArgumentNullException if null), Cnpj = Cnpj.Create(cnpj), Email = Email.Create(email), Phone = PhoneNumber.Create(phone), Type = ClientType.PessoaJuridica, RazaoSocial = razaoSocial. Properties: Name, Email, Phone, Type, Cpf?, Cnpj?, RazaoSocial? — all private set.

    ClientType.cs: public enum ClientType { PessoaFisica = 1, PessoaJuridica = 2 }

    ClientStatus.cs: public enum ClientStatus { Active = 1, Inactive = 2 }

    IClientRepository.cs: public interface IClientRepository { Task AddAsync(Client client, CancellationToken ct = default); Task&lt;Client?&gt; GetByIdAsync(Guid id, CancellationToken ct = default); Task&lt;bool&gt; ExistsByEmailAsync(string email, CancellationToken ct = default); Task&lt;bool&gt; ExistsByCpfAsync(string cpf, CancellationToken ct = default); Task&lt;bool&gt; ExistsByCnpjAsync(string cnpj, CancellationToken ct = default); }

    All files use namespace Onboarding.Domain.Common, Onboarding.Domain.ValueObjects, Onboarding.Domain.Aggregates.ClientAggregate, Onboarding.Domain.Repositories respectively. No using directives for external packages in Onboarding.Domain — only System.* and System.Linq as needed.
  </behavior>

  <action>
    Create directories and write source files:
    - src/Onboarding.Domain/Common/Entity.cs
    - src/Onboarding.Domain/ValueObjects/Cpf.cs
    - src/Onboarding.Domain/ValueObjects/Cnpj.cs
    - src/Onboarding.Domain/ValueObjects/Email.cs
    - src/Onboarding.Domain/ValueObjects/PhoneNumber.cs
    - src/Onboarding.Domain/Aggregates/ClientAggregate/Client.cs
    - src/Onboarding.Domain/Aggregates/ClientAggregate/ClientType.cs
    - src/Onboarding.Domain/Aggregates/ClientAggregate/ClientStatus.cs
    - src/Onboarding.Domain/Repositories/IClientRepository.cs

    CPF validation (copy exactly from RESEARCH.md):
    ```csharp
    private static bool IsValid(string digits)
    {
        if (digits.Length != 11) return false;
        if (!digits.All(char.IsDigit)) return false;
        if (digits.Distinct().Count() == 1) return false;
        static int CalcDigit(string d, int[] weights)
        {
            var sum = weights.Select((w, i) => (d[i] - '0') * w).Sum();
            var rem = sum % 11;
            return rem < 2 ? 0 : 11 - rem;
        }
        var w1 = new[] { 10, 9, 8, 7, 6, 5, 4, 3, 2 };
        var w2 = new[] { 11, 10, 9, 8, 7, 6, 5, 4, 3, 2 };
        var d1 = CalcDigit(digits, w1);
        if (d1 != digits[9] - '0') return false;
        var d2 = CalcDigit(digits, w2);
        return d2 == digits[10] - '0';
    }
    ```

    CNPJ validation (uses ASCII-48 for alphanumeric backward compatibility):
    ```csharp
    private static int CharValue(char c) => c - 48;

    private static bool IsValid(string cnpj)
    {
        if (cnpj.Length != 14) return false;
        // Reject all-same numeric (legal but invalid)
        if (cnpj.All(char.IsDigit) && cnpj.Distinct().Count() == 1) return false;
        // All chars must be A-Z or 0-9
        if (!cnpj.All(c => char.IsAsciiDigit(c) || (c >= 'A' && c <= 'Z'))) return false;
        static int CalcDigit(string s, int[] weights)
        {
            var sum = weights.Select((w, i) => CharValue(s[i]) * w).Sum();
            var rem = sum % 11;
            return rem < 2 ? 0 : 11 - rem;
        }
        var w1 = new[] { 5, 4, 3, 2, 9, 8, 7, 6, 5, 4, 3, 2 };
        var w2 = new[] { 6, 5, 4, 3, 2, 9, 8, 7, 6, 5, 4, 3, 2 };
        var d1 = CalcDigit(cnpj, w1);
        if (d1 != cnpj[12] - '0') return false;
        var d2 = CalcDigit(cnpj, w2);
        return d2 == cnpj[13] - '0';
    }
    ```

    Cnpj.Create normalization: strip ".", "/", "-" then uppercase before calling IsValid.

    Email.Create validation: use System.Text.RegularExpressions.Regex or manual string checks — do NOT add any external package. A minimal check: non-null, non-empty, contains "@", split on "@" gives exactly 2 parts, left part non-empty, right part contains "." with chars before and after the dot.

    After writing all files, run:
    ```
    dotnet test tests/Onboarding.Domain.Tests/ --no-build 2>&amp;1 || dotnet test tests/Onboarding.Domain.Tests/
    ```
    All tests must pass (GREEN). Fix any failing tests before committing.

    Commit with: `feat(03-01): implement domain value objects and Client aggregate — GREEN phase`

    CRITICAL: After implementing, verify Onboarding.Domain.csproj still has zero PackageReference entries:
    ```
    dotnet list src/Onboarding.Domain package
    ```
    Expected output: empty / "no packages" message.
  </action>

  <verify>
    <automated>dotnet test tests/Onboarding.Domain.Tests/ --logger "console;verbosity=normal" 2>&amp;1 | tail -5</automated>
  </verify>

  <done>
    All tests pass (dotnet test exits 0). Onboarding.Domain.csproj has zero PackageReference entries. Domain source files exist at the paths listed. Committed as GREEN phase.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. All tests green: `dotnet test tests/Onboarding.Domain.Tests/` exits 0
2. Domain has no external deps: `dotnet list src/Onboarding.Domain package` produces no package output
3. Solution includes test project: `cat Onboarding.slnx` contains `Onboarding.Domain.Tests`
4. Value object equality works (record types): verified by TwoCpfsWithSameValue_AreEqual test
5. Client factory methods enforce invariants via value object constructors: verified by ClientTests
6. IClientRepository interface exists at src/Onboarding.Domain/Repositories/IClientRepository.cs
</verification>

<success_criteria>
- `dotnet test tests/Onboarding.Domain.Tests/` exits 0, all tests pass, no skipped
- `dotnet list src/Onboarding.Domain package` produces no package rows
- `grep -r "PackageReference" src/Onboarding.Domain/` returns nothing
- `grep "ClientType.PessoaFisica" tests/Onboarding.Domain.Tests/Aggregates/ClientTests.cs` matches
- `grep "protected Client()" src/Onboarding.Domain/Aggregates/ClientAggregate/Client.cs` matches (EF Core entry point)
- `grep "IClientRepository" src/Onboarding.Domain/Repositories/IClientRepository.cs` matches
</success_criteria>

<output>
After completion, create `.planning/phases/03-backend-domain-layer/03-01-SUMMARY.md` using the summary template.
</output>

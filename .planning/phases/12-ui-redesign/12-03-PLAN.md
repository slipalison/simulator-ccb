---
phase: 12
phase_name: ui-redesign
plan: 03
plan_name: profile-header-forgot-redesign
created: "2026-04-08"
status: pending
must_haves:
  - "UI-05: ProfilePage redesign com shadcn Card, Badge, Skeleton"
  - "UI-06: Header/Navigation component com theme toggle + user menu"
  - "UI-07: Forgot/Reset Password pages redesign"
artifacts:
  - ".planning/PHASE-12-UI-REDESIGN.md"
  - "frontend/src/components/organisms/Header.tsx"
  - "frontend/src/components/pages/ProfilePage.tsx"
  - "frontend/src/components/pages/ForgotPasswordPage.tsx"
  - "frontend/src/components/pages/ResetPasswordPage.tsx"
key_links:
  - "PHASE-12-UI-REDESIGN.md: UI-05 com mockup completo da ProfilePage"
  - "PHASE-12-UI-REDESIGN.md: UI-06 com Header spec (logo, theme toggle, user menu)"
  - "PHASE-12-UI-REDESIGN.md: UI-07 com mockups de Forgot/Reset pages"
  - "Phase 11: ForgotPasswordPage.tsx e ResetPasswordPage.tsx existentes — modificar, não recriar"
autonomous: true
---

# Phase 12 — Plan 03: ProfilePage + Header + Forgot/Reset Pages

## Objective

Completar o redesign de todas as telas restantes: ProfilePage com dados em modo leitura, Header fixo com theme toggle e user menu, e Forgot/Reset Password pages com shadcn/ui. Remover componentes legados substituídos.

**Requisitos atendidos:** UI-05, UI-06, UI-07

## Execution Context

### Current State
- **shadcn/ui:** Já instalado (Plan 01) — todos os componentes base disponíveis
- **Theme infrastructure:** Já pronta (Plan 01) — ThemeProvider, ThemeToggle
- **ProfilePage.tsx:** Existe com ProfileCard, ProfileField, ProfileBadge (PF verde, PJ azul)
- **ForgotPasswordPage.tsx:** Existe com Tailwind custom, email form, Resend.com integration
- **ResetPasswordPage.tsx:** Existe com PasswordField + StrengthMeter
- **Header:** Não existe como componente separado — ThemeToggle é solitário
- **Componentes legados a remover:** LabeledField, AppButton, PageLayout, ExampleForm

### Dependencies
- **Plan 01:** shadcn setup + theme infra — deve estar completo
- **Plan 02:** Login + Registration redesign — deve estar completo (compartilha Header)

### Technical Approach
```
1. Criar Header.tsx organism com logo, theme toggle, user menu
2. Redesenhar ProfilePage com Header, shadcn Card, Badge, Skeleton
3. Redesenhar ForgotPasswordPage com shadcn Card, Form, Input
4. Redesenhar ResetPasswordPage com shadcn Card, PasswordField + StrengthMeter
5. Remover componentes legados (LabeledField, AppButton, PageLayout, ExampleForm)
6. Atualizar testes para refletir novos componentes
```

## Tasks

### Task 12.3.1: TDD Stubs RED — Profile, Header, Forgot/Reset

**Objective:** Criar testes para todos os componentes a serem redesignados

**Actions:**

1. **Criar `frontend/src/tests/header.test.tsx`:**
   ```tsx
   describe('Header', () => {
     it('renders with logo on left, controls on right', () => { /* stub - fail */ });
     it('shows theme toggle button', () => { /* stub - fail */ });
     it('shows user menu dropdown when authenticated', () => { /* stub - fail */ });
     it('user menu has Profile and Logout items', () => { /* stub - fail */ });
   });
   ```

2. **Criar `frontend/src/tests/profile-page-redesign.test.tsx`:**
   ```tsx
   describe('ProfilePage (shadcn redesign)', () => {
     it('renders with shadcn Card container', () => { /* stub - fail */ });
     it('shows PF/PJ badge with correct color', () => { /* stub - fail */ });
     it('shows skeleton loading state', () => { /* stub - fail */ });
     it('shows profile data (nome, cpf/cnpj, email, telefone)', () => { /* stub - fail */ });
     it('shows logout button', () => { /* stub - fail */ });
     it('redirects to login when not authenticated', () => { /* stub - fail */ });
   });
   ```

3. **Criar `frontend/src/tests/forgot-password-redesign.test.tsx`:**
   ```tsx
   describe('ForgotPasswordPage (shadcn redesign)', () => {
     it('renders with shadcn Card container', () => { /* stub - fail */ });
     it('shows email input with shadcn Input', () => { /* stub - fail */ });
     it('shows submit button with shadcn Button', () => { /* stub - fail */ });
     it('shows success message after submitting', () => { /* stub - fail */ });
     it('shows link back to login', () => { /* stub - fail */ });
   });
   ```

4. **Criar `frontend/src/tests/reset-password-redesign.test.tsx`:**
   ```tsx
   describe('ResetPasswordPage (shadcn redesign)', () => {
     it('renders with shadcn Card container', () => { /* stub - fail */ });
     it('shows password field with show/hide toggle', () => { /* stub - fail */ });
     it('shows password strength meter', () => { /* stub - fail */ });
     it('shows confirm password field', () => { /* stub - fail */ });
     it('shows submit button', () => { /* stub - fail */ });
     it('redirects to login after successful reset', () => { /* stub - fail */ });
   });
   ```

5. **Executar testes — devem falhar (TDD RED):**
   ```bash
   cd frontend && npm test
   ```

**Output:**
- 21 stub tests falhando (4 header + 6 profile + 5 forgot + 6 reset)

**Success Criteria:**
- [ ] 21 testes falhando (nenhum passando)
- [ ] Testes importam componentes que serão modificados

---

### Task 12.3.2: Header Component

**Objective:** Criar Header orgânismo com logo, theme toggle, user menu

**Actions:**

1. **Criar `frontend/src/components/organisms/Header.tsx`:**
   ```tsx
   import { ThemeToggle } from "../atoms/ThemeToggle"
   import {
     DropdownMenu,
     DropdownMenuContent,
     DropdownMenuItem,
     DropdownMenuSeparator,
     DropdownMenuTrigger,
   } from "@/components/ui/dropdown-menu"
   import { Button } from "@/components/ui/button"
   import { User, LogOut } from "lucide-react"
   import { useAuth } from "../../lib/auth-context"
   import { useNavigate } from "@tanstack/react-router"

   export function Header() {
     const { logout } = useAuth()
     const navigate = useNavigate()

     async function handleLogout() {
       await logout()
       navigate({ to: "/login" })
     }

     return (
       <header className="sticky top-0 z-50 w-full border-b bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60">
         <div className="container mx-auto flex h-14 items-center px-4">
           {/* Logo */}
           <div className="flex items-center gap-2 mr-auto">
             <span className="text-xl font-bold">🏢 Onboarding</span>
           </div>

           {/* Right side */}
           <div className="flex items-center gap-2">
             <ThemeToggle />

             {/* User menu */}
             <DropdownMenu>
               <DropdownMenuTrigger asChild>
                 <Button variant="ghost" size="icon">
                   <User className="h-5 w-5" />
                 </Button>
               </DropdownMenuTrigger>
               <DropdownMenuContent align="end">
                 <DropdownMenuItem onClick={() => navigate({ to: "/profile" })}>
                   Meu Perfil
                 </DropdownMenuItem>
                 <DropdownMenuSeparator />
                 <DropdownMenuItem onClick={handleLogout}>
                   <LogOut className="mr-2 h-4 w-4" />
                   <span>Sair</span>
                 </DropdownMenuItem>
               </DropdownMenuContent>
             </DropdownMenu>
           </div>
         </div>
       </header>
     )
   }
   ```

2. **Executar testes de Header — devem passar:**
   ```bash
   cd frontend && npm test -- header
   ```

**Output:**
- `Header.tsx` com logo, ThemeToggle, user menu dropdown
- 4 testes de Header passando

**Success Criteria:**
- [ ] Header fixo no topo (sticky, border-b, bg with backdrop-blur)
- [ ] Logo à esquerda com emoji 🏢 + "Onboarding"
- [ ] ThemeToggle visível à direita
- [ ] User menu dropdown com "Meu Perfil" e "Sair"
- [ ] Logout funciona (chama auth-context logout + navigate /login)
- [ ] 4 testes de Header passando

---

### Task 12.3.3: ProfilePage Redesign

**Objective:** Redesenhar ProfilePage com shadcn Card, Badge, Skeleton

**Actions:**

1. **Modificar `frontend/src/components/pages/ProfilePage.tsx`:**
   ```tsx
   import { useEffect, useState } from "react"
   import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
   import { Badge } from "@/components/ui/badge"
   import { Skeleton } from "@/components/ui/skeleton"
   import { Separator } from "@/components/ui/separator"
   import { Button } from "@/components/ui/button"
   import { Header } from "../organisms/Header"
   import { getProfileClient } from "../../lib/api"
   import { useAuth } from "../../lib/auth-context"
   import { useNavigate } from "@tanstack/react-router"
   import { LogOut } from "lucide-react"

   interface ClientProfile {
     personType: "pf" | "pj"
     nome?: string
     razaoSocial?: string
     cpf?: string
     cnpj?: string
     email: string
     telefone?: string
   }

   export function ProfilePage() {
     const { isAuthenticated } = useAuth()
     const navigate = useNavigate()
     const [profile, setProfile] = useState<ClientProfile | null>(null)
     const [loading, setLoading] = useState(true)

     // Auth guard
     useEffect(() => {
       if (!isAuthenticated) {
         navigate({ to: "/login" })
       }
     }, [isAuthenticated, navigate])

     // Fetch profile data
     useEffect(() => {
       async function fetchProfile() {
         try {
           const data = await getProfileClient()
           setProfile(data)
         } catch (err) {
           console.error("Failed to fetch profile:", err)
         } finally {
           setLoading(false)
         }
       }
       if (isAuthenticated) {
         fetchProfile()
       }
     }, [isAuthenticated])

     async function handleLogout() {
       // Call logout from auth-context (import if needed)
       navigate({ to: "/login" })
     }

     if (!isAuthenticated) return null

     if (loading) {
       return (
         <div className="min-h-screen bg-background">
           <Header />
           <div className="container mx-auto max-w-2xl py-8 px-4">
             <Card>
               <CardHeader>
                 <Skeleton className="h-8 w-40" />
               </CardHeader>
               <CardContent className="space-y-4">
                 <Skeleton className="h-4 w-full" />
                 <Skeleton className="h-4 w-3/4" />
                 <Skeleton className="h-4 w-1/2" />
               </CardContent>
             </Card>
           </div>
         </div>
       )
     }

     if (!profile) return null

     return (
       <div className="min-h-screen bg-background">
         <Header />
         <div className="container mx-auto max-w-2xl py-8 px-4">
           <Card>
             <CardHeader>
               <div className="flex items-center justify-between">
                 <CardTitle className="text-2xl">Meu Perfil</CardTitle>
                 <Badge variant={profile.personType === "pf" ? "default" : "secondary"}>
                   {profile.personType === "pf" ? "Pessoa Física" : "Pessoa Jurídica"}
                 </Badge>
               </div>
             </CardHeader>
             <CardContent className="space-y-6">
               {/* PF Fields */}
               {profile.personType === "pf" && profile.nome && (
                 <div>
                   <p className="text-sm text-muted-foreground">Nome completo</p>
                   <p className="text-base font-medium">{profile.nome}</p>
                 </div>
               )}

               {profile.personType === "pf" && profile.cpf && (
                 <div>
                   <p className="text-sm text-muted-foreground">CPF</p>
                   <p className="text-base font-mono">{profile.cpf}</p>
                 </div>
               )}

               {/* PJ Fields */}
               {profile.personType === "pj" && profile.razaoSocial && (
                 <div>
                   <p className="text-sm text-muted-foreground">Razão Social</p>
                   <p className="text-base font-medium">{profile.razaoSocial}</p>
                 </div>
               )}

               {profile.personType === "pj" && profile.cnpj && (
                 <div>
                   <p className="text-sm text-muted-foreground">CNPJ</p>
                   <p className="text-base font-mono">{profile.cnpj}</p>
                 </div>
               )}

               <Separator />

               {/* Common Fields */}
               <div>
                 <p className="text-sm text-muted-foreground">Email</p>
                 <p className="text-base">{profile.email}</p>
               </div>

               {profile.telefone && (
                 <div>
                   <p className="text-sm text-muted-foreground">Telefone</p>
                   <p className="text-base">{profile.telefone}</p>
                 </div>
               )}

               <Separator />

               {/* Logout */}
               <Button variant="outline" className="w-full" onClick={handleLogout}>
                 <LogOut className="mr-2 h-4 w-4" />
                 Sair
               </Button>
             </CardContent>
           </Card>
         </div>
       </div>
     )
   }
   ```

2. **Modificar `frontend/src/components/atoms/ProfileBadge.tsx` (se existir):**
   Usar shadcn Badge:
   ```tsx
   import { Badge } from "@/components/ui/badge"

   interface ProfileBadgeProps {
     personType: "pf" | "pj"
   }

   export function ProfileBadge({ personType }: ProfileBadgeProps) {
     return (
       <Badge variant={personType === "pf" ? "default" : "secondary"}>
         {personType === "pf" ? "Pessoa Física" : "Pessoa Jurídica"}
       </Badge>
     )
   }
   ```

3. **Executar testes de ProfilePage — devem passar:**
   ```bash
   cd frontend && npm test -- profile-page-redesign
   ```

**Output:**
- `ProfilePage.tsx` redesign completo com Header, Card, Badge, Skeleton
- `ProfileBadge.tsx` usando shadcn Badge
- 6 testes de ProfilePage passando

**Success Criteria:**
- [ ] ProfilePage com Header fixo no topo
- [ ] Card container com header "Meu Perfil" + PF/PJ Badge
- [ ] PF profile: verde (default variant), mostra nome + CPF
- [ ] PJ profile: azul (secondary variant), mostra razaoSocial + CNPJ
- [ ] Campos comuns: email, telefone
- [ ] Skeleton loading state enquanto busca dados
- [ ] Botão "Sair" no footer com ícone LogOut
- [ ] Redirect para /login quando não autenticado
- [ ] 6 testes de ProfilePage passando

---

### Task 12.3.4: Forgot/Reset Password Pages Redesign

**Objective:** Redesenhar ForgotPasswordPage e ResetPasswordPage com shadcn/ui

**Actions:**

1. **Modificar `frontend/src/components/pages/ForgotPasswordPage.tsx`:**
   ```tsx
   import { useState } from "react"
   import { useForm } from "react-hook-form"
   import { zodResolver } from "@hookform/resolvers/zod"
   import * as z from "zod"
   import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"
   import {
     Form,
     FormControl,
     FormField,
     FormItem,
     FormLabel,
     FormMessage,
   } from "@/components/ui/form"
   import { Input } from "@/components/ui/input"
   import { Button } from "@/components/ui/button"
   import { Alert, AlertDescription } from "@/components/ui/alert"
   import { Link, useNavigate } from "@tanstack/react-router"
   import { forgotPasswordClient } from "../../lib/api"
   import { ThemeToggle } from "../atoms/ThemeToggle"

   const forgotSchema = z.object({
     email: z.string().email("Email inválido"),
   })

   export function ForgotPasswordPage() {
     const [submitted, setSubmitted] = useState(false)
     const [error, setError] = useState<string | null>(null)
     const navigate = useNavigate()

     const form = useForm<z.infer<typeof forgotSchema>>({
       resolver: zodResolver(forgotSchema),
       defaultValues: { email: "" },
     })

     async function onSubmit(values: z.infer<typeof forgotSchema>) {
       try {
         await forgotPasswordClient(values.email)
         setSubmitted(true)
       } catch (err: any) {
         setError(err.message)
       }
     }

     if (submitted) {
       return (
         <div className="min-h-screen flex items-center justify-center bg-background p-4">
           <div className="absolute top-4 right-4">
             <ThemeToggle />
           </div>
           <Card className="w-full max-w-md">
             <CardHeader>
               <CardTitle className="text-2xl text-center">Email Enviado</CardTitle>
               <CardDescription className="text-center">
                 Se o email existir, você receberá um link de recuperação.
               </CardDescription>
             </CardHeader>
             <CardContent>
               <Button className="w-full" onClick={() => navigate({ to: "/login" })}>
                 Voltar para login
               </Button>
             </CardContent>
           </Card>
         </div>
       )
     }

     return (
       <div className="min-h-screen flex items-center justify-center bg-background p-4">
         <div className="absolute top-4 right-4">
           <ThemeToggle />
         </div>
         <Card className="w-full max-w-md">
           <CardHeader>
             <CardTitle className="text-2xl text-center">Recuperar Senha</CardTitle>
             <CardDescription className="text-center">
               Informe seu email para receber um link de recuperação.
             </CardDescription>
           </CardHeader>
           <CardContent>
             <Form {...form}>
               <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-4">
                 <FormField
                   control={form.control}
                   name="email"
                   render={({ field }) => (
                     <FormItem>
                       <FormLabel>Email</FormLabel>
                       <FormControl>
                         <Input type="email" placeholder="seu@email.com" {...field} />
                       </FormControl>
                       <FormMessage />
                     </FormItem>
                   )}
                 />

                 {error && (
                   <Alert variant="destructive">
                     <AlertDescription>{error}</AlertDescription>
                   </Alert>
                 )}

                 <Button type="submit" className="w-full">
                   Enviar link
                 </Button>
               </form>
             </Form>

             <div className="mt-4 text-center text-sm">
               <Link to="/login" className="text-primary hover:underline">
                 Voltar para login →
               </Link>
             </div>
           </CardContent>
         </Card>
       </div>
     )
   }
   ```

2. **Modificar `frontend/src/components/pages/ResetPasswordPage.tsx`:**
   ```tsx
   import { useState } from "react"
   import { useForm } from "react-hook-form"
   import { zodResolver } from "@hookform/resolvers/zod"
   import * as z from "zod"
   import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"
   import {
     Form,
     FormControl,
     FormField,
     FormItem,
     FormLabel,
     FormMessage,
   } from "@/components/ui/form"
   import { Button } from "@/components/ui/button"
   import { Alert, AlertDescription } from "@/components/ui/alert"
   import { Link, useNavigate, useSearchParams } from "@tanstack/react-router"
   import { resetPasswordClient } from "../../lib/api"
   import { PasswordField } from "../molecules/PasswordField"
   import { PasswordStrengthMeter } from "../molecules/PasswordStrengthMeter"
   import { ThemeToggle } from "../atoms/ThemeToggle"

   const resetSchema = z.object({
     password: z.string().min(8, "Senha deve ter no mínimo 8 caracteres"),
     confirmPassword: z.string(),
   }).superRefine((data, ctx) => {
     if (data.password !== data.confirmPassword) {
       ctx.addIssue({ code: z.ZodIssueCode.custom, message: "As senhas não coincidem", path: ["confirmPassword"] })
     }
   })

   export function ResetPasswordPage() {
     const [error, setError] = useState<string | null>(null)
     const navigate = useNavigate()
     const searchParams = useSearchParams()
     const token = searchParams.get("token")

     const form = useForm<z.infer<typeof resetSchema>>({
       resolver: zodResolver(resetSchema),
       defaultValues: { password: "", confirmPassword: "" },
     })

     async function onSubmit(values: z.infer<typeof resetSchema>) {
       if (!token) {
         setError("Token não fornecido")
         return
       }
       try {
         await resetPasswordClient(token, values.password)
         navigate({ to: "/login", state: { message: "Senha alterada com sucesso. Faça login." } })
       } catch (err: any) {
         setError(err.message)
       }
     }

     if (!token) {
       return (
         <div className="min-h-screen flex items-center justify-center bg-background p-4">
           <Card className="w-full max-w-md">
             <CardContent className="pt-6">
               <p className="text-center text-destructive">Token não fornecido</p>
             </CardContent>
           </Card>
         </div>
       )
     }

     return (
       <div className="min-h-screen flex items-center justify-center bg-background p-4">
         <div className="absolute top-4 right-4">
           <ThemeToggle />
         </div>
         <Card className="w-full max-w-md">
           <CardHeader>
             <CardTitle className="text-2xl text-center">Nova Senha</CardTitle>
             <CardDescription className="text-center">
               Defina sua nova senha
             </CardDescription>
           </CardHeader>
           <CardContent>
             <Form {...form}>
               <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-4">
                 <FormField
                   control={form.control}
                   name="password"
                   render={({ field }) => (
                     <FormItem>
                       <FormLabel>Senha</FormLabel>
                       <FormControl>
                         <PasswordField value={field.value} onChange={field.onChange} />
                       </FormControl>
                       <FormMessage />
                     </FormItem>
                   )}
                 />
                 <PasswordStrengthMeter password={form.watch("password")} />

                 <FormField
                   control={form.control}
                   name="confirmPassword"
                   render={({ field }) => (
                     <FormItem>
                       <FormLabel>Confirmar senha</FormLabel>
                       <FormControl>
                         <PasswordField value={field.value} onChange={field.onChange} />
                       </FormControl>
                       <FormMessage />
                     </FormItem>
                   )}
                 />
                 {form.watch("confirmPassword") && form.watch("password") === form.watch("confirmPassword") && (
                   <p className="text-sm text-green-600 dark:text-green-400">✓ As senhas coincidem</p>
                 )}

                 {error && (
                   <Alert variant="destructive">
                     <AlertDescription>{error}</AlertDescription>
                   </Alert>
                 )}

                 <Button type="submit" className="w-full">
                   Alterar senha
                 </Button>
               </form>
             </Form>

             <div className="mt-4 text-center text-sm">
               <Link to="/login" className="text-primary hover:underline">
                 Voltar para login →
               </Link>
             </div>
           </CardContent>
         </Card>
       </div>
     )
   }
   ```

3. **Executar testes de Forgot/Reset — devem passar:**
   ```bash
   cd frontend && npm test -- forgot-password-redesign
   cd frontend && npm test -- reset-password-redesign
   ```

**Output:**
- `ForgotPasswordPage.tsx` redesign com shadcn Card, Form
- `ResetPasswordPage.tsx` redesign com shadcn Card, PasswordField, StrengthMeter
- 11 testes de Forgot/Reset passando (5 forgot + 6 reset)

**Success Criteria:**
- [ ] ForgotPasswordPage com shadcn Card centralizado
- [ ] ThemeToggle visível
- [ ] Email input com label e placeholder
- [ ] Submit button "Enviar link"
- [ ] Success message após envio com botão "Voltar para login"
- [ ] ResetPasswordPage com shadcn Card
- [ ] Password field com show/hide toggle
- [ ] PasswordStrengthMeter visível
- [ ] Confirm password com match indicator
- [ ] Submit button "Alterar senha"
- [ ] Redirect para /login após reset bem-sucedido
- [ ] 11 testes de Forgot/Reset passando

---

### Task 12.3.5: Cleanup — Remover Componentes Legados

**Objective:** Remover componentes substituídos por shadcn/ui

**Actions:**

1. **Remover arquivos legados:**
   ```bash
   rm frontend/src/components/atoms/LabeledField.tsx
   rm frontend/src/components/atoms/AppButton.tsx
   rm frontend/src/components/molecules/PageLayout.tsx
   rm frontend/src/components/organisms/ExampleForm.tsx
   ```

2. **Remover testes legados associados:**
   ```bash
   rm frontend/src/tests/form-validation.test.tsx  # Se referencia ExampleForm
   ```

3. **Verificar imports quebrados:**
   ```bash
   cd frontend && npm run build
   ```
   Se houver erros de import, corrigir referências.

4. **Executar todos os testes:**
   ```bash
   cd frontend && npm test
   ```

**Output:**
- 4 arquivos removidos
- Nenhum import quebrado
- Todos os testes passando

**Success Criteria:**
- [ ] `LabeledField.tsx` removido (substituído por shadcn Form + Input)
- [ ] `AppButton.tsx` removido (substituído por shadcn Button)
- [ ] `PageLayout.tsx` removido (substituído por shadcn Card)
- [ ] `ExampleForm.tsx` removido (scaffold example, não mais necessário)
- [ ] Nenhum import quebrado — `npm run build` passa
- [ ] Todos os testes passando

---

## Verification

### Automated Tests
```bash
cd frontend && npm test
# Expected: 21 redesign tests passing (4 header + 6 profile + 5 forgot + 6 reset)
# Total frontend: 95 existing (Plan 02) + 21 new = 116 tests
```

### Manual Checks
```bash
cd frontend && npm run dev
```

1. **Profile Page:**
   - Navigate to http://localhost:5173/profile (logged in)
   - Verify: Header with logo, theme toggle, user menu
   - Verify: Card with profile data, PF/PJ badge, logout button
   - Click logout → verify: redirects to /login

2. **Forgot Password:**
   - Navigate to http://localhost:5173/forgot-password
   - Verify: Card with email form, clean layout
   - Submit email → verify: success message

3. **Reset Password:**
   - Navigate to http://localhost:5173/reset-password?token=test
   - Verify: Card with password fields + strength meter
   - Enter matching passwords → verify: match indicator

4. **Theme Toggle:**
   - On any page, click theme toggle → verify: smooth transition
   - Navigate between pages → verify: theme persists

### Build Check
```bash
cd frontend && npm run build
# Expected: No errors, no TypeScript issues
```

## Success Criteria (Overall Plan)

This plan is complete when ALL of the following are TRUE:

1. ✅ Header component with logo, theme toggle, user menu
2. ✅ Header is sticky at top with backdrop-blur
3. ✅ User menu dropdown with "Meu Perfil" and "Sair"
4. ✅ ProfilePage with Header, Card, Badge (PF/PJ), Skeleton loading
5. ✅ ProfilePage shows PF data (nome, CPF) or PJ data (razaoSocial, CNPJ)
6. ✅ ForgotPasswordPage with shadcn Card, Form, Input
7. ✅ ResetPasswordPage with shadcn Card, PasswordField, StrengthMeter
8. ✅ Legacy components removed (LabeledField, AppButton, PageLayout, ExampleForm)
9. ✅ No broken imports
10. ✅ 21 new tests passing (4 header + 6 profile + 5 forgot + 6 reset)
11. ✅ Total frontend: 116+ tests passing
12. ✅ `npm run build` sem erros

## Output

- **Files created:**
  - `frontend/src/components/organisms/Header.tsx` — Header with logo, theme toggle, user menu
  - `frontend/src/tests/header.test.tsx` — 4 tests
  - `frontend/src/tests/profile-page-redesign.test.tsx` — 6 tests
  - `frontend/src/tests/forgot-password-redesign.test.tsx` — 5 tests
  - `frontend/src/tests/reset-password-redesign.test.tsx` — 6 tests

- **Files modified:**
  - `frontend/src/components/pages/ProfilePage.tsx` — shadcn Card, Badge, Skeleton, Header
  - `frontend/src/components/pages/ForgotPasswordPage.tsx` — shadcn Card, Form
  - `frontend/src/components/pages/ResetPasswordPage.tsx` — shadcn Card, PasswordField, StrengthMeter
  - `frontend/src/components/atoms/ProfileBadge.tsx` — shadcn Badge

- **Files removed:**
  - `frontend/src/components/atoms/LabeledField.tsx`
  - `frontend/src/components/atoms/AppButton.tsx`
  - `frontend/src/components/molecules/PageLayout.tsx`
  - `frontend/src/components/organisms/ExampleForm.tsx`

- **Tests:** 21 new tests passing

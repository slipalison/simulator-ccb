---
phase: 04-observability
plan: "03"
type: execute
wave: 1
depends_on: ["04-00"]
files_modified:
  - compose.yaml
  - infra/alloy/config.alloy
  - infra/grafana/provisioning/datasources/datasources.yaml
  - infra/tempo/tempo.yaml
  - infra/loki/loki-config.yaml
autonomous: false
requirements: [OBS-02, OBS-03]

must_haves:
  truths:
    - "docker compose up starts Grafana Alloy, Loki, Tempo, Mimir, and Grafana alongside existing services"
    - "Grafana UI is accessible at http://localhost:3000 with Loki, Tempo, and Mimir datasources pre-configured"
    - "Alloy receives OTLP on port 4317 (gRPC) and routes logs→Loki, traces→Tempo, metrics→Mimir"
    - "All 5 new services have healthchecks and do not fail with exit code 1 in compose up"
  artifacts:
    - path: "infra/alloy/config.alloy"
      provides: "OTLP receiver routing logs/traces/metrics to Loki/Tempo/Mimir"
      contains: "otelcol.receiver.otlp"
    - path: "infra/grafana/provisioning/datasources/datasources.yaml"
      provides: "Auto-provisioned Loki, Tempo, Mimir datasources in Grafana"
      contains: "loki"
    - path: "compose.yaml"
      provides: "alloy, loki, tempo, mimir, grafana services with proper healthchecks and networking"
      contains: "grafana/alloy"
  key_links:
    - from: "api service (compose.yaml)"
      to: "alloy service (compose.yaml)"
      via: "OTEL_EXPORTER_OTLP_ENDPOINT: http://alloy:4317"
      pattern: "alloy:4317"
    - from: "infra/alloy/config.alloy"
      to: "loki service"
      via: "loki.write endpoint http://loki:3100/loki/api/v1/push"
      pattern: "loki:3100"
    - from: "infra/alloy/config.alloy"
      to: "tempo service"
      via: "otelcol.exporter.otlp tempo endpoint http://tempo:4317"
      pattern: "tempo:4317"
    - from: "infra/alloy/config.alloy"
      to: "mimir service"
      via: "prometheus.remote_write endpoint http://mimir:9009/api/v1/push"
      pattern: "mimir:9009"
---

<objective>
Add the complete Grafana observability stack (Alloy, Loki, Tempo, Mimir, Grafana) to compose.yaml, create Alloy pipeline configuration, Grafana datasource provisioning, and minimal Loki/Tempo config files. This delivers the local visualization layer for OBS-02 (distributed traces in Tempo) and OBS-03 (metrics in Mimir).

Purpose: Enables developers to see traces, logs, and metrics immediately after `docker compose up` via Grafana at http://localhost:3000.
Output: 5 new Docker services, infra/ config directory, Grafana auto-provisioned with 3 datasources.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-observability/04-CONTEXT.md
@.planning/phases/04-observability/04-RESEARCH.md

<interfaces>
<!-- Key patterns from RESEARCH.md that executor must use directly -->

Grafana Alloy config.alloy pipeline (Pattern 5 from RESEARCH.md):
- Input: OTLP gRPC on 0.0.0.0:4317, HTTP on 0.0.0.0:4318
- Processing: batch processor
- Output: traces→Tempo (OTLP), metrics→Mimir (Prometheus remote_write), logs→Loki

Grafana datasources.yaml provisioning (Pattern 6 from RESEARCH.md):
- Loki: type=loki, url=http://loki:3100
- Tempo: type=tempo, url=http://tempo:3200 (with tracesToLogs linking)
- Mimir: type=prometheus, url=http://mimir:9009/prometheus (isDefault=true)

Docker image versions (from RESEARCH.md):
- grafana/alloy: latest
- grafana/loki: 3
- grafana/tempo: latest
- grafana/mimir: latest
- grafana/grafana: latest

Current compose.yaml services: app_db, keycloak_db, keycloak, api, frontend
New services to add: alloy, loki, tempo, mimir, grafana

The api service in compose.yaml already has (set by Plan 04-02):
- OTEL_EXPORTER_OTLP_ENDPOINT: http://alloy:4317
- OTEL_SERVICE_NAME: onboarding-api
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create infra/ config files for Alloy, Tempo, Loki, and Grafana provisioning</name>
  <files>
    infra/alloy/config.alloy
    infra/grafana/provisioning/datasources/datasources.yaml
    infra/tempo/tempo.yaml
    infra/loki/loki-config.yaml
  </files>
  <read_first>
    - .planning/phases/04-observability/04-RESEARCH.md Pattern 5 (Alloy config) and Pattern 6 (Grafana datasources) — use the exact config from those patterns
    - .planning/phases/04-observability/04-CONTEXT.md D-08 and D-09 (Alloy routing decisions)
  </read_first>
  <action>
    Create the `infra/` directory structure and populate each file.

    **File 1: `infra/alloy/config.alloy`**

    Use Pattern 5 from RESEARCH.md exactly — the `otelcol.exporter.prometheus` block for metrics routing to Mimir via `prometheus.remote_write`:

    ```alloy
    // Grafana Alloy pipeline configuration
    // Receives OTLP from the API, routes to Loki (logs), Tempo (traces), Mimir (metrics)
    // Source: D-08, RESEARCH.md Pattern 5

    otelcol.receiver.otlp "default" {
      grpc {
        endpoint = "0.0.0.0:4317"
      }
      http {
        endpoint = "0.0.0.0:4318"
      }
      output {
        traces  = [otelcol.processor.batch.default.input]
        metrics = [otelcol.processor.batch.default.input]
        logs    = [otelcol.processor.batch.default.input]
      }
    }

    otelcol.processor.batch "default" {
      output {
        traces  = [otelcol.exporter.otlp.tempo.input]
        metrics = [otelcol.exporter.prometheus.mimir.input]
        logs    = [otelcol.exporter.loki.default.input]
      }
    }

    // Traces → Tempo via OTLP gRPC
    otelcol.exporter.otlp "tempo" {
      client {
        endpoint = "http://tempo:4317"
        tls {
          insecure = true
        }
      }
    }

    // Metrics → Mimir via Prometheus remote_write
    otelcol.exporter.prometheus "mimir" {
      forward_to = [prometheus.remote_write.mimir.receiver]
    }

    prometheus.remote_write "mimir" {
      endpoint {
        url = "http://mimir:9009/api/v1/push"
      }
    }

    // Logs → Loki
    otelcol.exporter.loki "default" {
      forward_to = [loki.write.local.receiver]
    }

    loki.write "local" {
      endpoint {
        url = "http://loki:3100/loki/api/v1/push"
      }
    }
    ```

    **File 2: `infra/grafana/provisioning/datasources/datasources.yaml`**

    Use Pattern 6 from RESEARCH.md exactly:

    ```yaml
    # Grafana datasource provisioning (D-09)
    # Auto-configures Loki, Tempo, Mimir on first Grafana boot
    apiVersion: 1

    datasources:
      - name: Loki
        type: loki
        access: proxy
        url: http://loki:3100
        isDefault: false

      - name: Tempo
        type: tempo
        access: proxy
        url: http://tempo:3200
        isDefault: false
        jsonData:
          tracesToLogs:
            datasourceUid: loki
            filterByTraceID: true
            filterBySpanID: false

      - name: Mimir
        type: prometheus
        access: proxy
        url: http://mimir:9009/prometheus
        isDefault: true
    ```

    **File 3: `infra/tempo/tempo.yaml`**

    Minimal single-binary Tempo config for local dev:

    ```yaml
    # Grafana Tempo — single-binary mode for local development
    stream_over_http_enabled: true
    server:
      http_listen_port: 3200
      log_level: info

    distributor:
      receivers:
        otlp:
          protocols:
            grpc:
              endpoint: 0.0.0.0:4317
            http:
              endpoint: 0.0.0.0:4318

    ingester:
      max_block_duration: 5m

    compactor:
      compaction:
        block_retention: 48h

    storage:
      trace:
        backend: local
        local:
          path: /var/tempo/traces
        wal:
          path: /var/tempo/wal
    ```

    **File 4: `infra/loki/loki-config.yaml`**

    Minimal single-process Loki config for local dev:

    ```yaml
    # Grafana Loki — single-process mode for local development
    auth_enabled: false

    server:
      http_listen_port: 3100
      grpc_listen_port: 9096

    common:
      instance_addr: 127.0.0.1
      path_prefix: /loki
      storage:
        filesystem:
          chunks_directory: /loki/chunks
          rules_directory: /loki/rules
      replication_factor: 1
      ring:
        kvstore:
          store: inmemory

    query_range:
      results_cache:
        cache:
          embedded_cache:
            enabled: true
            max_size_mb: 100

    schema_config:
      configs:
        - from: 2024-01-01
          store: tsdb
          object_store: filesystem
          schema: v13
          index:
            prefix: index_
            period: 24h

    ruler:
      alertmanager_url: http://localhost:9093
    ```
  </action>
  <verify>
    <automated>ls infra/alloy/config.alloy infra/grafana/provisioning/datasources/datasources.yaml infra/tempo/tempo.yaml infra/loki/loki-config.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `infra/alloy/config.alloy` exists and contains `otelcol.receiver.otlp`
    - `infra/alloy/config.alloy` contains `http://loki:3100/loki/api/v1/push`
    - `infra/alloy/config.alloy` contains `http://tempo:4317`
    - `infra/alloy/config.alloy` contains `http://mimir:9009/api/v1/push`
    - `infra/grafana/provisioning/datasources/datasources.yaml` contains `type: loki`
    - `infra/grafana/provisioning/datasources/datasources.yaml` contains `type: tempo`
    - `infra/grafana/provisioning/datasources/datasources.yaml` contains `type: prometheus`
    - `infra/tempo/tempo.yaml` exists with `http_listen_port: 3200`
    - `infra/loki/loki-config.yaml` exists with `http_listen_port: 3100`
  </acceptance_criteria>
  <done>All infra config files created. Alloy pipeline routes OTLP to all three backends. Grafana datasources provisioned.</done>
</task>

<task type="auto">
  <name>Task 2: Add Grafana stack services to compose.yaml</name>
  <files>
    compose.yaml
  </files>
  <read_first>
    - compose.yaml (read ENTIRE current file — must understand existing service names, network, volumes before appending)
    - infra/alloy/config.alloy (confirm path for volume mount)
    - infra/grafana/provisioning/datasources/datasources.yaml (confirm path for Grafana volume mount)
    - infra/tempo/tempo.yaml (confirm path for Tempo volume mount)
    - infra/loki/loki-config.yaml (confirm path for Loki volume mount)
  </read_first>
  <action>
    Edit `compose.yaml` to add 5 new services after the existing `frontend` service. Do NOT modify any existing service definitions in this task.

    Add the following services to the `services:` block:

    ```yaml
      # ── Observability Stack (Phase 4) ─────────────────────────────────────────

      alloy:
        image: grafana/alloy:latest
        volumes:
          - ./infra/alloy/config.alloy:/etc/alloy/config.alloy:ro
        command: ["run", "--server.http.listen-addr=0.0.0.0:12345", "/etc/alloy/config.alloy"]
        ports:
          - "127.0.0.1:12345:12345"   # Alloy UI (optional, useful for debugging)
          - "127.0.0.1:4317:4317"     # OTLP gRPC (exposed for direct testing outside compose)
          - "127.0.0.1:4318:4318"     # OTLP HTTP
        depends_on:
          loki:
            condition: service_started
          tempo:
            condition: service_started
          mimir:
            condition: service_started
        healthcheck:
          test: ["CMD-SHELL", "wget -qO- http://localhost:12345/-/ready || exit 1"]
          interval: 10s
          timeout: 5s
          retries: 5
          start_period: 20s

      loki:
        image: grafana/loki:3
        volumes:
          - ./infra/loki/loki-config.yaml:/etc/loki/local-config.yaml:ro
          - loki_data:/loki
        command: ["-config.file=/etc/loki/local-config.yaml"]
        ports:
          - "127.0.0.1:3100:3100"
        healthcheck:
          test: ["CMD-SHELL", "wget -qO- http://localhost:3100/ready || exit 1"]
          interval: 10s
          timeout: 5s
          retries: 5
          start_period: 30s

      tempo:
        image: grafana/tempo:latest
        volumes:
          - ./infra/tempo/tempo.yaml:/etc/tempo.yaml:ro
          - tempo_data:/var/tempo
        command: ["-config.file=/etc/tempo.yaml"]
        ports:
          - "127.0.0.1:3200:3200"   # Tempo HTTP API (Grafana datasource)
        healthcheck:
          test: ["CMD-SHELL", "wget -qO- http://localhost:3200/ready || exit 1"]
          interval: 10s
          timeout: 5s
          retries: 5
          start_period: 30s

      mimir:
        image: grafana/mimir:latest
        volumes:
          - mimir_data:/data
        command:
          - "--ingester.native-histograms-ingestion-enabled=true"
          - "--config.expand-env=true"
        ports:
          - "127.0.0.1:9009:9009"
        healthcheck:
          test: ["CMD-SHELL", "wget -qO- http://localhost:9009/ready || exit 1"]
          interval: 10s
          timeout: 5s
          retries: 5
          start_period: 30s

      grafana:
        image: grafana/grafana:latest
        volumes:
          - ./infra/grafana/provisioning/datasources:/etc/grafana/provisioning/datasources:ro
          - grafana_data:/var/lib/grafana
        environment:
          GF_SECURITY_ADMIN_USER: admin
          GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-admin}
          GF_AUTH_ANONYMOUS_ENABLED: "true"
          GF_AUTH_ANONYMOUS_ORG_ROLE: Viewer
        ports:
          - "127.0.0.1:3000:3000"   # D-09: Grafana UI
        depends_on:
          loki:
            condition: service_started
          tempo:
            condition: service_started
          mimir:
            condition: service_started
        healthcheck:
          test: ["CMD-SHELL", "wget -qO- http://localhost:3000/api/health || exit 1"]
          interval: 10s
          timeout: 5s
          retries: 5
          start_period: 30s
    ```

    Also add the new named volumes to the `volumes:` section at the bottom of compose.yaml:
    ```yaml
      loki_data:
      tempo_data:
      mimir_data:
      grafana_data:
    ```

    Note about Mimir single-process mode: Mimir 2.x requires a config file for production use, but for local dev you can pass command-line flags to use default single-process (monolithic) mode. If `grafana/mimir:latest` refuses to start without a config file, create `infra/mimir/mimir.yaml` with minimal config:

    ```yaml
    # infra/mimir/mimir.yaml — minimal single-process dev config
    multitenancy_enabled: false
    blocks_storage:
      backend: filesystem
      filesystem:
        dir: /data/blocks
      bucket_store:
        sync_dir: /data/tsdb-sync
      tsdb:
        dir: /data/tsdb
    compactor:
      data_dir: /data/data-compactor
    ruler_storage:
      backend: filesystem
      filesystem:
        dir: /data/rules
    common:
      storage:
        backend: filesystem
        filesystem:
          dir: /data/blocks
    ```

    And update the mimir service in compose.yaml to:
    ```yaml
      mimir:
        image: grafana/mimir:latest
        volumes:
          - ./infra/mimir/mimir.yaml:/etc/mimir/mimir.yaml:ro
          - mimir_data:/data
        command: ["--config.file=/etc/mimir/mimir.yaml"]
    ```

    Create `infra/mimir/mimir.yaml` if needed.
  </action>
  <verify>
    <automated>grep -c "grafana/alloy\|grafana/loki\|grafana/tempo\|grafana/mimir\|grafana/grafana" compose.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `compose.yaml` contains `grafana/alloy:latest`
    - `compose.yaml` contains `grafana/loki:3`
    - `compose.yaml` contains `grafana/tempo:latest`
    - `compose.yaml` contains `grafana/mimir:latest`
    - `compose.yaml` contains `grafana/grafana:latest`
    - `compose.yaml` volumes section contains `loki_data:`, `tempo_data:`, `mimir_data:`, `grafana_data:`
    - `compose.yaml` grafana service has port `3000:3000`
    - `compose.yaml` alloy service has port `4317:4317`
    - `grep -c "grafana/alloy\|grafana/loki\|grafana/tempo\|grafana/mimir\|grafana/grafana" compose.yaml` returns 5
    - `docker compose config` exits 0 (compose.yaml is valid YAML)
  </acceptance_criteria>
  <done>All 5 Grafana stack services added to compose.yaml with healthchecks, volumes, and port bindings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Grafana stack end-to-end via Docker Compose</name>
  <files>compose.yaml</files>
  <what-built>
    Grafana observability stack services added to compose.yaml with Alloy routing OTLP data to Loki/Tempo/Mimir, and Grafana pre-provisioned with all three datasources. The API (from Plans 04-01 and 04-02) exports OTLP to Alloy at http://alloy:4317.
  </what-built>
  <how-to-verify>
    1. Build the API image: `docker compose build api`
    2. Start all services: `docker compose up -d`
    3. Wait ~60 seconds for all services to become healthy
    4. Check all services are running: `docker compose ps` — every service should show "running" or "healthy"
    5. Open Grafana at http://localhost:3000 (login: admin / admin)
    6. Navigate to Connections → Data Sources — verify Loki, Tempo, and Mimir are listed
    7. Make a test request to the API: `curl http://localhost:8080/healthz/live` — should return 200
    8. In Grafana, open Explore → select Loki datasource → query `{service_name="onboarding-api"}` — should see JSON log entries with TraceId and SpanId fields
    9. Open Explore → select Tempo datasource → search for recent traces — should see spans
    10. Confirm: `docker compose ps` shows all 10 services (app_db, keycloak_db, keycloak, api, frontend, alloy, loki, tempo, mimir, grafana) without Exit status
  </how-to-verify>
  <action>Run docker compose build api then docker compose up -d. Wait 60 seconds. Open http://localhost:3000. Verify Loki, Tempo, Mimir datasources appear under Connections Data Sources. Run curl http://localhost:8080/healthz/live and confirm 200. Open Grafana Explore Loki, query {service_name=onboarding-api} and confirm log entries contain TraceId and SpanId fields.</action>
  <verify>
    <automated>docker compose config 2>&1 | tail -3</automated>
  </verify>
  <done>All 10 services healthy. Grafana at localhost:3000 shows Loki, Tempo, Mimir datasources. API log entries with TraceId visible in Loki Explore.</done>
  <resume-signal>Type "approved" if Grafana shows datasources and logs appear from the API. Describe any service that fails to start or any datasource that shows errors.</resume-signal>
</task>

</tasks>

<verification>
After Plan 03 (pre-checkpoint):
- `grep -c "grafana/alloy\|grafana/loki\|grafana/tempo\|grafana/mimir\|grafana/grafana" compose.yaml` returns 5
- `docker compose config` exits 0 (valid YAML)
- All infra config files exist and contain required keywords (verified by Task 1 acceptance criteria)

After checkpoint approval:
- All 10 services healthy in `docker compose ps`
- Grafana at localhost:3000 shows Loki, Tempo, Mimir datasources
- API logs visible in Grafana Loki with TraceId and SpanId fields
</verification>

<success_criteria>
`docker compose up` starts the full observability stack. Grafana is accessible at http://localhost:3000 with pre-provisioned datasources. API telemetry flows through Alloy to all three backends (OBS-02, OBS-03 verified end-to-end).
</success_criteria>

<output>
After completion, create `.planning/phases/04-observability/04-03-SUMMARY.md`
</output>

---
phase: 36-admin-management-ui
plan: 04
type: execute
wave: 3
depends_on:
  - "36-01"
  - "36-02"
  - "36-03"
files_modified:
  - frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx
autonomous: true
requirements:
  - MGMT-01
  - MGMT-02
  - MGMT-03
  - MGMT-04
  - MGMT-05
  - MGMT-06

must_haves:
  truths:
    - "Tabela paginada (20/page) com filtros nome, email e status funciona sem reload de página completa"
    - "Qualquer mudança em filtros ou busca reseta para página 1 (D-12)"
    - "Editar abre EditAdminDialog pré-populado; save bem-sucedido fecha dialog + toast + refetch"
    - "Resetar senha chama API antes de abrir dialog; dialog exibe senha gerada; fechar descarta senha"
    - "Desativar abre DeactivateAdminDialog; erro 409 mostra toast específico de último admin ativo (SEC-05)"
    - "Reativar abre ReactivateAdminDialog; confirmação dispara API + toast + refetch"
    - "Toolbar com dois AdminSearchBar (nome, email) e AdminStatusFilter com ADMIN_STATUS_OPTIONS"
    - "Loading skeleton durante fetch inicial; refetch com opacity-60; empty state e error state corretos"
  artifacts:
    - path: "frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx"
      provides: "Página completa de gestão de administradores com paginação, filtros e 4 ações"
      contains: "getAdministratorsPaginated|EditAdminDialog|ResetPasswordDialog|DeactivateAdminDialog|ReactivateAdminDialog"
  key_links:
    - from: "frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx"
      to: "frontend/backoffice/src/lib/admin-api.ts"
      via: "getAdministratorsPaginated, updateAdministrator, resetAdministratorPassword, toggleAdministratorStatus"
      pattern: "getAdministratorsPaginated"
    - from: "frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx"
      to: "frontend/backoffice/src/components/molecules/AdminAdministratorsTable.tsx"
      via: "result, isLoading, isError, onEdit, onResetPassword, onDeactivate, onReactivate props"
      pattern: "AdminAdministratorsTable"
---

<objective>
Reescrever AdminAdministratorsPage.tsx integrando todos os componentes criados nos planos anteriores: busca paginada com dois campos de filtro + status, os quatro dialogs de ação, e feedback via toast para cada operação.

Purpose: Entrega final da fase — a página existente é completamente substituída pela implementação completa com todas as funcionalidades MGMT-01 a MGMT-06.
Output: AdminAdministratorsPage.tsx reescrito — a rota /admin/administrators passa a ter funcionalidade completa.
</objective>

<execution_context>
@D:\REPO\keycloak-tests\.claude\get-shit-done\workflows\execute-plan.md
@D:\REPO\keycloak-tests\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@D:\REPO\keycloak-tests\.planning\phases\36-admin-management-ui\36-CONTEXT.md
@D:\REPO\keycloak-tests\.planning\phases\36-admin-management-ui\36-UI-SPEC.md

<interfaces>
<!-- Contratos estabelecidos nos planos anteriores -->

De frontend/backoffice/src/lib/admin-api.ts (Plano 01):
```typescript
export interface AdminUserDto {
  id: string;
  email: string;
  fullName: string;
  isEnabled: boolean;
  hasTemporaryPassword: boolean;
}
export interface PaginatedResult<T> {
  items: T[];
  totalCount: number;
  page: number;
  pageSize: number;
}
export async function getAdministratorsPaginated(params: GetAdministratorsPaginatedParams): Promise<PaginatedResult<AdminUserDto>>
export async function updateAdministrator(adminId: string, data: { fullName: string; email: string }): Promise<void>
export async function resetAdministratorPassword(adminId: string, targetUserName: string): Promise<ResetAdministratorPasswordResult>
export async function toggleAdministratorStatus(adminId: string, targetUserName: string, activate: boolean, reason?: string): Promise<void>
export class AdminApiError extends Error { status?: number }
```

De frontend/backoffice/src/components/molecules/ (Planos 02 e 03):
```typescript
// AdminAdministratorsTable — props:
interface AdminAdministratorsTableProps {
  result: PaginatedResult<AdminUserDto> | null;
  isLoading: boolean;
  isError: boolean;
  onRetry: () => void;
  onEdit: (admin: AdminUserDto) => void;
  onResetPassword: (admin: AdminUserDto) => void;
  onDeactivate: (admin: AdminUserDto) => void;
  onReactivate: (admin: AdminUserDto) => void;
}

// EditAdminDialog — props:
interface EditAdminDialogProps {
  open: boolean;
  admin: AdminUserDto | null;
  onClose: () => void;
  onSave: (adminId: string, data: AdminEditAdministratorInput) => Promise<void>;
}

// ResetPasswordDialog — props:
interface ResetPasswordDialogProps {
  open: boolean;
  generatedPassword: string | null;
  onClose: () => void;
}

// DeactivateAdminDialog — props:
interface DeactivateAdminDialogProps {
  open: boolean;
  admin: AdminUserDto | null;
  onClose: () => void;
  onDeactivate: (adminId: string) => Promise<void>;
}

// ReactivateAdminDialog — props:
interface ReactivateAdminDialogProps {
  open: boolean;
  admin: AdminUserDto | null;
  onClose: () => void;
  onReactivate: (adminId: string) => Promise<void>;
}
```

De frontend/backoffice/src/components/molecules/ (existentes, Plano 01):
```typescript
// AdminSearchBar — props: value, onChange, placeholder, disabled
// AdminPagination — props: page, pageSize, totalCount, onPageChange
// AdminStatusFilter — props: value, onChange, disabled, options?
// ADMIN_STATUS_OPTIONS exportado de AdminStatusFilter (após Plano 01)
```

Padrão AdminUsersPage.tsx (referência para orquestração de estado):
```typescript
// Estado: page, nameSearch, emailSearch, status, result, isLoading, isError
// useEffect([page, debouncedName, debouncedEmail, status]) → fetchAdmins()
// useEffect([nameSearch]) → debounce 300ms → setDebouncedName
// useEffect([debouncedName, debouncedEmail, status]) → setPage(1)
// Nota: AdminSearchBar já tem debounce interno — não duplicar com useEffect externo
```

Toast via Sonner:
```typescript
import { toast } from "sonner";
toast.success("Mensagem")
toast.error("Título", { description: "Detalhe" })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever AdminAdministratorsPage com orquestração de estado, filtros e dialogs</name>
  <files>frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx</files>
  <read_first>
    - frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx — ler arquivo atual COMPLETO antes de substituir
    - frontend/backoffice/src/components/pages/AdminUsersPage.tsx — ler padrão completo de orquestração de estado (useCallback, useEffect para debounce/reset de página)
    - frontend/backoffice/src/components/molecules/AdminSearchBar.tsx — confirmar que debounce é INTERNO (não duplicar useEffect externo)
    - frontend/backoffice/src/components/molecules/AdminPagination.tsx — confirmar props: page, pageSize, totalCount, onPageChange
    - frontend/backoffice/src/components/molecules/AdminStatusFilter.tsx — confirmar que ADMIN_STATUS_OPTIONS está exportado após Plano 01
  </read_first>
  <action>
Substituir o conteúdo completo de frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx pelo seguinte:

```typescript
import { useState, useEffect, useCallback } from "react";
import { Card, CardContent, CardHeader } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Shield, RefreshCw, UserPlus } from "lucide-react";
import { toast } from "sonner";
import {
  getAdministratorsPaginated,
  updateAdministrator,
  resetAdministratorPassword,
  toggleAdministratorStatus,
  AdminApiError,
} from "@/lib/admin-api";
import type { AdminUserDto, PaginatedResult } from "@/lib/admin-api";
import { AdminSearchBar } from "@/components/molecules/AdminSearchBar";
import { AdminStatusFilter, ADMIN_STATUS_OPTIONS } from "@/components/molecules/AdminStatusFilter";
import { AdminPagination } from "@/components/molecules/AdminPagination";
import { AdminAdministratorsTable } from "@/components/molecules/AdminAdministratorsTable";
import { EditAdminDialog } from "@/components/molecules/EditAdminDialog";
import { ResetPasswordDialog } from "@/components/molecules/ResetPasswordDialog";
import { DeactivateAdminDialog } from "@/components/molecules/DeactivateAdminDialog";
import { ReactivateAdminDialog } from "@/components/molecules/ReactivateAdminDialog";
import type { AdminEditAdministratorInput } from "@/lib/validation-schemas";

// ---------------------------------------------------------------------------
// Estado dos dialogs
// ---------------------------------------------------------------------------
type DialogState =
  | { type: "none" }
  | { type: "edit"; admin: AdminUserDto }
  | { type: "reset-password"; password: string }
  | { type: "deactivate"; admin: AdminUserDto }
  | { type: "reactivate"; admin: AdminUserDto };

export function AdminAdministratorsPage() {
  // Filtros e paginação
  const [page, setPage] = useState(1);
  const [nameSearch, setNameSearch] = useState("");
  const [emailSearch, setEmailSearch] = useState("");
  const [status, setStatus] = useState("all");

  // Estado da lista
  const [result, setResult] = useState<PaginatedResult<AdminUserDto> | null>(null);
  const [isLoading, setIsLoading] = useState(true);
  const [isError, setIsError] = useState(false);

  // Estado dos dialogs
  const [dialog, setDialog] = useState<DialogState>({ type: "none" });

  // D-12: qualquer mudança em filtros reseta para página 1
  // AdminSearchBar já tem debounce interno — não adicionar useEffect de debounce externo
  // Usamos o valor diretamente como dependência do fetch
  useEffect(() => {
    setPage(1);
  }, [nameSearch, emailSearch, status]);

  const fetchAdmins = useCallback(async () => {
    setIsLoading(true);
    setIsError(false);
    try {
      const data = await getAdministratorsPaginated({
        page,
        pageSize: 20,
        name: nameSearch || undefined,
        email: emailSearch || undefined,
        status: status === "all" ? undefined : status,
      });
      setResult(data);
    } catch {
      setIsError(true);
    } finally {
      setIsLoading(false);
    }
  }, [page, nameSearch, emailSearch, status]);

  useEffect(() => {
    fetchAdmins();
  }, [fetchAdmins]);

  // ---------------------------------------------------------------------------
  // Handlers dos dialogs — chamados pelo AdminActionsDropdown via AdminAdministratorsTable
  // ---------------------------------------------------------------------------

  const handleOpenEdit = (admin: AdminUserDto) => {
    setDialog({ type: "edit", admin });
  };

  const handleOpenResetPassword = async (admin: AdminUserDto) => {
    // Chamada API ANTES de abrir o dialog (UI-SPEC ResetPasswordDialog fluxo)
    try {
      const result = await resetAdministratorPassword(admin.id, admin.fullName);
      // Opcional: toast de confirmação de que a operação ocorreu
      toast.success("Senha temporária gerada.");
      setDialog({ type: "reset-password", password: result.temporaryPassword });
    } catch (err) {
      const status = err instanceof AdminApiError ? err.status : undefined;
      toast.error("Falha ao resetar senha", {
        description: status === 400 ? "Operação não permitida." : "Tente novamente.",
      });
    }
  };

  const handleOpenDeactivate = (admin: AdminUserDto) => {
    setDialog({ type: "deactivate", admin });
  };

  const handleOpenReactivate = (admin: AdminUserDto) => {
    setDialog({ type: "reactivate", admin });
  };

  const handleCloseDialog = () => {
    setDialog({ type: "none" });
  };

  // ---------------------------------------------------------------------------
  // Handlers de ação — executados dentro dos dialogs (passados como callbacks)
  // ---------------------------------------------------------------------------

  const handleSaveEdit = async (adminId: string, data: AdminEditAdministratorInput) => {
    try {
      await updateAdministrator(adminId, { fullName: data.fullName, email: data.email });
      toast.success("Administrador atualizado com sucesso.");
      fetchAdmins();
    } catch (err) {
      const apiErr = err instanceof AdminApiError ? err : null;
      if (apiErr?.status === 409) {
        toast.error("Email já está em uso.", {
          description: "Escolha outro email e tente novamente.",
        });
      } else {
        toast.error("Falha ao atualizar administrador", { description: "Tente novamente." });
      }
      // Re-lançar para que EditAdminDialog não feche (onSave lançou)
      throw err;
    }
  };

  const handleDeactivate = async (adminId: string) => {
    try {
      await toggleAdministratorStatus(
        adminId,
        // fullName disponível via dialog.admin quando type === "deactivate"
        dialog.type === "deactivate" ? dialog.admin.fullName : "",
        false // activate=false → desativar
      );
      toast.success("Administrador desativado.");
      fetchAdmins();
    } catch (err) {
      const apiErr = err instanceof AdminApiError ? err : null;
      if (apiErr?.status === 400 || apiErr?.status === 409) {
        toast.error("Não é possível desativar.", {
          description: "Deve existir ao menos um administrador ativo.",
        });
      } else {
        toast.error("Falha ao desativar administrador", { description: "Tente novamente." });
      }
      throw err;
    }
  };

  const handleReactivate = async (adminId: string) => {
    try {
      await toggleAdministratorStatus(
        adminId,
        dialog.type === "reactivate" ? dialog.admin.fullName : "",
        true // activate=true → reativar
      );
      toast.success("Administrador reativado.");
      fetchAdmins();
    } catch {
      toast.error("Falha ao reativar administrador", { description: "Tente novamente." });
      throw new Error("reactivate failed");
    }
  };

  // ---------------------------------------------------------------------------
  // Render
  // ---------------------------------------------------------------------------

  return (
    <div className="space-y-6" data-testid="admin-administrators-page">
      {/* Cabeçalho */}
      <div className="flex items-center justify-between">
        <div className="flex items-center gap-2">
          <Shield className="h-5 w-5 text-primary" aria-hidden="true" />
          <h2 className="text-xl font-semibold">Administradores</h2>
        </div>
        <div className="flex gap-2">
          <Button
            variant="outline"
            size="sm"
            onClick={fetchAdmins}
            disabled={isLoading}
            data-testid="refresh-button"
          >
            <RefreshCw className={`h-4 w-4 mr-1 ${isLoading ? "animate-spin" : ""}`} aria-hidden="true" />
            Atualizar
          </Button>
          <Button
            size="sm"
            onClick={() => { window.location.href = "/admin/create"; }}
            data-testid="create-admin-button"
          >
            <UserPlus className="h-4 w-4 mr-1" aria-hidden="true" />
            Criar Admin
          </Button>
        </div>
      </div>

      {/* Toolbar de filtros — D-09, D-10, D-11 */}
      <div className="flex flex-col sm:flex-row items-start sm:items-center gap-4">
        <AdminSearchBar
          value={nameSearch}
          onChange={(val) => setNameSearch(val)}
          placeholder="Buscar por nome..."
          disabled={isLoading}
        />
        <AdminSearchBar
          value={emailSearch}
          onChange={(val) => setEmailSearch(val)}
          placeholder="Buscar por email..."
          disabled={isLoading}
        />
        <AdminStatusFilter
          value={status}
          onChange={(val) => setStatus(val)}
          disabled={isLoading}
          options={ADMIN_STATUS_OPTIONS}
        />
      </div>

      {/* Tabela */}
      <Card>
        <CardHeader className="p-0" />
        <CardContent className="p-0">
          <AdminAdministratorsTable
            result={result}
            isLoading={isLoading}
            isError={isError}
            onRetry={fetchAdmins}
            onEdit={handleOpenEdit}
            onResetPassword={handleOpenResetPassword}
            onDeactivate={handleOpenDeactivate}
            onReactivate={handleOpenReactivate}
          />
        </CardContent>
      </Card>

      {/* Paginação — exibida somente quando há resultados */}
      {result && result.totalCount > 0 && (
        <div className="mt-4">
          <AdminPagination
            page={page}
            pageSize={20}
            totalCount={result.totalCount}
            onPageChange={setPage}
          />
        </div>
      )}

      {/* Dialogs */}
      <EditAdminDialog
        open={dialog.type === "edit"}
        admin={dialog.type === "edit" ? dialog.admin : null}
        onClose={handleCloseDialog}
        onSave={handleSaveEdit}
      />

      <ResetPasswordDialog
        open={dialog.type === "reset-password"}
        generatedPassword={dialog.type === "reset-password" ? dialog.password : null}
        onClose={handleCloseDialog}
      />

      <DeactivateAdminDialog
        open={dialog.type === "deactivate"}
        admin={dialog.type === "deactivate" ? dialog.admin : null}
        onClose={handleCloseDialog}
        onDeactivate={handleDeactivate}
      />

      <ReactivateAdminDialog
        open={dialog.type === "reactivate"}
        admin={dialog.type === "reactivate" ? dialog.admin : null}
        onClose={handleCloseDialog}
        onReactivate={handleReactivate}
      />
    </div>
  );
}
```

Notas de implementação:
- D-12: useEffect([nameSearch, emailSearch, status]) → setPage(1) — reset de página em qualquer mudança de filtro
- AdminSearchBar já tem debounce interno de 300ms — não adicionar useEffect de debounce externo (duplicaria o debounce)
- handleOpenResetPassword chama API ANTES de abrir dialog (UI-SPEC)
- handleSaveEdit re-lança erro para que EditAdminDialog mantenha o dialog aberto em caso de falha (não fechar no catch)
- handleDeactivate: erro 400 (self) ou 409 (último admin) → toast com mensagem específica de SEC-05
- DialogState discriminated union garante type-safety no acesso às props de cada dialog
- ADMIN_STATUS_OPTIONS importado de AdminStatusFilter (Plano 01) para variante de admins
- Paginação renderizada somente quando result.totalCount > 0
- Botão "Criar Admin" mantém comportamento atual (redirect para /admin/create)
  </action>
  <verify>
    <automated>cd "D:/REPO/keycloak-tests/frontend/backoffice" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "getAdministratorsPaginated" AdminAdministratorsPage.tsx retorna match (importação e uso)
    - grep "ADMIN_STATUS_OPTIONS" AdminAdministratorsPage.tsx retorna match (importação para AdminStatusFilter)
    - grep "EditAdminDialog\|ResetPasswordDialog\|DeactivateAdminDialog\|ReactivateAdminDialog" AdminAdministratorsPage.tsx retorna 4 matches de importação E 4 de uso no JSX
    - grep "AdminAdministratorsTable" AdminAdministratorsPage.tsx retorna match (importação e uso)
    - grep "setPage(1)" AdminAdministratorsPage.tsx retorna match dentro de useEffect([nameSearch, emailSearch, status]) — reset de página
    - grep "nameSearch\|emailSearch" AdminAdministratorsPage.tsx retorna matches (dois campos de busca separados D-09)
    - grep "Deve existir ao menos um administrador ativo" AdminAdministratorsPage.tsx retorna match (SEC-05 toast)
    - grep "Email já está em uso" AdminAdministratorsPage.tsx retorna match (SEC-04 toast)
    - grep "mt-4" AdminAdministratorsPage.tsx retorna match (posicionamento da paginação)
    - grep "type.*none.*edit.*reset-password.*deactivate.*reactivate\|DialogState" AdminAdministratorsPage.tsx retorna match
    - npx tsc --noEmit no diretório backoffice não retorna erros
  </acceptance_criteria>
  <done>AdminAdministratorsPage reescrita com: paginação 20/page, dois campos de busca (nome e email), status filter para admins, skeleton loading, refetch com opacity, todos os 4 dialogs integrados, toasts corretos para cada ação incluindo erros SEC-01/SEC-04/SEC-05, reset de página em mudança de filtro.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Frontend page → API | Operações de edição, reset, desativação e reativação cruzam para o backend |
| Usuário → Página | Inputs de filtro não validados são passados como query params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-08 | Elevation of Privilege | AdminAdministratorsPage — todas as ações | mitigate | [Authorize(Roles = "admin", AuthenticationSchemes = "BearerBackoffice")] no backend; 401 → redirect automático via fetchWithAuth |
| T-36-09 | Tampering | handleDeactivate — toggle com activate=false | mitigate | SEC-05 enforçado server-side com 400/409; toast específico exibido; frontend não assume sucesso antes de resposta |
| T-36-10 | Information Disclosure | handleOpenResetPassword — senha retornada pelo backend | mitigate | Senha nunca salva em localStorage; mantida apenas em estado React (DialogState) e descartada ao fechar (D-05) |
| T-36-11 | Spoofing | nameSearch/emailSearch inputs — query injection | accept | Valores passados como query params em URL; backend sanitiza entradas via FluentValidation e Keycloak Admin API |
</threat_model>

<verification>
1. `cd D:/REPO/keycloak-tests/frontend/backoffice && npx tsc --noEmit` — zero erros
2. `grep -c "EditAdminDialog\|ResetPasswordDialog\|DeactivateAdminDialog\|ReactivateAdminDialog" frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx` — retorna 8+ (importações + uso JSX)
3. `grep "setPage(1)" frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx` — retorna match
4. `grep "ADMIN_STATUS_OPTIONS" frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx` — retorna match
5. `grep "Deve existir ao menos um administrador ativo" frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx` — retorna match

Verificação funcional (manual ou com docker compose up):
- Navegar para /admin/administrators — tabela carrega com skeleton e exibe admins
- Digitar em "Buscar por nome..." — tabela filtra após 300ms sem reload
- Mudar status filter — tabela filtra e página reseta para 1
- Clicar ⋯ na própria linha — botão desabilitado com tooltip
- Clicar ⋯ em outra linha → "Editar" → dialog abre pré-populado
- Clicar "Resetar senha" → API chamada → dialog com senha monospace
- Clicar "Desativar" → dialog de confirmação → sucesso com toast
</verification>

<success_criteria>
- AdminAdministratorsPage substituída por implementação completa cobrindo MGMT-01 a MGMT-06
- Paginação de 20/página com AdminPagination
- Dois campos de busca separados (nome e email, D-09) com debounce interno do AdminSearchBar
- Status filter com ADMIN_STATUS_OPTIONS (Todos/Ativo/Inativo, D-11)
- Reset de página em qualquer mudança de filtro (D-12)
- Skeleton durante loading inicial; opacity-60 durante refetch (D-13)
- Quatro dialogs integrados com callbacks corretos
- Toast corretos para cada ação: editar, reset, desativar (incluindo erro SEC-05), reativar
- Senha descartada ao fechar ResetPasswordDialog (D-05)
- Zero erros TypeScript
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/36-admin-management-ui/36-04-SUMMARY.md`
</output>

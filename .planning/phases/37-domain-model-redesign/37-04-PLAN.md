---
phase: 37-domain-model-redesign
plan: 04
type: execute
wave: 3
depends_on: ["37-01", "37-02", "37-03"]
files_modified:
  - src/Onboarding.Application/Clients/Commands/RegisterClientCommand.cs
  - src/Onboarding.Application/Clients/Commands/RegisterClientCommandHandler.cs
  - src/Onboarding.Application/Clients/Validators/RegisterClientCommandValidator.cs
  - src/Onboarding.Application/Clients/DTOs/RegisterClientResult.cs
  - src/Onboarding.Application/Admin/Commands/UpdateUserCommand.cs
  - src/Onboarding.Application/Admin/Commands/DeleteUserCommand.cs
  - src/Onboarding.Application/Admin/Commands/BlockUserCommand.cs
  - src/Onboarding.Application/Admin/Commands/UnblockUserCommand.cs
  - src/Onboarding.Application/Admin/Queries/GetPaginatedUsersQuery.cs
  - src/Onboarding.Application/Admin/Queries/GetUserDetailsQuery.cs
  - src/Onboarding.Application/Admin/DTOs/UserSummaryDto.cs
  - src/Onboarding.Application/Admin/DTOs/UserDetailDto.cs
  - src/Onboarding.Application/Admin/DTOs/UpdateUserRequest.cs
  - src/Onboarding.Application/Admin/DTOs/CompanySummaryDto.cs
  - src/Onboarding.Application/Admin/DTOs/EmployeeSummaryDto.cs
  - src/Onboarding.Application/Admin/Commands/UpdateCompanyCommand.cs
  - src/Onboarding.Application/Admin/Commands/DeleteEmployeeCommand.cs
  - src/Onboarding.Application/Admin/Commands/BlockEmployeeCommand.cs
  - src/Onboarding.Application/Admin/Commands/UnblockEmployeeCommand.cs
  - src/Onboarding.Application/Admin/Queries/GetPaginatedCompaniesQuery.cs
  - src/Onboarding.Application/Admin/Queries/GetPaginatedEmployeesQuery.cs
  - src/Onboarding.Application/Admin/Queries/GetCompanyDetailsQuery.cs
  - src/Onboarding.Application/Admin/Queries/GetEmployeeDetailsQuery.cs
  - src/Onboarding.API/Controllers/RegistrationController.cs
  - src/Onboarding.API/Controllers/ClientsController.cs
  - src/Onboarding.API/Controllers/AdminUserController.cs
  - src/Onboarding.API/Controllers/RegisterClientRequest.cs
  - src/Onboarding.API/Controllers/CompaniesController.cs
  - src/Onboarding.API/Program.cs
autonomous: true
requirements:
  - REG-02
  - REG-04
  - REG-05

must_haves:
  truths:
    - "No Application/CQRS file references Client or IClientRepository"
    - "No API controller references Client or ClientsController or RegistrationController"
    - "AdminUserController operates on Company/Employee instead of Client"
    - "New CompaniesController replaces ClientsController"
    - "Program.cs registers new handlers and no old Client handlers"
    - "All obsolete CQRS/DTO/controller files deleted"
    - "dotnet build succeeds for entire solution"
  artifacts:
    - path: "src/Onboarding.API/Controllers/CompaniesController.cs"
      provides: "Company CRUD endpoints replacing ClientsController"
    - path: "src/Onboarding.API/Controllers/AdminUserController.cs"
      provides: "Migrated admin endpoints for Company/Employee"
    - path: "src/Onboarding.Application/Admin/DTOs/CompanySummaryDto.cs"
      provides: "Company summary DTO for admin listing"
    - path: "src/Onboarding.Application/Admin/DTOs/EmployeeSummaryDto.cs"
      provides: "Employee summary DTO for admin listing"
  key_links:
    - from: "src/Onboarding.API/Controllers/AdminUserController.cs"
      to: "Company/Employee CQRS handlers"
      via: "DI-injected handlers"
      pattern: "ICommandHandler|IQueryHandler"
    - from: "src/Onboarding.API/Controllers/CompaniesController.cs"
      to: "ICompanyRepository"
      via: "Company registration/query handlers"
      pattern: "CompaniesController"
---

<objective>
Migrate Application layer CQRS/DTOs and API controllers from Client to Company/Employee. Delete all obsolete Client-based code. Ensure the entire solution builds.

Purpose: Complete the removal of PF/Client artifacts from Application and API layers (D-18, D-19, D-20). Admin endpoints now operate on Company/Employee. Solution compiles clean.

Output: All obsolete Client CQRS/DTOs/controllers deleted, new Company/Employee equivalents created, AdminUserController migrated, Program.cs DI updated, `dotnet build` succeeds.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-domain-model-redesign/37-CONTEXT.md
@.planning/phases/37-domain-model-redesign/37-01-SUMMARY.md
@.planning/phases/37-domain-model-redesign/37-03-SUMMARY.md

<interfaces>
From AdminUserController.cs (existing — 651 lines, to be migrated):
- Uses handlers: GetPaginatedUsersQuery, GetUserDetailsQuery, UpdateUserCommand, BlockUserCommand, UnblockUserCommand, DeleteUserCommand
- Also has Phase 35 admin management handlers for administrators
- Auth: [Authorize(AuthenticationSchemes = "BearerBackoffice", Roles = "admin")]

From src/Onboarding.Application/Clients/ (to be deleted):
- RegisterClientCommand, RegisterClientCommandHandler, RegisterClientCommandValidator, RegisterClientResult

From src/Onboarding.Application/Admin/ (partially migrated):
- CQRS for User operations → become Employee operations for backoffice user management
- Queries GetPaginatedUsersQuery/GetUserDetailsQuery → GetPaginatedEmployeesQuery/GetEmployeeDetailsQuery
- Commands UpdateUserCommand → UpdateEmployeeCommand, DeleteUserCommand → DeleteEmployeeCommand, etc.
- DTOs UserSummaryDto/UserDetailDto → EmployeeSummaryDto/EmployeeDetailDto
- Admin management handlers (Phase 35) stay as-is — they operate on Keycloak admin accounts, not Client

From src/Onboarding.API/Controllers/ (to be migrated):
- RegistrationController → replaced by CompaniesController
- ClientsController → replaced by CompaniesController
- RegisterClientRequest → deleted
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete obsolete Client CQRS/DTOs + create Company/Employee replacements + migrate admin commands</name>
  <files>
    src/Onboarding.Application/Clients/Commands/RegisterClientCommand.cs
    src/Onboarding.Application/Clients/Commands/RegisterClientCommandHandler.cs
    src/Onboarding.Application/Clients/Validators/RegisterClientCommandValidator.cs
    src/Onboarding.Application/Clients/DTOs/RegisterClientResult.cs
    src/Onboarding.Application/Admin/Commands/UpdateUserCommand.cs
    src/Onboarding.Application/Admin/Commands/DeleteUserCommand.cs
    src/Onboarding.Application/Admin/Commands/BlockUserCommand.cs
    src/Onboarding.Application/Admin/Commands/UnblockUserCommand.cs
    src/Onboarding.Application/Admin/Queries/GetPaginatedUsersQuery.cs
    src/Onboarding.Application/Admin/Queries/GetUserDetailsQuery.cs
    src/Onboarding.Application/Admin/DTOs/UserSummaryDto.cs
    src/Onboarding.Application/Admin/DTOs/UserDetailDto.cs
    src/Onboarding.Application/Admin/DTOs/UpdateUserRequest.cs
    src/Onboarding.Application/Admin/DTOs/CompanySummaryDto.cs
    src/Onboarding.Application/Admin/DTOs/EmployeeSummaryDto.cs
    src/Onboarding.Application/Admin/Commands/UpdateCompanyCommand.cs
    src/Onboarding.Application/Admin/Commands/DeleteEmployeeCommand.cs
    src/Onboarding.Application/Admin/Commands/BlockEmployeeCommand.cs
    src/Onboarding.Application/Admin/Commands/UnblockEmployeeCommand.cs
    src/Onboarding.Application/Admin/Queries/GetPaginatedCompaniesQuery.cs
    src/Onboarding.Application/Admin/Queries/GetPaginatedEmployeesQuery.cs
    src/Onboarding.Application/Admin/Queries/GetCompanyDetailsQuery.cs
    src/Onboarding.Application/Admin/Queries/GetEmployeeDetailsQuery.cs
  </files>
  <read_first>
    src/Onboarding.Application/Clients/Commands/RegisterClientCommandHandler.cs
    src/Onboarding.Application/Admin/Commands/UpdateUserCommand.cs
    src/Onboarding.Application/Admin/Commands/DeleteUserCommand.cs
    src/Onboarding.Application/Admin/Commands/BlockUserCommand.cs
    src/Onboarding.Application/Admin/Queries/GetPaginatedUsersQuery.cs
    src/Onboarding.Application/Admin/Queries/GetUserDetailsQuery.cs
    src/Onboarding.Application/Admin/DTOs/UserSummaryDto.cs
    src/Onboarding.Application/Admin/DTOs/UserDetailDto.cs
  </read_first>
  <action>
    **1. DELETE entire `src/Onboarding.Application/Clients/` directory** (D-19):
    - RegisterClientCommand.cs, RegisterClientCommandHandler.cs, RegisterClientCommandValidator.cs, RegisterClientResult.cs

    **2. DELETE obsolete Admin CQRS** (D-19):
    - DELETE `Admin/Commands/UpdateUserCommand.cs`
    - DELETE `Admin/Commands/DeleteUserCommand.cs`
    - DELETE `Admin/Commands/BlockUserCommand.cs`
    - DELETE `Admin/Commands/UnblockUserCommand.cs`
    - DELETE `Admin/Queries/GetPaginatedUsersQuery.cs`
    - DELETE `Admin/Queries/GetUserDetailsQuery.cs`
    - DELETE `Admin/DTOs/UserSummaryDto.cs`
    - DELETE `Admin/DTOs/UserDetailDto.cs`
    - DELETE `Admin/DTOs/UpdateUserRequest.cs`

    **3. DELETE obsolete validators**:
    - DELETE `Admin/Validators/UpdateUserCommandValidator.cs`

    **4. Create replacement DTOs:**

    `CompanySummaryDto.cs`:
    ```csharp
    public sealed record CompanySummaryDto(
        Guid Id, string RazaoSocial, string Cnpj, string Email, string Phone,
        bool IsDeleted, string? KeycloakUserId);
    ```

    `EmployeeSummaryDto.cs`:
    ```csharp
    public sealed record EmployeeSummaryDto(
        Guid Id, string Nome, string Cpf, string Email, string Phone,
        Guid CompanyId, string? CompanyRazaoSocial, Guid AccessGroupId, string? AccessGroupName,
        bool IsDeleted, string? KeycloakUserId);
    ```

    **5. Create replacement Admin commands** (simplified — business logic in handlers, keep same patterns):

    `UpdateCompanyCommand.cs`: `public sealed record UpdateCompanyCommand(Guid CompanyId, string RazaoSocial, string Email, string Phone) : ICommand;`
    `DeleteEmployeeCommand.cs`: `public sealed record DeleteEmployeeCommand(Guid EmployeeId, string ActorSub) : ICommand;`
    `BlockEmployeeCommand.cs`: `public sealed record BlockEmployeeCommand(Guid EmployeeId, string ActorSub) : ICommand;`
    `UnblockEmployeeCommand.cs`: `public sealed record UnblockEmployeeCommand(Guid EmployeeId, string ActorSub) : ICommand;`

    **6. Create replacement Admin queries:**

    `GetPaginatedCompaniesQuery.cs`:
    ```csharp
    public sealed record GetPaginatedCompaniesQuery(int Page, int PageSize, string? Search, string? Status)
        : IQuery<PaginatedResult<CompanySummaryDto>>;
    ```

    `GetPaginatedEmployeesQuery.cs`:
    ```csharp
    public sealed record GetPaginatedEmployeesQuery(int Page, int PageSize, string? Search, string? Status, Guid? CompanyId)
        : IQuery<PaginatedResult<EmployeeSummaryDto>>;
    ```

    `GetCompanyDetailsQuery.cs`:
    ```csharp
    public sealed record GetCompanyDetailsQuery(Guid CompanyId)
        : IQuery<CompanySummaryDto>;
    ```

    `GetEmployeeDetailsQuery.cs`:
    ```csharp
    public sealed record GetEmployeeDetailsQuery(Guid EmployeeId)
        : IQuery<EmployeeSummaryDto>;
    ```

    **7. Create stub handlers** for all new commands/queries — they will be fully implemented in Phase 38/41. For now, create handler classes that throw `NotImplementedException("Full implementation in Phase 38/41")`. This keeps the solution compilable while deferring complex handler logic to the phase that owns the full API behavior.

    Each handler: `public sealed class XxxCommandHandler : ICommandHandler<XxxCommand, TResult> { public Task<TResult> Handle(XxxCommand cmd, CancellationToken ct) => throw new NotImplementedException(); }`

    **8. Delete the `Clients/` directory entirely if empty** after file deletion.
  </action>
  <verify>
    <automated>dotnet build src/Onboarding.Application/Onboarding.Application.csproj 2>&1 | Select-String "error" ; echo "EXIT=$LASTEXITCODE"</automated>
  </verify>
  <done>
    - No file in Application references Client or IClientRepository
    - Clients/ directory deleted entirely
    - Admin commands/queries for User operations replaced with Company/Employee equivalents
    - New DTOs: CompanySummaryDto, EmployeeSummaryDto
    - Stub handlers for all new commands/queries (throw NotImplementedException)
    - Application project compiles
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate controllers + update Program.cs DI + full build verification</name>
  <files>
    src/Onboarding.API/Controllers/RegistrationController.cs
    src/Onboarding.API/Controllers/ClientsController.cs
    src/Onboarding.API/Controllers/AdminUserController.cs
    src/Onboarding.API/Controllers/RegisterClientRequest.cs
    src/Onboarding.API/Controllers/CompaniesController.cs
    src/Onboarding.API/Program.cs
  </files>
  <read_first>
    src/Onboarding.API/Controllers/RegistrationController.cs
    src/Onboarding.API/Controllers/ClientsController.cs
    src/Onboarding.API/Controllers/AdminUserController.cs
    src/Onboarding.API/Controllers/RegisterClientRequest.cs
    src/Onboarding.API/Program.cs
  </read_first>
  <action>
    **1. DELETE obsolete API files:**
    - DELETE `src/Onboarding.API/Controllers/RegistrationController.cs`
    - DELETE `src/Onboarding.API/Controllers/ClientsController.cs`
    - DELETE `src/Onboarding.API/Controllers/RegisterClientRequest.cs`

    **2. Rewrite `AdminUserController.cs`** to operate on Company/Employee:
    - Remove all `User`-named handler injections (GetPaginatedUsersQuery, GetUserDetailsQuery, UpdateUserCommand, BlockUserCommand, UnblockUserCommand, DeleteUserCommand)
    - Replace with Company/Employee equivalents:
      ```csharp
      private readonly IQueryHandler<GetPaginatedCompaniesQuery, PaginatedResult<CompanySummaryDto>> _paginatedCompaniesHandler;
      private readonly IQueryHandler<GetCompanyDetailsQuery, CompanySummaryDto> _companyDetailsHandler;
      private readonly IQueryHandler<GetPaginatedEmployeesQuery, PaginatedResult<EmployeeSummaryDto>> _paginatedEmployeesHandler;
      private readonly IQueryHandler<GetEmployeeDetailsQuery, EmployeeSummaryDto> _employeeDetailsHandler;
      private readonly ICommandHandler<UpdateCompanyCommand, Unit> _updateCompanyHandler;
      private readonly ICommandHandler<DeleteEmployeeCommand, Unit> _deleteEmployeeHandler;
      private readonly ICommandHandler<BlockEmployeeCommand, Unit> _blockEmployeeHandler;
      private readonly ICommandHandler<UnblockEmployeeCommand, Unit> _unblockEmployeeHandler;
      ```
    - Keep Phase 35 admin management handlers as-is (CreateAdminCommand, etc.)
    - Keep AuthController, audit, and admin management endpoints unchanged
    - Endpoint changes:
      - `GET /api/admin/companies` → GetPaginatedCompaniesQuery
      - `GET /api/admin/companies/{id}` → GetCompanyDetailsQuery
      - `GET /api/admin/employees` → GetPaginatedEmployeesQuery
      - `GET /api/admin/employees/{id}` → GetEmployeeDetailsQuery
      - `PUT /api/admin/companies/{id}` → UpdateCompanyCommand
      - `POST /api/admin/employees/{id}/block` → BlockEmployeeCommand
      - `POST /api/admin/employees/{id}/unblock` → UnblockEmployeeCommand
      - `DELETE /api/admin/employees/{id}` → DeleteEmployeeCommand
    - Remove old `/api/admin/users` endpoints
    - Remove old UpdateUserCommand validator injection
    - Add validators for new commands if any exist (minimal for Phase 37 — full validation in Phase 38/41)

    **3. Create `CompaniesController.cs`** (replaces both RegistrationController and ClientsController):
    ```csharp
    [ApiController]
    [Route("api/companies")]
    [Authorize(AuthenticationSchemes = "BearerClient")]
    public sealed class CompaniesController : ControllerBase
    {
        // GET /api/companies/me — current user's company profile
        // POST /api/companies/registration — company registration (replaces RegistrationController)
    }
    ```
    - For Phase 37, create skeleton endpoints that return placeholder results. Full implementation in Phase 38.
    - `GET /api/companies/me` → returns company data for the authenticated user (uses ICompanyRepository.GetByKeycloakSubAsync)
    - `POST /api/companies/registration` → placeholder — full registration flow is Phase 38

    **4. Update `Program.cs`:**
    - Remove all Client-related DI registrations:
      - `AddScoped<ICommandHandler<RegisterClientCommand, RegisterClientResult>, RegisterClientCommandHandler>()`
      - `AddScoped<IQueryHandler<GetPaginatedUsersQuery, ...>, ...>()`
      - `AddScoped<IQueryHandler<GetUserDetailsQuery, ...>, ...>()`
      - `AddScoped<ICommandHandler<UpdateUserCommand, ...>, ...>()`
      - `AddScoped<ICommandHandler<BlockUserCommand, ...>, ...>()`
      - `AddScoped<ICommandHandler<UnblockUserCommand, ...>, ...>()`
      - `AddScoped<ICommandHandler<DeleteUserCommand, ...>, ...>()`
      - `AddScoped<IValidator<UpdateUserCommand>, UpdateUserCommandValidator>()`
    - Add new Company/Employee handler registrations (stub handlers for now):
      - All GetPaginatedCompanies/GetCompanyDetails/GetPaginatedEmployees/GetEmployeeDetails query handlers
      - All UpdateCompany/DeleteEmployee/BlockEmployee/UnblockEmployee command handlers
    - Add `ICurrentCompanyService` → `CurrentCompanyService` registration (Scoped)
    - Ensure `ICompanyRepository`, `IEmployeeRepository`, `IAccessGroupRepository` are registered (from Plan 03)

    **5. Full build verification:**
    ```
    dotnet build src/Onboarding.API/Onboarding.API.csproj
    ```
    - Fix any remaining compilation errors. Common issues:
      - Old `IClientRepository` references in other handlers
      - Old `Client` type references in test or API files not covered above
      - Missing `using` directives
    - Iterate until clean build. Use `grep -r "IClientRepository\|Client\b" src/` (excluding obj/bin) to find stray references.

    **6. Run domain tests:**
    ```
    dotnet test tests/Onboarding.Domain.Tests/ --filter "FullyQualifiedName~Company|FullyQualifiedName~Employee|FullyQualifiedName~AccessGroup|FullyQualifiedName~TermsAcceptance|FullyQualifiedName~Permissions" --no-restore
    ```
    - All new domain tests should pass (GREEN).
    - Any failing test from Plan 02 indicates a bug in Plan 01 domain code — fix it.
  </action>
  <verify>
    <automated>dotnet build src/Onboarding.API/Onboarding.API.csproj 2>&1 | Select-String "error" ; if ($LASTEXITCODE -eq 0) { echo "FULL_BUILD_OK" } else { echo "BUILD_ERRORS" }</automated>
  </verify>
  <done>
    - RegistrationController.cs, ClientsController.cs, RegisterClientRequest.cs are DELETED
    - AdminUserController.cs operates on Company/Employee endpoints (no User endpoints)
    - CompaniesController.cs exists with skeleton /me and /registration endpoints
    - Program.cs has no Client-related DI registrations
    - All new handler stubs registered in DI
    - ICurrentCompanyService registered Scoped
    - `dotnet build` of entire solution succeeds with zero errors
    - Domain tests for Company, Employee, AccessGroup, TermsAcceptance, Permissions pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted input — validated via FluentValidation on commands |
| API → Application | CQRS handlers mediate all operations — no direct DB access from controllers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04-01 | Tampering | AdminUserController endpoints | mitigate | [Authorize(Roles = "admin")] preserved on all admin endpoints |
| T-37-04-02 | Information Disclosure | Old Client references | mitigate | All Client code deleted — no stale data paths remain (D-19) |
| T-37-04-03 | Elevation of Privilege | CompaniesController | mitigate | Skeleton endpoints use existing BearerClient auth scheme |
</threat_model>

<verification>
1. `dotnet build` of entire solution succeeds — zero errors
2. No file in src/ references `IClientRepository`, `Client` (as aggregate), `ClientType`, `ClientsController`, or `RegistrationController`
3. AdminUserController has `api/admin/companies` and `api/admin/employees` endpoints (not `api/admin/users`)
4. CompaniesController.cs exists with `/api/companies/me` endpoint
5. Domain tests pass: `dotnet test tests/Onboarding.Domain.Tests/ --filter "Company|Employee|AccessGroup|TermsAcceptance|Permissions"`
6. Program.cs has no `RegisterClientCommandHandler` or `IClientRepository` registrations
</verification>

<success_criteria>
1. Entire solution builds with zero compilation errors
2. All Client/PF artifacts deleted from Domain, Infrastructure, Application, and API layers (D-18, D-19)
3. AdminUserController migrated to Company/Employee endpoints
4. New CompaniesController replaces RegistrationController and ClientsController
5. All domain tests for Company, Employee, AccessGroup, TermsAcceptance, Permissions pass (GREEN)
6. Program.cs DI container has no stale Client references
7. ICurrentCompanyService registered for HasQueryFilter injection
</success_criteria>

<output>
After completion, create `.planning/phases/37-domain-model-redesign/37-04-SUMMARY.md`
</output>
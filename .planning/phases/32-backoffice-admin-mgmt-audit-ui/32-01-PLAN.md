---
phase: 32-backoffice-admin-mgmt-audit-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/backoffice/src/tests/create-admin-page.test.tsx
  - frontend/backoffice/src/tests/admin-administrators-page.test.tsx
  - frontend/backoffice/src/tests/audit-log-page.test.tsx
  - frontend/backoffice/src/tests/password-change-page.test.tsx
autonomous: true
requirements:
  - ADM-01
  - ADM-02
  - ADM-03
  - ADM-04
  - AUD-02
  - AUD-03

must_haves:
  truths:
    - "CreateAdminPage: formulário nome+email → chama createAdmin() → exibe senha temporária com copy-to-clipboard"
    - "AdminAdministratorsPage: tabela com Nome, Email, Status, Senha Temporária — já existe (Phase 30)"
    - "AuditLogPage: tabela paginada com filtros (data, tipo de ação, admin) — já existe"
    - "PasswordChangePage: formulário com validação Zod → chama forcePasswordChange()"
    - "Todas as páginas exigem sessão autenticada (Auth Code Flow da Phase 31)"
  artifacts:
    - path: "frontend/backoffice/src/tests/create-admin-page.test.tsx"
      provides: "Testes para CreateAdminPage"
    - path: "frontend/backoffice/src/tests/audit-log-page.test.tsx"
      provides: "Testes para AuditLogPage"
    - path: "frontend/backoffice/src/tests/admin-administrators-page.test.tsx"
      provides: "Testes para AdminAdministratorsPage"
    - path: "frontend/backoffice/src/tests/password-change-page.test.tsx"
      provides: "Testes para PasswordChangePage"
  key_links:
    - from: "CreateAdminPage"
      to: "createAdmin()"
      via: "import de admin-api.ts"
    - from: "AuditLogPage"
      to: "getAuditLog()"
      via: "import de admin-api.ts"
    - from: "PasswordChangePage"
      to: "forcePasswordChange()"
      via: "import de admin-api.ts"
---

<objective>
Criar testes para as 4 páginas da Phase 32 que já existem mas não têm cobertura de testes: CreateAdminPage, AdminAdministratorsPage, AuditLogPage, PasswordChangePage.

Purpose: Garantir que todas as páginas da Phase 32 tenham testes unitários cobrindo renderização, interação do usuário, chamadas de API e estados (loading, error, empty, success).
Output: 4 arquivos de teste, todos passando em vitest.
</objective>

<execution_context>
@~/.qwen/get-shit-done/workflows/execute-plan.md
@~/.qwen/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-backoffice-admin-mgmt-audit-ui/32-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testes para CreateAdminPage</name>
  <files>
    frontend/backoffice/src/tests/create-admin-page.test.tsx
  </files>
  <read_first>
    - frontend/backoffice/src/components/pages/CreateAdminPage.tsx (componente completo)
    - frontend/backoffice/src/lib/admin-api.ts (createAdmin, CreateAdminResult)
    - frontend/backoffice/src/tests/delete-dialog.test.tsx (padrão de teste com mock de admin-api)
  </read_first>
  <action>
Criar `frontend/backoffice/src/tests/create-admin-page.test.tsx`:

Mock `@/lib/admin-api` com `createAdmin: vi.fn()`.

Testes:
1. "renders form with name and email fields" — verifica inputs e botão "Criar Admin"
2. "shows validation errors for empty fields on submit" — submit vazio, verifica mensagens de erro
3. "calls createAdmin with fullName and email on valid submit" — mock createAdmin resolve, verifica chamada
4. "shows temporary password result card after successful creation" — mock retorna {adminId, temporaryPassword}, verifica card verde com senha
5. "copy password button copies to clipboard" — mock navigator.clipboard.writeText, verifica chamada
6. "Create Another button clears result and resets form" — clica "Criar Outro", verifica que card some
7. "shows error toast on 409 conflict" — mock rejeita com status 409, verifica toast.error
8. "shows error toast on other failures" — mock rejeita com status 500, verifica toast.error
  </action>
  <done>
    - create-admin-page.test.tsx existe com 8 testes
    - npx vitest run src/tests/create-admin-page.test.tsx exits 0
  </done>
  <acceptance_criteria>
    - grep "CreateAdminPage" frontend/backoffice/src/tests/create-admin-page.test.tsx retorna match
    - grep "createAdmin" frontend/backoffice/src/tests/create-admin-page.test.tsx retorna match
    - npx vitest run src/tests/create-admin-page.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Testes para AdminAdministratorsPage</name>
  <files>
    frontend/backoffice/src/tests/admin-administrators-page.test.tsx
  </files>
  <read_first>
    - frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx (componente completo)
    - frontend/backoffice/src/lib/admin-api.ts (getAdministrators, AdminUserDto)
  </read_first>
  <action>
Criar `frontend/backoffice/src/tests/admin-administrators-page.test.tsx`:

Mock `@/lib/admin-api` com `getAdministrators: vi.fn()`.

Testes:
1. "renders loading state initially" — verifica "Carregando administradores"
2. "renders administrators table with data" — mock retorna lista de admins, verifica tabela com 4 colunas
3. "shows active/blocked badges based on isEnabled" — verifica Badge "Ativo" e "Bloqueado"
4. "shows temp password badges based on hasTemporaryPassword" — verifica Badge "Pendente" e "Definida"
5. "shows empty state when no administrators" — mock retorna [], verifica "Nenhum administrador encontrado"
6. "shows error state and retry button on failure" — mock rejeita, verifica erro + botão "Tentar novamente"
7. "retry button refetches data" — clica retry, verifica getAdministrators chamado novamente
  </action>
  <done>
    - admin-administrators-page.test.tsx existe com 7 testes
    - npx vitest run src/tests/admin-administrators-page.test.tsx exits 0
  </done>
  <acceptance_criteria>
    - grep "AdminAdministratorsPage" frontend/backoffice/src/tests/admin-administrators-page.test.tsx retorna match
    - grep "getAdministrators" frontend/backoffice/src/tests/admin-administrators-page.test.tsx retorna match
    - npx vitest run src/tests/admin-administrators-page.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Testes para AuditLogPage</name>
  <files>
    frontend/backoffice/src/tests/audit-log-page.test.tsx
  </files>
  <read_first>
    - frontend/backoffice/src/components/pages/AuditLogPage.tsx (componente completo)
    - frontend/backoffice/src/lib/admin-api.ts (getAuditLog)
  </read_first>
  <action>
Criar `frontend/backoffice/src/tests/audit-log-page.test.tsx`:

Mock `@/lib/admin-api` com `getAuditLog: vi.fn()`.

Testes:
1. "renders loading state initially" — verifica "Carregando"
2. "renders audit log table with entries" — mock retorna items, verifica tabela com colunas Data/Hora, Admin, Acao, Alvo, IP
3. "shows action type labels translated" — verifica "Usuario Bloqueado" para UserBlocked
4. "shows empty state when no entries" — mock retorna items=[], verifica "Nenhuma entrada"
5. "shows error state on failure" — mock rejeita, verifica erro
6. "filter button calls fetchAuditLog with filter params" — preenche filtros, clica Filtrar, verifica getAuditLog com params
7. "reset button clears all filters and resets page" — clica Limpar, verifica campos vazios
8. "pagination shows next/previous buttons when multiple pages" — mock retorna totalCount=50, pageSize=20, verifica botões
  </action>
  <done>
    - audit-log-page.test.tsx existe com 8 testes
    - npx vitest run src/tests/audit-log-page.test.tsx exits 0
  </done>
  <acceptance_criteria>
    - grep "AuditLogPage" frontend/backoffice/src/tests/audit-log-page.test.tsx retorna match
    - grep "getAuditLog" frontend/backoffice/src/tests/audit-log-page.test.tsx retorna match
    - npx vitest run src/tests/audit-log-page.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Testes para PasswordChangePage</name>
  <files>
    frontend/backoffice/src/tests/password-change-page.test.tsx
  </files>
  <read_first>
    - frontend/backoffice/src/components/pages/PasswordChangePage.tsx (componente completo)
    - frontend/backoffice/src/lib/admin-api.ts (forcePasswordChange)
  </read_first>
  <action>
Criar `frontend/backoffice/src/tests/password-change-page.test.tsx`:

Mock `@/lib/admin-api` com `forcePasswordChange: vi.fn()`.

Testes:
1. "renders password change form with new password and confirm fields" — verifica inputs e botão
2. "shows validation error for password too short" — digita "abc", submit, verifica erro "8 caracteres"
3. "shows validation error for missing uppercase" — digita "abcdefgh1!", verifica erro maiuscula
4. "shows validation error for passwords not matching" — senhas diferentes, verifica erro
5. "calls forcePasswordChange with new password on valid submit" — mock resolve, verifica chamada
6. "shows success card after password change" — mock resolve, verifica card verde "Senha Alterada"
7. "shows error toast on failure" — mock rejeita, verifica toast.error
  </action>
  <done>
    - password-change-page.test.tsx existe com 7 testes
    - npx vitest run src/tests/password-change-page.test.tsx exits 0
  </done>
  <acceptance_criteria>
    - grep "PasswordChangePage" frontend/backoffice/src/tests/password-change-page.test.tsx retorna match
    - grep "forcePasswordChange" frontend/backoffice/src/tests/password-change-page.test.tsx retorna match
    - npx vitest run src/tests/password-change-page.test.tsx exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. npx vitest run src/tests/create-admin-page.test.tsx exits 0
2. npx vitest run src/tests/admin-administrators-page.test.tsx exits 0
3. npx vitest run src/tests/audit-log-page.test.tsx exits 0
4. npx vitest run src/tests/password-change-page.test.tsx exits 0
5. npx vitest run (todos) — 149 + novos testes passando
6. npx tsc --noEmit exits 0
</verification>

<success_criteria>
- 4 arquivos de teste criados com 30 testes no total (8+7+8+7)
- Todos os testes passando
- CreateAdminPage: form, validation, API call, result card, copy password, error handling
- AdminAdministratorsPage: loading, table, badges, empty, error, retry
- AuditLogPage: loading, table, labels, empty, error, filters, pagination
- PasswordChangePage: form, validation, API call, success card, error handling
- tsc --noEmit exits 0
</success_criteria>

<output>
Após execução, criar 32-01-SUMMARY.md com status, changes, deviations, commits, verification results.
</output>

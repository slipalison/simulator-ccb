---
phase: 09-login-ui
plan: "03"
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - frontend/src/tests/login-flow.test.tsx
  - frontend/src/tests/auth-context.test.tsx
autonomous: true

# Goal-backward verification (derived during planning, verified after execution)
must_haves:
  truths:
    - "Login flow test verifies form → API → redirect chain"
    - "Auth context test confirms memory-only token storage"
    - "Profile guard test confirms redirect for unauthenticated users"
  artifacts:
    - path: "frontend/src/tests/login-flow.test.tsx"
      provides: "Integration tests for login flow"
      min_lines: 40
    - path: "frontend/src/tests/auth-context.test.tsx"
      provides: "Unit tests for AuthContext behaviors"
      min_lines: 30
  key_links:
    - from: "login-flow.test.tsx"
      to: "LoginForm.tsx"
      via: "renders form, fills fields, submits, asserts redirect"
      pattern: "LoginForm"
    - from: "auth-context.test.tsx"
      to: "auth-context.tsx"
      via: "imports AuthProvider, useAuth"
      pattern: "AuthProvider"
---

<objective>
Write tests for login flow and auth context to verify memory-only token storage and correct redirect behaviors.

Purpose: Phase 09 requires confidence that tokens are never persisted to browser storage and that the login → redirect chain works correctly. Tests serve as regression safety net.
Output: login-flow.test.tsx (integration), auth-context.test.tsx (unit tests)
</objective>

<execution_context>
@~/.qwen/get-shit-done/workflows/execute-plan.md
@~/.qwen/get-shit-done/templates/summary.md
@~/.qwen/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan outputs
@.planning/phases/09-login-ui/09-01-SUMMARY.md
@.planning/phases/09-login-ui/09-02-SUMMARY.md

# Existing test patterns:
@frontend/src/tests/form-validation.test.tsx
@frontend/src/tests/routing.test.tsx
@frontend/src/tests/setup.ts

# Files to test:
@frontend/src/lib/auth-context.tsx
@frontend/src/components/pages/LoginPage.tsx
@frontend/src/components/pages/ProfilePage.tsx

# Vitest config:
@frontend/vitest.config.ts

# Requirements this plan addresses:
- SEC-10: JWT armazenado em memória no frontend (never localStorage/sessionStorage)
- AUTH-01: Login flow works end-to-end
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write auth-context unit tests</name>
  <files>frontend/src/tests/auth-context.test.tsx</files>
  <action>
    Write unit tests for AuthContext behaviors.

    Test cases:
    1. "initial state is unauthenticated" — auth.isAuthenticated === false, tokens null
    2. "login stores accessToken and refreshToken" — after login() call, tokens are set
    3. "login calculates expiresAt from expiresIn" — expiresAt is approximately now + expiresIn seconds
    4. "logout clears all state" — after logout(), tokens null, isAuthenticated false
    5. "tokens are NOT written to localStorage" — assert localStorage.getItem is never called
    6. "tokens are NOT written to sessionStorage" — assert sessionStorage.getItem is never called

    Test setup:
    - Use vitest mock for fetch or loginClient
    - Wrap test component in AuthProvider
    - Use act() for async operations

    What to avoid:
    - Don't make real API calls — mock fetch or loginClient
    - Don't test LoginForm in this file — that's a separate concern
    - Focus on token storage behavior and auth state transitions
  </action>
  <verify>
    1. All 6 tests pass with npm run test
    2. No localStorage/sessionStorage calls detected in any test
  </verify>
  <done>All auth context tests pass, confirming memory-only token storage</done>
</task>

<task type="auto">
  <name>Task 2: Write login flow integration tests</name>
  <files>frontend/src/tests/login-flow.test.tsx</files>
  <action>
    Write integration tests for the login flow.

    Test cases:
    1. "LoginPage renders LoginForm with email and password fields" — form fields visible
    2. "Submitting empty form shows validation errors" — inline errors appear
    3. "Successful login redirects to /profile" — mock loginClient returning 200, assert navigation
    4. "Failed login shows error message" — mock loginClient throwing LoginError, assert error displayed
    5. "Form is not cleared on login failure" — email field retains value after error

    Test setup:
    - Mock loginClient using vi.mock('@/lib/api')
    - Use render with Router context if needed for navigation assertions
    - Use screen.getByRole, screen.getByLabelText for accessibility-aware queries

    Profile guard tests:
    6. "ProfilePage redirects to /login when unauthenticated" — render ProfilePage without auth, assert redirect
    7. "ProfilePage shows placeholder when authenticated" — render with mock auth, assert placeholder visible

    What to avoid:
    - Don't test validation details (covered by form-validation.test.tsx)
    - Don't test Keycloak ROPC exchange — that's backend territory
    - Focus on frontend behaviors: rendering, navigation, error display
  </action>
  <verify>
    1. All 7 tests pass with npm run test
    2. Redirect assertion works (verify navigation mock was called with /profile)
  </verify>
  <done>Login flow and profile guard tests pass</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run test` passes (all tests, not just new ones)
- [ ] auth-context.test.tsx has 6+ test cases
- [ ] login-flow.test.tsx has 7+ test cases
- [ ] No test makes real network calls (all mocked)
- [ ] SEC-10 verified: no localStorage/sessionStorage writes in any test
- [ ] Existing tests still pass (no regressions)
</verification>

<success_criteria>

- All tasks completed
- auth-context.test.tsx verifies memory-only token storage
- login-flow.test.tsx verifies form → API → redirect chain
- Profile guard tests confirm unauthenticated redirect to /login
- All tests pass (npm run test)
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/09-login-ui/09-03-SUMMARY.md` with:
- auth-context tests (count, what they verify)
- login-flow tests (count, what they verify)
- Test run results (pass/fail count)
- Any deviations from plan
</output>

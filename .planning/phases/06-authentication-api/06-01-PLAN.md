---
phase: 06-authentication-api
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/Onboarding.API.Tests/Authentication/JwtBearerConfigurationTests.cs
  - tests/Onboarding.API.Tests/Authentication/AuthorizationMiddlewareTests.cs
  - tests/Onboarding.API.Tests/Api/ClientsMeEndpointTests.cs
  - tests/Onboarding.API.Tests/Api/LoginEndpointTests.cs
  - tests/Onboarding.API.Tests/Api/RefreshTokenEndpointTests.cs
  - tests/Onboarding.API.Tests/Authentication/FakeJwtTokenHelper.cs
  - tests/Onboarding.API.Tests/Authentication/AuthTestApiFactory.cs
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - "Todos os 5 arquivos de teste existem e compilam sem erros"
    - "Cada stub tem xunit.Trait Category=Unit ou Category=Integration declarado"
    - "Todos os stubs falham com NotImplementedException ou ShouldBeFalse ao serem executados"
    - "FakeJwtTokenHelper pode gerar um JWT unsigned com claim email"
    - "AuthTestApiFactory herda de WebApplicationFactory<Program> e registra PostConfigure<JwtBearerOptions>"
  artifacts:
    - path: "tests/Onboarding.API.Tests/Authentication/JwtBearerConfigurationTests.cs"
      provides: "Stubs RED para AUTH-02 (configuração JWT)"
      exports: []
    - path: "tests/Onboarding.API.Tests/Authentication/AuthorizationMiddlewareTests.cs"
      provides: "Stubs RED para AUTH-02 ([Authorize] retorna 401)"
      exports: []
    - path: "tests/Onboarding.API.Tests/Api/ClientsMeEndpointTests.cs"
      provides: "Stubs RED para AUTH-03 (GET /api/clients/me)"
      exports: []
    - path: "tests/Onboarding.API.Tests/Api/LoginEndpointTests.cs"
      provides: "Stubs RED para AUTH-04 (POST /api/auth/login)"
      exports: []
    - path: "tests/Onboarding.API.Tests/Api/RefreshTokenEndpointTests.cs"
      provides: "Stubs RED para AUTH-04 (POST /api/auth/refresh)"
      exports: []
    - path: "tests/Onboarding.API.Tests/Authentication/FakeJwtTokenHelper.cs"
      provides: "Geração de JWT falso para testes de endpoint protegido"
      exports: ["FakeJwtTokenHelper"]
    - path: "tests/Onboarding.API.Tests/Authentication/AuthTestApiFactory.cs"
      provides: "WebApplicationFactory configurada para testes de auth sem Keycloak real"
      exports: ["AuthTestApiFactory"]
  key_links:
    - from: "tests/Onboarding.API.Tests/Api/ClientsMeEndpointTests.cs"
      to: "tests/Onboarding.API.Tests/Authentication/AuthTestApiFactory.cs"
      via: "herança ou uso de factory para criar HttpClient com JWT falso"
      pattern: "AuthTestApiFactory"
    - from: "tests/Onboarding.API.Tests/Authentication/AuthTestApiFactory.cs"
      to: "tests/Onboarding.API.Tests/Authentication/FakeJwtTokenHelper.cs"
      via: "uso de GenerateFakeJwt para injetar token no Authorization header"
      pattern: "FakeJwtTokenHelper"
---

<objective>
Wave 0 TDD: criar todos os arquivos de teste com stubs RED (failing) antes que qualquer código de produção da Fase 6 exista.

Purpose: Garantir que cada requisito (AUTH-02, AUTH-03, AUTH-04) tenha um teste nomeado que falha antes da implementação — ciclo RED correto.
Output: 7 arquivos no diretório de testes. `dotnet test --filter "Category=Authentication"` executa e falha com NotImplementedException ou ShouldBeFalse. Nenhum arquivo de produção é criado neste plano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-authentication-api/06-CONTEXT.md
@.planning/phases/06-authentication-api/06-VALIDATION.md

<interfaces>
<!-- Padrão de stub RED estabelecido na Fase 5 (04-00-PLAN.md) -->
<!-- xUnit 2.9.3 não tem Assert.Fail — usar: true.ShouldBeFalse("RED stub — not implemented yet") -->

Padrão de stub RED do projeto (Phase 4):
```csharp
[Fact]
[Trait("Category", "Unit")]
public Task SomeBehavior_Should_DoSomething()
{
    // RED stub — implement in Wave 2
    true.ShouldBeFalse("RED stub — not implemented yet");
    return Task.CompletedTask;
}
```

IClientRepository existente (src/Onboarding.Domain/Repositories/IClientRepository.cs):
```csharp
public interface IClientRepository
{
    Task AddAsync(Client client, CancellationToken ct = default);
    Task<Client?> GetByIdAsync(Guid id, CancellationToken ct = default);
    Task<bool> ExistsByEmailAsync(string email, CancellationToken ct = default);
    Task<bool> ExistsByCpfAsync(string cpf, CancellationToken ct = default);
    Task<bool> ExistsByCnpjAsync(string cnpj, CancellationToken ct = default);
    Task DeleteAsync(Guid id, CancellationToken ct = default);
    // GetByEmailAsync será adicionado no Plan 02
}
```

Padrão WebApplicationFactory existente (tests/Onboarding.API.Tests/Registration/RegistrationControllerTests.cs):
```csharp
internal sealed class RegistrationTestApiFactory : WebApplicationFactory<Program>
{
    public IClientRepository RepositoryMock { get; } = Substitute.For<IClientRepository>();
    public IKeycloakUserService KeycloakMock { get; } = Substitute.For<IKeycloakUserService>();

    protected override void ConfigureWebHost(IWebHostBuilder builder)
    {
        builder.UseSetting("Keycloak:RealmUrl", "http://localhost:8180/realms/onboarding");
        builder.UseSetting("Keycloak:AuthServerUrl", "http://localhost:8180/");
        builder.UseSetting("Keycloak:AdminClientId", "onboarding-api-admin");
        builder.UseSetting("Keycloak:AdminClientSecret", "test-secret");
        builder.UseSetting("Keycloak:Realm", "onboarding");
        builder.ConfigureTestServices(services =>
        {
            // remove health checks, replace infrastructure with mocks
        });
    }
}
```

WebAppFactoryCollection (tests/Onboarding.API.Tests/WebAppFactoryCollection.cs):
```csharp
[CollectionDefinition(Name, DisableParallelization = true)]
public sealed class WebAppFactoryCollection : ICollectionFixture<object>
{
    public const string Name = "WebApplicationFactory";
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar FakeJwtTokenHelper e AuthTestApiFactory</name>
  <files>
    tests/Onboarding.API.Tests/Authentication/FakeJwtTokenHelper.cs
    tests/Onboarding.API.Tests/Authentication/AuthTestApiFactory.cs
  </files>
  <read_first>
    - tests/Onboarding.API.Tests/Registration/RegistrationControllerTests.cs (padrão WebApplicationFactory do projeto)
    - tests/Onboarding.API.Tests/WebAppFactoryCollection.cs (definição do collection name)
    - tests/Onboarding.API.Tests/Onboarding.API.Tests.csproj (referências existentes)
    - src/Onboarding.Application/Common/IKeycloakUserService.cs (interface a ser mockada)
    - src/Onboarding.Domain/Repositories/IClientRepository.cs (interface a ser mockada)
  </read_first>
  <behavior>
    FakeJwtTokenHelper.GenerateFakeJwt("joao@example.com") deve retornar uma string JWT válida (3 partes separadas por ponto) com claim "email" = "joao@example.com".
    AuthTestApiFactory deve inicializar sem erro com PostConfigure<JwtBearerOptions> desabilitando todas as validações.
  </behavior>
  <action>
Criar `tests/Onboarding.API.Tests/Authentication/FakeJwtTokenHelper.cs`:

```csharp
using System.IdentityModel.Tokens.Jwt;
using System.Security.Claims;
using Microsoft.IdentityModel.Tokens;

namespace Onboarding.API.Tests.Authentication;

/// <summary>
/// Generates unsigned JWT tokens for testing protected endpoints without a real Keycloak.
/// Used with AuthTestApiFactory which disables signature validation via PostConfigure.
/// </summary>
public static class FakeJwtTokenHelper
{
    public static string GenerateFakeJwt(string email, string? sub = null)
    {
        var claims = new List<Claim>
        {
            new("email", email),
            new("sub", sub ?? Guid.NewGuid().ToString())
        };

        var token = new JwtSecurityToken(
            issuer: "http://localhost",
            audience: "http://localhost",
            claims: claims,
            expires: DateTime.UtcNow.AddHours(1));

        return new JwtSecurityTokenHandler().WriteToken(token);
    }
}
```

Criar `tests/Onboarding.API.Tests/Authentication/AuthTestApiFactory.cs`:

```csharp
using Microsoft.AspNetCore.Authentication.JwtBearer;
using Microsoft.AspNetCore.Hosting;
using Microsoft.AspNetCore.Mvc.Testing;
using Microsoft.AspNetCore.TestHost;
using Microsoft.Extensions.DependencyInjection;
using Microsoft.Extensions.Diagnostics.HealthChecks;
using Microsoft.Extensions.Options;
using NSubstitute;
using Onboarding.Application.Common;
using Onboarding.Domain.Repositories;

namespace Onboarding.API.Tests.Authentication;

/// <summary>
/// WebApplicationFactory for authentication endpoint tests.
/// Disables JWT signature validation via PostConfigure so tests run without a real Keycloak.
/// Uses PostConfigure (not Configure) to ensure override applies after app's own JWT configuration.
/// </summary>
internal sealed class AuthTestApiFactory : WebApplicationFactory<Program>
{
    public IClientRepository RepositoryMock { get; } = Substitute.For<IClientRepository>();
    public IKeycloakTokenService TokenServiceMock { get; } = Substitute.For<IKeycloakTokenService>();

    protected override void ConfigureWebHost(IWebHostBuilder builder)
    {
        builder.UseSetting("ConnectionStrings:AppDb",
            "Host=localhost;Port=5432;Database=test;Username=test;Password=test");
        builder.UseSetting("Keycloak:RealmUrl",
            "http://localhost:8180/realms/onboarding");
        builder.UseSetting("Keycloak:AuthServerUrl", "http://localhost:8180/");
        builder.UseSetting("Keycloak:AdminClientId", "onboarding-api-admin");
        builder.UseSetting("Keycloak:AdminClientSecret", "test-secret");
        builder.UseSetting("Keycloak:Realm", "onboarding");
        builder.UseSetting("Keycloak:PublicClientId", "onboarding-app");

        builder.ConfigureTestServices(services =>
        {
            // Remove real health checks so TestServer starts without real infrastructure
            var configureOptionsType = typeof(IConfigureOptions<HealthCheckServiceOptions>);
            var toRemove = services
                .Where(d => d.ServiceType == configureOptionsType)
                .ToList();
            foreach (var d in toRemove)
                services.Remove(d);

            services.AddHealthChecks()
                .AddCheck("stub-healthy", () => HealthCheckResult.Healthy("stub-ok"), ["ready"]);

            // Replace real infrastructure with mocks (no DB, no Keycloak required)
            services.AddScoped<IClientRepository>(_ => RepositoryMock);
            services.AddScoped<IKeycloakTokenService>(_ => TokenServiceMock);

            // Disable JWT validation for tests — PostConfigure overrides app configuration
            // D-04/D-05: in tests we use FakeJwtTokenHelper to generate unsigned tokens
            services.PostConfigure<JwtBearerOptions>(
                JwtBearerDefaults.AuthenticationScheme, options =>
                {
                    options.TokenValidationParameters.ValidateIssuerSigningKey = false;
                    options.TokenValidationParameters.ValidateIssuer = false;
                    options.TokenValidationParameters.ValidateAudience = false;
                    options.TokenValidationParameters.ValidateLifetime = false;
                    options.TokenValidationParameters.RequireSignedTokens = false;
                });
        });
    }
}
```

Nota: `IKeycloakTokenService` ainda não existe em produção (será criado no Plan 02). O arquivo irá compilar com erro até o Plan 02 criar a interface — isso é esperado no Wave 0.
  </action>
  <verify>
    <automated>dotnet build tests/Onboarding.API.Tests/Onboarding.API.Tests.csproj 2>&1 | grep -E "(error|warning)" || echo "compile check done"</automated>
  </verify>
  <acceptance_criteria>
    - tests/Onboarding.API.Tests/Authentication/FakeJwtTokenHelper.cs existe
    - tests/Onboarding.API.Tests/Authentication/AuthTestApiFactory.cs existe
    - FakeJwtTokenHelper.cs contém "new JwtSecurityToken("
    - AuthTestApiFactory.cs contém "PostConfigure&lt;JwtBearerOptions&gt;"
    - AuthTestApiFactory.cs contém "ValidateIssuerSigningKey = false"
    - AuthTestApiFactory.cs contém "ValidateLifetime = false"
    - AuthTestApiFactory.cs contém "RequireSignedTokens = false"
    - AuthTestApiFactory.cs contém "IKeycloakTokenService"
  </acceptance_criteria>
  <done>FakeJwtTokenHelper e AuthTestApiFactory criados com o padrão PostConfigure exato do RESEARCH.md Padrão 7</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar stubs RED para AUTH-02, AUTH-03, AUTH-04</name>
  <files>
    tests/Onboarding.API.Tests/Authentication/JwtBearerConfigurationTests.cs
    tests/Onboarding.API.Tests/Authentication/AuthorizationMiddlewareTests.cs
    tests/Onboarding.API.Tests/Api/ClientsMeEndpointTests.cs
    tests/Onboarding.API.Tests/Api/LoginEndpointTests.cs
    tests/Onboarding.API.Tests/Api/RefreshTokenEndpointTests.cs
  </files>
  <read_first>
    - tests/Onboarding.API.Tests/Authentication/FakeJwtTokenHelper.cs (recém criado — Task 1)
    - tests/Onboarding.API.Tests/Authentication/AuthTestApiFactory.cs (recém criado — Task 1)
    - tests/Onboarding.API.Tests/WebAppFactoryCollection.cs (collection name = "WebApplicationFactory")
    - .planning/phases/06-authentication-api/06-VALIDATION.md (mapeamento exato de test class names)
  </read_first>
  <behavior>
    Cada arquivo deve ter exatamente os test methods correspondentes ao VALIDATION.md:
    - JwtBearerConfigurationTests: stub para AUTH-02 (JWT Bearer valida token do Keycloak OIDC)
    - AuthorizationMiddlewareTests: stub para AUTH-02 ([Authorize] em rota retorna 401 sem token)
    - ClientsMeEndpointTests: stub para AUTH-03 (GET /api/clients/me retorna 200 com token válido; retorna 401 sem token)
    - LoginEndpointTests: stubs para AUTH-04 (POST /api/auth/login retorna access_token + refresh_token; retorna 401 com credenciais inválidas)
    - RefreshTokenEndpointTests: stubs para AUTH-04 (POST /api/auth/refresh retorna novo access_token; retorna 401 com refresh_token inválido)
    Todos os stubs devem falhar ao executar (true.ShouldBeFalse("RED stub — not implemented yet")).
  </behavior>
  <action>
Criar `tests/Onboarding.API.Tests/Authentication/JwtBearerConfigurationTests.cs`:

```csharp
using Shouldly;

namespace Onboarding.API.Tests.Authentication;

/// <summary>
/// RED stubs for AUTH-02: JWT Bearer configuration validates tokens issued by Keycloak.
/// Test name matches VALIDATION.md task 06-01-01: JwtBearerConfigurationTests.
/// </summary>
public class JwtBearerConfigurationTests
{
    [Fact]
    [Trait("Category", "Unit")]
    public Task JwtBearer_Should_BeConfiguredWithKeycloakAuthority()
    {
        // RED stub — implement when AddJwtBearer is wired in Plan 02
        true.ShouldBeFalse("RED stub — not implemented yet");
        return Task.CompletedTask;
    }

    [Fact]
    [Trait("Category", "Unit")]
    public Task JwtBearer_Should_HaveValidateAudienceDisabled()
    {
        // RED stub — D-05: ValidateAudience = false required for Keycloak ROPC tokens
        true.ShouldBeFalse("RED stub — not implemented yet");
        return Task.CompletedTask;
    }

    [Fact]
    [Trait("Category", "Unit")]
    public Task JwtBearer_Should_HaveMapInboundClaimsFalse()
    {
        // RED stub — D-04: MapInboundClaims = false required so "email" claim stays as "email"
        true.ShouldBeFalse("RED stub — not implemented yet");
        return Task.CompletedTask;
    }
}
```

Criar `tests/Onboarding.API.Tests/Authentication/AuthorizationMiddlewareTests.cs`:

```csharp
using Shouldly;
using System.Net;

namespace Onboarding.API.Tests.Authentication;

/// <summary>
/// RED stubs for AUTH-02: [Authorize] returns 401 without Bearer token.
/// Test name matches VALIDATION.md task 06-01-02: AuthorizationMiddlewareTests.
/// </summary>
[Collection(WebAppFactoryCollection.Name)]
public class AuthorizationMiddlewareTests : IAsyncLifetime
{
    private AuthTestApiFactory? _factory;
    private HttpClient? _client;

    public Task InitializeAsync()
    {
        _factory = new AuthTestApiFactory();
        _client = _factory.CreateClient();
        return Task.CompletedTask;
    }

    public async Task DisposeAsync()
    {
        _client?.Dispose();
        if (_factory is not null)
            await _factory.DisposeAsync();
    }

    [Fact]
    [Trait("Category", "Unit")]
    public async Task GetClientsMe_WithoutToken_Returns401()
    {
        // RED stub — implement when GET /api/clients/me exists with [Authorize] in Plan 03
        true.ShouldBeFalse("RED stub — not implemented yet");
        await Task.CompletedTask;
    }
}
```

Criar `tests/Onboarding.API.Tests/Api/ClientsMeEndpointTests.cs`:

```csharp
using Shouldly;
using System.Net;
using System.Net.Http.Headers;
using Onboarding.API.Tests.Authentication;

namespace Onboarding.API.Tests.Api;

/// <summary>
/// RED stubs for AUTH-03: GET /api/clients/me returns 200 with valid JWT.
/// Test name matches VALIDATION.md task 06-01-03: ClientsMeEndpointTests.
/// </summary>
[Collection(WebAppFactoryCollection.Name)]
public class ClientsMeEndpointTests : IAsyncLifetime
{
    private AuthTestApiFactory? _factory;
    private HttpClient? _client;

    public Task InitializeAsync()
    {
        _factory = new AuthTestApiFactory();
        _client = _factory.CreateClient();
        return Task.CompletedTask;
    }

    public async Task DisposeAsync()
    {
        _client?.Dispose();
        if (_factory is not null)
            await _factory.DisposeAsync();
    }

    [Fact]
    [Trait("Category", "Integration")]
    public async Task GetMe_WithValidToken_Returns200WithClientProfile()
    {
        // RED stub — implement when GET /api/clients/me + GetByEmailAsync + ClientsController exist in Plan 03
        true.ShouldBeFalse("RED stub — not implemented yet");
        await Task.CompletedTask;
    }

    [Fact]
    [Trait("Category", "Integration")]
    public async Task GetMe_WithoutToken_Returns401()
    {
        // RED stub — implement when [Authorize] middleware is wired in Plan 02
        true.ShouldBeFalse("RED stub — not implemented yet");
        await Task.CompletedTask;
    }

    [Fact]
    [Trait("Category", "Integration")]
    public async Task GetMe_WhenClientNotFoundInDb_Returns404()
    {
        // RED stub — implement when D-09 (404 ProblemDetails genérico) exists in Plan 03
        true.ShouldBeFalse("RED stub — not implemented yet");
        await Task.CompletedTask;
    }
}
```

Criar `tests/Onboarding.API.Tests/Api/LoginEndpointTests.cs`:

```csharp
using Shouldly;
using System.Net;
using System.Net.Http.Json;
using Onboarding.API.Tests.Authentication;

namespace Onboarding.API.Tests.Api;

/// <summary>
/// RED stubs for AUTH-02/AUTH-04: POST /api/auth/login returns access_token + refresh_token.
/// Test name matches VALIDATION.md task 06-02-01: LoginEndpointTests.
/// </summary>
[Collection(WebAppFactoryCollection.Name)]
public class LoginEndpointTests : IAsyncLifetime
{
    private AuthTestApiFactory? _factory;
    private HttpClient? _client;

    public Task InitializeAsync()
    {
        _factory = new AuthTestApiFactory();
        _client = _factory.CreateClient();
        return Task.CompletedTask;
    }

    public async Task DisposeAsync()
    {
        _client?.Dispose();
        if (_factory is not null)
            await _factory.DisposeAsync();
    }

    [Fact]
    [Trait("Category", "Integration")]
    public async Task Login_WithValidCredentials_Returns200WithTokens()
    {
        // RED stub — implement when POST /api/auth/login + AuthController + IKeycloakTokenService exist in Plan 03
        // Expected: { access_token, refresh_token, expires_in, token_type } in response body
        true.ShouldBeFalse("RED stub — not implemented yet");
        await Task.CompletedTask;
    }

    [Fact]
    [Trait("Category", "Integration")]
    public async Task Login_WithInvalidCredentials_Returns401WithGenericMessage()
    {
        // RED stub — D-13: SEC-08 generic message, no user enumeration
        true.ShouldBeFalse("RED stub — not implemented yet");
        await Task.CompletedTask;
    }

    [Fact]
    [Trait("Category", "Integration")]
    public async Task Login_WithMissingEmail_Returns422()
    {
        // RED stub — FluentValidation: email required
        true.ShouldBeFalse("RED stub — not implemented yet");
        await Task.CompletedTask;
    }

    [Fact]
    [Trait("Category", "Integration")]
    public async Task Login_WithMissingPassword_Returns422()
    {
        // RED stub — FluentValidation: password required
        true.ShouldBeFalse("RED stub — not implemented yet");
        await Task.CompletedTask;
    }
}
```

Criar `tests/Onboarding.API.Tests/Api/RefreshTokenEndpointTests.cs`:

```csharp
using Shouldly;
using System.Net;
using System.Net.Http.Json;
using Onboarding.API.Tests.Authentication;

namespace Onboarding.API.Tests.Api;

/// <summary>
/// RED stubs for AUTH-04: POST /api/auth/refresh returns new access_token.
/// Test name matches VALIDATION.md task 06-02-02: RefreshTokenEndpointTests.
/// </summary>
[Collection(WebAppFactoryCollection.Name)]
public class RefreshTokenEndpointTests : IAsyncLifetime
{
    private AuthTestApiFactory? _factory;
    private HttpClient? _client;

    public Task InitializeAsync()
    {
        _factory = new AuthTestApiFactory();
        _client = _factory.CreateClient();
        return Task.CompletedTask;
    }

    public async Task DisposeAsync()
    {
        _client?.Dispose();
        if (_factory is not null)
            await _factory.DisposeAsync();
    }

    [Fact]
    [Trait("Category", "Integration")]
    public async Task Refresh_WithValidRefreshToken_Returns200WithNewTokens()
    {
        // RED stub — implement when POST /api/auth/refresh + AuthController exist in Plan 03
        true.ShouldBeFalse("RED stub — not implemented yet");
        await Task.CompletedTask;
    }

    [Fact]
    [Trait("Category", "Integration")]
    public async Task Refresh_WithInvalidRefreshToken_Returns401WithGenericMessage()
    {
        // RED stub — D-13: SEC-08 generic message for expired/invalid refresh token
        true.ShouldBeFalse("RED stub — not implemented yet");
        await Task.CompletedTask;
    }

    [Fact]
    [Trait("Category", "Integration")]
    public async Task Refresh_WithMissingRefreshToken_Returns422()
    {
        // RED stub — FluentValidation: refresh_token required
        true.ShouldBeFalse("RED stub — not implemented yet");
        await Task.CompletedTask;
    }
}
```
  </action>
  <verify>
    <automated>dotnet test tests/Onboarding.API.Tests/ --filter "Category=Unit" --no-build 2>&1 | grep -E "(FAILED|passed|error)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/Onboarding.API.Tests/Authentication/JwtBearerConfigurationTests.cs existe e contém "RED stub"
    - tests/Onboarding.API.Tests/Authentication/AuthorizationMiddlewareTests.cs existe e contém "[Collection(WebAppFactoryCollection.Name)]"
    - tests/Onboarding.API.Tests/Api/ClientsMeEndpointTests.cs existe e contém "GetMe_WithValidToken_Returns200WithClientProfile"
    - tests/Onboarding.API.Tests/Api/LoginEndpointTests.cs existe e contém "Login_WithValidCredentials_Returns200WithTokens"
    - tests/Onboarding.API.Tests/Api/RefreshTokenEndpointTests.cs existe e contém "Refresh_WithValidRefreshToken_Returns200WithNewTokens"
    - Todos os stubs contêm "true.ShouldBeFalse" (padrão RED do projeto)
    - Todos os arquivos contêm Trait("Category", "Unit") ou Trait("Category", "Integration")
    - `dotnet test --filter "FullyQualifiedName~JwtBearerConfigurationTests"` retorna FAILED (não "no tests found")
  </acceptance_criteria>
  <done>12 stubs RED criados em 5 arquivos de teste. Todos falham com ShouldBeFalse ao executar. Ciclo TDD RED confirmado.</done>
</task>

</tasks>

<threat_model>
  <threat id="T-6-01" severity="low">
    <description>Stubs de teste com tokens falsos poderiam vazar para builds de produção se o AuthTestApiFactory fosse incluído no assembly de produção.</description>
    <mitigation>AuthTestApiFactory existe apenas em tests/Onboarding.API.Tests/ — projeto de teste separado sem referência no Onboarding.API.csproj. Estrutura de solução garante isolamento.</mitigation>
  </threat>
</threat_model>

<verification>
Após executar este plano:

```bash
# Verificar que os 7 arquivos existem
ls tests/Onboarding.API.Tests/Authentication/
ls tests/Onboarding.API.Tests/Api/

# Verificar que os stubs compilam (com erros esperados por IKeycloakTokenService ainda não existir)
dotnet build tests/Onboarding.API.Tests/Onboarding.API.Tests.csproj

# Confirmar que stubs Unit falham (RED)
dotnet test tests/Onboarding.API.Tests/ --filter "Category=Unit"
```

O build PODE falhar até o Plan 02 criar `IKeycloakTokenService`. Isso é esperado no Wave 0 — stubs são criados antes do código de produção.
</verification>

<success_criteria>
- 7 arquivos criados (FakeJwtTokenHelper, AuthTestApiFactory, 5 arquivos de stub)
- Cada stub tem um test method nomeado correspondente ao VALIDATION.md
- Todos os stubs usam o padrão RED: `true.ShouldBeFalse("RED stub — not implemented yet")`
- AuthTestApiFactory usa PostConfigure (não Configure) para desabilitar JWT validation
- Nenhum arquivo de código de produção é criado ou modificado neste plano
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-authentication-api/06-01-SUMMARY.md` com:
- Arquivos criados
- Contagem de stubs RED por requisito
- Nota sobre dependência: IKeycloakTokenService deve ser criado no Plan 02 para AuthTestApiFactory compilar
</output>

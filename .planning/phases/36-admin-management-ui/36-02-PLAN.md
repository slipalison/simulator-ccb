---
phase: 36-admin-management-ui
plan: 02
type: execute
wave: 2
depends_on:
  - "36-01"
files_modified:
  - frontend/backoffice/src/components/molecules/AdminAdministratorsTable.tsx
  - frontend/backoffice/src/components/molecules/AdminActionsDropdown.tsx
autonomous: true
requirements:
  - MGMT-01
  - MGMT-02
  - MGMT-05
  - MGMT-06

must_haves:
  truths:
    - "AdminAdministratorsTable renderiza tabela semântica com colunas Nome, Email, Status, Senha Temp, Ações"
    - "Skeleton de 5 linhas é exibido quando isLoading=true e result=null"
    - "Quando result existe e isLoading=true (refetch), tabela exibe dados com opacity-60"
    - "AdminActionsDropdown exibe botão ⋯ desabilitado com Tooltip na própria linha (D-06, D-07)"
    - "Dropdown contextual: admin ativo mostra Desativar; admin inativo mostra Reativar (D-02)"
    - "Itens fixos Editar e Resetar senha presentes em todos dropdowns não-self (D-03)"
  artifacts:
    - path: "frontend/backoffice/src/components/molecules/AdminAdministratorsTable.tsx"
      provides: "Tabela de administradores com skeleton loading e estados empty/error"
      exports: ["AdminAdministratorsTable"]
    - path: "frontend/backoffice/src/components/molecules/AdminActionsDropdown.tsx"
      provides: "Dropdown contextual por linha com detecção de self (D-06, D-07, D-08)"
      exports: ["AdminActionsDropdown"]
  key_links:
    - from: "frontend/backoffice/src/components/molecules/AdminActionsDropdown.tsx"
      to: "frontend/backoffice/src/lib/admin-auth-context.tsx"
      via: "useAdminAuth().admin.adminId"
      pattern: "useAdminAuth|adminId"
    - from: "frontend/backoffice/src/components/molecules/AdminAdministratorsTable.tsx"
      to: "frontend/backoffice/src/components/molecules/AdminActionsDropdown.tsx"
      via: "prop onEdit, onResetPassword, onDeactivate, onReactivate"
      pattern: "AdminActionsDropdown"
---

<objective>
Criar dois novos componentes moleculares reutilizáveis: a tabela de administradores com todos seus estados visuais, e o dropdown contextual de ações por linha com lógica de self-detection.

Purpose: Isolar a tabela e o dropdown em componentes testáveis independentes antes de integrá-los na página (Plano 04).
Output: AdminAdministratorsTable.tsx e AdminActionsDropdown.tsx — componentes puros que recebem dados e callbacks via props.
</objective>

<execution_context>
@D:\REPO\keycloak-tests\.claude\get-shit-done\workflows\execute-plan.md
@D:\REPO\keycloak-tests\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@D:\REPO\keycloak-tests\.planning\phases\36-admin-management-ui\36-CONTEXT.md
@D:\REPO\keycloak-tests\.planning\phases\36-admin-management-ui\36-UI-SPEC.md

<interfaces>
<!-- Contratos estabelecidos no Plano 01 (disponíveis após wave 1) -->

De frontend/backoffice/src/lib/admin-api.ts:
```typescript
export interface AdminUserDto {
  id: string;
  email: string;
  fullName: string;
  isEnabled: boolean;
  hasTemporaryPassword: boolean;
}

export interface PaginatedResult<T> {
  items: T[];
  totalCount: number;
  page: number;
  pageSize: number;
}
```

De frontend/backoffice/src/lib/admin-auth-context.tsx (após Plano 01):
```typescript
// useAdminAuth().admin.adminId — sub do JWT do admin logado (string | null)
const { admin } = useAdminAuth();
const isSelf = admin.adminId === targetAdmin.id;
```

De frontend/backoffice/src/components/ui/ (shadcn — disponíveis):
```typescript
// dropdown-menu.tsx: DropdownMenu, DropdownMenuTrigger, DropdownMenuContent,
//   DropdownMenuItem, DropdownMenuSeparator
// tooltip.tsx: Tooltip, TooltipTrigger, TooltipContent, TooltipProvider
// skeleton.tsx: Skeleton
// badge.tsx: Badge — variant="default"|"destructive"|"outline"
// button.tsx: Button — variant="ghost" size="icon"
```

Padrão de skeleton do AdminUsersPage (referência):
```typescript
// Renderizar N linhas enquanto isLoading && !result
// Cada linha: <Skeleton className="h-4 w-full" /> por célula
// Durante refetch (isLoading && result): tabela com opacity-60
```

Ícone MoreHorizontal (lucide-react):
```typescript
import { MoreHorizontal } from "lucide-react";
// <MoreHorizontal className="h-4 w-4" />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar AdminActionsDropdown — botão ⋯ contextual por linha com self-detection</name>
  <files>frontend/backoffice/src/components/molecules/AdminActionsDropdown.tsx</files>
  <read_first>
    - frontend/backoffice/src/components/ui/dropdown-menu.tsx — verificar exports disponíveis
    - frontend/backoffice/src/components/ui/tooltip.tsx — verificar exports disponíveis
    - frontend/backoffice/src/components/ui/button.tsx — verificar variant="ghost" size="icon"
    - frontend/backoffice/src/lib/admin-auth-context.tsx — confirmar que adminId está disponível após Plano 01
  </read_first>
  <action>
Criar arquivo frontend/backoffice/src/components/molecules/AdminActionsDropdown.tsx:

```typescript
import { MoreHorizontal } from "lucide-react";
import { Button } from "@/components/ui/button";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuSeparator,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu";
import {
  Tooltip,
  TooltipContent,
  TooltipProvider,
  TooltipTrigger,
} from "@/components/ui/tooltip";
import { useAdminAuth } from "@/lib/admin-auth-context";
import type { AdminUserDto } from "@/lib/admin-api";

interface AdminActionsDropdownProps {
  admin: AdminUserDto;
  onEdit: (admin: AdminUserDto) => void;
  onResetPassword: (admin: AdminUserDto) => void;
  onDeactivate: (admin: AdminUserDto) => void;
  onReactivate: (admin: AdminUserDto) => void;
}

export function AdminActionsDropdown({
  admin,
  onEdit,
  onResetPassword,
  onDeactivate,
  onReactivate,
}: AdminActionsDropdownProps) {
  const { admin: authAdmin } = useAdminAuth();
  const isSelf = authAdmin.adminId !== null && authAdmin.adminId === admin.id;

  if (isSelf) {
    return (
      <TooltipProvider>
        <Tooltip>
          <TooltipTrigger asChild>
            {/* span wrapper necessário pois Button disabled não dispara eventos de mouse para Tooltip */}
            <span className="inline-flex" aria-disabled="true">
              <Button
                variant="ghost"
                size="icon"
                disabled
                className="opacity-50 cursor-not-allowed h-8 w-8"
                aria-label="Ações desabilitadas"
                data-testid="actions-dropdown-disabled"
              >
                <MoreHorizontal className="h-4 w-4" />
              </Button>
            </span>
          </TooltipTrigger>
          <TooltipContent>
            <p>Você não pode modificar a própria conta</p>
          </TooltipContent>
        </Tooltip>
      </TooltipProvider>
    );
  }

  return (
    <DropdownMenu>
      <DropdownMenuTrigger asChild>
        <Button
          variant="ghost"
          size="icon"
          className="h-8 w-8"
          aria-label="Abrir menu de ações"
          data-testid="actions-dropdown-trigger"
        >
          <MoreHorizontal className="h-4 w-4" />
        </Button>
      </DropdownMenuTrigger>
      <DropdownMenuContent align="end" data-testid="actions-dropdown-content">
        <DropdownMenuItem
          onClick={() => onEdit(admin)}
          data-testid="action-edit"
        >
          Editar
        </DropdownMenuItem>
        <DropdownMenuItem
          onClick={() => onResetPassword(admin)}
          data-testid="action-reset-password"
        >
          Resetar senha
        </DropdownMenuItem>
        <DropdownMenuSeparator />
        {admin.isEnabled ? (
          <DropdownMenuItem
            onClick={() => onDeactivate(admin)}
            className="text-destructive focus:text-destructive"
            data-testid="action-deactivate"
          >
            Desativar
          </DropdownMenuItem>
        ) : (
          <DropdownMenuItem
            onClick={() => onReactivate(admin)}
            data-testid="action-reactivate"
          >
            Reativar
          </DropdownMenuItem>
        )}
      </DropdownMenuContent>
    </DropdownMenu>
  );
}
```

Notas de implementação:
- D-02: item de status é EXCLUSIVO por estado (nunca Desativar e Reativar juntos)
- D-03: Editar e Resetar senha SEMPRE presentes (exceto própria linha)
- D-06: própria linha tem botão disabled + opacity-50
- D-07: Tooltip exibe "Você não pode modificar a própria conta"
- D-08: comparação `authAdmin.adminId === admin.id`
- Separador antes do item de status (destruição visual)
- TooltipProvider wrapping necessário para Tooltip funcionar
  </action>
  <verify>
    <automated>cd "D:/REPO/keycloak-tests/frontend/backoffice" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo criado em frontend/backoffice/src/components/molecules/AdminActionsDropdown.tsx
    - grep "export function AdminActionsDropdown" retorna match
    - grep "useAdminAuth" retorna match (importação do context)
    - grep "adminId.*admin.id\|admin.id.*adminId" retorna match (comparação self)
    - grep "action-deactivate\|action-reactivate" retorna ambos (itens contextuais)
    - grep "actions-dropdown-disabled" retorna match (botão desabilitado para self)
    - grep "Você não pode modificar a própria conta" retorna match (tooltip D-07)
    - npx tsc --noEmit no diretório backoffice não retorna erros
  </acceptance_criteria>
  <done>AdminActionsDropdown exportado; botão ⋯ desabilitado com tooltip para própria linha; dropdown contextual com Desativar/Reativar exclusivos; Editar e Resetar senha sempre presentes.</done>
</task>

<task type="auto">
  <name>Task 2: Criar AdminAdministratorsTable — tabela semântica com skeleton loading e estados da UI</name>
  <files>frontend/backoffice/src/components/molecules/AdminAdministratorsTable.tsx</files>
  <read_first>
    - frontend/backoffice/src/components/pages/AdminAdministratorsPage.tsx — ver estrutura atual da tabela (colunas, classes CSS, badges)
    - frontend/backoffice/src/components/ui/skeleton.tsx — confirmar que Skeleton não precisa de props obrigatórias
    - frontend/backoffice/src/components/ui/badge.tsx — confirmar variants disponíveis
  </read_first>
  <action>
Criar arquivo frontend/backoffice/src/components/molecules/AdminAdministratorsTable.tsx:

```typescript
import { Skeleton } from "@/components/ui/skeleton";
import { Badge } from "@/components/ui/badge";
import { AdminActionsDropdown } from "@/components/molecules/AdminActionsDropdown";
import type { AdminUserDto, PaginatedResult } from "@/lib/admin-api";

interface AdminAdministratorsTableProps {
  result: PaginatedResult<AdminUserDto> | null;
  isLoading: boolean;
  isError: boolean;
  onRetry: () => void;
  onEdit: (admin: AdminUserDto) => void;
  onResetPassword: (admin: AdminUserDto) => void;
  onDeactivate: (admin: AdminUserDto) => void;
  onReactivate: (admin: AdminUserDto) => void;
}

// Número de linhas de skeleton exibidas durante loading inicial
const SKELETON_ROWS = 5;

export function AdminAdministratorsTable({
  result,
  isLoading,
  isError,
  onRetry,
  onEdit,
  onResetPassword,
  onDeactivate,
  onReactivate,
}: AdminAdministratorsTableProps) {
  // Estado: loading inicial (sem dados anteriores)
  if (isLoading && !result) {
    return (
      <div
        aria-busy="true"
        aria-label="Carregando administradores..."
        data-testid="table-loading"
      >
        <table className="w-full text-sm">
          <thead>
            <tr className="border-b bg-muted/50">
              <th className="text-left py-3 px-4 font-semibold text-muted-foreground">Nome</th>
              <th className="text-left py-3 px-4 font-semibold text-muted-foreground">Email</th>
              <th className="text-left py-3 px-4 font-semibold text-muted-foreground w-[100px]">Status</th>
              <th className="text-left py-3 px-4 font-semibold text-muted-foreground w-[120px]">Senha Temp</th>
              <th className="text-left py-3 px-4 font-semibold text-muted-foreground w-[64px]">Ações</th>
            </tr>
          </thead>
          <tbody>
            {Array.from({ length: SKELETON_ROWS }).map((_, i) => (
              <tr key={i} className="border-b last:border-0">
                <td className="py-3 px-4"><Skeleton className="h-4 w-full" /></td>
                <td className="py-3 px-4"><Skeleton className="h-4 w-full" /></td>
                <td className="py-3 px-4"><Skeleton className="h-4 w-16" /></td>
                <td className="py-3 px-4"><Skeleton className="h-4 w-16" /></td>
                <td className="py-3 px-4"><Skeleton className="h-8 w-8 rounded" /></td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    );
  }

  // Estado: erro sem dados
  if (isError && !result) {
    return (
      <div
        className="p-6 text-center text-sm text-destructive"
        data-testid="table-error"
      >
        Falha ao carregar administradores. Tente novamente.{" "}
        <button
          type="button"
          className="underline hover:no-underline"
          onClick={onRetry}
          data-testid="retry-button"
        >
          Tentar novamente
        </button>
      </div>
    );
  }

  // Estado: sem dados após carregamento
  if (!isLoading && result && result.totalCount === 0) {
    return (
      <div
        className="p-6 text-center space-y-1"
        data-testid="table-empty"
      >
        <p className="text-sm font-semibold">Nenhum administrador encontrado.</p>
        <p className="text-sm text-muted-foreground">Ajuste os filtros ou crie um novo administrador.</p>
      </div>
    );
  }

  // Estado: dados disponíveis (com ou sem refetch em progresso)
  const items = result?.items ?? [];

  return (
    <div
      className={isLoading ? "opacity-60 pointer-events-none" : undefined}
      aria-busy={isLoading}
      data-testid="administrators-table-wrapper"
    >
      <table className="w-full text-sm" data-testid="administrators-table">
        <thead>
          <tr className="border-b bg-muted/50">
            <th scope="col" className="text-left py-3 px-4 font-semibold text-muted-foreground">Nome</th>
            <th scope="col" className="text-left py-3 px-4 font-semibold text-muted-foreground">Email</th>
            <th scope="col" className="text-left py-3 px-4 font-semibold text-muted-foreground w-[100px]">Status</th>
            <th scope="col" className="text-left py-3 px-4 font-semibold text-muted-foreground w-[120px]">Senha Temp</th>
            <th scope="col" className="text-left py-3 px-4 font-semibold text-muted-foreground w-[64px]">Ações</th>
          </tr>
        </thead>
        <tbody>
          {items.map((admin) => (
            <tr
              key={admin.id}
              className="border-b last:border-0 hover:bg-muted/30 transition-colors"
              data-testid={`admin-row-${admin.id}`}
            >
              <td className="py-3 px-4 font-semibold">{admin.fullName}</td>
              <td className="py-3 px-4 text-muted-foreground">{admin.email}</td>
              <td className="py-3 px-4">
                {admin.isEnabled ? (
                  <Badge variant="default" data-testid={`badge-status-active-${admin.id}`}>
                    Ativo
                  </Badge>
                ) : (
                  <Badge variant="destructive" data-testid={`badge-status-inactive-${admin.id}`}>
                    Inativo
                  </Badge>
                )}
              </td>
              <td className="py-3 px-4">
                {admin.hasTemporaryPassword ? (
                  <Badge
                    variant="outline"
                    className="text-amber-600 border-amber-300"
                    data-testid={`badge-temp-password-${admin.id}`}
                  >
                    Pendente
                  </Badge>
                ) : (
                  <Badge
                    variant="outline"
                    className="text-green-600 border-green-300"
                    data-testid={`badge-password-set-${admin.id}`}
                  >
                    Definida
                  </Badge>
                )}
              </td>
              <td className="py-3 px-4">
                <AdminActionsDropdown
                  admin={admin}
                  onEdit={onEdit}
                  onResetPassword={onResetPassword}
                  onDeactivate={onDeactivate}
                  onReactivate={onReactivate}
                />
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  );
}
```

Notas de implementação:
- D-13: skeleton com SKELETON_ROWS=5 linhas para loading inicial
- UI-SPEC: refetch (isLoading && result) → opacity-60 nos dados existentes, sem flash de skeleton
- UI-SPEC coluna Status: Badge "Inativo" usa variant="destructive" (não "Bloqueado")
- UI-SPEC: cabeçalhos com `font-semibold text-muted-foreground` e `scope="col"` para acessibilidade
- aria-busy="true" no container durante loading (acessibilidade)
- data-testid únicos por row usando admin.id para testes futuros
  </action>
  <verify>
    <automated>cd "D:/REPO/keycloak-tests/frontend/backoffice" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo criado em frontend/backoffice/src/components/molecules/AdminAdministratorsTable.tsx
    - grep "export function AdminAdministratorsTable" retorna match
    - grep "AdminActionsDropdown" retorna match (importação e uso)
    - grep "SKELETON_ROWS = 5" retorna match (skeleton count)
    - grep "opacity-60" retorna match (estado de refetch)
    - grep "Inativo" retorna match (badge para admin inativo — NÃO "Bloqueado")
    - grep "Ativo" retorna match (badge para admin ativo)
    - grep "Nenhum administrador encontrado" retorna match (empty state heading)
    - grep "Ajuste os filtros" retorna match (empty state body)
    - grep "Falha ao carregar administradores. Tente novamente." retorna match (error state)
    - grep "scope=\"col\"" retorna match (cabeçalhos semânticos)
    - npx tsc --noEmit no diretório backoffice não retorna erros
  </acceptance_criteria>
  <done>AdminAdministratorsTable exportado; 5 estados corretos (loading inicial, refetch, erro, vazio, dados); tabela semântica com scope="col"; AdminActionsDropdown integrado; badges Ativo/Inativo corretos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Frontend UI → Auth Context | adminId lido do contexto para detectar própria linha |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03 | Tampering | AdminActionsDropdown — self-detection client-side | accept | SEC-01 é enforçado server-side; a detecção client-side é apenas UX — o backend rejeita operações self com 400 independentemente |
| T-36-04 | Denial of Service | AdminAdministratorsTable — opacity-60 durante refetch | accept | pointer-events-none durante refetch evita double-submit acidental |
</threat_model>

<verification>
1. `cd D:/REPO/keycloak-tests/frontend/backoffice && npx tsc --noEmit` — zero erros
2. `grep "export function AdminActionsDropdown" frontend/backoffice/src/components/molecules/AdminActionsDropdown.tsx` — match
3. `grep "export function AdminAdministratorsTable" frontend/backoffice/src/components/molecules/AdminAdministratorsTable.tsx` — match
4. `grep "Inativo" frontend/backoffice/src/components/molecules/AdminAdministratorsTable.tsx` — match (badge correto)
</verification>

<success_criteria>
- AdminActionsDropdown: botão self desabilitado com tooltip, dropdown contextual Desativar/Reativar por status
- AdminAdministratorsTable: 5 estados visuais corretos, tabela semântica, skeleton 5 linhas, refetch com opacity
- Zero erros TypeScript
- Nenhum componente existente quebrado
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/36-admin-management-ui/36-02-SUMMARY.md`
</output>

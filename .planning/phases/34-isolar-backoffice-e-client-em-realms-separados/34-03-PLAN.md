---
wave: 3
depends_on: ["34-02-PLAN"]
files_modified:
  - "src/Onboarding.Infrastructure/Keycloak/KeycloakUserService.cs"
  - "src/Onboarding.Infrastructure/Keycloak/IKeycloakUserService.cs"
  - "src/Onboarding.API/appsettings.json"
autonomous: false
---

# Plan 34-03: Backend Infrastructure & SDK 

## Tasks

```xml
<task>
  <description>Refactor KeycloakUserService for Multi-Realm</description>
  <read_first>
    - src/Onboarding.Infrastructure/Keycloak/KeycloakUserService.cs
  </read_first>
  <action>
    Modify `KeycloakUserService` to accept a `targetRealm` parameter for operations. If the SDK `IKeycloakUserClient` strictly locks to one realm, you must either:
    - Register two HTTP clients / SDK clients.
    - Or rewrite the wrapper to use raw `HttpClient` for the second realm.
    Since Keycloak Admin API uses `/admin/realms/{realm}/users`, you can often just pass the `realm` in the URL if using raw HttpClient, or check if the SDK supports passing a realm name dynamically (e.g. `CreateUserAsync(realm, user)`).
    *This requires manual intervention or careful code writing during execution.*
  </action>
  <acceptance_criteria>
    - Service supports operations on both "backoffice" and "client" realms.
  </acceptance_criteria>
</task>

<task>
  <description>Update AppSettings with Dual Realm Configuration</description>
  <read_first>
    - src/Onboarding.API/appsettings.json
  </read_first>
  <action>
    Add `BackofficeRealmUrl` and `ClientRealmUrl` to the `Keycloak` section in `appsettings.json`.
  </action>
  <acceptance_criteria>
    - `appsettings.json` contains `BackofficeRealmUrl` and `ClientRealmUrl`.
  </acceptance_criteria>
</task>
```

---
phase: 07-frontend-foundation
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - frontend/vitest.config.ts
  - frontend/src/tests/setup.ts
  - frontend/src/tests/smoke.test.tsx
  - frontend/src/tests/atomic-structure.test.ts
  - frontend/src/tests/routing.test.tsx
  - frontend/src/tests/form-validation.test.tsx
autonomous: true
requirements:
  - FRONT-01
  - FRONT-02
  - FRONT-03
  - FRONT-04
  - FRONT-05

must_haves:
  truths:
    - "Vitest esta configurado no projeto frontend e roda com `npm test`"
    - "Quatro arquivos de teste existem — um por criterio de sucesso da fase"
    - "Todos os 10 stubs falham (RED) antes de qualquer implementacao"
    - "Os stubs usam falha explicita (`throw new Error`) — nao dependem de estado do filesystem"
  artifacts:
    - path: "frontend/vitest.config.ts"
      provides: "Configuracao do Vitest com jsdom environment e alias @/*"
      contains: "jsdom"
    - path: "frontend/src/tests/setup.ts"
      provides: "Import de @testing-library/jest-dom para matchers DOM"
    - path: "frontend/src/tests/smoke.test.tsx"
      provides: "Stub RED: app renderiza sem erros (criterio 1)"
    - path: "frontend/src/tests/atomic-structure.test.ts"
      provides: "Stub RED: 5 niveis Atomic Design existem (criterio 2)"
    - path: "frontend/src/tests/routing.test.tsx"
      provides: "Stub RED: rota desconhecida exibe NotFoundPage tipada (criterio 3)"
    - path: "frontend/src/tests/form-validation.test.tsx"
      provides: "Stub RED: RHF+Zod exibe erros inline (criterio 4)"
  key_links:
    - from: "frontend/vitest.config.ts"
      to: "frontend/package.json scripts.test"
      via: "npm test"
      pattern: "vitest"
---

<objective>
Configurar Vitest no frontend e criar 4 arquivos de teste com stubs genuinamente RED.
Os stubs cobrem todos os criterios de sucesso da Fase 7 e falham explicitamente com
`throw new Error('not implemented')` — nunca por ausencia de arquivo ou estado externo.

Proposito: Estabelecer o contrato de validacao da fase antes de qualquer implementacao.
O plano 07-03 tornara todos esses stubs GREEN.

Output: vitest.config.ts + setup.ts + 4 arquivos de teste RED em frontend/src/tests/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-frontend-foundation/07-CONTEXT.md
@.planning/phases/07-frontend-foundation/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Instalar dependencias de teste e criar vitest.config.ts</name>
  <files>
    frontend/vitest.config.ts
    frontend/src/tests/setup.ts
    frontend/package.json
  </files>
  <action>
A partir do diretorio raiz do repositorio, instalar as dependencias de teste no frontend:

```bash
cd frontend && npm install -D vitest@^4.1.2 @testing-library/react@^16.3.2 @testing-library/jest-dom@^6.9.1 @testing-library/user-event@^14.0.0 jsdom@^26.0.0 @vitejs/plugin-react@^6.0.1
```

Criar `frontend/vitest.config.ts`:

```typescript
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/tests/setup.ts'],
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
})
```

Criar `frontend/src/tests/setup.ts`:

```typescript
import '@testing-library/jest-dom'
```

Adicionar os scripts `test` e `test:watch` em `frontend/package.json` sem remover os scripts
existentes (`dev`, `build`, `start`). O scripts block resultante deve ser:

```json
{
  "dev": "vinxi dev --port 5173 --host",
  "build": "vinxi build",
  "start": "vinxi start",
  "test": "vitest run",
  "test:watch": "vitest"
}
```

Nota: o `"type": "module"` ja existe no package.json — preservar.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --reporter=verbose 2>&1 | tail -5 || true</automated>
  </verify>
  <done>
vitest.config.ts existe com `environment: 'jsdom'` e alias `@/*`.
setup.ts existe com import de @testing-library/jest-dom.
`npm test` executa sem erro de configuracao (pode sair com codigo != 0 por ausencia de testes — isso e aceitavel aqui).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Criar 4 arquivos de stubs RED (10 stubs no total)</name>
  <files>
    frontend/src/tests/smoke.test.tsx
    frontend/src/tests/atomic-structure.test.ts
    frontend/src/tests/routing.test.tsx
    frontend/src/tests/form-validation.test.tsx
  </files>
  <behavior>
    - Todos os 10 stubs DEVEM falhar com "not implemented"
    - NENHUM stub deve verificar filesystem — usar `throw new Error(...)` diretamente
    - smoke.test.tsx: 1 stub (criterio 1 — app renderiza)
    - atomic-structure.test.ts: 5 stubs (criterio 2 — um por nivel Atomic)
    - routing.test.tsx: 2 stubs (criterio 3 — 404 route + typed component)
    - form-validation.test.tsx: 3 stubs (criterio 4 — erros inline RHF+Zod)
  </behavior>
  <action>
Criar os 4 arquivos de stubs. CADA stub usa `throw new Error(...)`. Nao usar verificacoes
de filesystem (`fs.existsSync`) — os stubs devem falhar de forma deterministicamente independente
do estado do projeto.

**frontend/src/tests/smoke.test.tsx:**
```typescript
// Criterio de sucesso 1: docker compose up + root URL carrega sem erros
// Substituir em 07-03 por um teste de render real do componente raiz
import { describe, it } from 'vitest'

describe('App smoke test', () => {
  it('renders root without throwing', () => {
    throw new Error('not implemented: app smoke test — implementar em 07-03')
  })
})
```

**frontend/src/tests/atomic-structure.test.ts:**
```typescript
// Criterio de sucesso 2: estrutura Atomic Design com um componente em cada nivel
// Substituir em 07-03 por imports reais dos componentes criados em 07-02
import { describe, it } from 'vitest'

describe('Atomic Design structure', () => {
  it('has an atom component (AppButton)', () => {
    throw new Error('not implemented: atom — implementar em 07-02 e verificar em 07-03')
  })

  it('has a molecule component (LabeledField)', () => {
    throw new Error('not implemented: molecule — implementar em 07-02 e verificar em 07-03')
  })

  it('has an organism component (ExampleForm)', () => {
    throw new Error('not implemented: organism — implementar em 07-02/03 e verificar em 07-03')
  })

  it('has a template component (PageLayout)', () => {
    throw new Error('not implemented: template — implementar em 07-02 e verificar em 07-03')
  })

  it('has a page component (HomePage or NotFoundPage)', () => {
    throw new Error('not implemented: page — implementar em 07-02 e verificar em 07-03')
  })
})
```

**frontend/src/tests/routing.test.tsx:**
```typescript
// Criterio de sucesso 3: rota desconhecida exibe NotFoundPage tipada via notFoundComponent
// Substituir em 07-03 apos TanStack Router estar implementado em 07-02
import { describe, it } from 'vitest'

describe('TanStack Router 404 handling', () => {
  it('shows NotFoundPage on unknown route', () => {
    throw new Error('not implemented: 404 routing — implementar em 07-02 e verificar em 07-03')
  })

  it('NotFoundPage is a typed component registered via notFoundComponent (not NotFoundRoute)', () => {
    throw new Error('not implemented: typed NotFoundPage via notFoundComponent — implementar em 07-02 e verificar em 07-03')
  })
})
```

**frontend/src/tests/form-validation.test.tsx:**
```typescript
// Criterio de sucesso 4: RHF+Zod exibe erros inline abaixo dos campos
// Substituir em 07-03 apos ExampleForm estar implementado
import { describe, it } from 'vitest'

describe('React Hook Form + Zod inline validation', () => {
  it('shows inline error below name field when empty on submit', () => {
    throw new Error('not implemented: RHF+Zod inline error — implementar em 07-03')
  })

  it('shows inline error below email field when format is invalid on submit', () => {
    throw new Error('not implemented: RHF+Zod email error — implementar em 07-03')
  })

  it('does not show validation errors before first submit attempt', () => {
    throw new Error('not implemented: RHF mode=onSubmit behavior — implementar em 07-03')
  })
})
```

Confirmar que todos os stubs falham:
```bash
cd frontend && npm test 2>&1 | grep -E "not implemented|FAIL"
```
Esperado: 10 linhas com "not implemented".
  </action>
  <verify>
    <automated>cd frontend && npm test 2>&1 | grep "not implemented" | wc -l</automated>
  </verify>
  <done>
10 stubs existem e todos falham. `npm test` mostra 10 testes falhando com "not implemented".
Nenhum teste passa — todos RED confirmados.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner -> filesystem | Vitest le arquivos de teste do disco local (dev only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-00-01 | Information Disclosure | vitest.config.ts | accept | Arquivo de configuracao de dev — sem dados sensiveis; nao incluido em imagem de producao |
</threat_model>

<verification>
Apos ambas as tarefas:
1. `ls frontend/vitest.config.ts frontend/src/tests/setup.ts` — ambos existem
2. `ls frontend/src/tests/` — 5 arquivos (setup.ts + 4 testes)
3. `cd frontend && npm test 2>&1 | grep "not implemented" | wc -l` — retorna 10
4. `cd frontend && npm test 2>&1 | grep "passed" | grep -v "0 passed"` — retorna vazio (zero passando)
</verification>

<success_criteria>
- vitest.config.ts existe com `environment: 'jsdom'` e alias `@/*`
- setup.ts importa @testing-library/jest-dom
- 4 arquivos de teste existem em frontend/src/tests/
- Todos os 10 stubs falham com "not implemented" — zero testes passando
- `npm test` executa sem erro de configuracao (apenas falha nos stubs, nao na infra de testes)
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/07-frontend-foundation/07-frontend-foundation-00-SUMMARY.md`
seguindo o template em @$HOME/.claude/get-shit-done/templates/summary.md
</output>

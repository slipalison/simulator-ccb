---
phase: 15-production-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Onboarding.API/Controllers/AuthController.cs
  - src/Onboarding.API/Program.cs
  - src/Onboarding.API/appsettings.json
  - src/Onboarding.API/appsettings.Development.json
  - src/Onboarding.API/appsettings.Production.json
  - frontend/src/client.tsx (delete)
  - frontend/src/components/molecules/LabeledField.tsx (delete)
  - tests/Onboarding.API.Tests/Authentication/JwtBearerConfigurationTests.cs
  - tests/Onboarding.API.Tests/Authentication/AuthorizationMiddlewareTests.cs
  - tests/Onboarding.API.Tests/Api/RefreshTokenEndpointTests.cs
  - tests/Onboarding.API.Tests/Api/LoginEndpointTests.cs
  - tests/Onboarding.API.Tests/Api/ClientsMeEndpointTests.cs
  - tests/Onboarding.API.Tests/HealthChecks/HealthCheckEndpointTests.cs
autonomous: true
---

<objective>
Production cleanup: configure cookie Secure flag per environment, delete dead code, fix failing tests, remove stale TDD comments.

Purpose: Close P1-01 (cookie security), P1-02 (dead code), and residual tech debt from v2.0 audit to bring codebase to production-ready quality.
Output: Clean codebase with environment-configured cookies, no orphan files, all 55 backend tests passing.
</objective>

<execution_context>
@~/.qwen/get-shit-done/workflows/execute-plan.md
@~/.qwen/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/Onboarding.API/Controllers/AuthController.cs
@src/Onboarding.API/Program.cs
@tests/Onboarding.API.Tests/Authentication/JwtBearerConfigurationTests.cs
@tests/Onboarding.API.Tests/Authentication/AuthorizationMiddlewareTests.cs
@tests/Onboarding.API.Tests/Api/RefreshTokenEndpointTests.cs
@tests/Onboarding.API.Tests/Api/LoginEndpointTests.cs
@tests/Onboarding.API.Tests/Api/ClientsMeEndpointTests.cs
@tests/Onboarding.API.Tests/HealthChecks/HealthCheckEndpointTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Environment-configured Cookie Secure flag</name>
  <files>src/Onboarding.API/Controllers/AuthController.cs, src/Onboarding.API/Program.cs, src/Onboarding.API/appsettings.json, src/Onboarding.API/appsettings.Development.json, src/Onboarding.API/appsettings.Production.json</files>
  <action>
    Currently Auth hardcodes `Secure = false` in CookieOptions at 5 locations in AuthController.cs (lines 65, 130, 200 for Append; lines 154, 221 for Delete).

    1. Add `CookieSecure` boolean setting to appsettings.json schema:
       - appsettings.json (base): `"CookieSecure": true`
       - appsettings.Development.json override: `"CookieSecure": false`
       - Create appsettings.Production.json: `"CookieSecure": true`

    2. In Program.cs, read `CookieSecure` from configuration and register as a singleton/options value.

    3. In AuthController, inject IConfiguration (or IOptions) and use the configured value:
       - Replace `Secure = false` with `Secure = _config.GetValue<bool>("CookieSecure")` in all CookieOptions instances (5 locations).
       - For CookieOptions.Delete, the Secure flag must match the original Append — read the same config value.

    4. Create a CookieSettings class with Secure flag, register via IOptions&lt;CookieSettings&gt; pattern for cleaner DI.

    Why IOptions over raw IConfiguration: strongly-typed, testable, follows .NET conventions.
  </action>
  <verify>dotnet build succeeds; appsettings.Development.json contains CookieSecure: false; appsettings.Production.json contains CookieSecure: true; AuthController has no hardcoded Secure = false</verify>
  <done>Cookie Secure flag is environment-configured (false for dev, true for prod). No hardcoded Secure values remain. CookieSettings class registered via IOptions.</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead code files</name>
  <files>frontend/src/client.tsx (delete), frontend/src/components/molecules/LabeledField.tsx (delete)</files>
  <action>
    Two orphan files exist:
    - frontend/src/client.tsx — Phase 1 infrastructure placeholder (13 lines, renders "Onboarding — placeholder")
    - frontend/src/components/molecules/LabeledField.tsx — Molecule component no longer imported anywhere (replaced by shadcn/ui patterns in Phase 12)

    1. Verify neither file is imported anywhere: grep for "from.*client" and "from.*LabeledField" and "import.*LabeledField" across frontend/src/
    2. Delete both files.
    3. Run frontend build to confirm no breakage: `cd frontend && npm run build`
  </action>
  <verify>grep confirms no imports reference deleted files; frontend npm run build succeeds</verify>
  <done>client.tsx and LabeledField.tsx deleted. Frontend builds without errors.</done>
</task>

<task type="auto">
  <name>Task 3: Remove stale TDD comments from test files</name>
  <files>tests/Onboarding.API.Tests/Authentication/JwtBearerConfigurationTests.cs, tests/Onboarding.API.Tests/Authentication/AuthorizationMiddlewareTests.cs, tests/Onboarding.API.Tests/Api/RefreshTokenEndpointTests.cs, tests/Onboarding.API.Tests/Api/LoginEndpointTests.cs, tests/Onboarding.API.Tests/Api/ClientsMeEndpointTests.cs</files>
  <action>
    Five test files contain stale TDD phase comments from RED→GREEN cycle:
    - JwtBearerConfigurationTests.cs: "/// GREEN tests for AUTH-02"
    - AuthorizationMiddlewareTests.cs: "/// GREEN tests for AUTH-02"
    - RefreshTokenEndpointTests.cs: "/// GREEN tests for AUTH-04"
    - LoginEndpointTests.cs: "/// GREEN tests for AUTH-02/AUTH-04"
    - ClientsMeEndpointTests.cs: "/// GREEN tests for AUTH-03"

    Remove these TDD lifecycle comments. They served their purpose during implementation but are now noise.
    Replace with descriptive XML doc comments about what the test class validates (e.g., "Tests for JWT Bearer authentication configuration" instead of "GREEN tests for AUTH-02").
  </action>
  <verify>grep for "// TDD\|// RED\|// GREEN\|// TODO.*TDD" returns 0 matches; dotnet build succeeds</verify>
  <done>All stale TDD comments removed. Test files have clean descriptive comments. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 4: Fix failing tests (3 failures → 0)</name>
  <files>tests/Onboarding.API.Tests/Api/RefreshTokenEndpointTests.cs, tests/Onboarding.API.Tests/Api/LoginEndpointTests.cs</files>
  <action>
    Three tests currently fail:
    - RefreshTokenEndpointTests.Refresh_WithValidRefreshToken_Returns200WithNewTokens [FAIL]
    - RefreshTokenEndpointTests.Refresh_WithMissingRefreshToken_Returns422 [FAIL]
    - LoginEndpointTests.Login_WithValidCredentials_Returns200WithTokens [FAIL]

    Root cause investigation required. Likely candidates:
    1. Testcontainers/Keycloak connection issues in test environment
    2. Cookie Secure flag change (Task 1) may have broken test expectations if tests assert cookie properties
    3. Mock/stub setup for Keycloak token endpoints in test factory

    Steps:
    1. Run the 3 failing tests individually with full output to identify exact assertion failures
    2. Fix the root cause (update test expectations, fix mocks, or fix implementation)
    3. Do NOT disable or skip tests — fix them.

    Common patterns to check:
    - RefreshTokenEndpointTests: May expect refresh token in response body, but it's now in cookie (AuthController returns only access token)
    - LoginEndpointTests: Same issue — refresh token in cookie, not in JSON response
  </action>
  <verify>dotnet test tests/Onboarding.API.Tests/ --filter "RefreshTokenEndpointTests|LoginEndpointTests" passes; all 3 previously failing tests now pass</verify>
  <done>All 3 failing tests fixed. Root cause identified and resolved. Tests assert cookie behavior correctly.</done>
</task>

<task type="auto">
  <name>Task 5: Full test suite verification</name>
  <files>tests/Onboarding.API.Tests/</files>
  <action>
    Run the complete backend test suite and verify zero failures.
    1. dotnet build src/Onboarding.API/
    2. dotnet test tests/Onboarding.API.Tests/
    3. Confirm: 0 failures, 0 skipped (or document any intentional skips)
  </action>
  <verify>dotnet test reports 0 failures, 0 errors, all tests passing</verify>
  <done>Full backend test suite passes with no failures. Test count: 55 (current baseline).</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Cookie Secure flag configured via IOptions&lt;CookieSettings&gt; with environment overrides
- [ ] appsettings.Development.json has CookieSecure: false
- [ ] appsettings.Production.json has CookieSecure: true
- [ ] frontend/src/client.tsx deleted, no broken imports
- [ ] frontend/src/components/molecules/LabeledField.tsx deleted, no broken imports
- [ ] No stale TDD comments remain (grep returns 0)
- [ ] All 3 previously failing tests now pass
- [ ] Full backend test suite: 0 failures
- [ ] dotnet build succeeds with no warnings
</verification>

<success_criteria>

- All tasks completed
- Cookie Secure flag is environment-configured (true in production, false in dev)
- Orphan files deleted: client.tsx, LabeledField.tsx
- Stale TDD comments removed from all 5 test files
- All backend tests passing (0 failures, 0 errors)
- No build warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/15-production-cleanup/15-01-SUMMARY.md` documenting:
- What was cleaned up (cookie config, deleted files, comment cleanup)
- Root cause of 3 failing tests and how they were fixed
- Final test suite results (count, pass rate)
- Any remaining concerns or tech debt
</output>

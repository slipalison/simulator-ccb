---
phase: 07-frontend-foundation
plan: 03
type: execute
wave: 4
depends_on:
  - 07-02-PLAN.md
files_modified:
  - frontend/src/components/organisms/ExampleForm.tsx
  - frontend/src/__tests__/smoke.test.tsx
  - frontend/src/__tests__/atomic-structure.test.ts
  - frontend/src/__tests__/routing.test.tsx
  - frontend/src/__tests__/form-validation.test.tsx
autonomous: true
requirements:
  - FRONT-01
  - FRONT-02
  - FRONT-03
  - FRONT-04
  - FRONT-05

must_haves:
  truths:
    - "ExampleForm valida com RHF + Zod e exibe erros inline antes de qualquer submit real"
    - "Todos os 10 stubs do Wave 0 estão GREEN (nenhum falha)"
    - "Erro de campo vazio aparece no DOM com role=alert logo após tentativa de submit"
    - "Submit com dados válidos não exibe erros"
  artifacts:
    - path: "frontend/src/components/organisms/ExampleForm.tsx"
      provides: "Organism completo com useForm + zodResolver + erros inline"
      contains: "zodResolver"
    - path: "frontend/src/__tests__/smoke.test.tsx"
      provides: "Stub GREEN — App renderiza sem exceção"
    - path: "frontend/src/__tests__/atomic-structure.test.ts"
      provides: "Stub GREEN — estrutura Atomic Design confirmada"
    - path: "frontend/src/__tests__/routing.test.tsx"
      provides: "Stub GREEN — NotFoundPage renderiza em rota desconhecida"
    - path: "frontend/src/__tests__/form-validation.test.tsx"
      provides: "Stub GREEN — erro inline aparece em campo inválido"
  key_links:
    - from: "frontend/src/__tests__/form-validation.test.tsx"
      to: "frontend/src/components/organisms/ExampleForm.tsx"
      via: "render(ExampleForm) + fireEvent.submit"
      pattern: "ExampleForm"
    - from: "frontend/src/__tests__/routing.test.tsx"
      to: "frontend/src/router.ts"
      via: "createMemoryHistory({ initialEntries: ['/nao-existe'] })"
      pattern: "notFoundComponent"
    - from: "ExampleForm.tsx"
      to: "zod schema"
      via: "zodResolver(schema) no useForm"
      pattern: "zodResolver"
---

<objective>
Adicionar React Hook Form v7 + Zod v3 ao ExampleForm (wiring completo com erros inline)
e converter todos os 10 stubs RED do Wave 0 em GREEN.

Esta wave é o critério de conclusão da Phase 7: quando `npm test` passa com 10/10,
a phase está completa.

Purpose: Entregar FRONT-04 (validação inline) e fechar o ciclo TDD da phase.
Wave 0 criou os contratos; esta wave os satisfaz.

Output: ExampleForm com validação real + todos os testes GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-frontend-foundation/07-CONTEXT.md
@.planning/phases/07-frontend-foundation/07-RESEARCH.md
@frontend/src/components/organisms/ExampleForm.tsx
@frontend/src/__tests__/smoke.test.tsx
@frontend/src/__tests__/routing.test.tsx
@frontend/src/__tests__/form-validation.test.tsx
@frontend/src/router.ts
</context>

<interfaces>
<!-- Contratos de que esta wave depende (criados em 07-02) -->

ExampleForm.tsx atual (Wave 2 — sem lógica RHF):
```tsx
import { LabeledField } from "@/components/molecules/LabeledField";
import { AppButton } from "@/components/atoms/AppButton";
export function ExampleForm() {
  return (
    <form className="space-y-4 w-full max-w-sm">
      <LabeledField id="name" label="Nome" inputProps={{ placeholder: "Seu nome completo" }} />
      <LabeledField id="email" label="Email" inputProps={{ type: "email", placeholder: "seu@email.com" }} />
      <AppButton type="submit" className="w-full">Enviar</AppButton>
    </form>
  );
}
```

Router (src/router.ts) — para testes de roteamento:
```typescript
export const router = createRouter({ routeTree });
// rootRoute tem notFoundComponent: NotFoundPage
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar RHF + Zod no ExampleForm (RED → GREEN para FRONT-04)</name>
  <files>
    frontend/src/components/organisms/ExampleForm.tsx
  </files>
  <behavior>
    - Submit com campo "name" vazio: erro "Nome deve ter pelo menos 2 caracteres" aparece no DOM
    - Submit com "name" de 1 caractere: mesmo erro aparece
    - Submit com "name" válido e "email" inválido: erro de email aparece, sem erro de name
    - Submit com todos os campos válidos: nenhum erro no DOM, onSubmit chamado com dados
    - Erros aparecem inline abaixo do campo (não toast, não alert dialog)
    - Erros têm role="alert" (via LabeledField — acessibilidade garantida)
  </behavior>
  <action>
Substituir `frontend/src/components/organisms/ExampleForm.tsx` pelo conteúdo com RHF + Zod:

```tsx
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { z } from "zod";
import { LabeledField } from "@/components/molecules/LabeledField";
import { AppButton } from "@/components/atoms/AppButton";

// Schema Zod — fonte da verdade para validação client-side
const exampleSchema = z.object({
  name: z
    .string()
    .min(1, "Nome é obrigatório")
    .min(2, "Nome deve ter pelo menos 2 caracteres"),
  email: z.string().min(1, "Email é obrigatório").email("Email inválido"),
});

type ExampleFormData = z.infer<typeof exampleSchema>;

/**
 * Organism: formulário de exemplo com React Hook Form + Zod.
 * Demonstra validação inline — erros aparecem abaixo de cada campo.
 * onSubmit é no-op nesta phase (sem integração de API).
 */
export function ExampleForm() {
  const {
    register,
    handleSubmit,
    formState: { errors },
  } = useForm<ExampleFormData>({
    resolver: zodResolver(exampleSchema),
    defaultValues: { name: "", email: "" },
  });

  const onSubmit = (data: ExampleFormData) => {
    // No-op nesta phase — integração com API em Phase 8
    console.log("Dados válidos:", data);
  };

  return (
    <form
      onSubmit={handleSubmit(onSubmit)}
      className="space-y-4 w-full max-w-sm"
      noValidate
    >
      <LabeledField
        id="name"
        label="Nome"
        error={errors.name?.message}
        inputProps={{
          placeholder: "Seu nome completo",
          ...register("name"),
        }}
      />
      <LabeledField
        id="email"
        label="Email"
        error={errors.email?.message}
        inputProps={{
          type: "email",
          placeholder: "seu@email.com",
          ...register("email"),
        }}
      />
      <AppButton type="submit" className="w-full">
        Enviar
      </AppButton>
    </form>
  );
}
```

NOTAS TÉCNICAS:
- `noValidate` no form desativa a validação nativa do browser — deixa o Zod validar
- `zodResolver(exampleSchema)` conecta o Zod ao RHF via @hookform/resolvers
- `errors.name?.message` passa a mensagem do Zod para o LabeledField
- LabeledField renderiza com role="alert" quando error está presente (Wave 2)
- Spread de `register("name")` em inputProps conecta o campo ao RHF
  </action>
  <verify>
    <automated>cd frontend && npm test -- --reporter=verbose 2>&1 | grep -E "form-validation|PASS|FAIL|✓|✗" | head -20</automated>
  </verify>
  <done>
ExampleForm importa useForm, zodResolver e z.
errors.name?.message e errors.email?.message passados para LabeledField.
Testes de form-validation.test.tsx passam (GREEN).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Converter todos os stubs RED em GREEN (smoke, atomic-structure, routing)</name>
  <files>
    frontend/src/__tests__/smoke.test.tsx
    frontend/src/__tests__/atomic-structure.test.ts
    frontend/src/__tests__/routing.test.tsx
    frontend/src/__tests__/form-validation.test.tsx
  </files>
  <behavior>
    - smoke: render(RouterProvider) não lança exceção — 1 teste passa
    - atomic-structure: 5 diretórios com .tsx existem — 5 testes passam
    - routing: NotFoundPage aparece ao navegar para rota desconhecida — 2 testes passam
    - form-validation: erros inline em submit inválido — 3 testes passam
    - Total: 11 testes passam (10 originais + 1 adicionado nesta task se necessário)
  </behavior>
  <action>
### smoke.test.tsx — GREEN

Substituir o conteúdo por:

```tsx
import { describe, it, expect } from "vitest";
import { render } from "@testing-library/react";
import { createMemoryHistory, RouterProvider } from "@tanstack/react-router";
import { router } from "@/router";

describe("FRONT-02: App SPA smoke test", () => {
  it("renderiza o componente raiz sem lançar exceção", () => {
    // Usar memoryHistory para evitar dependência de window.location em jsdom
    expect(() =>
      render(<RouterProvider router={router} />)
    ).not.toThrow();
  });
});
```

### atomic-structure.test.ts — GREEN

Este arquivo NÃO muda — os testes já verificam o filesystem real.
Os 5 testes ficam GREEN automaticamente quando os diretórios existirem (criados em 07-02).
Verificar que passam:
```bash
cd frontend && npx vitest run src/__tests__/atomic-structure.test.ts --reporter=verbose
```

### routing.test.tsx — GREEN

Substituir o conteúdo por:

```tsx
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
import { RouterProvider, createRouter, createMemoryHistory } from "@tanstack/react-router";
import { router } from "@/router";

// Criar router com history apontando para rota desconhecida
function renderWithUnknownRoute() {
  const memoryHistory = createMemoryHistory({ initialEntries: ["/rota-que-nao-existe"] });
  // Criar nova instância do router com history customizado para o teste
  const testRouter = createRouter({
    routeTree: router.options.routeTree,
    history: memoryHistory,
  });
  return render(<RouterProvider router={testRouter} />);
}

describe("FRONT-03: TanStack Router — rota 404 type-safe", () => {
  it("navegando para rota desconhecida renderiza NotFoundPage", () => {
    renderWithUnknownRoute();
    // NotFoundPage exibe "404" como texto
    expect(screen.getByText("404")).toBeInTheDocument();
  });

  it("NotFoundPage contém texto visível de página não encontrada", () => {
    renderWithUnknownRoute();
    expect(
      screen.getByText(/página não encontrada/i)
    ).toBeInTheDocument();
  });
});
```

### form-validation.test.tsx — GREEN

Substituir o conteúdo por:

```tsx
import { describe, it, expect } from "vitest";
import { render, screen, fireEvent, waitFor } from "@testing-library/react";
import { ExampleForm } from "@/components/organisms/ExampleForm";

describe("FRONT-04: React Hook Form + Zod — validação inline", () => {
  it("submeter ExampleForm com campo 'name' vazio mostra mensagem de erro inline", async () => {
    render(<ExampleForm />);

    // Submit sem preencher nenhum campo
    fireEvent.submit(screen.getByRole("button", { name: /enviar/i }));

    // Aguardar validação assíncrona do RHF
    await waitFor(() => {
      expect(screen.getByRole("alert")).toBeInTheDocument();
    });

    // Verificar mensagem de erro específica do Zod
    expect(screen.getByText(/nome é obrigatório|nome deve ter/i)).toBeInTheDocument();
  });

  it("mensagem de erro aparece no DOM antes de qualquer requisição de rede", async () => {
    // Spy em fetch para confirmar que nenhuma chamada de rede é feita
    const fetchSpy = vi.spyOn(globalThis, "fetch");

    render(<ExampleForm />);
    fireEvent.submit(screen.getByRole("button", { name: /enviar/i }));

    await waitFor(() => {
      expect(screen.getAllByRole("alert").length).toBeGreaterThan(0);
    });

    // Confirmar que nenhuma chamada de rede foi feita
    expect(fetchSpy).not.toHaveBeenCalled();
    fetchSpy.mockRestore();
  });
});

describe("FRONT-05: Tailwind CSS — componentes usam classes utilitárias", () => {
  it("ExampleForm possui elementos com classes Tailwind (space-y-*, text-*, etc.)", () => {
    const { container } = render(<ExampleForm />);

    // Verificar que o form tem classes Tailwind
    const form = container.querySelector("form");
    expect(form?.className).toMatch(/space-y/);
  });
});
```

### Executar suite completa e verificar GREEN

```bash
cd frontend && npm test 2>&1 | tail -20
```

Esperado: todos os testes passam. Se algum falhar, diagnosticar e corrigir antes de
considerar esta task concluída.

NOTA: `vi.spyOn` está disponível globalmente porque `vitest.config.ts` tem `globals: true`.
  </action>
  <verify>
    <automated>cd frontend && npm test 2>&1 | grep -E "Tests|passed|failed|✓|✗" | tail -10</automated>
  </verify>
  <done>
`npm test` executa e TODOS os testes passam (0 falhas).
Output mostra: Tests: X passed, 0 failed.
Nenhum stub permanece RED.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário → ExampleForm | Input do usuário validado por Zod antes de qualquer processamento |
| ExampleForm → onSubmit handler | Dados tipados só chegam ao handler após validação completa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | ExampleForm inputs | mitigate | zodResolver valida todos os inputs antes de onSubmit — dados inválidos nunca chegam ao handler |
| T-07-03-02 | Information Disclosure | erros de validação inline | accept | Mensagens de erro são genéricas (sem detalhes de sistema); mostrar "Email inválido" não é information leakage |
| T-07-03-03 | Denial of Service | submit repetido | accept | Fase de foundation — sem rate limiting. Phase 8+ adicionará proteção no submit real |
</threat_model>

<verification>
```bash
# Suite completa deve passar com 0 falhas
cd frontend && npm test -- --reporter=verbose 2>&1 | tail -30

# Verificar que ExampleForm tem zodResolver
grep "zodResolver" frontend/src/components/organisms/ExampleForm.tsx

# Verificar que router.ts tem acesso ao routeTree (para testes de routing)
grep "routeTree" frontend/src/router.ts

# Smoke test: verificar que npm run dev não falha na inicialização
# (apenas verificação de config — não bloqueia CI)
cd frontend && timeout 10 npm run dev 2>&1 | head -20 || true
```
</verification>

<success_criteria>
- `npm test` passa com 0 falhas (todos os 10+ stubs GREEN)
- ExampleForm: useForm + zodResolver + formState.errors presentes
- Erros inline: errors.name?.message e errors.email?.message passados para LabeledField
- Teste de routing: render com rota desconhecida encontra "404" e "Página não encontrada"
- Teste de form: fireEvent.submit sem dados encontra role="alert" no DOM
- Teste de Tailwind: form.className inclui "space-y"
- Nenhum teste usa `expect(true).toBe(false)` (nenhum stub permanece RED)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/07-frontend-foundation/07-frontend-foundation-03-SUMMARY.md`
seguindo o template em @$HOME/.claude/get-shit-done/templates/summary.md
</output>

---
phase: 30-audit-log-admin-backend
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - src/Onboarding.Application/Common/IAuditService.cs
  - src/Onboarding.Application/Common/IKeycloakUserService.cs
  - src/Onboarding.Application/Admin/Queries/GetAdministratorsQuery.cs
  - src/Onboarding.Application/DependencyInjection.cs
  - src/Onboarding.Infrastructure/Keycloak/KeycloakUserService.cs
  - src/Onboarding.API/Controllers/AdminUserController.cs
  - frontend/backoffice/src/lib/admin-api.ts
  - tests/Onboarding.Domain.Tests/Application/Commands/GetAdministratorsQueryHandlerTests.cs
  - tests/Onboarding.API.Tests/Admin/AdminAuthorizationTests.cs
autonomous: true
requirements:
  - ADM-01
  - ADM-02
  - ADM-03
  - ADM-04

must_haves:
  truths:
    - "GET /api/admin/administrators retorna lista de admins (ativos e bloqueados) com HasTemporaryPassword correto"
    - "POST /api/admin/administrators cria admin (rota renomeada de /api/admin/users)"
    - "Endpoint /api/admin/users (antigo CreateAdmin) não existe mais — retorna 404"
    - "Frontend backoffice chama /api/admin/administrators (não /api/admin/users) no createAdmin"
    - "Ambos endpoints exigem role admin — non-admin recebe 403"
  artifacts:
    - path: "src/Onboarding.Application/Common/IAuditService.cs"
      provides: "AdminUserDto record adicionado"
      contains: "record AdminUserDto"
    - path: "src/Onboarding.Application/Admin/Queries/GetAdministratorsQuery.cs"
      provides: "GetAdministratorsQuery + GetAdministratorsQueryHandler"
      exports: ["GetAdministratorsQuery", "GetAdministratorsQueryHandler"]
    - path: "src/Onboarding.Infrastructure/Keycloak/KeycloakUserService.cs"
      provides: "GetUsersByRoleAsync implementado"
      contains: "GetUsersByRoleAsync"
  key_links:
    - from: "AdminUserController GET /administrators"
      to: "GetAdministratorsQueryHandler"
      via: "IQueryHandler<GetAdministratorsQuery, IReadOnlyList<AdminUserDto>>"
      pattern: "_administratorsHandler.HandleAsync"
    - from: "GetAdministratorsQueryHandler"
      to: "IKeycloakUserService.GetUsersByRoleAsync"
      via: "await _keycloakUserService.GetUsersByRoleAsync(\"admin\", ct)"
      pattern: "GetUsersByRoleAsync.*admin"
    - from: "KeycloakUserService.GetUsersByRoleAsync"
      to: "GET /admin/realms/onboarding/roles/admin/users"
      via: "_adminHttpClient.GetAsync"
      pattern: "roles/{roleName}/users"
---

<objective>
Implementar GET /api/admin/administrators (lista todos os admins via Keycloak), renomear POST /api/admin/users para POST /api/admin/administrators, e atualizar o frontend backoffice para usar a nova rota.

Purpose: Atender ADM-01 (criar admin), ADM-04 (listar admins) com rota correta e consistente. A criação de admin já funciona; só a rota e o novo endpoint de listagem precisam ser adicionados.
Output: GetAdministratorsQuery + handler, KeycloakUserService.GetUsersByRoleAsync, AdminUserController atualizado, frontend admin-api.ts atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-audit-log-admin-backend/30-CONTEXT.md
@.planning/phases/30-audit-log-admin-backend/30-RESEARCH.md
@.planning/phases/30-audit-log-admin-backend/30-01-SUMMARY.md

<!-- Interfaces extraídas do codebase para referência do executor -->
<interfaces>
<!-- IKeycloakUserService — interface atual a ser estendida -->
From src/Onboarding.Application/Common/IKeycloakUserService.cs:
```csharp
public interface IKeycloakUserService
{
    Task<string> CreateAdminUserAsync(string email, string temporaryPassword, string fullName, CancellationToken ct = default);
    Task<KeycloakUser?> GetUserByEmailAsync(string email, CancellationToken ct = default);
    Task<KeycloakUserDetails?> GetUserByIdAsync(string userId, CancellationToken ct = default);
    Task BlockUserAsync(string keycloakUserId, CancellationToken ct = default);
    Task UnblockUserAsync(string keycloakUserId, CancellationToken ct = default);
    // ... outros métodos
}
public sealed record KeycloakUserDetails(string Id, string Email, bool Enabled, bool EmailVerified, IReadOnlyList<string> RequiredActions);
```

<!-- AdminUserController — construtor atual e rota a renomear -->
From src/Onboarding.API/Controllers/AdminUserController.cs:
```csharp
[ApiController]
[Route("api/admin")]
[Authorize(Roles = "admin")]
public sealed class AdminUserController : ControllerBase
{
    // ... campos existentes ...
    private readonly ICommandHandler<CreateAdminCommand, CreateAdminResult> _createAdminHandler;

    // Rota a RENOMEAR (linha ~305):
    [HttpPost("users")]   // ← trocar para [HttpPost("administrators")]
    public async Task<IActionResult> CreateAdmin(...)
}
```

<!-- IQueryHandler — padrão CQRS do projeto -->
From src/Onboarding.Application/Common/ (inferido do DependencyInjection.cs):
```csharp
public interface IQueryHandler<TQuery, TResult>
{
    Task<TResult> HandleAsync(TQuery query, CancellationToken ct = default);
}
```

<!-- GetAuditLogQuery — referência para GetAdministratorsQuery -->
From src/Onboarding.Application/Admin/Queries/GetAuditLogQuery.cs:
```csharp
public sealed record GetAuditLogQuery(int Page, int PageSize, ...);
public sealed class GetAuditLogQueryHandler : IQueryHandler<GetAuditLogQuery, PaginatedResult<AdminAuditLogDto>>
{
    private readonly IAdminAuditLogRepository _repo;
    public GetAuditLogQueryHandler(IAdminAuditLogRepository repo) => _repo = repo;
    public async Task<PaginatedResult<AdminAuditLogDto>> HandleAsync(GetAuditLogQuery query, ...) { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AdminUserDto + GetAdministratorsQuery + GetUsersByRoleAsync</name>
  <files>
    src/Onboarding.Application/Common/IAuditService.cs,
    src/Onboarding.Application/Common/IKeycloakUserService.cs,
    src/Onboarding.Application/Admin/Queries/GetAdministratorsQuery.cs,
    src/Onboarding.Infrastructure/Keycloak/KeycloakUserService.cs,
    src/Onboarding.Application/DependencyInjection.cs,
    tests/Onboarding.Domain.Tests/Application/Commands/GetAdministratorsQueryHandlerTests.cs
  </files>
  <read_first>
    - src/Onboarding.Application/Common/IAuditService.cs (adicionar AdminUserDto record aqui)
    - src/Onboarding.Application/Common/IKeycloakUserService.cs (adicionar GetUsersByRoleAsync)
    - src/Onboarding.Application/Admin/Queries/GetAuditLogQuery.cs (padrão de query handler)
    - src/Onboarding.Infrastructure/Keycloak/KeycloakUserService.cs (ver _adminHttpClient e padrão de chamada HTTP)
    - src/Onboarding.Application/DependencyInjection.cs (onde registrar GetAdministratorsQueryHandler)
  </read_first>
  <behavior>
    - GetAdministratorsQueryHandler.HandleAsync chama IKeycloakUserService.GetUsersByRoleAsync("admin", ct) exatamente 1 vez
    - GetAdministratorsQueryHandler retorna o resultado de GetUsersByRoleAsync sem transformação adicional
    - KeycloakUserService.GetUsersByRoleAsync mapeia requiredActions contendo "UPDATE_PASSWORD" para HasTemporaryPassword=true
    - KeycloakUserService.GetUsersByRoleAsync mapeia requiredActions sem "UPDATE_PASSWORD" para HasTemporaryPassword=false
    - KeycloakUserService.GetUsersByRoleAsync usa Email ?? Username como fallback para campo Email vazio
  </behavior>
  <action>
**Passo 1: Adicionar AdminUserDto em IAuditService.cs**

No arquivo `src/Onboarding.Application/Common/IAuditService.cs`, adicionar após a interface IAuditService:
```csharp
/// <summary>
/// DTO para representação de um administrador na listagem GET /api/admin/administrators.
/// HasTemporaryPassword=true quando UPDATE_PASSWORD está nos requiredActions do Keycloak.
/// </summary>
public sealed record AdminUserDto(
    string Id,
    string Email,
    string FullName,
    bool IsEnabled,
    bool HasTemporaryPassword);
```

**Passo 2: Adicionar GetUsersByRoleAsync em IKeycloakUserService.cs**

Adicionar ao final da interface IKeycloakUserService (antes do fechamento da interface):
```csharp
/// <summary>
/// Retorna todos os usuários com a role especificada no Keycloak.
/// Fonte: GET /admin/realms/{realm}/roles/{roleName}/users
/// Inclui ativos (Enabled=true) e bloqueados (Enabled=false).
/// HasTemporaryPassword deriva da presença de "UPDATE_PASSWORD" nos requiredActions.
/// </summary>
Task<IReadOnlyList<AdminUserDto>> GetUsersByRoleAsync(string roleName, CancellationToken ct = default);
```

**Passo 3: Implementar GetUsersByRoleAsync em KeycloakUserService.cs**

Adicionar o método à classe KeycloakUserService. Localizar o campo `_adminHttpClient` (HttpClient named "keycloak-admin-api") e o campo `_realm` (string com nome do realm). Adicionar após os métodos existentes:

```csharp
public async Task<IReadOnlyList<AdminUserDto>> GetUsersByRoleAsync(
    string roleName, CancellationToken ct = default)
{
    var response = await _adminHttpClient.GetAsync(
        $"admin/realms/{_realm}/roles/{Uri.EscapeDataString(roleName)}/users", ct);
    response.EnsureSuccessStatusCode();

    var users = await response.Content.ReadFromJsonAsync<List<KeycloakUserRepresentation>>(
        cancellationToken: ct) ?? [];

    return users.Select(u => new AdminUserDto(
        Id: u.Id ?? string.Empty,
        Email: u.Email ?? u.Username ?? string.Empty,
        FullName: BuildFullName(u.FirstName, u.LastName),
        IsEnabled: u.Enabled ?? true,
        HasTemporaryPassword: u.RequiredActions?.Contains("UPDATE_PASSWORD") ?? false
    )).ToList().AsReadOnly();
}

private static string BuildFullName(string? firstName, string? lastName)
    => string.Join(" ", new[] { firstName, lastName }
        .Where(s => !string.IsNullOrWhiteSpace(s) && s != "-")).Trim();

private sealed record KeycloakUserRepresentation(
    string? Id,
    string? Username,
    string? Email,
    string? FirstName,
    string? LastName,
    bool? Enabled,
    [property: System.Text.Json.Serialization.JsonPropertyName("requiredActions")]
    List<string>? RequiredActions);
```

Verificar se `BuildFullName` já existe no KeycloakUserService — se sim, não duplicar. Verificar se `KeycloakUserRepresentation` já existe — se sim, estender com o campo `RequiredActions` em vez de criar novo record.

Adicionar `using System.Net.Http.Json;` se não estiver presente.

**Passo 4: Criar GetAdministratorsQuery.cs**

Criar `src/Onboarding.Application/Admin/Queries/GetAdministratorsQuery.cs`:
```csharp
using Onboarding.Application.Common;

namespace Onboarding.Application.Admin.Queries;

/// <summary>
/// Query: retorna todos os administradores via Keycloak.
/// Sem paginação — lista de admins é pequena.
/// </summary>
public sealed record GetAdministratorsQuery();

public sealed class GetAdministratorsQueryHandler
    : IQueryHandler<GetAdministratorsQuery, IReadOnlyList<AdminUserDto>>
{
    private readonly IKeycloakUserService _keycloakUserService;

    public GetAdministratorsQueryHandler(IKeycloakUserService keycloakUserService)
        => _keycloakUserService = keycloakUserService;

    public async Task<IReadOnlyList<AdminUserDto>> HandleAsync(
        GetAdministratorsQuery query, CancellationToken ct = default)
        => await _keycloakUserService.GetUsersByRoleAsync("admin", ct);
}
```

**Passo 5: Registrar em DependencyInjection.cs**

Em `src/Onboarding.Application/DependencyInjection.cs`, adicionar após o registro do GetAuditLogQuery:
```csharp
// Admin administrators query (Phase 30 — ADM-04)
services.AddScoped<
    IQueryHandler<GetAdministratorsQuery, IReadOnlyList<AdminUserDto>>,
    GetAdministratorsQueryHandler>();
```
Adicionar using `using Onboarding.Application.Admin.Queries;` e `using Onboarding.Application.Common;` se não estiverem presentes.

**Passo 6: Criar testes unitários**

Criar `tests/Onboarding.Domain.Tests/Application/Commands/GetAdministratorsQueryHandlerTests.cs`:
```csharp
using NSubstitute;
using Onboarding.Application.Admin.Queries;
using Onboarding.Application.Common;
using Shouldly;

namespace Onboarding.Domain.Tests.Application.Commands;

[Trait("Category", "Unit")]
public sealed class GetAdministratorsQueryHandlerTests
{
    private readonly IKeycloakUserService _keycloakMock = Substitute.For<IKeycloakUserService>();
    private readonly GetAdministratorsQueryHandler _sut;

    public GetAdministratorsQueryHandlerTests()
        => _sut = new GetAdministratorsQueryHandler(_keycloakMock);

    [Fact]
    public async Task HandleAsync_CallsGetUsersByRoleAsync_WithAdminRole()
    {
        var expected = new List<AdminUserDto>
        {
            new("id1", "admin@test.com", "Admin One", true, false)
        }.AsReadOnly();

        _keycloakMock
            .GetUsersByRoleAsync("admin", Arg.Any<CancellationToken>())
            .Returns(Task.FromResult<IReadOnlyList<AdminUserDto>>(expected));

        var result = await _sut.HandleAsync(new GetAdministratorsQuery());

        result.Count.ShouldBe(1);
        result[0].Email.ShouldBe("admin@test.com");
        await _keycloakMock.Received(1).GetUsersByRoleAsync("admin", Arg.Any<CancellationToken>());
    }

    [Fact]
    public async Task HandleAsync_WhenNoAdmins_ReturnsEmptyList()
    {
        _keycloakMock
            .GetUsersByRoleAsync("admin", Arg.Any<CancellationToken>())
            .Returns(Task.FromResult<IReadOnlyList<AdminUserDto>>(new List<AdminUserDto>().AsReadOnly()));

        var result = await _sut.HandleAsync(new GetAdministratorsQuery());

        result.ShouldBeEmpty();
    }
}
```
  </action>
  <verify>
    <automated>cd /d/REPO/keycloak-tests && dotnet test tests/Onboarding.Domain.Tests --filter "GetAdministratorsQueryHandlerTests" --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - AdminUserDto record existe em IAuditService.cs com campos: Id, Email, FullName, IsEnabled, HasTemporaryPassword
    - IKeycloakUserService.GetUsersByRoleAsync existe com assinatura Task&lt;IReadOnlyList&lt;AdminUserDto&gt;&gt;
    - GetAdministratorsQuery.cs existe com query record e handler
    - KeycloakUserService implementa GetUsersByRoleAsync com mapeamento de requiredActions
    - GetAdministratorsQueryHandler registrado em DependencyInjection.cs
    - Testes GetAdministratorsQueryHandlerTests passam
  </done>
  <acceptance_criteria>
    - grep "record AdminUserDto" src/Onboarding.Application/Common/IAuditService.cs returns match
    - grep "GetUsersByRoleAsync" src/Onboarding.Application/Common/IKeycloakUserService.cs returns match
    - grep "class GetAdministratorsQueryHandler" src/Onboarding.Application/Admin/Queries/GetAdministratorsQuery.cs returns match
    - grep "GetUsersByRoleAsync" src/Onboarding.Infrastructure/Keycloak/KeycloakUserService.cs returns match
    - grep "HasTemporaryPassword.*UPDATE_PASSWORD" src/Onboarding.Infrastructure/Keycloak/KeycloakUserService.cs returns match
    - grep "GetAdministratorsQueryHandler" src/Onboarding.Application/DependencyInjection.cs returns match
    - dotnet test tests/Onboarding.Domain.Tests --filter "GetAdministratorsQueryHandlerTests" exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Atualizar AdminUserController (renomear rota + GET /administrators) e frontend admin-api.ts</name>
  <files>
    src/Onboarding.API/Controllers/AdminUserController.cs,
    frontend/backoffice/src/lib/admin-api.ts,
    tests/Onboarding.API.Tests/Admin/AdminAuthorizationTests.cs
  </files>
  <read_first>
    - src/Onboarding.API/Controllers/AdminUserController.cs (ler completo — construtor, todos os actions, helper GetAuditContext)
    - frontend/backoffice/src/lib/admin-api.ts (localizar linha ~324 com /api/admin/users na função createAdmin)
    - tests/Onboarding.API.Tests/Admin/AdminAuthorizationTests.cs (verificar se há teste de rota /api/admin/users para CreateAdmin — atualizar para /administrators)
    - src/Onboarding.Application/Common/IAuditService.cs (verificar AdminUserDto adicionado na Task 1)
    - src/Onboarding.Application/Admin/Queries/GetAdministratorsQuery.cs (GetAdministratorsQuery criado na Task 1)
  </read_first>
  <action>
**Passo 1: Adicionar campo e parâmetro de construtor em AdminUserController**

No construtor de AdminUserController, adicionar APÓS o campo `_auditLogQueryHandler`:
```csharp
private readonly IQueryHandler<GetAdministratorsQuery, IReadOnlyList<AdminUserDto>> _administratorsHandler;
```

No construtor, adicionar o parâmetro:
```csharp
IQueryHandler<GetAdministratorsQuery, IReadOnlyList<AdminUserDto>> administratorsHandler,
```

No corpo do construtor, adicionar a atribuição:
```csharp
_administratorsHandler = administratorsHandler;
```

Adicionar usings se necessário:
```csharp
using Onboarding.Application.Common;  // AdminUserDto
// GetAdministratorsQuery já no namespace Onboarding.Application.Admin.Queries
```

**Passo 2: Renomear ação CreateAdmin de [HttpPost("users")] para [HttpPost("administrators")]**

Localizar no controller a action com atributo `[HttpPost("users")]` que chama `_createAdminHandler` (não o action [HttpGet("users")] de listagem de clientes paginada).

Trocar APENAS o atributo da rota:
```csharp
// ANTES:
[HttpPost("users")]

// DEPOIS:
[HttpPost("administrators")]
```

O corpo do action NÃO muda. O nome do método (CreateAdmin) NÃO muda.

**ATENÇÃO:** NÃO alterar o action `[HttpGet("users")]` (listagem paginada de clientes) — essa rota continua igual.

**Passo 3: Adicionar action GET /administrators**

Adicionar APÓS o action CreateAdmin (o novo POST /administrators):
```csharp
/// <summary>
/// GET /api/admin/administrators — Lista todos os administradores via Keycloak.
/// Inclui ativos (IsEnabled=true) e bloqueados (IsEnabled=false).
/// HasTemporaryPassword=true indica que o admin ainda não trocou a senha temporária inicial.
/// </summary>
[HttpGet("administrators")]
[ProducesResponseType(typeof(IReadOnlyList<AdminUserDto>), StatusCodes.Status200OK)]
[ProducesResponseType(typeof(ProblemDetails), StatusCodes.Status503ServiceUnavailable)]
public async Task<IActionResult> GetAdministrators(CancellationToken ct = default)
{
    try
    {
        var result = await _administratorsHandler.HandleAsync(new GetAdministratorsQuery(), ct);
        return Ok(result);
    }
    catch (HttpRequestException ex)
    {
        _logger.LogError(ex, "Keycloak unavailable when listing administrators");
        return StatusCode(StatusCodes.Status503ServiceUnavailable, new ProblemDetails
        {
            Title = "Service Unavailable",
            Status = StatusCodes.Status503ServiceUnavailable,
            Detail = "Unable to retrieve administrators from identity provider."
        });
    }
}
```

**Passo 4: Atualizar frontend/backoffice/src/lib/admin-api.ts**

Abrir o arquivo e localizar a função `createAdmin` (aproximadamente linha 324). Dentro dessa função, trocar a URL:
```typescript
// ANTES (buscar por esta string):
/api/admin/users

// DEPOIS:
/api/admin/administrators
```

Confirmar que apenas a URL da função `createAdmin` é alterada — NÃO alterar a URL de `getUsers`, `blockUser`, `unblockUser`, `deleteUser`, `updateUser` etc.

**Passo 5: Atualizar testes de autorização se necessário**

Em `tests/Onboarding.API.Tests/Admin/AdminAuthorizationTests.cs`:
- Verificar se existe teste para POST /api/admin/users (CreateAdmin) — se sim, atualizar para POST /api/admin/administrators
- Verificar se existe teste para verificar que POST /api/admin/users retorna 404 (rota não existe mais) — se não existe, adicionar

Adicionar (se não existir) teste de autorização para o novo GET /api/admin/administrators:
```csharp
[Fact]
[Trait("Category", "Integration")]
public async Task GetAdministrators_WithoutAdminRole_Returns403()
{
    // Use token sem role admin
    _client!.DefaultRequestHeaders.Authorization =
        new AuthenticationHeaderValue("Bearer", FakeJwtTokenHelper.GenerateClientJwt()); // ou token sem roles
    var response = await _client.GetAsync("/api/admin/administrators");
    response.StatusCode.ShouldBe(HttpStatusCode.Forbidden);
}
```

Verificar a assinatura de FakeJwtTokenHelper para usar o método correto que gera token sem role admin. Se não houver método adequado, criar um token com claims vazias.
  </action>
  <verify>
    <automated>cd /d/REPO/keycloak-tests && dotnet test tests/Onboarding.API.Tests 2>&1 | tail -10</automated>
  </verify>
  <done>
    - AdminUserController tem [HttpPost("administrators")] (não [HttpPost("users")]) para CreateAdmin
    - AdminUserController tem [HttpGet("administrators")] com action GetAdministrators
    - AdminUserController construtor injeta IQueryHandler&lt;GetAdministratorsQuery, IReadOnlyList&lt;AdminUserDto&gt;&gt;
    - admin-api.ts usa /api/admin/administrators na função createAdmin
    - dotnet test tests/Onboarding.API.Tests exits 0
  </done>
  <acceptance_criteria>
    - grep 'HttpPost.*administrators' src/Onboarding.API/Controllers/AdminUserController.cs returns match
    - grep 'HttpGet.*administrators' src/Onboarding.API/Controllers/AdminUserController.cs returns match
    - grep 'HttpPost.*"users"' src/Onboarding.API/Controllers/AdminUserController.cs returns no match (CreateAdmin foi renomeado)
    - grep 'HttpGet.*"users"' src/Onboarding.API/Controllers/AdminUserController.cs returns match (listagem de clientes permanece)
    - grep 'GetAdministrators' src/Onboarding.API/Controllers/AdminUserController.cs returns match
    - grep '/api/admin/administrators' frontend/backoffice/src/lib/admin-api.ts returns match in createAdmin function
    - grep '/api/admin/users' frontend/backoffice/src/lib/admin-api.ts does NOT return match in createAdmin function
    - dotnet test tests/Onboarding.API.Tests exits 0
    - dotnet build src/Onboarding.API exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller → IQueryHandler | GET /administrators não recebe input do cliente — sem superfície de injection |
| KeycloakUserService → Keycloak Admin API | HTTP externo com token de service account — credenciais gerenciadas por Duende.AccessTokenManagement |
| Frontend → POST /api/admin/administrators | Body com FullName + Email — validação por FluentValidation existente (não alterada) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05 | Elevation of Privilege | GET /api/admin/administrators | mitigate | Endpoint protegido por [Authorize(Roles = "admin")] herdado do controller. Non-admin recebe 403 por middleware ASP.NET Core. Verificado por AdminAuthorizationTests. |
| T-30-06 | Information Disclosure | AdminUserDto (HasTemporaryPassword) | accept | HasTemporaryPassword expõe estado de segurança interno. Acesso requer autenticação de admin. Não expõe a senha temporária em si (nunca armazenada). |
| T-30-07 | Spoofing | KeycloakUserService.GetUsersByRoleAsync | mitigate | Token de service account obtido automaticamente por Duende.AccessTokenManagement com client_credentials. Token renovado antes de expirar. _adminHttpClient configurado com AddClientCredentialsTokenHandler. |
| T-30-08 | Denial of Service | GET /api/admin/administrators | accept | Lista de admins é pequena (< 100). Sem paginação é aceitável. Rate limiting não implementado nesta fase — frontend admin não é público. |
| T-30-09 | Tampering | frontend/backoffice/src/lib/admin-api.ts (URL change) | accept | Mudança de URL é cosmética — segurança real está no backend ([Authorize(Roles = "admin")]). Frontend não contém lógica de autorização. |
</threat_model>

<verification>
Verificação global do Plano 02:

1. `dotnet build src/Onboarding.API --no-incremental` retorna 0 erros
2. `grep "HttpPost.*administrators" src/Onboarding.API/Controllers/AdminUserController.cs` retorna resultado
3. `grep "HttpGet.*administrators" src/Onboarding.API/Controllers/AdminUserController.cs` retorna resultado
4. `grep "/api/admin/administrators" frontend/backoffice/src/lib/admin-api.ts` retorna resultado
5. `dotnet test tests/Onboarding.API.Tests` retorna 0 falhas
6. `dotnet test tests/Onboarding.Domain.Tests` retorna 0 falhas
7. `grep "GetUsersByRoleAsync" src/Onboarding.Infrastructure/Keycloak/KeycloakUserService.cs` retorna resultado
</verification>

<success_criteria>
- AdminUserDto record definido em Application.Common com Id, Email, FullName, IsEnabled, HasTemporaryPassword
- IKeycloakUserService.GetUsersByRoleAsync adicionado com assinatura correta
- KeycloakUserService implementa GetUsersByRoleAsync chamando GET /admin/realms/onboarding/roles/{roleName}/users
- HasTemporaryPassword derivado de "UPDATE_PASSWORD" nos requiredActions do Keycloak
- GetAdministratorsQuery + GetAdministratorsQueryHandler criados e registrados
- AdminUserController: POST mudou de /users para /administrators, GET /administrators adicionado
- GET /api/admin/users (clientes paginados) NÃO foi alterado
- frontend/backoffice/src/lib/admin-api.ts usa /api/admin/administrators na função createAdmin
- dotnet build src/Onboarding.API exits 0
- dotnet test tests/Onboarding.API.Tests exits 0
- dotnet test tests/Onboarding.Domain.Tests exits 0
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/30-audit-log-admin-backend/30-02-SUMMARY.md`
</output>
